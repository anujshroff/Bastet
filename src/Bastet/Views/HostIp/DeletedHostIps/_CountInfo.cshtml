@model DeletedHostIpListViewModel

<div class="alert alert-secondary mb-4">
    <p class="mb-0">
        <i class="bi bi-info-circle me-2"></i>
        Showing <strong>@Model.TotalCount</strong> deleted host IP assignment(s) for subnet <strong>@Model.SubnetName</strong>.
    </p>
</div>
