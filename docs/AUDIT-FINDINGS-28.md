# Bastet - Round-28 Audit Findings

Branch: audit/round-28
HEAD: 8891320
Test baseline: 950 passed / 0 failed / 0 build warnings
Date: 2026-09-06

Round 28 filed 12 findings, of which 4 are residue of previous rounds' fixes (23-L2-remainder x3, 17 (round-level; ledger has no per-finding ids before round 18) x1). Funnel: 20 raw from 18 finders -> 15 merged (4 x2, 11 x1) -> 12 survived, 3 refuted by verification, 0 dropped at merge.

GROWTH:
src/Bastet/Controllers/HostIpController.cs: 731 lines (was 731)
src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml: 702 lines (was 702)
src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml: 113 lines (was 286)
test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs: 101 lines (was 0; not present at 1d4c81b)

# Critical

# High

# Medium

# Low

## L1 - Create refusal tells the operator to select a container that the same validator then refuses (host IPs / fully allocated) `[x1]` `strings`
**Where:** src/Bastet/Controllers/SubnetController.Helpers.cs:220; src/Bastet/Controllers/SubnetController.Helpers.cs:233
**Breaks:** R=10.0.0.0/16 > B=10.0.0.0/24, B holding a host IP (or marked fully allocated). POST Create 10.0.0.0/26 with parent R -> "A more specific parent subnet exists: B (10.0.0.0/24). Please select it instead."; no parent -> "This subnet must be a child of subnet B (10.0.0.0/24)." Selecting B -> "Cannot create child subnets in a subnet that has host IP assignments..." / "Cannot create a child subnet under a subnet marked as fully allocated." The named remedy is refused and the fact (the address falls inside a container that cannot take children) is never stated. Bulk path unaffected (AzureBulkImportPlanner.cs:389-405).
**Repro:** Verifier instance v2b2p1_r2, requests+bs4: six POST /Subnet/Create of 10.0.0.0/26 and 10.0.1.0/26 against a host-IP B and a fully-allocated FA, all 200 redisplays with the messages above; the parent dropdown lists every subnet (SubnetController.Create.cs:18-27). No test pins either message.
**Fix:** In ValidateSubnetCreation (:215-238), once bestParent is known and is not the selected parent, apply exactly the two gates :131-149 refuse on (`context.HostIpAssignments.AnyAsync(h => h.SubnetId == bestParent.Id)`, `bestParent.IsFullyAllocated`) before naming it; when either holds, add a ModelState error on NetworkAddress stating the fact and no remedy ("This subnet falls inside {bestParent.Name} ({net}/{cidr}), which has host IP assignments, so it cannot be created." / "... which is marked fully allocated, so it cannot be created.") and return false in both the :217 and :230 branches. Pin: two Create tests (host-IP and fully-allocated container, parent omitted and parent = grandparent) asserting neither "select it instead" nor "must be a child of" appears; reverting the gate reddens both.
**Residue of:** none

## L2 - HostIp Edit redisplay renders a blank Subnet and 'Created 1/1/0001 12:00 AM' after a lock timeout, validation failure or save error `[x1]`
**Where:** src/Bastet/Controllers/HostIpController.cs:312; src/Bastet/Controllers/HostIpController.cs:260, src/Bastet/Controllers/HostIpController.cs:283, src/Bastet/Controllers/HostIpController.cs:296
**Breaks:** On /HostIp/Edit, submit a Name with HTML, or hit the lock timeout, an indeterminate or a generic save error: the right banner appears but the "Subnet Information" card reads "Subnet" (blank) and "Created 1/1/0001 12:00 AM" instead of "leaf (10.0.9.0/24)" / the stored date. The form posts only IP/Name/SubnetId/RowVersion; every redisplay branch except CONCURRENCY_CONFLICT (:236-245) returns View(viewModel) without reloading SubnetInfo/CreatedAt/LastModifiedAt, unlike HostIp Create (:163-170) and Subnet Edit (SubnetController.Edit.cs:195-227).
**Repro:** Verifier instance v2b4p1r: POST Name='<b>bold</b>' -> 200, NoHtml banner, sidebar "Subnet |  | Created | 1/1/0001 12:00 AM" (:312); stale RowVersion control -> sidebar correct; sp_getapplock 'Bastet:SubnetOperations' held 60 s, POST -> 200 after 30.0 s, timeout banner, same blank sidebar (:283), stored Name unchanged.
**Fix:** The finder's fix was judged unsound: deleting :236-245 and the :283 early return so branches "fall through" cannot work because :260 returns from inside the ExecuteWithSubnetLockAsync lambda (changing its contract is a restructure) and :296 (DbUpdateConcurrencyException catch) is a direct return it never lists; as written it regresses the one correct branch. Corrected fix: one private `Task<IActionResult> RedisplayEditAsync(string ip, EditHostIpViewModel viewModel)` that loads `context.HostIpAssignments.Include(h => h.Subnet).AsNoTracking().FirstOrDefaultAsync(h => h.IP == ip)`, returns NotFound() when null, stamps SubnetInfo/CreatedAt/LastModifiedAt and returns View(viewModel); replace the four `return View(viewModel)` at :260, :283, :296, :312 with `return await RedisplayEditAsync(ip, viewModel)` and delete :236-245. Pin (SubnetLockTimeoutTests.cs or a HostIpManagement sibling): HostIpEdit_LockTimesOut_RedisplaysStoredSubnetAndCreatedAt with AlwaysTimingOutLockService, plus one ModelState-invalid case (ModelState.AddModelError before calling Edit), both asserting SubnetInfo == "<name> (<net>/<cidr>)" and CreatedAt equals the seeded value; both red before the fix.
**Residue of:** none

## L3 - Rename-only prefix reason asserts "Everything in this prefix is already imported" above subnets the same screen badges "Cannot import" `[x1]` `strings`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:233
**Breaks:** Linked target whose Bastet name drifted, with an Azure subnet un-importable because a hand-made row sits inside it. Planner: AlreadyImported, reason "Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add.", RenameOnlyCandidate=true. With "Rename matched Bastet subnets" on, the client discards that and prints "Everything in this prefix is already imported. The only change would be renaming the Bastet subnet to match the VNet name." directly above the row badged "Cannot import" (with the filter on, above its own "N subnet(s) in this prefix cannot be imported ... and are hidden."). Same substitution fires for the host-IP and fully-allocated AlreadyImported branches where nothing is imported at all. The subnet-row path (:273-275) appends to the planner's reason; the prefix path replaces it.
**Repro:** Verifier instance v3b5p1, playwright: imported 10.61.0.0/16 target only, created 10.61.2.0/25 under it, renamed to drifted-a, reopened /Azure/BulkImport: rename OFF shows the planner's reason, rename ON shows "Rename only | Everything in this prefix is already imported. ..." with s2 "Cannot import" beneath; fully-allocated and host-IP targets identical. blame :232-235 -> 23233f2 (#167); `git log -S'either already recorded or cannot be imported'` -> f4a0a87 (Audit 17 #170), which reworded the planner without the client substitute.
**Fix:** At :232-235 pass `renameOnly ? prefixInfo.reason + " The only change would be renaming the Bastet subnet to match the VNet name." : prefixInfo.reason` to reasonHtml, the shape :273-275 already uses. No unit seam: record in the ledger row and as an /e2e assertion that the Rename-only prefix reason still begins with the planner's "Already imported as Bastet subnet '...'" text.
**Residue of:** 17 (round-level; ledger has no per-finding ids before round 18)

## L4 - Edit sidebar tells the operator CIDR is the modifiable value on an Azure-linked row whose form has fixed it `[x1]` `strings`
**Where:** src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml:11; src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml:13-16, src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml:17-32
**Breaks:** /Subnet/Edit/{id} for any row with a non-empty AzureResourceId: the form renders CIDR readonly+disabled with a hidden field and "Imported from Azure, so the prefix is fixed here..." (Edit/_EditForm.cshtml:18-33, gated on Model.IsAzureLinked), while the static sidebar lists "CIDR - The subnet mask size in CIDR notation" under "You can modify the following properties", says "Network address cannot be changed. Only the CIDR value can be modified." and prints the "CIDR Modification Rules" block. A POST with a changed Cidr is refused.
**Repro:** Verifier instance vb5p1x2: 10.99.0.0/24 with AzureResourceId set via sqlcmd; Edit renders `<input value="24" class="form-control" readonly disabled />` + hidden Cidr + the note, Information card byte-identical to the unlinked one; POST Cidr=25 -> 200 refusal, DB Cidr still 24. blame: sidebar 46b3e69 (#17); linked branch 0de1293 (Audit Cleanup 6 #143). No test references _InformationSidebar.
**Fix:** Declare `@model EditSubnetViewModel` in the partial (Edit.cshtml:16 calls Html.PartialAsync without a model, so the page model flows in; passing Model explicitly is equivalent) and branch on the same Model.IsAzureLinked the form uses: when linked, omit the CIDR bullet and the rules block and close the note as "Network address and CIDR cannot be changed here."; otherwise render the current text. Pin: source-content test in the SubnetDetailsModalScriptTests style asserting the partial carries the IsAzureLinked branch and "Only the CIDR value can be modified" sits only in the unlinked branch.
**Residue of:** none

## L5 - CIDR modal validates a number-input string with parseInt, so 24.5 is approved and handed to a Create form that refuses it (and 2e1 = /20 is refused) `[x1]`
**Where:** src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:74; src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:106, src/Bastet/Views/Subnet/Details/_CidrInputModal.cshtml:19
**Breaks:** Type 24.5 into #cidrInput (type=number, Chromium reports stepMismatch): :74 `parseInt('24.5')` -> 24, so the modal indexes networkAddressByCidr[24], paints is-valid, prints 254 usable and enables Create; :106 navigates with the raw string, cidr=24.5. Create GET cannot bind `int? cidr`: "The value '24.5' is not valid.", no suggested Name; POST -> "The value '24.5' is not valid for CIDR Notation." Mirror: 2e1 (valueAsNumber 20, in range) is refused because parseInt('2e1') = 2.
**Repro:** Verifier instance v2b5p2_1, playwright on 192.168.0.0/16: fill('24.5') -> is-valid, size 254, Create enabled, URL cidr=24.5, GET error, POST refused; fill('2e1') -> is-invalid. Scratch tree with the two-line corrected fix: 24.5 refused, 2e1 accepted end to end as /20 (URL cidr=20, POST 302), 24 unchanged, '' and 40 refused; SubnetDetailsModalScriptTests green. `git log -S'parseInt($(this).val())'` -> 46b3e69 (#17); #189 rewrote both lines and carried it over.
**Fix:** The finder's fix was judged unsound: `if (!Number.isInteger(cidrValue)) { reset + refuse }` re-implements the decision the table lookup already makes (`networkAddressByCidr[24.5]` and `[NaN]` are undefined and fall into the existing :77-82 branch), and `step="1"` is a no-op (default step is 1). Corrected fix: :74 `const cidrValue = this.valueAsNumber;` and :106 `cidr: $('#cidrInput').prop('valueAsNumber'),` - nothing else. Pin: source-content assert in SubnetDetailsModalScriptTests that the script reads `valueAsNumber` and contains neither `parseInt(` nor `$('#cidrInput').val()`; record 24.5 (refused) and 2e1 (accepted as /20, URL cidr=20) in the /e2e phase F modal bullet.
**Residue of:** none

## L6 - HostIp Create renders every model-level error twice, and HostIp Edit shows an empty 'Error' alert for property-level errors `[x2]`
**Where:** src/Bastet/Views/HostIp/Create/_HostIpForm.cshtml:15; src/Bastet/Views/HostIp/Create.cshtml:8, src/Bastet/Views/HostIp/Edit/_Header.cshtml:8, src/Bastet/Views/HostIp/Create/_ErrorAlert.cshtml:1
**Breaks:** Create.cshtml:8 renders Create/_ErrorAlert (hand-rolled loop over ModelState[""]) and _HostIpForm.cshtml:15 renders a ModelOnly asp-validation-summary for the same errors, so every `ModelState.AddModelError("", ...)` refusal (HostIpController.cs:114,147,152,159) shows as two identical red boxes. Edit/_Header.cshtml:8 opens an "Error" alert whenever ErrorCount > 0 but wraps a ModelOnly summary, so a property-level NoHtml refusal shows a red "Error" heading with an empty body while the message sits beside the field. Subnet Create/_SubnetForm:6 and Edit/_EditForm:9 use the single tag-helper summary.
**Repro:** Verifier instance vb5p2_2, playwright: POST /HostIp/Create with an out-of-range IP -> .alert-danger ['IP address 172.16.5.10 is outside the subnet range 172.16.2.0/24'] x2, both visible; network-address and duplicate-IP refusals likewise x2; Name '<b>x</b>' on Create -> alerts [], field span only. Edit with Name '<b>x</b>' -> .alert-danger ['Error'], inner_html exactly the heading. blame all four sites 841c272 (#18).
**Fix:** Take the merge note's direction rather than the filed one: delete src/Bastet/Views/HostIp/Create/_ErrorAlert.cshtml and Create.cshtml:8, leaving the tag-helper summary at _HostIpForm.cshtml:15 as the single renderer; in Edit/_Header.cshtml replace lines 8-13 with `<div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>`. Do not gate the Edit block on `ViewData.ModelState[""]?.Errors.Count > 0` (a second copy of the partial's condition). Pin: source-content test in test/Bastet.Tests/HostIpManagement asserting no _ErrorAlert.cshtml under Views/HostIp, exactly one asp-validation-summary="ModelOnly" in each of Create/_HostIpForm.cshtml and Edit/_Header.cshtml, none in Create.cshtml.
**Residue of:** none

## L7 - 'Back to Host IPs' on the deleted-host-IPs page links to HostIp/Index, which refuses subnets that have children or are fully allocated, so the link lands on an error banner `[x1]`
**Where:** src/Bastet/Views/HostIp/DeletedHostIps.cshtml:23; src/Bastet/Controllers/HostIpController.cs:39
**Breaks:** Details/_HostIpAssignments.cshtml:93 offers "View Deleted Host IPs" on every subnet. DeletedHostIps.cshtml:21-26 always renders "Back to Host IPs" -> /HostIp?subnetId=N (the View-role else-branch :27-32 is unreachable under RequireViewRole). HostIpController.Index:39-43 refuses with TempData "This subnet cannot have host IP assignments because it has child subnets or is fully allocated." and redirects to Details, so Back yields a red error on the page the operator started from.
**Repro:** Verifier instance v2_b5p2_3, playwright: subnet with a deleted host IP then a child; the DeletedHostIps page's only back link is ('Back to Host IPs', '/HostIp?subnetId=2'); click -> /Subnet/Details/2 with the .alert-danger refusal. Fully-allocated variant identical; plain-subnet control lands on /HostIp with no alert. blame view :19-33 and controller :39-43 -> 841c272 (#18).
**Fix:** Replace the role-gated block at DeletedHostIps.cshtml:19-33 with the single existing "Back to Subnet" anchor (asp-controller="Subnet" asp-action="Details" asp-route-id="@Model.SubnetId"), deleting the @inject and both branches; Details never refuses. Do not add a CanAddHostIp-style property (second implementation of the Index gate). Pin: source-content test asserting the view links Subnet/Details and carries no HostIp asp-action="Index" link.
**Residue of:** none

## L8 - Wizard read-path failures are always reported as 'Error connecting to server:' (blank on transport failure, HTTP reason on 5xx), and the reconcile page wraps every scan failure in a static 'Because Azure could not be read' `[x1]` `strings`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:77; src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:141, src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:493, src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:102, src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:159, src/Bastet/Views/Azure/Reconcile/_StepReview.cshtml:15
**Breaks:** The five non-commit handlers print "Error connecting to server: " + errorThrown; errorThrown is '' for a transport failure or an XHR that followed an off-origin auth redirect, so the banner ends in a bare colon, and for a real HTTP answer it reads "Error connecting to server: Internal Server Error" although the server was reached. On Reconcile, _StepReview.cshtml:11-16 appends the static "Nothing was checked. ... Because Azure could not be read ... Fix the connection and scan again." to every showScanError caller, including the catch-all "The reconcile scan failed. Details have been logged." (database failure after Azure was read fine); the real Azure-unreadable case already names itself via plan.globalErrors. 25-L3 fixed this class on the two commit handlers only.
**Repro:** Verifier instance v_b5p2_4, playwright, real failures: BulkImport with the app stopped -> " Error connecting to server:"; Reconcile with the database set OFFLINE -> "The reconcile scan failed. Details have been logged." inside the "Because Azure could not be read" paragraph while app.log shows "SqlException: Cannot open database" after the Azure inventory read; Reconcile with the app stopped -> bare colon inside the same paragraph. Intercepted 503 -> "Error connecting to server: Service Unavailable". blame :77/:141/:493 -> 73fc76f (#108); :102/:159 and _StepReview :9-18 -> aedd0bd (#133); f8bd697 (25-L3) touched only the commit handlers, which AzureWizardClientWordingTests pins.
**Fix:** Apply 25-L3's branching to the five read-path handlers: `xhr.status === 0 ? 'The server could not be reached.' : 'The server returned status ' + xhr.status + '.'` (no outcome-unknown clause; these are reads), stated once per script as a small named function (e.g. readErrorMessage(xhr)). In _StepReview.cshtml drop the Azure clause: "BASTET cannot tell which resources still exist, so nothing is offered for deletion. Fix the problem shown above and scan again." Pin: extend AzureWizardClientWordingTests in the 27-L2 style (one whitespace-tolerant regex per handler binding the status-0 condition and both arms), asserting "Error connecting to server" absent from both scripts and "Because Azure could not be read" absent from _StepReview.cshtml.
**Residue of:** none

## L9 - CIDR modal keeps the adjusted address and its "adjusted to avoid overlaps" note on screen while refusing with "No compatible network address found" `[x1]`
**Where:** src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:84; src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:43 (refuse() lacks the reset), src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:78 (the reset only the undefined branch performs)
**Breaks:** Parent 10.11.0.0/24 with children 10.11.0.0/32 and 10.11.0.128/25. Create Subnet on the free range, type 26: address moves to 10.11.0.64 with warning border and "This network address has been adjusted to avoid overlaps." ArrowDown to 25: "No compatible network address found for this CIDR size.", Create disabled, yet the address field still shows 10.11.0.64 with the warning border and the adjusted sentence. Only the `address === undefined` branch (:78-79) resets; the `address === null` branch (:84-85) does not.
**Repro:** Verifier instance vb6deep2, playwright, two seeded layouts: fill 26 -> 10.11.0.64 adjusted; ArrowDown -> cidr 25, refusal visible, Create disabled, addr STILL 10.11.0.64 with border-warning and help. Clearing the input resets. fixcheck.py served the filed fix via page.route: ArrowDown to 25 -> addr 10.11.0.1, no warning, help hidden; 27 afterwards -> 10.11.0.32 adjusted. blame :73-100 -> 8891320 (#189); shape predates #189 (5a9caa2 :91-98, 46b3e69).
**Fix:** Move the two reset lines into refuse(): `function refuse(message, sizeText) { $('#networkAddressDisplay').val(activeSuggestion.startIp); makeNetworkAddressReadOnly(); ...existing four lines... }` and drop :78-79, so every refusal shows the clicked range start with no adjustment note. Pin in SubnetDetailsModalScriptTests.cs with a whitespace-tolerant regex scoped to the `function refuse(` body asserting both `val(activeSuggestion.startIp)` and `makeNetworkAddressReadOnly()` (the val call also exists at :54, so a file-wide Contains is vacuous); extend the /e2e phase F sentence to "an out-of-range, empty, or no-home CIDR resets the address to the range start and hides the warning".
**Residue of:** 23-L2-remainder

## L10 - Refusal sentence "No compatible network address found for this CIDR size." asserts a parent-wide absence, but the table only searched at or after the clicked range `[x1]` `strings`
**Where:** src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:85; .claude/skills/e2e/SKILL.md:654 (record says the refusal fires only when no block is free anywhere), test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:55 (pins the parent-wide wording)
**Breaks:** Parent 10.1.0.0/24 with children 10.1.0.64/26, 10.1.0.160/27, 10.1.0.192/26; Details lists free 10.1.0.0-.63 and 10.1.0.128-.159. Create Subnet on the 10.1.0.128 row, type 26: "No compatible network address found for this CIDR size." while the row above offers 10.1.0.0/26 and the app accepts it. SuggestChildSubnets answers "lowest free aligned block at or after this range start" (pinned `oracle >= start`) but the sentence never says so, and the modal itself hops forward into later ranges, so the operator cannot read the refusal as range-scoped. SKILL.md:654 records the refusal as "free anywhere".
**Repro:** Verifier instance v2b6deep2: server table for 10.1.0.128 = {25:null, 26:null, 27:'10.1.0.128',...}, for 10.1.0.0 = {..., 26:'10.1.0.0',...}. Click the 10.1.0.128 row, fill 26 -> refusal; click the 10.1.0.0 row, fill 26 -> Create -> /Subnet/Create?networkAddress=10.1.0.0&cidr=26&parentId=1, POST -> 302 created. `git log -S` sentence -> e05c3b1 (#11), 46b3e69 (#17); the pre-#189 client engine also walked forward from the clicked start; #189 re-implemented it server-side (IpUtilityService.cs:310-351), pinned the wording and recorded the refusal as "no home at all".
**Fix:** The finder's fix was judged unsound: its second sentence ("Pick a smaller CIDR or start from another range.") names two remedies the app can refuse - refusal only fires for a CIDR below the recommendation, so the working remedy is a larger cidr number, and "another range" is false when the parent has a single unallocated range or no other range fits. Corrected fix, fact only in the 24-L3 style: refuse with the template string "No free /${cidrValue} block starts at or after ${activeSuggestion.startIp}." and size text "Invalid - none free at or after this range" (both strings on :85); update the Assert.Contains at SubnetDetailsModalScriptTests.cs:55 to the new sentence and add Assert.DoesNotContain for the old one; correct SKILL.md:654-656 to say the refusal means no aligned block of the chosen size is free at or after the clicked range start. No arithmetic change; deleting the at-or-after axis would change the pinned `oracle >= start` property and is not the narrow fix.
**Residue of:** none

## L11 - Suggestion property suite has no layout with a short tail range, so a scope-hoist mutant of SuggestChildSubnets stays green `[x2]`
**Where:** test/Bastet.Tests/Services/IpArithmeticPropertyTests.cs:148; test/Bastet.Tests/Services/IpArithmeticPropertyTests.cs:202 (suggestion theory)
**Breaks:** Move `long? lowestBlockAtOrAfter = null;` (src/Bastet/Services/IpUtilityService.cs:324) above the `for (int cidr = ...)` loop at :321: range 0's block for cidr c-1 carries into cidr c, so for the last free range a cidr that does not fit is stamped with an earlier range's block instead of null. On 10.0.0.0/24 carved into 10.0.0.128/26 and 10.0.0.240/29, the modal from 10.0.0.248 with /28 offers 10.0.0.0 (before the range start) instead of refusing. Every #189 test stays green because no Layouts row (:133-149) has a tail free range shorter than a block size an earlier range holds.
**Repro:** Verifier scratch tree: pristine three guard classes 69/69; mutant m_hoist -> 69/69 green and full suite 950/950 green; live drive of the mutant build: Details JSON for range 10.0.0.248 = {25:null, 26:"10.0.0.0", 27:"10.0.0.0", 28:"10.0.0.0", 29:"10.0.0.248"}. Both candidate rows appended in the mutant tree: 2 failed, both the suggestion theory on those rows ("Assert.Null() Failure ... Actual: "10.0.0.0"" at :262); IpUtilityService.cs restored -> 73/73 green with the rows. blame :324 and the three #189 Layouts rows -> 8891320.
**Fix:** Test-only: append `{ "10.0.0.0", 24, ["10.0.0.128/26", "10.0.0.240/29"] }` to Layouts after :148 (b7p1-1's alternate row `{ "10.0.0.0", 24, ["10.0.0.64/26", "10.0.0.192/27", "10.0.0.240/28"] }` behaves identically; one row suffices). Pristine green, hoist mutant red on that row only; the ranges theory stays green with it.
**Residue of:** 23-L2-remainder

## L12 - CidrModalScript_OnlyIndexesTheServerSuggestionTable binds token presence, not the lookups: a client-side usable-count or address re-derivation in the modal stays green `[x2]`
**Where:** test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:52; test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:54, test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:50, test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:42 (CidrModalScript_CarriesNoIpArithmetic, the name-bound sibling; stays as the literal-revert guard)
**Breaks:** Mutant P6: _SubnetCalculationScripts.cshtml:100 `usableByCidr[cidrValue].toLocaleString()` -> `Math.max(0, 2 ** (32 - cidrValue) - 2).toLocaleString()`: the modal shows 0 usable for /31 and /32 where IpUtilityService.UsableWithin says 2 and 1 on the same page. Mutant P7: :75 `const address = activeSuggestion.networkAddressByCidr[cidrValue];` -> `cidrValue >= minCidr && cidrValue <= maxCidr ? activeSuggestion.startIp : undefined`: the table is never consulted. Both survive because the tokens `networkAddressByCidr` and `IpUtility.CalculateUsableIpAddresses` still occur in the Razor serialization block and the denylist (`<<|>>>|Math\.pow|& ?255`) does not cover `**`. The /e2e phase-F record never asserts #subnetSizeDisplay, so P6 is caught by neither.
**Repro:** Verifier scratch tree, `dotnet test --project test/Bastet.Tests -- --filter-class Bastet.Tests.SubnetManagement.SubnetDetailsModalScriptTests`: pristine 15/15; P6 15/15 green; P7 15/15 green. With the filed fix applied (plus `using System.Text.RegularExpressions;`): pristine 15/15; P6, P7 and P8 (:65 recommended-size display re-derived) each fail exactly CidrModalScript_OnlyIndexesTheServerSuggestionTable. The merge note's widened idiom regex (split('.'), [*/%] ?256, ** ?(32, 16777216, 65536) plus the existing denylist) has zero hits on the pristine script. blame test :45-57 -> 8891320 (#189).
**Fix:** Test-only, in CidrModalScript_OnlyIndexesTheServerSuggestionTable: bind the use sites - `Assert.Contains("const address = activeSuggestion.networkAddressByCidr[cidrValue];", script);` and collect every `#subnetSizeDisplay` write with `Regex.Matches(script, @"#subnetSizeDisplay[\"']\)\.text\(([^;]*)\);")`, assert exactly 3 and that each argument matches `^(usableByCidr\[(activeSuggestion\.recommendedCidr|cidrValue)\]\.toLocaleString\(\)|sizeText)$`. Add `using System.Text.RegularExpressions;` (not a csproj global using). Absorbing the widened idiom denylist into the same test is safe (zero pristine hits). Keep the ClientArithmeticIdentifiers theory as the revert guard.
**Residue of:** 23-L2-remainder

# Info

# Refuted - reported by a finder, killed by the verifier

| id | title | reason |
|---|---|---|
| b8p2-2 | SqliteSubnetLockingService is production-dead since 24-I2 deleted the provider dispatch; it survives in src only because one test instantiates it | Fact reproduced but not a finding: PRODUCT-MODEL section 5 as amended by the round-26 section 8 rulings defines a finding as an operator-visible wrong behaviour reproducible at HEAD or a broken-rule test finding, "nothing else"; a production-dead class produces no operator-visible behaviour. The cited "deletes machinery" sentence is section 3's Reconcile withhold contract. Earlier Info dead-code rows (23-I5, 24-I2, 24-I9) predate the ruling, and this is exactly the class it targets: a finding the loop generated from its own prior fix. |
| b8p1-2 | Hidden form inputs posted to Subnet Edit and HostIp Create are never read; EditSubnetViewModel.OriginalCidr is write-only | The candidate's own scenario concedes posting these fields "changes nothing the operator sees or the database stores" and the live drive confirms it; dead-input cleanup is not a finding under section 5, and no decision is implemented twice because the posted copy is never consulted. The "never read" claim is also false: when the subnet is deleted between GET and POST, HostIpController.Create's subnet==null branch (:117-124, :163-170 skip the re-stamp) renders the posted SubnetInfo/NetworkAddress/Cidr/SubnetRange, the only thing keeping that page from a blank name, "/0" and empty range; the filed fix and interim would introduce that regression. |
| b8p1-3 | UpdateHostIpDto data annotations never execute and its Name/RowVersion members are never read | Accurate description of the code but not a finding: nothing executes the attributes or reads the members, so there is no reachable wrong output. The cited section 5 write-path parity sentence is not violated: the DTO is not a write path and Create/Edit still accept and refuse identical input via their view models. UpdateHostIpDto exists solely to feed the `dto.IP != originalIp` branch of ValidateHostIpUpdate, which section 7 lists as accepted and never re-filed; trimming the DTO around it is a slice of the same accepted dead machinery and "while we're here" work. |
