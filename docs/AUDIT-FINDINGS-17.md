# Bastet - Round-17 Audit Findings

branch `audit/round-17` / HEAD `425ec8d` / 752 tests passing / 2026-08-08

Round 17 filed 21 findings, of which 12 are residue of round 16's own fixes.

# Critical

# High

## Q1 - A VNet prefix is declared "Every Azure subnet in this prefix is already recorded" when a contained Azure subnet is unrecorded and blocked, and the wizard's filter then hides it entirely `[x2]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:234 (the false reason string, 234-239); src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:396 (AnySubnetCanBeAdded - the `s.IsSelectable` test itself); src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:225-231 (sibling `exact.IsFullyAllocated && isTopUp` arm with the invented provenance clause); src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:164-172 (prefixHasWork) and :192-195 (the suppression it drives); src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:296-304 (suppressed-prefix note renders only when the whole tree is empty)
**Breaks:** AnySubnetCanBeAdded tests `s.IsSelectable`, which AnnotateSubnet clears for both AlreadyImported and Blocked, so "nothing selectable" becomes AlreadyImported with "Every Azure subnet in this prefix is already recorded, so there is nothing to add." With Bastet holding rig-gaps-17 10.20.0.0/16 plus a hand-made 10.20.9.0/27, the same JSON response marks Azure subnet db 10.20.9.0/26 Blocked - live Azure space no Bastet row records - while asserting completeness. prefixHasWork uses the same proxy, so ticking "Only show what would change" removes the prefix and the blocked row with no count shown. At the same moment /Subnet/Details/1 advertised 10.20.9.32-10.20.255.254 as free with a Create Subnet button; 10.20.9.32-.63 is inside db. The sibling arm at :227 reproduces independently: a target marked fully allocated by hand renders "which an Azure subnet covering the whole prefix has marked fully allocated. There is nothing left to add." while two unrecorded Azure subnets sit under it.
**Repro:** Own rig (port 5518, catalog bastet_advregd2_9741, scratch copy of HEAD). Case A: bulk-imported 10.20.0.0/16 with web+app, then POST /Subnet/Create manual-carve 10.20.9.0/27 -> 302. GET /Azure/BulkGetVNets returned the prefix AlreadyImported/selectable=False with the "every Azure subnet … is already recorded" reason in the same response as `SUBNET db 10.20.9.0/26 Blocked | Would contain existing Bastet subnet 'manual-carve'`; /Subnet/Details/1 listed 10.20.9.32-10.20.255.254 as free with a Create Subnet button. Playwright: after `pg.check("#bulk-hide-imported")`, rig-gaps-17, the prefix, the db row, its explanation and any suppressed note were all absent. Correction to the filed narrative: `_StepSelection.cshtml:46` has no `checked`, so the filter is opt-in. Case B: imported rig-mutable-17 10.40.0.0/16 with no subnets, POST /HostIp/SetAllocationStatus IsFullyAllocated=true -> 302; BulkGetVNets then emitted the "an Azure subnet covering the whole prefix has marked fully allocated" clause with both Azure subnets Available and unrecorded. Blame: :227-231, :233-238, :396-401 and prefixHasWork all 23233f2.
**Fix:** A verifier judged the filed fix unsound. Replacing `s.IsSelectable` with `s.Status != AlreadyImported` in AnySubnetCanBeAdded drops through to `Status = WillUpdateExisting; IsSelectable = true` (planner:241-247), producing a tickable prefix whose only unrecorded subnet is Blocked and therefore cannot be ticked - a commit that creates nothing, i.e. the wizard offering work that is not work. The existing 752 tests pass with it because AzureBulkImportZeroWorkTests only arranges genuinely recorded subnets. Corrected narrow fix, three edits, no new status: (1) gate the AlreadyImported arm at :234 on a companion predicate (e.g. EverySubnetIsRecorded, reusing the same containment enumeration) that requires every contained Azure subnet to have `Status == AlreadyImported`; keep today's status, selectability and wording when it holds. (2) In the else-branch - nothing selectable but some contained subnet Blocked - keep the prefix AlreadyImported and not selectable, and change only the reason to "Already imported as Bastet subnet 'X'. Some Azure subnets in this prefix could not be imported - see the rows below." (3) At :227-230 drop the invented provenance: either state "Bastet subnet 'X' is marked as fully allocated, so there is nothing left to add." unconditionally, or keep the Azure clause only when a contained Azure subnet actually covers the whole prefix (the planner already computes that when setting WillMarkFullyAllocated/FullyAllocatingAzureSubnetName). Separately and lower value: render the suppressed-prefix note at _BulkScripts.cshtml:296 whenever suppressedPrefixes > 0, not only when the tree is empty. Do NOT make prefixHasWork treat a Blocked subnet as work - a Blocked subnet changes nothing.
**Residue of:** 23233f2 (Mass Claude Audit Mess Cleanup #167); round-16 finding id unrecoverable, that round's findings file was deleted by its own reconcile.

## Q2 - Bulk import reclassifies a fully-allocated exact-match target as "AlreadyImported … nothing left to add", hiding unimported Azure subnets and offering a rename the planner then refuses `[x1]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:225 (the `isTopUp` exemption); :234 (the "every Azure subnet ... already recorded" branch); :396 (AnySubnetCanBeAdded); :495 (BuildPlanItem, the refusal with no isTopUp exemption); :326 (AnnotateSubnet asserting the same unproven attribution for a collapsed subnet); :213 (WouldRenameTarget set before the fully-allocated branch); src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:144 (isRenameOnly) and :205 / :239 (prefix checkbox enabled by renameOnly; every contained subnet disabled when the prefix is not selectable)
**Breaks:** AnnotatePrefix:225 short-circuits on `exact.IsFullyAllocated && isTopUp` without asking whether any Azure subnet covers the prefix and without calling AnySubnetCanBeAdded. Four reachable states then state a falsehood the operator cannot act on: (i) a prefix imported alone and hand-marked fully allocated reports "…which an Azure subnet covering the whole prefix has marked fully allocated. There is nothing left to add." while three unrecorded Azure subnets sit under it with all checkboxes disabled ("Its VNet prefix cannot be imported"); (ii) a genuinely collapsed target re-carved in Azure reports the same while Bastet still shows the whole /24 fully allocated and 10.171.0.128/25 is free in Azure; (iii) the only Azure subnet Blocked by a manual carve yields "Every Azure subnet in this prefix is already recorded" when it is not recorded at all, while Bastet's free-space panel offers 10.172.7.128/25; (iv) with renames requested, isRenameOnly enables the checkbox and BuildPlanItem:495 then refuses the identical item, canCommit false. Before 23233f2 state (i) produced Blocked "Bastet subnet 'X' is marked as fully allocated." - accurate and actionable.
**Repro:** Own instance (port 5211, catalog bastet_verqd1), Playwright chromium on the real pages. (i) wizard-imported 10.20.0.0/16 alone, clicked "Mark as Fully Allocated"; BulkGetVNets then returned the false attribution with web/app/db all Available server-side and all three disabled in the wizard, GO PREVIEW disabled. (ii) rig-verqd1-17 10.171.0.0/24 with subnet `whole` imported as one collapsed row; deleted `whole` in Azure and created `lower` 10.171.0.0/25 -> same message, `lower` disabled, /Subnet/Details/2 still "Fully Allocated - No child subnets can be added", /Azure/Reconcile "Everything imported from this subscription still exists in Azure." (iii) rig-verqd2-17 10.172.0.0/16 imported prefix-only plus manual child 10.172.7.0/25 -> "Every Azure subnet in this prefix is already recorded" while `sub` was Blocked, and /Subnet/Details/3 offered 10.172.7.128 as free. (iv) renamed row 1, ticked rename -> "Rename only", prefix enabled, preview error "…is marked as fully allocated.", commit disabled. Extra state: rig-verqd3-17 with zero Azure subnets produced the same Azure-attribution clause with `subnets: []`. Fixtures deleted, catalog dropped, repo clean.
**Fix:** A verifier judged the filed fix unsound. Dropping the isTopUp exemption and always returning Blocked reverts what 23233f2 landed - the contract names "a collapsed target this same VNet has already marked fully allocated" as AlreadyImported and not selectable, and that steady state is annotated correctly today. Flipping AnySubnetCanBeAdded to `Status != AlreadyImported` makes arm (iii) selectable while the Blocked child's checkbox stays disabled at _BulkScripts:239, so the commit adds nothing. Corrected narrow fix: (a) gate the :225 branch behind AnySubnetCanBeAdded so arms (i) and (ii) fall through to the truthful annotation; (b) drop the "which an Azure subnet covering the whole prefix has marked fully allocated" clause, or make it conditional on actually finding a contained Azure subnet whose prefix equals the target range - `vnet.Subnets` is in scope - and give AnnotateSubnet:326 the same treatment; (c) split only the message at :236-238 for the Blocked case, naming the subnet and its own blocker, leaving status and selectability alone; (d) close the rename arm server-side by not setting `result.WouldRenameTarget` at :213 when `exact.IsFullyAllocated`, so the client never offers a rename BuildPlanItem:495 will refuse. Do not loosen :495 here - see Q3, which fixes that guard on its own axis.
**Residue of:** none

## Q3 - Bulk import wizard offers "Rename only" on a fully-allocated Azure target, then the preview refuses the very rename it advertised `[x2]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:495 (BuildPlanItem, the unconditional refusal); :213 (WouldRenameTarget set before the fully-allocated branch); :227-231 (the AlreadyImported arm that makes the offer); src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:144-148 (isRenameOnly), :200-217 (checkbox enabled, "Rename only" badge and reason text), :164 (prefixHasWork keeps it visible under the filter), :340 (`#bulk-select-all-btn` ticks it via `:not(:disabled)`); src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:206 (CanCommit, all-or-nothing); src/Bastet/Controllers/AzureController.cs:121 and src/Bastet/Controllers/SubnetController.BulkAzure.cs:164 (one item error becomes a 400 for the whole batch)
**Breaks:** A collapsed VNet target (one Bastet row, IsFullyAllocated=1, AzureResourceId set) renamed by the operator comes back `{statusName:"AlreadyImported", wouldRenameTarget:true}` because :213 runs before the :225 branch. isRenameOnly force-enables the checkbox and promises "The only change would be renaming the Bastet subnet to match the VNet name." Preview then hits the unqualified `if (exact.IsFullyAllocated)` at :495 and errors "matched Bastet subnet 'X' … is marked as fully allocated.", CanCommit false. The error names full allocation, which is irrelevant to a rename that puts nothing inside the target, and names no remedy. Because CanCommit is all-or-nothing, one such row refuses the entire batch: the wizard's own Select all ticked it and all 13 items were refused with a 400.
**Repro:** Own rig (port 5417, catalog bastet_verifyd1), Playwright chromium. Imported rig-simple-17 10.10.0.0/24 (single subnet `whole`) -> one row, IsFullyAllocated=1; renamed to lab-core-net via /Subnet/Edit/1. Reopened the wizard: before ticking rename `{"disabled":true,"label":"… Already imported"}`; after ticking `#bulk-rename-matched` `{"disabled":false,"label":"… Rename only"}`, visible under the filter, GO-PREVIEW enabled. Preview rendered "will rename to rig-simple-17" plus "Errors - Cannot import VNet prefix 10.10.0.0/24: matched Bastet subnet 'lab-core-net' (10.10.0.0/24) is marked as fully allocated."; commit disabled. Control: the same flow on a non-fully-allocated target committed cleanly, "renamed 1 target(s)". Batch: the wizard's own Select all ticked it, 13 item cards, one error line, commit disabled. Fix validated in a scratch copy: 752/752, 0 warnings, and the live rename committed with IsFullyAllocated and its note preserved.
**Fix:** Guard the refusal with what it actually protects: at AzureBulkImportPlanner.cs:495 change to `if (exact.IsFullyAllocated && p.Subnets.Any(s => !s.FullyEncompasses))`. The error then fires exactly when the item would place something inside the target, which is the only hazard it serves; a rename-only or link-only item creates nothing. Do NOT use the `!IsSameVNet(...)` variant - with it, a same-VNet fully-allocated target selected together with ordinary child subnets produces no error at all and BulkCreateFromAzurePlanCore's child loop (SubnetController.BulkAzure.cs:365 onward) creates them, since SubnetValidationService has no fully-allocated-parent check. Do not close this by deleting WouldRenameTarget/isRenameOnly - renames must stay offered. Add a regression test pairing AnnotateAvailability and BuildPlan on one fixture (fully-allocated same-VNet target with a diverging name); AzureBulkImportZeroWorkTests exercises only AnnotatePrefix and cannot catch a disagreement between the two halves.
**Residue of:** 23233f2 (Audit 16 Cleanup #167) - the AlreadyImported-for-fully-allocated arm and WouldRenameTarget/isRenameOnly landed there while :495 (original, 73fc76f2) was never relaxed to match; round-16 finding id unrecoverable.

## Q4 - The first unallocated range names the network address as free space and its size contradicts its own start/end; a one-address head gap renders as "0 IP addresses" `[x2]`
**Where:** src/Bastet/Services/IpUtilityService.cs:306-315 (the head branch; the count at :313); :230-255 (the no-children early return, `subnetSize - 2` over a full span); :330-349 (the tail branch, `lastIp--`); src/Bastet/Views/Subnet/Details/_UnallocatedRanges.cshtml:38-40 (renders StartIp/EndIp/AddressCount verbatim) and :46-52 (seeds the Create Subnet button from StartIp)
**Breaks:** All three branches report fewer allocatable addresses than the IPAM will actually accept, and the head branch prints a span it then contradicts. 10.100.0.0/24 with child 10.100.0.1/32 renders a row `10.100.0.0 | 10.100.0.0 | 0 IP addresses` with a live Create Subnet button - and creating 10.100.0.0/32 succeeds. 10.0.0.0/24 with child 10.0.0.64/26 renders `10.0.0.0 | 10.0.0.63 | 63 IP addresses`, a 64-address span labelled 63; a /26 fits and the row then vanishes. An empty /24 renders `254 IP addresses` over a 256-address span. The tail branch has the same defect mirrored: `10.3.0.128 | 10.3.0.254 | 127 IP addresses` while 10.3.0.128/25 (128 addresses, including .255) is accepted. Rule 1's second half - the free-space table systematically states less allocatable space than exists, on every subnet with a head or tail gap.
**Repro:** Own rig (port 5451, catalog bastet_verifyb3), all POSTs through the real HTTP API. (1) 10.100.0.0/24 + child 10.100.0.1/32 -> rows `['10.100.0.0','10.100.0.0','0 IP addresses','Create Subnet']` and `['10.100.0.2','10.100.0.254','253 IP addresses']`; POST 10.100.0.0/32 -> 302. (2) 10.0.0.0/24 + child 10.0.0.64/26 -> `['10.0.0.0','10.0.0.63','63 IP addresses']`; POST 10.0.0.0/26 -> 302 and the head row vanished. (3) empty 10.2.0.0/24 -> `['10.2.0.0','10.2.0.255','254 IP addresses']`. (4) 10.3.0.0/24 + child 10.3.0.0/25 -> `['10.3.0.128','10.3.0.254','127 IP addresses']`; POST 10.3.0.128/25 -> 302. Blame: line 313 is bf120d6, whose hunk replaced the conditional count with the unconditional `Start - currentPosition - 1` and added the pinning test.
**Fix:** A verifier judged the filed fix unsound in both variants. Suppressing the zero-count row converts a mis-sized report into a silent omission of genuinely allocatable space (the /32 create succeeds), and copying the tail branch's convention onto the head propagates the same under-count, since the tail is wrong in the same direction. Corrected narrow fix - put all three branches on the allocatable-span axis so `AddressCount == EndIp - StartIp + 1` everywhere and matches what ValidateSubnetCreation accepts: (1) delete the `currentPosition == startIp && cidr < 31` special case at :306-315 outright - the existing `else` at :316-325 already emits the true span; (2) at :230-255 collapse the two arms of the no-children early return to `AddressCount = subnetSize`; (3) at :330-349 drop `if (cidr < 31) { lastIp--; }` and relax the entry condition to `currentPosition <= endIp`. Round 16's top-of-address-space protection survives: currentPosition is already a `long` and `(long)End + 1` cannot wrap. Expect to rewrite the tests that encode round 4's opinion - SubnetPropertyCalculationTests.cs:185 (254->256), :201 (127->128), :222 (0->1, and its name becomes wrong), :235 (1->2), :248 (start/end .254->.255, 1->2); IpUtilityServiceTests stays green except for counts on gaps that now include a boundary address. Stated separately, not part of this fix: if the Size column is meant to be a usable-host count, the arithmetic is fine and the display is wrong - rename the column, print StartIp=network+1 / EndIp=broadcast-1, and stop seeding Create Subnet from an excluded address.
**Residue of:** bf120d6 (Audit 4 Cleanup #141), bullet "Always exclude the network address from a leading unallocated range count"; round-4 finding id unrecoverable.

## Q5 - Reconcile can never report an Azure range removed while the resource still exists: the inventory drops zero-IPv4 VNets and subnets, so a range removal is classified as an absence and permanently vetoed `[x1]`
**Where:** src/Bastet/Services/Azure/AzureService.cs:118-121 (the `continue` that omits an IPv4-less VNet); :170-181 (BuildInventorySubnetRows returns zero rows for an IPv6-only subnet); src/Bastet/Controllers/AzureController.cs:88 (BulkGetVNets, where the filter should move); src/Bastet/Services/Azure/AzureReconciler.cs:288-290 (the VNetDeleted reason whose "or no longer has any IPv4 address space" clause becomes unreachable after the fix)
**Breaks:** Azure accepts an IPv6-only subnet inside a dual-stack VNet and an IPv6-only VNet address space. When the IPv4 range is dropped, the resource id never reaches `liveSubnetPrefixes`/`liveVNets`, so EvaluateSubnetLevel/EvaluateVNetLevel classify it as SubnetDeleted/VNetDeleted - an absence. ConfirmProposedDeletionsAsync then asks ARM directly, ARM answers 200, and ApplyConfirmations moves the item into the `stillLive` warning. Reconcile reports nothing deletable and prints "…were missing from the subscription listing but still exist in Azure, so they have been withheld from deletion", telling the operator the resource is fine while Azure no longer has that range at all. Every subsequent scan repeats the veto, so Bastet pins the range as allocated forever - against "never pin an allocation record Azure says is gone. A withheld deletion is not a safe default."
**Repro:** Own rig (port 5766, catalog bastet_verA1x, own fixture rig-verifya1-17 10.99.0.0/16 + 2001:db8:99::/48 with subnet `dual` 10.99.1.0/24 + 2001:db8:99::/64, imported through the real wizard; baseline scan clean). Step 1: `az network vnet subnet update -n dual --address-prefixes 2001:db8:99::/64` -> ReconcileScan items [], warnings ["1 Azure-linked subnet(s) … withheld from deletion: 'dual' (10.99.1.0/24)."]. Step 2: `az network vnet update --address-prefixes 2001:db8:99::/48` -> same shape naming both rows. Control: the identical removal expressed as an IPv4 re-range produced a proper SubnetPrefixChanged item, canCommit true. Fixture deleted, catalog dropped. Origin: `git log -L 118,121` -> 73fc76f (Azure Bulk Import #108), not an audit round.
**Fix:** Stop dropping these resources from the inventory. Delete the `if (vnetVm.Ipv4AddressPrefixes.Count == 0) { continue; }` block at AzureService.cs:118-121, and in BuildInventorySubnetRows (:170-181) emit one BulkAzureSubnetViewModel carrying the resource id and name with BOTH `AddressPrefix` empty and `Ipv4AddressPrefixes` empty when `prefixes` is empty - that pair is what makes `Ipv4PrefixesOf` (AzureReconciler.cs:325-328) return [] and produce the "now none" wording; populating either defeats it. EvaluateVNetLevel then yields VNetPrefixRemoved and EvaluateSubnetLevel yields SubnetPrefixChanged, both non-absence, so no delete is proposed on an absence claim. Carry one rider in the same change: drop the now-unreachable "or no longer has any IPv4 address space" clause from the VNetDeleted reason at AzureReconciler.cs:288-290. To leave the selection screen unchanged, apply the zero-IPv4 filter as a VNet-level filter in AzureController.BulkGetVNets (:88) only - never filter empty-prefix subnet rows out of the shared inventory, which is precisely what the reconciler needs. The wizard is already safe without that optional filter: AnnotatePrefix is not reached for a VNet with no IPv4 prefixes, and an empty-prefix subnet row is Blocked at planner:344.
**Residue of:** none

## Q6 - Reconcile step 1 still advertises the un-imported-range hunt and the hand-made-content cascade, both deleted machinery `[x2]`
**Where:** src/Bastet/Views/Azure/Reconcile/_StepSubscription.cshtml:5-8 (clause (a) "plus Azure ranges BASTET does not record" at :6; clause (b) the cascade sentence at :7-8); src/Bastet/Views/Subnet/Details/_UnallocatedRanges.cshtml:16-21 ("Run Azure Reconcile to check before allocating from these ranges")
**Breaks:** (a) BuildPlan only emits items from EvaluateSubnetLevel/EvaluateVNetLevel, both keyed on `snapshot.AzureResourceId`; there is no producer that walks the Azure inventory for unrecorded ranges, and 23233f2 deleted ReportAzureRangesNoBastetSubnetRecords. An operator who partially imports a subscription and takes step 1 at its word believes every Azure range is recorded when most are not. (b) Neither cascade can happen: a snapshot with `ManualDescendantCount > 0 || HostIpCount > 0` is diverted to HeldByManualContent, and WithholdTargetsWhoseCascadeIsBlocked removes any item with a different-subscription descendant, so a row offered for deletion never contains either. (c) The Subnet Details banner is the harmful one: it truthfully warns that un-imported Azure space still shows as free, then points at a check that returns a clean all-clear on exactly that data, directly above a Create Subnet button.
**Repro:** Own rig (port 5327, catalog bastet_verq17a); both sentences rendered verbatim from GET /Azure/Reconcile. Partially imported rig-gaps-17 (prefix + `web` only), leaving app 10.20.5.0/24, db 10.20.9.0/26 and three whole VNets unrecorded; POST /Azure/ReconcileScan -> `{"items":[],"reviewItems":[],"globalErrors":[],"warnings":[],"canCommit":false}`. Added child `handmade` and repointed row 2's link -> reviewItems HeldByManualContent plus the manual-content warning; then set row 3's AzureResourceId to a foreign subscription -> the different-subscription warning, items still empty. Chromium dump of /Subnet/Details/1 showed "Run Azure Reconcile to check before allocating from these ranges" above `10.20.2.0 - 10.20.255.254 | 65,023 IP addresses | [Create Subnet]`, a range containing app and db, while BulkGetVNets reported both as Available/selectable. Blame: _StepSubscription :6-8 = d18327e; _UnallocatedRanges :11-23 = 8afa2df.
**Fix:** Text only; no reconciler code change, no new status, no new withhold - re-adding the range hunt is what 23233f2 removed and the contract forbids. The fix must land on all three lines or it closes the wrong half. (1) _StepSubscription.cshtml:5-8: state only that BASTET reports Azure-linked rows whose VNet or subnet no longer exists in Azure, and rows whose Azure resource's current prefix list no longer contains the recorded range; drop "plus Azure ranges BASTET does not record" outright. (2) The cascade sentence: "Deleting a flagged subnet archives the Azure-imported subnets beneath it. Rows that hold subnets or host IP assignments created here are listed under Needs review and are never deleted." (_StepReview.cshtml:72 renders literally "Needs review".) (3) _UnallocatedRanges.cshtml:19: repoint at `asp-controller="Azure" asp-action="BulkImport"` and keep the preceding warning sentence, which is the only true statement in the paragraph and load-bearing for Rule 1. Non-blocking and out of scope: both the old and new links are Admin- and flag-gated while the panel renders for any View-role user (see Q9).
**Residue of:** d18327e (Audit 16 Cleanup #165), fix "State on the reconcile subscription step what a delete actually archives" - both clauses were true when written and were invalidated by 23233f2, which deleted the producer and added the manual-content hold without touching this view; round-16 finding id unrecoverable. The third site is older: 8afa2df (Audit 14 Cleanup #160).

## Q7 - Bulk import permanently refuses an Azure subnet whose Bastet parent is more specific than the VNet prefix, while the app's own Create form accepts the identical range `[x1]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:368-378 (AnnotateSubnet's moreSpecificParent block); :724-730 (DetectExistingBastetSubnetConflicts raises the same refusal as a GlobalError on the commit path); src/Bastet/Controllers/SubnetController.BulkAzure.cs:360 and :385 (planned children always parented to `targetSubnet.Id`, which is why the refusal exists); src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:438 (FindDeepestContainer, already available); :359-365 (the sibling containment guard, which must NOT be removed); :396 (AnySubnetCanBeAdded, which compounds the false prefix message)
**Breaks:** With Bastet holding rig-gaps-17 10.20.0.0/16 and a hand-made covers-app 10.20.4.0/22 under it, Azure subnet app 10.20.5.0/24 is Blocked "Has a more specific existing Bastet parent 'covers-app' (10.20.4.0/22), so it cannot be imported into this VNet prefix." - a refusal naming no remedy - and the commit path repeats it as a GlobalError. POST /Subnet/Create with ParentSubnetId=covers-app creates the identical range without complaint, so the IPAM itself has no objection; a rule that holds for a manually created subnet must hold identically for an imported one. After creating it by hand the Azure subnet becomes permanently unlinkable ("Bastet subnet 'manual-app' already uses 10.20.5.0/24") - no writer for AzureResourceId exists outside the wizard - so Azure can never be the source of truth for it and reconcile can never report it deleted or re-ranged. Compounding: clearing IsSelectable makes the whole prefix report "Every Azure subnet in this prefix is already recorded", so no UI path even reaches the commit refusal.
**Repro:** Own rig (port 5241, catalog bastet_verifqb2). Seeded via BulkCreateFromAzurePlan, then POST /Subnet/Create covers-app 10.20.4.0/22 ParentSubnetId=1 -> 302. BulkGetVNets: app Blocked with the more-specific-parent reason, selectable False. POST /Subnet/Create manual-app 10.20.5.0/24 ParentSubnetId=4 -> 302, row 5 with AzureResourceId null; re-annotating -> "already uses 10.20.5.0/24", and no writer for AzureResourceId exists outside the wizard. With that row removed, BulkImportPreview -> globalErrors carrying the same text, canCommit false; BulkCreateFromAzurePlan -> HTTP 400. Base validation probed directly: POST /Subnet/Create 10.20.5.0/24 ParentSubnetId=1 -> 200 with "A more specific parent subnet exists: covers-app (10.20.4.0/22). Please select it instead."
**Fix:** A verifier judged the filed fix unsound as ordered. Deleting the two guards alone does not push an invalid hierarchy into the database - ValidateSubnetCreation (SubnetController.Helpers.cs:222-240) already rejects it - but it makes the wizard offer app as Available and selectable and then roll the whole transaction back with the generic "The import could not be applied.", converting a specific refusal into a late opaque whole-batch failure and offering work it structurally cannot perform. Corrected fix, inverted order, single change: (a) in AzureBulkImportPlanner, resolve each planned child's parent with the existing FindDeepestContainer(network, cidr, existingSubnets) at :438 and record the resolved parent id/name on BulkImportPlannedChildSubnet; (b) in SubnetController.BulkAzure.cs replace the hardcoded `ParentSubnetId = targetSubnet.Id` at :360 and :385 with that resolved id, falling back to `targetSubnet.Id` when no deeper container exists, resolved against treeCache so children created earlier in the same batch can parent later ones; (c) extend DescribeApprovedPlanDivergences / BulkImportExpectedTargetDto so a change in a child's resolved parent between preview and commit is reported like the other divergences; (d) only then delete planner:368-378 and :724-730, which become dead. Do NOT remove the sibling guard at :359-365 - it mirrors a rule the manual Create form also enforces with no remedy available, so it is a correct refusal.
**Residue of:** none

## Q8 - Subnet delete confirmation is bound to the reviewed subtree but not to the reviewed subnet's own range, so a widened row is archived on a stale approval `[x1]`
**Where:** src/Bastet/Controllers/SubnetController.Delete.cs:176-183 (the stale-scope branch, re-deriving only MaxDescendantSubnetIdAsync and MaxSubtreeHostIpTicksAsync); :32-43 (the confirmation built from ConfirmedMaxSubnetId / ConfirmedMaxHostIpTicks only); src/Bastet/Views/Subnet/Delete/_DeleteConfirmationForm.cshtml (posts only those two hidden fields plus Id and confirmation); src/Bastet/Models/Subnet.cs:44 (`[Timestamp] RowVersion`, already present)
**Breaks:** Nothing binds the reviewed row's own NetworkAddress/Cidr/Name/Description/AzureResourceId/IsFullyAllocated to the approval. Both scope numbers are 0 for a childless leaf and stay 0 no matter how the target row itself is mutated, so an operator who reviewed and approved archiving "DMZ 10.77.0.0/24" can have a /16 archived instead if another operator widened the row in between - 256x the reviewed space, irreversibly, with the success banner reporting the row's new name and no mention that anything changed. Rule 2: never destroy an allocation record on incomplete information. The reversible Edit path already refuses on a RowVersion mismatch, so the irreversible operation is currently weaker than the reversible one.
**Repro:** Own rig (port 5297, catalog bastet_vlockb1, scratch copy of HEAD). Run 1: created DMZ 10.77.0.0/24; GET /Subnet/Delete/1 rendered the range as 10.77.0.0/24 and carried only `confirmedMaxSubnetId=0` / `confirmedMaxHostIpTicks=0` (no RowVersion, no NetworkAddress, no Cidr); POST /Subnet/Edit/1 widened Cidr 24->16 -> 302; replaying the stale delete body -> 302 with no refusal, Subnets count 0, DeletedSubnets row `DMZ|10.77.0.0|16`. Run 2 (CORE 10.88.0.0/24, widened to /16 and renamed CORE-WIDENED): the stale delete succeeded and the banner read "Subnet 'CORE-WIDENED' and 0 child subnet(s) were deleted successfully."
**Fix:** Carry the reviewed row's `RowVersion` in DeleteSubnetViewModel and as a hidden field on _DeleteConfirmationForm.cshtml; inside DeleteConfirmedCore (i.e. inside ExecuteWithSubnetLockAsync), alongside the existing check at :176, take the stale branch when the loaded `subnet.RowVersion` is non-null and the posted value is null OR differs - a missing posted token must be a refusal, not a skip, or omitting the hidden field bypasses the guard. Guard on the LOADED value being non-null (not the posted one) so SQLite, where `[Timestamp]` has no value generator, does not fail every delete; do not copy HostIpValidationService.CompareRowVersions, which returns false on a null. Word the message as "the subnet itself changed since it was reviewed". Keep the subtree max-id/max-ticks checks - RowVersion does not move when a descendant is inserted. Any regression test must seed RowVersion explicitly (as SubnetControllerConcurrencyRedisplayTests.cs:72 does) or it passes vacuously under the in-memory SQLite harness. Accepted cost, stated not hidden: a Tags edit or a SetAllocationStatus toggle between GET and POST also triggers the refusal - a re-review prompt on an irreversible operation, and the same stance Edit already takes.
**Residue of:** d18327e (Audit 16 Cleanup #165), bullet "Bind the reviewed subtree to the subnet delete confirmation"; round-16 finding id unrecoverable.

## Q9 - The free-space panel tells every viewer to "Run Azure Reconcile", a page Admin-only and feature-flag gated, and the check it names does not answer the question `[x1]`
**Where:** src/Bastet/Views/Subnet/Details/_UnallocatedRanges.cshtml:19 (the only `asp-controller="Azure"` outside Views/Azure and _Layout); src/Bastet/Controllers/AzureController.cs:11 (`[Authorize(Policy = "RequireAdminRole")]`) and :133-136 (403 redirect when BASTET_AZURE_IMPORT is not true); src/Bastet/Program.cs:214-215 (RequireAdminRole is Admin only); src/Bastet/Views/Shared/_Layout.cshtml:46-60 (the correct gating pattern); src/Bastet/Controllers/SubnetController.Read.cs:31 (Details is RequireViewRole)
**Breaks:** The panel gates the remedy sentence on nothing. A View/Edit/Delete-role operator - exactly the person about to allocate - lands on AccessDenied; with the flag off, everyone including Admins is redirected to a 403 reading "Azure Import feature is not enabled" while the nav six lines away correctly hides the same link. Worse, even for an Admin with the flag on the named check does not perform the promised verification: AzureReconcileStatus is keyed entirely on linked resource ids and reconcile never hunts for un-imported Azure space, so an operator following the app's own Rule 1 hedge gets a false all-clear and then allocates over Azure-assigned space.
**Repro:** Own rig (port 5217, catalog bastet_verD1). Flag off: created a row and set AzureResourceId to a real VNet id; GET /Subnet/Details/1 rendered `<a href="/Azure/Reconcile">Run Azure Reconcile</a>` while "Bulk Azure Import" was absent from the nav; GET /Azure/Reconcile -> 302 /Error/403 -> 403 "Azure Import feature is not enabled". Flag on: link present, nav present, /Azure/Reconcile 200. Then set the row to 10.10.0.0/24, matching rig-simple-17 whose subnet `whole` 10.10.0.0/24 was never imported: Details listed 10.10.0.0-10.10.0.255 as free with the instruction, POST /Azure/ReconcileScan returned `{"items":[],"reviewItems":[],"globalErrors":[],"warnings":[],"canCommit":false}`, and BulkGetVNets reported `whole` as Available/selectable. Role half not driven live - DevAuthHandler issues Admin unconditionally in Development - and rests on the attribute plus the policy definition.
**Fix:** A verifier judged the filed fix unsound: `AzureController.IsAzureImportEnabled()` is `internal static` in Bastet.dll and views compile into a separate Bastet.Views.dll with no InternalsVisibleTo, so a view cannot call it (which is why _Layout reads the environment variable inline); and gating alone leaves the wrong remedy named for the population that can reach it. Corrected narrow fix, in _UnallocatedRanges.cshtml only: keep the warning sentence unconditional - it is the Rule 1 hedge every role needs; replace the remedy with `asp-controller="Azure" asp-action="BulkImport"` ("Run a Bulk Azure Import against this VNet to see the Azure subnets BASTET has not recorded"), which is the surface that actually reports un-imported Azure subnets and the same remedy AzureReconciler.cs:296 and :318 already name; render that link only when `bool.TryParse(Environment.GetEnvironmentVariable("BASTET_AZURE_IMPORT"), out bool e) && e` and `UserHasRole(ApplicationRoles.Admin)`, expressed inline exactly as _Layout.cshtml:46 does; when suppressed, close the sentence as plain prose ("ask an administrator to run a Bulk Azure Import before allocating from these ranges") rather than dropping the next step. No write path gains a guard. (Alternatively make IsAzureImportEnabled public and have _Layout call it too, so the flag test exists once.)
**Residue of:** none (origin 8afa2df, Audit 14 Cleanup #160, which authored the whole block; round-14 finding ids unrecoverable)

## Q10 - Reconcile deletes the finding, not just the delete: a cascade-blocked row's range change is never reported `[x1]`
**Where:** src/Bastet/Services/Azure/AzureReconciler.cs:245-266 (WithholdTargetsWhoseCascadeIsBlocked; the `plan.Items.RemoveAll(blocked.Contains)` at :263), reached from :117 (liveLinked), :121 (different-subscription descendants), :126 (manual-content descendants) and :223 (ApplyConfirmations NotVisible/Unknown/Live cascade)
**Breaks:** The helper destroys the item outright and leaves one warning that says only that the row was withheld from deletion - no status, no reason, no remedy. The item's own truthful text ("…no longer has the address prefix 10.180.0.0/16. Delete it here if you want to, then use the Azure import wizard to bring in the VNet's current address space.") is thrown away. Reconcile exists to do exactly two things - report Azure resources that are gone and report Azure resources whose range changed - and both are suppressed for any row with a live linked descendant. Bastet goes on asserting the stale range, and every subsequent scan repeats the same contentless warning. It applies to every status, absence and change alike.
**Repro:** Own rig (port 5310, catalog bastet_verq17d, own fixtures rig-vdq-17 / rig-vdq2-17). Control: imported 10.200.0.0/16 + qsub 10.200.1.0/24, scan clean. (1) `az network vnet update --address-prefixes 10.200.0.0/15` -> items [], reviewItems [], warnings ["1 subnet(s) were withheld from deletion because archiving them would also archive Azure-linked subnet(s) beneath them that still exist in Azure: 'rig-vdq-17' (10.200.0.0/16)."]; "no longer has the address prefix" appeared nowhere in the response, and the entire visible text of wizard step 2 was that one warning with both result sections `d-none`. (2) higher consequence: with a second VNet imported beneath it, `az network vnet delete -n rig-vdq-17` -> items carried only the child's SubnetDeleted; row 1's VNetDeleted item was produced and destroyed, so the operator is never told the VNet is gone while Bastet keeps a top-level /16 for it.
**Fix:** In WithholdTargetsWhoseCascadeIsBlocked (:245-266) stop destroying the item: move each blocked item into `plan.ReviewItems` with its Status and Reason preserved verbatim, as the HeldByManualContent path at :95-104 already does. Do NOT append an invented remedy - the existing Reason already carries the app's real one; append only a factual cascade sentence saying reconcile will not perform the delete and naming what stands in the way, or the combined message reads "Delete it here if you want to" beside "withheld from deletion". Fix it inside that one helper and nowhere else - all four call sites go through it and the `because` string is already parameterised. Deletion stays blocked: SubnetController.AzureReconcile.cs:64-101 builds `stillStale` from `plan.Items` only. No view change is needed: _ReconcileScripts.cshtml:177-186 already maps all six statuses and :248-259 renders review items with badge and reason. Separate, do not smuggle in: whether the liveLinked withhold at :117-119 belongs at all is a behaviour change to the delete path; and the `noLongerStale` wording at AzureReconcile.cs:95-101 is inaccurate for these ids either way.
**Residue of:** a8f669b (Audit 8 Cleanup #152), bullet "Stop Azure reconcile archiving descendants the same scan proved live or withheld" - blame attributes the whole helper body including the RemoveAll to that commit; round-8 finding id unrecoverable.

# Medium

## Q11 - The Create form refuses a subnet name the Edit form stores: [SafeText] is on CreateSubnetViewModel.Name only `[x2]`
**Where:** src/Bastet/Models/ViewModels/SubnetViewModels.cs:11 (`[SafeText]` on Create only); src/Bastet/Models/ViewModels/EditSubnetViewModel.cs:23 (the counterpart without it); src/Bastet/Services/Security/InputSanitizationService.cs:11 (SafeTextPattern) and Services/Security/ValidationAttributes.cs:27; src/Bastet/Controllers/SubnetController.BulkAzure.cs:280 and :353 (AzureImportSubnetViewModel built in code, DataAnnotations never run); src/Bastet/Controllers/SubnetController.Helpers.cs:105 (ValidateSubnetCreation performs only IP/hierarchy checks); test/Bastet.Tests/Security/SubnetViewModelValidationParityTests.cs (the parity test that exists for exactly this and never covers [SafeText]); src/Bastet/Models/ViewModels/HostIpViewModels.cs:26 and :47 (already agree - listed so the fix does not touch them)
**Breaks:** `SafeTextPattern` is `^[a-zA-Z0-9\s\-_.,!?@#$%&()+=]*$`, so `/`, `:`, `;`, `'`, `"`, `[`, `]`, `|` and every non-ASCII letter are refused by Create and accepted by Edit. An operator renames a subnet to "core/edge (site A)" successfully, then cannot create its sibling "core/edge (site B)" - refused with "Subnet name contains invalid characters" while the tree on screen displays a subnet holding exactly that character, put there by the app itself. The message names neither the permitted set nor a remedy. Not a security defect: [NoHtml] is on both forms and blocked a markup payload on Edit, and stored names render Razor-encoded on /Subnet and /Subnet/Details.
**Repro:** Own rig (port 5921, catalog bastet_vsecb1). Create refused `Prod: DC1`, `Zürich core`, `core/edge (site B)` and `Café / "core"`, each on its own free /16, all HTTP 200 with "Subnet name contains invalid characters" and nothing written. Edit accepted every one of the same strings on subnet 1, each 302, each verified in the DB. Markup probe on Edit -> HTTP 200, row unchanged; /Subnet and /Subnet/Details both render `Caf&#xE9; / &quot;core&quot;`. Blame: `[NoHtml]` at EditSubnetViewModel.cs:23 from 8cefc64, whose bullet list contains "Reject on the Subnet Edit form the input Create already refuses"; `git log -S SafeText` on that file is empty, and on SubnetViewModels.cs returns 3940c98 (#43).
**Fix:** A verifier judged the filed fix unsound. Adding `[SafeText]` to EditSubnetViewModel.Name makes every row already named outside the allow-list uneditable in every respect: SubnetController.Edit.cs:63 gates the save on `ModelState.IsValid` and the GET prefills Name from the row, so a description-only change to a row named `Café / "core"` comes back invalid and writes nothing. Such rows demonstrably exist (Edit creates them, and SubnetCreateGetPrefillTests is parameterised on parents named `Prod/Web`, `Bob's Lab`, `DC1:Core`). Corrected narrow fix, in the removal direction: delete `[SafeText]` from CreateSubnetViewModel.Name (SubnetViewModels.cs:11) so all write paths agree on `[Required][StringLength(100)][NoHtml][SanitizeName]` - [NoHtml] is what actually blocks markup and is already on both forms. No test asserts that the Create view model rejects a non-SafeText name: SubnetCreateGetPrefillTests constructs `new SafeTextAttribute()` directly, and GeneratedNameSafeTextTests / SubnetNamingSafeTextTests / AzureBulkImportTargetNameTests exercise `SubnetNaming.ToSafeText` and the planner. Add the parity assertion (same attribute set per shared property) so a future divergence fails the build. Separate commits, not folded in: `SubnetNaming.ToSafeText` and its caller at SubnetController.Create.cs:56 become unnecessary afterwards; and ValidateSubnetCreation never evaluates DataAnnotations at all, so the bulk-import commit applies neither rule.
**Residue of:** 8cefc64 (Audit 5 Cleanup #142), fix "Reject on the Subnet Edit form the input Create already refuses", which added [NoHtml] and left [SafeText] unmirrored; round-5 finding id unrecoverable.

## Q12 - Multi-prefix VNet target naming is derived from the selection in the planner but from the VNet in the annotator, so one-prefix-at-a-time imports create two identically named rows and the wizard offers a rename that renames nothing `[x2]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:134-141 (multiPrefixVNetIds, built from the selection); :803-814 (TargetName, the selection-shaped rule used for the rename decision at :511-518 and the AutoCreate names at :531 and :547); :403-412 (ProposedTargetName, keyed off `vnet.Ipv4AddressPrefixes.Count > 1`); :213-214 (the WouldRenameTarget assignment); src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:144 (isRenameOnly), :164 (prefixHasWork), :205 (checkbox enablement), :209-217 (the "Rename only" badge and promise); src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:81-93 (BulkImportSelectedVNetPrefixDto) and _BulkScripts.cshtml:411-436 (buildSelectionFromUI, which already holds `vnets[vIdx].ipv4AddressPrefixes`); test/Bastet.Tests/Azure/AzureBulkImportZeroWorkTests.cs:250 (asserts WouldRenameTarget for exactly this shape without checking it against BuildPlan), :184 and :215 (the flag-vs-plan agreement tests, single-prefix only)
**Breaks:** The two halves disagree in both directions. Importing rig-multi-17's prefixes one at a time never qualifies the name, so 10.30.0.0/16 and 172.16.0.0/20 both become top-level rows named `rig-multi-17` - two disjoint ranges, indistinguishable in the tree, defeating the qualifier's whole purpose; importing both at once yields the qualified names, so identical inputs give different names depending only on how many boxes were ticked. Conversely, for a correctly qualified row, ProposedTargetName computes a difference and sets WouldRenameTarget, so the wizard badges the prefix "Rename only" and promises a rename that BuildPlan does not perform (renamed 0). And on a prefix that also has a new Azure subnet, the annotation says wouldRenameTarget=false while the commit silently strips the qualifier back to the bare VNet name.
**Repro:** Own rig (port 5738, catalog bastet_vd2b). (1) Preview+commit for 10.30.0.0/16 + p1sub -> `autoCreateTargetName='rig-multi-17'`, then the same for 172.16.0.0/20 + p2sub -> the same name again; DB rows 1 and 3 both named rig-multi-17. Both prefixes in one selection instead produced `rig-multi-17 (10.30.0.0-16)` and `rig-multi-17 (172.16.0.0-20)`. (2) BulkGetVNets `wouldRename=True`; preview with `renameMatchedBastetSubnets=true` -> `willRename=False newName=None canCommit=True`; commit -> `renamedTargets:0`, name unchanged. (3) Playwright: both prefixes `disabled=False` with "Rename only" and "The only change would be renaming the Bastet subnet to match the VNet name". (4) Reverse arm: with rows correctly qualified and p2sub unimported, BulkGetVNets said `wouldRename=False` while preview said `willRename=True newName='rig-multi-17'` and the commit reported `renamedTargets:1`, leaving row 7 as bare `rig-multi-17`.
**Fix:** A verifier judged the filed fix unsound. Setting WouldRenameTarget only when the name differs from both candidates touches the annotation alone: duplicate-name creation is untouched, and the reverse arm gets strictly worse - the wizard would report no rename while the commit still strips the qualifier, turning a visible false promise into a silent unannounced rename. Dropping the qualification from ProposedTargetName makes the annotation agree by adopting the wrong rule and documents the duplicate names. Corrected fix - one naming rule, keyed off the VNet's own IPv4 prefix count: add the VNet's IPv4 prefix list (or its count) to BulkImportSelectedVNetPrefixDto; populate it in buildSelectionFromUI from `vnets[vIdx].ipv4AddressPrefixes`; have TargetName (:803-814) qualify from that; delete multiPrefixVNetIds (:133-141) and its threading through BuildPlanItem, leaving ProposedTargetName as the single implementation so WouldRenameTarget agrees with WillRename by construction. Treat a missing or short list as "not multi-prefix" for direct API callers - the field only ever affects a name, never a range - and keep the qualified name going through `SubnetNaming.WithSuffix` with MaxSubnetNameLength.
**Residue of:** 23233f2 (Mass Claude Audit Mess Cleanup #167) - it introduced ProposedTargetName/WouldRenameTarget, which collide with the selection-shaped mechanism from 8afa2df (Audit 14 Cleanup #160); no finding id exists on disk for either half.

## Q13 - Toggling "Rename matched Bastet subnets to VNet names" silently discards the whole selection but leaves "Next: Preview" enabled, so the wizard posts an empty body `[x2]`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:333 (the `#bulk-rename-matched` change handler); :349-367 (the `#bulk-hide-imported` sibling that saves, restores and calls updateGoPreviewBtn - the reference behaviour); :122-125 and :134-137 (the loadVNets failure paths, which leave the previous subscription's tree, ticks and an enabled preview button in place)
**Breaks:** The handler calls invalidatePlan() then renderVNetTree(); renderVNetTree empties `#bulk-vnet-tree` and rebuilds every checkbox unticked, and unlike its sibling it neither snapshots/restores the ticked state nor calls updateGoPreviewBtn(). The operator curates a selection, ticks the rename switch - precisely because they want renames applied to the batch they just built - and the tree silently empties while "Next: Preview" stays enabled from the previous state. Pressing it posts `{"vNetPrefixes":[]}` and step 3 reads "Cannot import: No VNet address prefixes were selected."
**Repro:** Own rig (port 5461, catalog bastet_verifyuib2), Playwright driving the real wizard with a request listener on the preview POST. After Select all: prefixes 12/13, subnets 14/15, previewBtnDisabled=False. After `pg.check("#bulk-rename-matched")`: prefixes 0/13, subnets 0/15, previewBtnDisabled=False, no message. Captured POST body: `{"subscriptionId":"…","subscriptionName":"Main","vNetPrefixes":[],"renameMatchedBastetSubnets":true}`; step 3 rendered the global error. Control in the same build: `#bulk-hide-imported` ON then OFF preserved every tick. Provenance: 23233f2 replaced the one-line `on("change", invalidatePlan)` with the version that re-renders.
**Fix:** Extract the snapshot / renderVNetTree() / restore / updateGoPreviewBtn() sequence at :350-366 into one named function and call it from both change handlers - a named function, not a duplicated block, so the two cannot drift again. This is correct on the rename axis in both directions: turning rename ON only widens the enabled set, so restoring the snapshot onto `:not(:disabled)` boxes reinstates every prior tick and leaves newly offered rename-only prefixes unticked; turning it OFF disables a previously ticked rename-only prefix, whose key the same restore loop drops, and the trailing updateGoPreviewBtn() re-derives the button honestly. Carry the same treatment to the loadVNets failure paths at :122-125 and :134-137, which otherwise let a failed reload after switching subscription post the old subscription's prefixes under the new subscriptionId.
**Residue of:** none

## Q14 - "Only show what would change" hides WillUpdateExisting prefixes, suppressing the work of linking an existing unlinked Bastet subnet to Azure `[x1]`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:164-172 (prefixHasWork) and :192 (the suppression); :296-304 (the empty-state banner asserting the hidden prefixes are "either already imported or cannot be imported"); src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:241-247 (WillUpdateExisting + IsSelectable for an unlinked exact match); src/Bastet/Controllers/SubnetController.BulkAzure.cs:239-266 (the write that makes it real work)
**Breaks:** prefixHasWork returns true only for a contained selectable subnet, `statusName === "Available"`, or rename-only. A prefix whose exact-match Bastet row is not yet linked to this VNet comes back WillUpdateExisting with IsSelectable=true - selecting it writes the VNet resource id onto the row and counts in linkedTargets, and the contract says linking a not-yet-linked target must stay offered - but with zero selectable contained subnets it falls through to false and the whole prefix vanishes under the filter, counted into suppressedPrefixes. When it is the only survivor the banner asserts it is already imported or unimportable, both false. Downstream the row never gets an AzureResourceId, so reconcile can never report that VNet deleted or re-ranged.
**Repro:** Own rig (port 5491, catalog bastet_verui_a2, own fixture rig-vera2-17 10.92.0.0/16 with its only subnet deleted), plus hand-created Bastet row manual-92 10.92.0.0/16, AzureResourceId NULL. BulkGetVNets returned `"statusName":"WillUpdateExisting","reason":"Will import into existing Bastet subnet 'manual-92'.","isSelectable":true`. Playwright: with the switch off the prefix rendered "Will update existing"; after `pg.check("#bulk-hide-imported")` the checkbox and the VNet name were absent. Ticking it with the switch off and committing set the row's AzureResourceId - so the filter hid a state-changing operation. With the tree reduced to that one VNet, the switch-on banner read "Nothing here would change anything. 1 VNet prefix(es) are either already imported or cannot be imported, so they are hidden." Provenance: 23233f2 changed the test from `!prefixInfo.isSelectable` to `!prefixHasWork(...)`.
**Fix:** In prefixHasWork (:164-172) add `if (prefixInfo.statusName === "WillUpdateExisting") { return true; }` alongside the existing "Available" test. It cannot over-fire: the only two ways to reach WillUpdateExisting are an unlinked exact-match target, where linking is a real write the contract requires be offered, or `isTopUp` with AnySubnetCanBeAdded true, which requires a selectable contained subnet and so already returned true at :165 - redundant there, never wrong. This also closes the second site without a separate edit: everything the filter still hides is then genuinely AlreadyImported or Blocked, making the :296-304 wording true. Note this is client-side, so no C# test will cover it; the guard against regression is that the predicate is now three explicit status cases rather than an inferred one.
**Residue of:** none

## Q15 - Host IP delete reports a definite failure for a write whose outcome is unknown `[x2]`
**Where:** src/Bastet/Controllers/HostIpController.cs:407 (the blanket `catch (Exception ex)` over the archive transaction at :371-402); :382 (`int subnetId = hostIp.SubnetId;` declared inside the try - relevant to the fix); src/Bastet/Controllers/SubnetController.Delete.cs:200-216 (the sibling that does classify, and the wording to mirror); src/Bastet/Services/Data/SqlSaveOutcome.cs:31 (IsIndeterminateTransaction, the overload that accepts a bare SqlException)
**Breaks:** DeleteConfirmed opens a transaction, inserts the DeletedHostIpAssignment archive row, removes the assignment and commits. Its only handler asserts "Error deleting host IP. Details have been logged." - a statement that the delete did not happen. If the connection is severed at CommitAsync, SQL Server may well have committed: the assignment is archived and the address is free, while the operator has been told it was not deleted and therefore believes the address is still assigned. Every sibling destructive path was given the classifier; this one was not.
**Repro:** Own rig (port 5244, catalog bastet_v17_lockb2). Leg A: held `TABLOCKX` on DeletedHostIpAssignments, POST /HostIp/Delete ip=10.190.0.5 confirmation=approved -> 30.1 s, 302, banner "Error deleting host IP. Details have been logged."; app.log shows DbUpdateException wrapping SqlException "Error Number:-2" (CommandTimeout), on which both IsIndeterminate and IsIndeterminateTransaction return true - it fell through the blanket catch only because no classifying catch exists here. Leg B (control): the same fault class on DeletedSubnets via POST /Subnet/Delete/4 -> "BASTET could not confirm whether this subnet was deleted. Check the subnet list and the deleted-subnets list before retrying.", log "Subnet delete outcome unknown for subnet 4", same error number. Redirect probe: after a successful delete, GET /HostIp/Delete?ip=&lt;deleted&gt; returns 404. Purge probe: the same fault on PurgeAllDeletedHostIps returns a plain 500 with no banner and no claim.
**Fix:** A verifier judged the filed fix unsound in two respects. Keeping `RedirectToAction(nameof(Delete), new { ip })` makes the new message invisible in the only case it exists for - the GET returns 404 when the archive landed, a 404 renders no layout, and the TempData surfaces later on an unrelated page. And `int subnetId` is declared inside the try, so a catch attached to it cannot reach `RedirectToAction(nameof(Index), new { subnetId })` without hoisting the local. Corrected narrow fix: immediately before the generic catch at :407 add `catch (Exception ex) when (SqlSaveOutcome.IsIndeterminateTransaction(ex))` that logs "Host IP delete outcome unknown for {Ip}", calls `TransactionCleanup.RollbackQuietlyAsync`, sets "BASTET could not confirm whether this host IP was deleted. Check the subnet's host IPs and the deleted host IPs list before retrying.", and returns `RedirectToAction(nameof(AllHostIps))` - a page that renders regardless of whether the archive landed and needs no local from inside the try. Use IsIndeterminateTransaction, not IsIndeterminate: CommitAsync surfaces a bare SqlException and only that overload accepts one. Leave the existing generic catch and its redirect alone. Explicitly out of scope: the two ExecuteDeleteAsync archive purges (SubnetController.Delete.cs:353, HostIpController.cs:594) return a plain 500 and assert nothing, and touch archive rows rather than allocation records - wrapping them would add an unknown-outcome message where nothing is currently claimed.
**Residue of:** d18327e (Audit 16 Cleanup #165), bullet "Treat a severed commit as an unknown outcome on the subnet archive path" - blame puts the sibling classifier there while HostIpController.cs:407 remains 6edef5c4; round-16 finding id unrecoverable.

## Q16 - Bulk import wizard blocks linking a not-yet-linked target purely because it has children `[x1]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:216-220 (`exact.HasChildSubnets && !isTopUp` in AnnotatePrefix); :485-489 (BuildPlanItem raises the same rule as a per-item error, which is what stops the commit)
**Breaks:** The refusal keys on provenance - is this row already linked to THIS VNet - not on any actual conflict. A hand-created Bastet row with any child at all cannot be linked to its Azure VNet, and the message is a false diagnosis ("Already imported?") naming no remedy; the only escape is to delete the operator's own subnet. The identical situation with the link already present is not merely allowed but advertised: "Will add any missing subnets to existing Bastet subnet 'X'. Subnets already imported are left untouched." Against "linking a not-yet-linked target still counts as work and must stay offered" and "a rule that holds for a manually created subnet holds identically for an imported one". Consequence in the IPAM view: /Subnet/Details lists an unallocated range containing an Azure-allocated subnet the sanctioned import path refuses to record.
**Repro:** Own rig (port 5219, catalog bastet_vazd2). Created hand-carved-range 172.16.0.0/20 with child hand-kid 172.16.5.0/24 (non-overlapping with Azure's p2sub 172.16.2.0/24). BulkGetVNets: prefix Blocked "already has child subnets and is not linked to this VNet. Already imported?", selectable False, while p2sub itself was Available. BulkImportPreview -> canCommit false with the same text as an item error; BulkCreateFromAzurePlan -> `{"success":false,…}`, nothing written. Guard redundant: with a genuinely overlapping hand-made child, DetectExistingBastetSubnetConflicts produced the refusal independently. Guard is the only blocker: deleting just the two hand-made children made the byte-identical selection commit. Patched build on port 5220 with both branches deleted: prefix became WillUpdateExisting/selectable, commit succeeded, hand-kid untouched with AzureResourceId NULL, and the overlapping-child hazard case was still refused by the per-subnet detectors.
**Fix:** Delete the `exact.HasChildSubnets && !isTopUp` branch at :216-220 and the matching error at :485-489. Nothing else is needed: the per-subnet detectors (AnnotatePrefix :263-268, AnnotateSubnet :356-378, DetectExistingBastetSubnetConflicts :707-731, DetectVNetPrefixWouldContainExistingSubnet :752-763) already refuse every overlapping and containment shape, the collapsed whole-prefix case keeps its own guards at :565-572 and :317-324, ValidateSubnetCreation still runs for every created child at commit, and the WillUpdateExisting reason at :243 already has the has-children wording. Four tests pin the removed behaviour and must be updated or CI goes red: AzureBulkImportTopUpTests.cs:73 (APopulatedTargetWithNoAzureLink_IsStillRefused) and AzureBulkImportPlannerTests.cs:156, :732, :977 (the last flips its vnet-blocked/count pre-assertions to Contains/4 while its CanCommit assertion is untouched). Do NOT touch AzureBulkImportTopUpTests:114 - it matches the same "already has child subnets" substring but comes from the separate collapsed-subnet guard at :565-572, which must survive. With those edits the suite is 752/752.
**Residue of:** 8afa2df (Audit 14 Cleanup #160), bullet "Let an already-imported VNet be topped up with the subnets Azure has since added", which narrowed the refusal to `!isTopUp` and put it on the provenance axis; the refusal itself is original (73fc76f, #108).

## Q17 - The Bastet:Migration app lock is taken in whichever catalog the lock connection opened, so it excludes nothing across the master-bootstrap split and stalls unrelated deployments with a false cause `[x1]`
**Where:** src/Bastet/Program.cs:307-326 (sp_getapplock 'Bastet:Migration' on whichever connection was obtained; the throw at :319-325); :236 (Configured connection) and :245 (MasterBootstrap fallback); :339-345 (the same "another replica appears to be stuck" text emitted for any SqlSaveOutcome.IsIndeterminateErrorNumber); :346-373 (the release block)
**Breaks:** sp_getapplock is database-scoped. On a cold start the lock is held in `master` for the whole migration, and the master hold is global to the SQL Server instance: a second, unrelated Bastet deployment on the same server (different catalog) that is also starting for the first time blocks on it for up to the 300 s @LockTimeout and then aborts startup with "Could not acquire the 'Bastet:Migration' application lock (result code -1). Another replica appears to be stuck applying migrations." That is false - no replica of that deployment is doing anything - and it sends the operator to the wrong system. The same wrong text is produced at :339-345 for a plain connection reset during migration. (The same-deployment window is harmless: EF Core takes its own `__EFMigrationsLock` in the target database, which is why Bastet's lock earns nothing there.)
**Repro:** Against bastet-audit-sql-17. (1) Scope: a master-held `Bastet:Migration` returns -1 to a second master session and 0 to a session in another catalog. (2) With that master lock held, a start against an existing catalog came up in 2 s - the lock excludes nothing. (3) Polling sys.dm_tran_locks across a genuine cold start: 04:30:01.974 master lock GRANT with DB_ID(target)=NULL, 04:30:02.416 same lock held with the DB now created, 04:30:02.814 `__EFMigrationsLock` GRANT inside the target - EF's lock only appears after the database exists. (4) The defect, no artificial holder: two unrelated deployments cold-started against two different missing catalogs (ports 5191/5192); session 61 sat in WAIT on session 60's master lock for ~1.3 s, bounded only by the peer's whole migration. (5) Corroboration: during a 150 s master hold, another agent's Bastet instance appeared in sys.dm_exec_requests with `LCK_M_X, wait_time 21858` in master for a catalog that was not its own. The 300 s abort was deliberately not driven live to avoid stalling other agents on the shared container.
**Fix:** A verifier judged the filed fix unsound. Deleting the hand-rolled lock and relying on EF's `__EFMigrationsLock` cannot work - that lock is taken inside the target database and appears only after it exists, so it cannot cover CREATE DATABASE; two replicas can both see the catalog missing and one dies with SQL 1801. Deleting MasterBootstrap also breaks the 4060 bootstrap path outright, making the 4060 diagnostics unreachable and self-hosted first run impossible. Corrected narrow fix: (1) on the 4060 path only, open master, take the applock there, create the database explicitly and nothing else (`Database.GetService<IRelationalDatabaseCreator>()`, `if (!creator.Exists()) creator.Create();`), then sp_releaseapplock and dispose that connection - the instance-global hold is now bounded by CREATE DATABASE, not by the migration; (2) on both paths, open a connection to the CONFIGURED catalog, take `Bastet:Migration` there, and hold it across the Migrate() calls; (3) change the :319-325 throw to name the resource, the catalog the lock was attempted in, and the fact that another session on this server holds it - a diagnosis the operator can act on via sys.dm_tran_locks - and change :339-345 to report an indeterminate migration outcome without naming a peer, since IsIndeterminateErrorNumber covers transport resets, not lock waits. Do not widen the lock or add a second guard.
**Residue of:** none

# Low

## Q18 - NoHtml refuses ordinary operator text as "HTML tags": "HQ <-> DR" and "temp < 5 and load > 3" are rejected on both Create and Edit `[x2]`
**Where:** src/Bastet/Services/Security/InputSanitizationService.cs:14 (`HtmlTagPattern` = `<[^>]*>`); src/Bastet/Services/Security/ValidationAttributes.cs:21-23 (NoHtmlAttribute fails whenever StripHtml changes the value); src/Bastet/Models/ViewModels/SubnetViewModels.cs:11 (the `[SafeText]` that keeps the name half refused after the regex change); src/Bastet/Models/ViewModels/HostIpViewModels.cs:26 and :47 (host names carry the same pair); test/Bastet.Tests/Security/SubnetViewModelValidationParityTests.cs:62 (asserts the false rejection)
**Breaks:** Any `<` followed later by any `>` is treated as a tag, so "HQ <-> DR" and "temp < 5 and load > 3" are refused with "HTML tags are not allowed in subnet names" / "… in descriptions" - a cause the operator cannot find or remove. The rule is also asymmetric in a way nobody can hold in their head: "reserve if load > 50% and temp < 5" is accepted because the `>` precedes the `<`. It is not what makes the app safe - markup stored in a name renders fully encoded on /Subnet, /Subnet/Details and inside the bulk import wizard.
**Repro:** Own rig (port 5233, catalog bastet_verif_secb2), curl with scraped tokens. Create `HQ <-> DR` -> 200 with the Name field error; Create with description `temp < 5 and load > 3` -> 200 with the description error; Create with description `reserve if load > 50% and temp < 5` -> 302, stored verbatim. Edit repeated both refusals on subnet 1. Decisive probe: Create `HQ < DR` (no `>`, so HtmlTagPattern cannot match) -> 200 "Subnet name contains invalid characters" - that is [SafeText] - while Edit with the same name -> 302, stored as `HQ < DR`. Regex corpus check over every angle-bracket literal in the test suite: old `<[^>]*>` and proposed `</?[A-Za-z][^>]*>` strip identically for all thirteen markup strings; only the two intended sentences change.
**Fix:** A verifier judged the filed fix incomplete: narrowing the regex alone leaves "HQ <-> DR" refused on Create, because CreateSubnetViewModel.Name also carries `[SafeText]`, whose pattern admits neither `<` nor `>` - only the message changes, and the Create/Edit asymmetry gets worse. It also misses the two host-IP name sites. Corrected fix, two parts: (1) tighten `HtmlTagPattern` at InputSanitizationService.cs:14 from `<[^>]*>` to `</?[A-Za-z][^>]*>` so only element-shaped input counts as a tag, and change the input of SubnetViewModelValidationParityTests.MarkupInDescription_RejectedByBoth (line 62) to a genuinely tag-shaped string such as "load <b>high</b>" so it asserts the rule rather than the bug. (2) Drop `[SafeText]` from CreateSubnetViewModel.Name (SubnetViewModels.cs:11) rather than widening SafeTextPattern - Edit has never carried it and stores `<` correctly today, so this makes Create agree with the path that already works and leaves [NoHtml] deciding for both; apply the same removal at HostIpViewModels.cs:26 and :47 if host names are in scope. This overlaps Q11, which removes the same attribute for a different reason - do both in one commit. If only one line can be touched, re-scope the finding to descriptions plus the Edit-side name and defer the Create-name half explicitly. Stated separately: NoHtml as a whole has no contract sentence behind it now that output encoding is verified complete at every sink; removing it entirely is a larger decision.
**Residue of:** none (origin 3940c98, Input Sanitization #43; note 8cefc64, Audit 5 Cleanup #142, is what put [NoHtml] on the Edit view model, so the Edit half exists only because round 5 propagated the mechanism there)

## Q19 - The only legitimate reconcile refusal tells the operator to "Move" the manual content, an operation the application does not offer `[x1]`
**Where:** src/Bastet/Services/Azure/AzureReconciler.cs:98-100 ("Move or delete it here first, then run the scan again."); src/Bastet/Views/Azure/Reconcile/_StepReview.cshtml:76 ("The reason column says which. Move or delete that content here first, then scan again.")
**Breaks:** There is no move, and it is not merely un-offered but structurally impossible: a host IP's IP is readonly and SubnetId is a hidden field the Edit POST never applies, and a subnet has no ParentSubnetId on EditSubnetViewModel, no parent selector in the Edit form, and a hidden immutable NetworkAddress - the parent of a manual child is fixed by its address. The operator following the instruction looks for a move and finds none. Delete is the only remedy, and it works.
**Repro:** Own rig (port 5247, catalog bastet_verq4, own fixture rig-verq4-17 10.90.0.0/16 + sub1 10.90.5.0/24, imported through the real wizard in chromium). Added host IP 10.90.5.10, deleted the Azure subnet, ran the scan: reason cell verbatim "…destroyed with no way to restore it. Move or delete it here first, then run the scan again.", with the panel banner above it repeating "Move or delete that content here first, then scan again." GET /HostIp/Edit?ip=10.90.5.10 renders four inputs (IP readonly, Name, hidden SubnetId, hidden RowVersion), no `<select>` and no occurrence of "move"; POSTing SubnetId=3 with a scraped token returned 302 and left SubnetId=4 unchanged. GET /Subnet/Edit/4 has no ParentSubnetId and no `<select>` at all. After deleting the host IP the same scan moved the row into the deletable list as "Subnet deleted".
**Fix:** A verifier judged the filed fix incomplete: it names only the planner string, and both sentences render in the same response, so editing one alone leaves the screen contradicting itself about whether a move exists. Change both, and only both (a repo-wide grep for the word returns exactly these two source sites): AzureReconciler.cs:100 -> "destroyed with no way to restore it. Delete it here first, then run the scan again."; _StepReview.cshtml:76 -> "The reason column says which. Delete that content here first, then scan again." No behavioural change, no new status, no new withhold.
**Residue of:** none

# Info

## Q20 - Azure services and view models carry members the mass cleanup left write-only `[x2]`
**Where:** src/Bastet/Services/Azure/AzureService.cs:11 and :16 (the IIpUtilityService ctor parameter and `_ipUtilityService` field - the assembly's only CA1823); src/Bastet/Models/ViewModels/AzureReconcileViewModels.cs:23 (AzureLinkedSubnetSnapshot.IsFullyAllocated, written at src/Bastet/Services/Azure/AzureSubnetSnapshotService.cs:65); src/Bastet/Models/ViewModels/AzureReconcileViewModels.cs:73 (AzureReconcileItem.IsVNetLevel, parameter at src/Bastet/Services/Azure/AzureReconciler.cs:333, written at :344, passed from :73, :288, :294, :309, :316); src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:69 and src/Bastet/Models/ViewModels/AzureReconcileViewModels.cs:104 (IsFeatureEnabled, set at src/Bastet/Controllers/AzureController.cs:46 and :138, rendered by no view)
**Breaks:** No user-visible failure - orphaned state, filed at the severity of its consequence. Every AzureService resolution pulls a scoped IPAM service it never uses; every reconcile scan projects Subnet.IsFullyAllocated into a snapshot field no code reads and serialises `isVNetLevel` to the browser where no script reads it; both Azure controllers set IsFeatureEnabled after the gate that already 403s. The cost is a later reader taking these for live inputs - IsVNetLevel in particular reads as if the reconcile UI still distinguishes VNet-level from subnet-level rows, which it does not.
**Repro:** Scratch copy of HEAD. `dotnet build src/Bastet/Bastet.csproj --no-incremental -p:AnalysisMode=AllEnabledByDefault` -> 0 errors, 497 warnings, of which the only CA1823 is `AzureService.cs(16,44): Unused field '_ipUtilityService'`. Live wire: seeded an Azure-linked row pointing at a nonexistent VNet; POST /Azure/ReconcileScan returned an item carrying `"isVNetLevel":true`. Reader search: `grep -rn -i -e vnetlevel -e featureenabled src/Bastet/Views/` -> nothing; `grep -rn -i isvnetlevel src/ test/` -> three hits, all declaration/parameter/write; AzureReconciler.cs contains no IsFullyAllocated; `grep -rn "new AzureService("` -> nothing (DI only). Fix applied verbatim in the scratch copy: 0 warnings / 0 errors, 752/752, CA1823 count 0, and the live ReconcileScan payload byte-identical apart from the removed field.
**Fix:** Pure deletions, no behaviour change and no DI registration change (IIpUtilityService stays registered for its real consumers): drop the IIpUtilityService ctor parameter and `_ipUtilityService` field from AzureService; delete AzureLinkedSubnetSnapshot.IsFullyAllocated and its assignment in AzureSubnetSnapshotService; delete AzureReconcileItem.IsVNetLevel together with the `isVNetLevel` parameter of AzureReconciler.Item and the five call sites (:73, :288, :294, :309, :316); delete both IsFeatureEnabled properties and the two object initialisers that set them. Optional tidying, not required for soundness: with IsFeatureEnabled gone, BulkImportInitialViewModel and AzureReconcileInitialViewModel are empty classes existing only to satisfy the `@model` directives - both pages still return 200 in that state, and the tidier end state drops both classes with their `@model` lines and passes `View()`.
**Residue of:** 23233f2 (Mass Claude Audit Mess Cleanup #167) for `_ipUtilityService`, snapshot.IsFullyAllocated and item.IsVNetLevel; NOT for the two IsFeatureEnabled sites, which trace only to 73fc76f / aedd0bd and have never had a reader.

## Q21 - Orphaned members and usings left behind by the deletion of the single-VNet wizard and the reconcile withholds `[x1]`
**Where:** src/Bastet/Controllers/SubnetController.Azure.cs:1-5,7 (six dead usings: Bastet.Models.ViewModels, Bastet.Models, Bastet.Services.Data, Bastet.Services.Security, Bastet.Services.Validation, Microsoft.AspNetCore.Authorization); src/Bastet/Controllers/AzureController.cs:1, :3, :7 (Bastet.Data, Bastet.Services, Microsoft.EntityFrameworkCore); src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:271-296 (FindMoreSpecificParent's unread `BulkAzureSubnetViewModel subnet` parameter) and :368 (its only call site); src/Bastet/Services/Security/IInputSanitizationService.cs:6 and src/Bastet/Services/Security/InputSanitizationService.cs:26 (SanitizeString, no production caller), with its exclusive dependents EncodeHtml (:44, :50, :68 and IInputSanitizationService.cs:10), RemoveDangerousScripts (:42, :123) and DangerousScriptPattern (:18, :130); test/Bastet.Tests/Security/InputSanitizationServiceTests.cs:173-176
**Breaks:** No runtime consequence - import rot and dead surface. 23233f2 cut SubnetController.Azure.cs from 401 lines to 36 and AzureController.cs from 450 to 212 without touching their using lists, so AzureController still imports Bastet.Data and Microsoft.EntityFrameworkCore for a DbContext it no longer takes - the "AzureController reads and plans; SubnetController writes" boundary reads as if it were still crossed.
**Repro:** Scratch copy (repo untouched). Baseline `dotnet build Bastet.sln -p:UseRazorSourceGenerator=false --no-incremental` -> 0 warnings, 0 errors. With the nine usings deleted: 0/0. With the FindMoreSpecificParent parameter and SanitizeString also removed: 0/0; the test project then failed CS1061 on SanitizeString, and after deleting that theory the suite ran 747/747 (752 minus its five InlineData cases). Greps at HEAD: SanitizeString has four hits total (interface, impl, two test lines); EncodeHtml and RemoveDangerousScripts/DangerousScriptPattern have no caller outside SanitizeString; FindMoreSpecificParent has one declaration and one call. (Note: `dotnet build` needs `-p:UseRazorSourceGenerator=false` on this box - a toolchain fault reproducible on a virgin `dotnet new mvc`, not a Bastet defect.)
**Fix:** A verifier judged the filed fix unsound on its sanitization half: removing SanitizeString silently orphans three more members - it is the sole caller of EncodeHtml and of RemoveDangerousScripts, which is the sole consumer of DangerousScriptPattern - so a "remove the dead method" fix leaves a larger dead surface than it found, with no warning from the toolchain. And "fold its single test into the typed helpers" is not executable: the `allowHtml:true` cases exercise RemoveDangerousScripts, which no typed helper calls, and no helper HTML-encodes; the test is deleted with the code, not migrated. Land three separate commits: (1) delete the nine dead usings; (2) drop the unused `subnet` parameter from FindMoreSpecificParent and update its one call site; (3) remove the sanitization cluster as one unit - SanitizeString, EncodeHtml (interface and implementation), RemoveDangerousScripts and DangerousScriptPattern - deleting both the SanitizeString_SanitizesCorrectly and EncodeHtml_EncodesCorrectly theories. StripHtml, IsSafeText, IsValidIpAddress, SanitizeNetworkInput and the four typed Sanitize* helpers all keep real callers and must stay. If (3) is more surface than wanted, leave the cluster alone entirely rather than doing the half that orphans EncodeHtml.
**Residue of:** mixed - the nine usings and SanitizeString are residue of none (23233f2 orphaned the usings; SanitizeString traces to 3940c98, #43), but FindMoreSpecificParent's unused parameter is residue of d18327e (Audit 16 Cleanup #165), fix "Mirror the more-specific-parent rule into the bulk selection screen", which introduced the method with the parameter already unread; round-16 finding id unrecoverable.

# Refuted - reported by a finder, killed by the verifier

| Title | Where | Reason |
| --- | --- | --- |
| Reconcile scan-failure staleness warning is unreachable - it writes into a node inside the results panel it just hid | src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:173 | Mechanism confirmed exactly as described (the note lives in `#rec-review-section` inside `#rec-scan-content`, which showScanError never un-hides, and renderPlan:268 overwrites it with the success text before un-hiding at :270), but it produces no wrong output - three dead lines. Driven with Playwright and a forced scan failure: the operator sees "Nothing was checked. / <error> / Because Azure could not be read, BASTET cannot tell which resources still exist, so nothing is offered for deletion. Fix the connection and scan again." - correct, complete and actionable, with no stale allocation picture displayed at all. No reachable state makes the invisible string visible, and the finder's own preferred remedy is to delete an unreachable statement, which changes nothing an operator can observe. Residue settled with git: the id was `rec-relink-rescan-note` at d18327e and lived inside the hidden re-link failure panel, so the write has been dead on this path across both revisions - not a round-16 regression. |
| AzureReconcilePlanViewModel.CanCommit is a commit gate nothing consults | src/Bastet/Models/ViewModels/AzureReconcileViewModels.cs:99 | Citation accurate and the reconcile-side property genuinely has no consumer, but every clause of it is independently enforced twice. Server: SubnetController.AzureReconcile.cs:53 refuses on `!plan.ScanSucceeded || plan.GlobalErrors.Count > 0`, and clause 3 is enforced more tightly per selected id at :39 and :65-66/:97-108 (any id not in the freshly rebuilt Items yields a 409), so an empty Items list can never yield a delete. Client: renderPlan re-checks scanSucceeded (:203) and globalErrors (:208) and bails to showScanError, while the confirm button gates on `selectedIds().length` plus the typed "approved". No delete proceeds that the flag would have stopped and none is blocked that it would have allowed; the only stated harm is a hypothetical future maintainer editing a dead property. Deleting it is harmless tidying, not a defect fix. |
| The children-only CalculateUnallocatedRanges overload has no production caller and reports host-IP addresses as free | src/Bastet/Services/IIpUtilityService.cs:22 | Citations accurate - IpUtilityService.cs:178-179 does forward an empty host-IP list - but the path is unreachable and the claimed wrong output does not occur. A full-repo grep (including .cshtml/.js/.json) finds one non-test caller, SubnetController.Read.cs:97, which passes four arguments including `subnet.HostIpAssignments`; IpUtilityService is the sole implementor and is registered once. Driving the exact scenario on the live rig, a /24 with host IPs and no children rendered two unallocated ranges with the assigned addresses carved out, not the single 254-address range predicted. The failure is explicitly conditional on a caller that does not exist, i.e. API hygiene about dead code with no runtime consequence. Also: the fix understates the blast radius - deleting the overload re-points twelve test call sites in SubnetPropertyCalculationTests.cs, three of which are argument-validation tests whose coverage would vanish rather than move. Provenance settled with git: e05c3b1 (#11) and 841c272 (#18), neither an audit round. |
