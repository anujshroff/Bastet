# Bastet — Round-21 Audit Findings

Round 21 filed 4 findings, of which 4 are residue of round 20's own fixes. Branch `audit/round-21`, HEAD `6fb8557`, test baseline 866 passed / 0 failed / 0 skipped, date 2026-08-16, residue rate 4/4 (100%).

# Critical

# High

# Medium

# Low

## L1 — Whole-prefix Azure subnet over a never-imported unlinked row is badged "Already imported" `[x1]` `strings`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:338
**Breaks:** Operator hand-builds Bastet subnet 'hand-b6a' 10.60.0.0/24 (never imported, no AzureResourceId) and marks it fully allocated. Azure VNet rig-r21-b6a-vnet has address prefix 10.60.0.0/24 whose single subnet covers the whole prefix. Wizard step 2 renders one card whose prefix line correctly says "Will update existing — Will link existing Bastet subnet 'hand-b6a' to this VNet" (the row is NOT yet linked/imported), while the subnet row directly beneath it is badged "Already imported" — untrue: nothing was ever imported and the row carries no link. The same status feeds the "Only show what would change" hidden-row summary, which reports it under "already imported" ("All 1 subnet(s) in this prefix are already imported."). Round 20's hunk removed the `&& IsSameVNet(encompassedTarget, vnet)` term from this branch (old code reached AlreadyImported only for a genuinely linked top-up target), so the badge now fires for unlinked and other-VNet-linked targets too. Every other AlreadyImported assignment in the planner (lines 211-224, 233-241, 419) is still link-gated; this is the only unlinked instance.
**Repro:** Finder: live rig, own app instance (port 5316, catalog bastet_r21_beat6a), fixture VNet rig-r21-b6a-vnet 10.60.0.0/24 with whole-prefix subnet; script rig/beat6a/drive4.py created 'hand-b6a', marked fully allocated, opened Bulk Azure Import step 2 — observed prefix "Will update existing / Will link existing Bastet subnet 'hand-b6a'..." immediately above subnet "Already imported / Bastet subnet 'hand-b6a' is marked fully allocated, so there is nothing to do." (5/5 checks). Verifier 1 independently reproduced via GET /Azure/BulkGetVNets (port 5317, own fixture 10.61.0.0/24): subnet statusName=AlreadyImported under prefix statusName=WillUpdateExisting; confirmed git diff 6a21950..6fb8557 removed the IsSameVNet term and that hidden-row bucketing keys on statusName==="AlreadyImported" (_BulkScripts.cshtml:155, 276-288). Verifier 2 independently reproduced in-browser via Playwright (port 5324, own fixture, 7/7 checks) including the hidden-row summary sentence.
**Fix:** In AnnotateSubnet's encompassing-subnet branch, keep Status = AlreadyImported only when IsSameVNet(encompassedTarget, vnet); for an unlinked (or other-VNet-linked) fully-allocated encompassed target set Status = Blocked with the existing reason text ("Bastet subnet 'X' is marked fully allocated, so there is nothing to do."). The row is non-selectable in both cases and CanCarrySubnetWork/adoption is computed independently — verifiers traced all consumers (badge, hidden-row bucketing, RenameOnlyCandidate, AnySubnetCannotBeImported reached only under isTopUp which is false in both affected cases) — so only the badge and hidden-row bucketing change.
**Residue of:** 18-R4

## L2 — 18-R10's server-computed wizard selectability properties (CanCarrySubnetWork, both RenameOnlyCandidate) have no test at all — each mutates to a constant with the full 866-test suite green `[x2]`
**Where:** src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:55, plus src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:37 and src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:57
**Breaks:** The 18-R10 fix deleted the client's isPrefixUsable re-derivation and made _BulkScripts.cshtml consume server-computed canCarrySubnetWork (gates rendering of subnet checkboxes under a prefix, lines 174/239) and renameOnlyCandidate (gates the Rename-only badge/checkbox, lines 144/148). No test references any of the three properties. A regression — e.g. CanCarrySubnetWork inverted or re-derived from a status enumeration that misses a new status, exactly the shipped bug class 18-R10 fixed ('Only show what would change' hid every linkable row) — ships with dotnet test fully green: the wizard renders selectable work under Blocked prefixes or drops every rename offer, and only a manual browser drive can catch it.
**Repro:** Finder, in a scratch clone of 6fb8557: (1) sed CanCarrySubnetWork to `=> true`, dotnet test → 866 passed, 0 failed; (2) sed prefix RenameOnlyCandidate to `=> false` → 866 passed; (3) sed subnet RenameOnlyCandidate to `=> false` → 866 passed; grep over test/ finds zero references to either property name. Verifier reran the mutation protocol in own scratch clone: all three flipped together → 866/866 green; CanCarrySubnetWork alone → 866/866 green; case-insensitive grep rules out serialized-JSON assertions; git blame puts all three lines in 6fb8557 (#179).
**Fix:** Add mutation-proven assertions to AzureBulkImportSelectabilityTests pinning all three properties: a Blocked prefix has CanCarrySubnetWork false and any non-Blocked status true; prefix RenameOnlyCandidate true exactly for AlreadyImported && WouldRenameTarget (false when either term is absent); subnet RenameOnlyCandidate likewise for AlreadyImported && WouldRenameSubnet. Verify each new assertion by re-running the three constant mutations and confirming red.
**Residue of:** 18-R10

## L3 — Edit concurrency message: the Description, Tags and CIDR 'stored values that differ' branches are not mutation-load-bearing — deleting all three survives the suite green `[x1]`
**Where:** src/Bastet/Controllers/SubnetController.Edit.cs:242, plus src/Bastet/Controllers/SubnetController.Edit.cs:249 and src/Bastet/Controllers/SubnetController.Edit.cs:256
**Breaks:** 18-R3-rem's ledger claim is 'one unified message names each differing stored value', but only the Name clause is pinned. Delete the Description branch (lines 242-247), the Tags branch (249-254) and the CIDR branch (256-259) and the suite stays 866/866 green: an operator whose edit collided with another user's Description/Tags/CIDR change gets a message naming nothing that differs, telling them to 're-apply the changes that still make sense' with no way to see which values moved — the exact defect the fix closed, back, with a green suite. The 'is now empty' wording forms are likewise unreachable by any test.
**Repro:** Finder, in a clone at 6fb8557: removed the three branches from the differing-list construction, rebuilt, full suite → 866 total, 0 failed. SubnetControllerConcurrencyRedisplayTests only drives a Name difference (Assert.Contains("Name is now 'web'")) and asserts DoesNotContain("Tags") on a Name-only conflict; reverting the whole Edit.cs fix does fail all 3 new tests, so the tests guard the unified-path mechanism — just not three of its four value clauses. Both verifiers reran the exact mutation in fresh clones → 866/866 green; grep across the test tree finds zero references to "Description is now", "Tags are now", "CIDR is now" or "is now empty"; git blame/git log -S attribute all three branches solely to 6fb8557; verifier confirmed the CIDR clause and both empty-wording forms are reachable through the same redisplay path the existing tests drive (including the stale-token-on-validation-failure route).
**Fix:** Extend Edit_POST_ConcurrencyConflict_NamesTheStoredValuesThatDiffer (or add one sibling case) with a stored row whose Description, Tags and Cidr all differ from the posted form, asserting "Description is now", "Tags are now" and "CIDR is now /" each appear, plus one case asserting the "Tags are now empty" form.
**Residue of:** 18-R3-rem

## L4 — 18-R3-rem's HostIp concurrency-message alignment has no guarding test — reverting the hunk leaves all 866 tests green `[x2]`
**Where:** src/Bastet/Controllers/HostIpController.cs:294
**Breaks:** Round 20 aligned the HostIp Edit DbUpdateConcurrencyException message with the unified Subnet Edit message ('...Reload the page to see the current values, then re-apply the changes that still make sense.'). The only test touching a HostIp concurrency message (SubnetHostIpInteractionTests.cs:681) asserts Contains("modified by another user"), a substring the pre-fix message ('The host IP was modified by another user. Please reload and try again.') also contains — and its stale-RowVersion setup drives the validation branch (line 248), not the line-294 catch. A regression restoring the old short message at line 294, silently un-aligning the sibling that 18-R3-rem aligned, ships with the suite green.
**Repro:** Finder, in a scratch clone of 6fb8557: `git checkout 6a21950 -- src/Bastet/Controllers/HostIpController.cs` (reverts exactly the delta's one HostIp hunk), dotnet test → 866 passed, 0 failed, 0 skipped; grep "host IP was modified" over test/ → no hits. Verifier reran the revert in a fresh clone (git diff --stat: 1 insertion, 3 deletions) → 866/866 green; confirmed the delta's HostIpController.cs diff is exactly the one catch-message hunk, that the validation-path message (line 248-250) was already aligned at 6a21950, and that the sole existing assertion exercises that validation path, not the catch.
**Fix:** Add a test that drives the DbUpdateConcurrencyException catch itself with the row still existing (e.g. a context/interceptor that throws DbUpdateConcurrencyException from SaveChangesAsync after HostIpExists returns true) and pin the aligned sentence, e.g. Assert.Contains("then re-apply the changes that still make sense", message). The verifier established that strengthening only the existing SubnetHostIpInteractionTests.cs:681 assertion would not fail on this revert — that test exercises the validation-path message, already aligned pre-delta — so the new catch-path test is the part that closes the gap.
**Residue of:** 18-R3-rem

# Info

# Refuted

| id | title | reason |
|---|---|---|
