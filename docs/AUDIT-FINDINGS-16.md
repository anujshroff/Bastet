# Bastet — Round-16 Audit Findings

**Target branch:** `audit/round-16` · **HEAD:** `1a31228` · **Baseline:** 892 tests passing, 0 build warnings · **Date:** 2026-08-07 · **Round letter:** P (findings P1–P15)

---

## Reconciliation — final state

**All fifteen findings are fixed, verified and struck.** One commit per finding on `audit/round-16`, except P14, which is recorded as closed inside P5's commit because both are the same endpoint and P14's own text says the decision should be taken once.

| | |
|---|---|
| Build | 0 warnings after deleting `bin`/`obj` and rebuilding `--no-incremental` |
| Tests | 892 → **929** (+37, none removed) |
| App sweep | 13 areas requested, asserting rendered content rather than HTTP 200 — all pass |
| Security headers | `X-Content-Type-Options`, `Referrer-Policy`, `Content-Security-Policy`, `X-Frame-Options` all present on a normal response |
| Log | **0 `fail:` outside the two deliberate lock-timeout reproductions** (P4, P12). Warnings are the pre-existing DataProtection and EF multiple-include advisories, plus the 403s counter-test 1 logs by design |
| Azure end to end | discovery → both wizards → reconcile scan → delete commit, against live ARM |
| Discrimination | **both counter-tests pass** (below) |
| `main` | `1a31228`, byte-identical to the pre-round state |

**The two counter-tests, which are what stop an over-blocking regression passing as a fix.** A VNet was created in the resource group only principal B can see and imported while the app ran as B; the app was then restarted as principal A, which has no assignment on that group, against the same database. Scan: **0 items offered for deletion**, `canCommit: false`, and a warning naming the cause — *"2 Azure-linked subnet(s) were missing from the subscription listing, and Azure denied access when asked about them directly…"*. Conversely, a VNet in A's own group was imported and then genuinely deleted in Azure: it was **still offered**, `canCommit: true`, and the delete committed `200 {"targetsDeleted":1,"subnetsArchived":2}` with the next scan returning 0 items. Withholding what it cannot see, deleting what is really gone.

**Residue rate: round 16 filed 15 findings, of which 11 were residue of round 15's own fixes.**

`P1←O1,O2,O3 · P5←O6 · P6←O16 · P7←O13 · P8←O1 · P9←O1,O2 · P10←O5 · P11←O8 · P13←O11,O12 · P14←O6,O12 · P15←O14`

Only P2, P3, P4 and P12 are independent of the previous round. That number is the reason `/audit-reconcile` gained a mandatory sibling sweep and a string-truth check partway through this reconciliation, and it is the number to compare round 17 against. Four of the eleven — P5, P13, P14, P15 — are the same shape the watch list names: a predicate updated on the planner side and not on the controller side. All four are now closed, and the sweep this round found and fixed **three further sites** in that family that were never filed (the transactional "no changes were saved" handlers under P4), so they cannot become round 17's findings.

**Deliberately not done, each recorded in full in the struck entry it belongs to.** Nothing recovers rows already archived before P1 — `DeletedSubnets` has no restore path and `Subnet.AzureResourceId` has no editor, for the third round running, and the owner chose the code fix over adding an admin repair action. P15's shared-helper consolidation and P12's `RelinkAzureSubnet` arm are recommendations to the owner rather than changes taken: the first is a refactor across a service/controller boundary, the second was measured to repair nothing.

---

## Verdict

**Five findings need a decision from you and here is what it costs.** Read this table before anything else in the file. It is the part that cannot be delegated: in each case the code change is small and measured, and the thing that is not decided is what happens to the state the defect already produced, or which of two shapes the app should take.

| Finding | The decision | What it costs |
|---|---|---|
| **P1** (critical) — reconcile archives a row whose Azure subnet was re-carved **in place** | Nothing repairs rows this path has already archived. `DeletedSubnets` has no restore and the archive does not carry `AzureResourceId`. One-off admin repair action, or hand-written SQL at release time? | The code fix is four lines and measured green (built, 892/892 unchanged, failure converted to a correctly-worded withheld review item). Recovery of already-archived rows is **not** in it — the same open item O1/O2/O3 left on round 15's watch list. Severity is graded on the consequence (an allocated Azure range presented as free after a terminal archive), not on the fix cost. |
| **P2** (critical) — one ordinary hand-created aggregate silences the whole inbound check beneath it | Accept a review item per un-imported Azure range under every coarse aggregate, or keep today's silence? | Today's silence was measured handing out a live Azure range while the scan reported clean. Tightening it produces one item per unrecorded range on a large estate's first scan, decaying as they are recorded. **The obvious predicate is the wrong one** — the verifier reproduced a partially-tiled state where `IsFullyAllocated` gating creates permanent unclearable noise; the correct predicate is BASTET's own free-space computation. See P2's fix correction before anyone implements this. |
| **P5** (medium) — the single-VNet wizard offers rows its own commit refuses, and one such row discards every other selection in the batch | Should that wizard's subnet list gain a per-row status/reason contract like the bulk one? | The full form changes this endpoint's JSON contract and `_ImportScripts.cshtml`'s row markup — the shared-helper consolidation round 15's O6 recommended and declined. The scoped interim closes the batch-poisoning limb alone for about four lines but leaves the operator with no explanation for a row that vanished. Neither cost lowers the severity of what was reproduced. |
| **P9** (low) — two reconcile reason strings name a first remedy the application refuses 100% of the time | Reword only, or add a real repair path for a linked row's recorded range? | Reword is about six lines and closes the false instruction, leaving the row repairable only by delete-and-re-import. A repair action means a new Admin-only route, a validation path that re-checks child containment, an audit entry — and, if it is to verify the new prefix against ARM, a fail-closed path for offline deployments. `Subnet.AzureResourceId` still has no editor anywhere, which is why this keeps recurring. |
| **P14** (low) — the single-VNet wizard offers a fully-encompassing row on a populated target whose only outcome is a rollback | Filter the row again on a populated target, or give this endpoint the same status/reason contract as P5? | Three lines, and the wizard then prints *"No compatible subnets found in this VNet"* on a VNet that visibly holds one subnet — correctly scoped but mute. The shared-helper form is the same change P5 asks for and also closes O6's declared residue (a non-encompassing Azure subnet colliding with an unrelated BASTET row is still silently dropped from this list). |

**Read P1 first.** Round 15's O1 closed the delete-and-recreate case and left the simpler one open. An operator narrowing a subnet in place — `az network vnet subnet update --address-prefixes 10.219.1.0/25`, one CLI call, ARM resource id preserved — produces a `SubnetPrefixChanged` row that the overlap guard deliberately skips, because the guard excludes the row's own resource id. The row is offered for archive with a ticked-able checkbox, no warning, and no ARM re-confirmation; the archive is accepted and is terminal; the parent's Details page then advertises the ARM-held range as free with a **Create Subnet** button; and `POST /Subnet/Create` inside it returns 302 and persists. Five independent beats drove that chain end to end, and the A/B control — the same geometry with the resource id changed instead of preserved — is correctly withheld, which isolates the single conjunct at line 757 rather than leaving it inferred.

**Then read P2, because it is the same wrong answer arriving by a different route.** The reconciler is the control the product points operators at: the Details page's own banner says *"Run Azure Reconcile to check before allocating from these ranges."* One hand-created aggregate — `10.60.0.0/18` under an import target, created through the ordinary `/Subnet/Create` form, no Azure link, no children — turns a correct `AzureRangeNotImported` item into `items: [], reviewItems: []`. The page that then offers the range carries no Azure caveat at all, because that banner is gated on the row's own `AzureResourceId`. The operator followed the instruction, got "nothing to clean up", and allocated over live Azure space.

**Do you need to act today?** If anyone re-carves Azure address space in a subscription you reconcile against, yes: P1's interim is one clause and stops the archive. If any install models a top-down plan with aggregate rows above its Azure targets — which forced parenting makes the normal shape — then P2 is already answering wrongly on every scan, and there is no interim that keeps the answer correct; only the fix does. P3 (the bulk wizard asserting subnets are imported that never were) is one supported click away on the default screen and costs nothing but a string to make honest. Everything else is recoverable in-band or costs an operator a wasted round trip.

**Eight of the fifteen proposed fixes were judged unsound or incomplete by a verifier and corrected inline.** That is again the most valuable thing the round produced. Three of them would have shipped a new defect: P5's "cheaper interim" would empty the wizard's subnet list on **every** VNet (the target's own row is in `context.Subnets`, so the proposed containment test is true for every offered row); P13's interim would relabel a message and block nothing, because `CanCommit` is all-or-nothing and the commit gates on it; and P6's part (2) would latch a one-way sentence that then asserts the mirror falsehood after any earlier scan failure. Two more (P11, P12) proposed guards for holes the tree already closes, which is exactly the unreachable residue these rounds keep finding. Each correction is in the finding it belongs to.

---

## How this audit ran

**Eight beats**, each a lens over the whole codebase rather than a directory: (1) `azure-integration` — discovery, ARM shapes, the reconcile scan against a live subscription; (2) `logic-data-integrity` — the planner's and reconciler's decision rules against the writes they authorise; (3) `regression-correctness` — the round-15 delta (`O1`–`O16`) re-driven against the behaviour it claims; (4) `regression-tests` — whether round 15's new tests pin what their names say; (5) `locking-lifecycle` — `sp_getapplock`, connection lifetime, the guarded save paths; (6) `dead-code-residue` — code shipped but unreachable, and gates relaxed on one side only; (7) `ui-client-js` — the three wizard state machines and what the screen asserts; (8) `security-web` — authorisation, antiforgery, headers, cookies.

**Two independent passes** ran every beat without sight of each other, plus a **deep sweep** on beats 1, 2, 3 and 4 — last round's delta and the surfaces where a regression would be newest and most consequential. That is 8 × 2 + 4 = **20 finders**, all of which returned.

**Tag meaning.** `[x2]` = the same defect was found independently by **both** passes. `[x1]` = found by **one** pass only. **`[x1]` is weak evidence of absence, not evidence of weakness** — a defect on a surface only one pass happened to drive is precisely the defect that reaches production. Thirteen of the fifteen findings here are `[x1]`, including one of the two criticals, the only high, and both mediums. Every `[x1]` candidate therefore got a second verifier on a reachability-and-consequence lens: can a real user reach this without a crafted request, and what do they actually see and lose?

**Adversarial verification with live reproduction.** Verifiers were told to kill findings, not confirm them. Each stood up its own application instance on its own port, its own SQL catalog, and — where the finding touched Azure — its **own** live ARM fixtures rather than replaying the finder's. **All fifteen reproduced on a running instance; none is inferred from reading code alone**, and several were driven through real headless Chromium rather than through hand-built posts (P3, P6, P11, P14, P15 among them). Where a fix could be built it was built and measured: `git archive HEAD` into scratch, patch, `dotnet build --no-incremental`, `dotnet test`, and the scenario re-driven on the patched binary against the same database and the same Azure state. That is how P1's fix was confirmed green (892/892, failure converted to a correctly-worded withheld review item with no Re-link suggestion) and how P5's and P13's offered interims were caught being no-ops or worse.

**Verified discrimination — the counter-test that stops an over-blocking regression passing as a fix.** The RBAC mapping was *discovered*, not assumed: both service principals hold exactly one role assignment each, both at **resource-group** scope, neither at subscription scope, so no inheritance makes the filtering vacuous. Existence was settled first — each group resolves for its own owner, so no 403 below is a typo. The observed matrix, run through `az` with isolated config directories: SP1 reads and writes `bastet-visible` (VNet create then delete both succeeded) and gets `AuthorizationFailed` on both verbs against `bastet-hidden`; SP2 is the mirror image. Then proved **through the application**, not only the CLI: two instances launched with per-principal credentials and `AZURE_TOKEN_CREDENTIALS` deliberately unset so `EnvironmentCredential` wins the chain. `GET /Azure/BulkGetVNets` returned `success: true` for both, listing only `rig-vnet-simple` and `rig-vnet-multiaddr` as SP1 and only `rig-vnet-hidden` and `rig-vnet-hidden-multi` as SP2. Neither instance saw a VNet from the other group. Fixture address ranges are deliberately disjoint between groups (10.10/16 and 10.20/16 + 172.16/16 against 192.168/16 and 192.168.128/17 + 10.99/16) so any leak is visible on sight.

**Funnel.**

| Stage | Count |
|---|---|
| Finders dispatched | 20 |
| Finders returned | 20 |
| Raw findings | 28 |
| Dropped at merge (duplicates / out of scope) | 1 |
| Candidates carried to verification | 15 |
| — found by both passes `[x2]` | 2 |
| — found by one pass `[x1]` | 13 |
| Survived | 15 |
| — critical / high / medium / low / info | 2 / 1 / 2 / 10 / **0** |
| Refuted | 0 |
| Reproduced live | 15 of 15 |
| Not runnable | 0 |
| Proposed fixes judged unsound or incomplete | 8 of 15 |
| Flagged as needing an owner decision | 5 |

Both `[x2]` candidates survived. **Nothing was refuted this round** — see the Refuted section for what that does and does not mean.

**Rig hygiene, and two incidents recorded rather than buried.** Every agent that ran the application used its own port and its own catalog, killed only by PID it captured itself, and wrote nothing into `/home/anuj/code/Bastet` — the tree was verified clean (`git status --porcelain` empty) after each run, and none of round 15's stray `.pid` files recurred. Azure fixtures were rig-prefixed and appended to `azure-inventory.json`. Incident one: port collisions were frequent enough to matter. At least three agents found their chosen port already bound by another agent's instance, and in one case the losing process's Kestrel bind failed silently while the agent went on reading the *other* agent's app; that agent detected it, discarded the run and redid it on a verified-free port. Incident two, from the same class: before detecting it, that agent wrote two rows — `rig-vrelnk-vnet` and `rig-vrelnk-s1` — into catalog `bastet_verifp16b`, which belongs to another agent. Deleting them was blocked by the sandbox, so they are still there and are trivially identifiable by the `rig-vrelnk` prefix. Anyone reusing that catalog should ignore them. Nothing in this file depends on that catalog.

---

# Critical

## P1 — O1's overlap guard excludes the row's own Azure resource id, so an in-place `subnet update --address-prefixes` still lands the row in the deletable list; reconcile archives it while ARM holds the range, and BASTET then hands the range out `[x2]` — FIXED

_P1 is fixed and committed. The overlap arm of `FindLiveOwnerOfRange` no longer excludes candidates carrying the row's own Azure resource id. Narrowing or widening a subnet in place — `az network vnet subnet update --address-prefixes`, one CLI call — preserves the ARM resource id, so the subnet still holding the range **is** the row's own resource, and excluding it was excluding the only evidence that mattered. The exclusion is kept for the degenerate same-resource-**and**-identical-prefix case only, which the exact index above has already answered._

_The fix is the one the finding proposed and the verifier confirmed sound, taken verbatim. The verifier's first note — that the retained exclusion clause is in practice unreachable and could simply be dropped — was **not** acted on: it is kept because it documents at the point of use why the exact arm excludes the row's own id and the overlap arm does not, and the two arms sitting three lines apart with opposite treatment is precisely the shape that produced this defect. It is self-documenting, not load-bearing, and the code says so._

_The remarks at `:699-702` were rewritten, which the finding called for and which matters more than the code change. Their old justification — "a row whose own recorded resource still owns the range is not stale in the first place and never reaches here" — is true of the exact range and false of any subset or superset, because `EvaluateSubnetLevel` tests membership of the **exact** recorded prefix. That sentence is what made the exclusion look correct in review. It now states the distinction explicitly and names the in-place-update case._

_**Sibling sweep** (`grep -rn` for `Equals(o.ResourceId`, `Equals(c.Owner.ResourceId`, `snapshot.AzureResourceId` across `src/`, plus every caller of `FindLiveOwnerOfRange` and every bidirectional `IsSubnetContainedInParent` pair). Two self-exclusions exist in the codebase and both are in this one method: the exact arm at `:750`, which is correct and untouched, and the overlap arm at `:769`, which is the defect. `FindLiveOwnerOfRange` has exactly one caller. **The sweep's real result is the VNet-level sibling:** `EvaluateVNetLevel` asks the same question through `OverlapsRecorded` (`:667`, called at `:825`), resolving the VNet by the row's **own** `AzureResourceId` and deliberately **not** excluding it — which is exactly the behaviour this fix restores at the subnet level. The two levels were answering the same question differently; they now agree. Nothing else in `src/` implements this rule._

_**Strings checked** (step 6). The per-item reason is unaffected: the overlap branch at `:228-233` is already direction-neutral and cause-free, and reads correctly when the owner is the row's own subnet — driven output "Azure subnet 'sn-a' … now holds 10.231.1.0/25, which overlaps the recorded range 10.231.1.0/24". **One string is made false in a new way by this fix and is not repaired here:** the aggregate warning at `:275` asserts the withheld ranges are "still assigned in Azure under a different resource - which is what a subnet rename looks like". After this fix that sentence also covers same-resource re-carves, where both clauses are false. That sentence is **P8**'s entire subject, it is filed with a corrected replacement wording, and it is fixed there rather than pre-empted here — this entry records the dependency so the intermediate state is deliberate and traceable rather than discovered. The reason text's trailing remedy ("correct this subnet to match Azure") is likewise false, and is **P9**'s subject._

_Not done, deliberately, and it is the owner's call rather than the round's: **nothing recovers rows already archived by this path.** `DeletedSubnets` has no restore and the archive does not carry `AzureResourceId`, so a row this defect archived cannot be repaired through any screen. Closing that means either a one-off admin repair action or hand-written SQL at release time. It is the same open item O1/O2/O3 left on round 15's watch list, now for the third round, and it is why P1 is graded critical rather than high: the wrong output is not merely wrong, it is terminal._

_Tests: 892 → 894. Both fail against the unfixed reconciler with the row in `plan.Items` and `ReviewItems` empty — the archive being offered, which is the defect. `SubnetPrefixChanged_WhereTheRowsOwnAzureSubnetStillHoldsPartOfTheRange_IsWithheld` covers the narrowing direction and additionally asserts the review reason names the live `/25` and that **no** Re-link suggestion is offered, since an overlapping owner holds a different range. `SubnetPrefixChanged_WhereTheRowsOwnAzureSubnetNowHoldsAWiderRange_IsWithheld` covers widening. The existing counter-test `ASubnetThatStillExistsWithItsRecordedPrefix_IsReportedNowhere` pins the degenerate case the retained exclusion protects and still passes, so the fix cannot have over-blocked into it._

---

## P2 — `AccountsFor`'s containment arm silences the inbound check for any Azure range inside an ordinary BASTET row, including a row whose own free-space table prints that range as entirely unallocated `[x1]` — FIXED

_P2 is fixed and committed. `AccountsFor` is gone, replaced by `IsRangeRecordedByBastet`, which asks the question the item's own text asserts: does BASTET's free-space computation offer any part of this Azure range? It is answered with `CalculateUnallocatedRanges` — **the same call that renders the Details page** — so the reconciler and the screen cannot disagree about what is free. That function now has exactly two callers, `SubnetController.Read` and the reconciler._

_**The predicate the finding offered was not taken, because the verifier proved it wrong.** Gating on `existing.IsFullyAllocated` breaks on a partially tiled parent: a /18 holding 10.60.1.0/25 and 10.60.1.128/25 is not fully allocated, yet its free-space table correctly does not offer 10.60.1.0/24. Under that gate the /24 becomes an item that is false on its own wording and cannot be cleared — creating 10.60.1.0/24 is refused because the two /25s occupy it, and the only escape is marking the /18 fully allocated, which hides real free space elsewhere. `ARangeFullyCoveredByRowsInsideIt_IsNotReported` pins that case, and it is the test that decides the shape of the fix._

_`IsFullyAllocated` survives in one place only, as a **silencer**: a row marked fully allocated cannot receive children at all (`SubnetController.Helpers.cs:197` refuses it outright), so BASTET cannot hand the range out. That direction is sound; the positive test is what it cannot support. `ARangeUnderAFullyAllocatedRow_IsNotReported` pins it._

_**The owner decided to report.** The question put was: Azure has a subnet BASTET has no row for, BASTET has a bigger block covering that area — say it, or stay quiet? Reporting means one line per Azure range BASTET has no record of on the first scan after this change, each clearing for good once it is imported or a subnet is created for it. Staying quiet means the reproduced defect stands: BASTET created 10.70.1.0/25 while Azure held it, immediately after the scan reported clean. The decision was **report**._

_**Three existing tests asserted the opposite answer and were inverted, which is the substance of this change rather than an afterthought.** `ARangeContainedByACoarserBastetSubnet_IsNotReported` argued "an IPAM routinely records a coarser allocation than Azure carves out of it" — but a /18 with nothing under it does not record 10.90.77.0/24; open its Details page and that range is printed as free with a Create Subnet button, and creating there succeeds while Azure holds the addresses. It is now `ARangeInsideACoarserRowThatNothingRecords_IsReported`. `AHandReserveInsideTheVNetPrefix_StillAccountsForTheRange` (shipped by N3 as a false positive to suppress) is now `AHandReserveInsideTheVNetPrefixWithNothingUnderIt_IsReported`. `WhenNoVNetPrefixContainsTheRange_AContainingRowStillAccountsForIt` is now `ARangeOutsideEveryVNetPrefixThatNothingRecords_IsReported` — its stated reason was that such items are unclearable, and under the free-space test they are clearable: create 172.16.5.0/24 under the /16 and the item goes away._

_**Sibling sweep** — and it answers a question round 15 left open on the watch list, which asked whether any other consumer of "BASTET records this range" uses a proxy rather than the free-space computation. Grepped `CalculateUnallocatedRanges` and every non-test `IsFullyAllocated` read across `src/`. **No other consumer uses a proxy.** The four planner reads (`AzureBulkImportPlanner.cs:309, 333, 531, 580`), `SubnetController.Read.cs:138`, `HostIpValidationService.cs:226` and `SubnetViewModels.cs:113-114` all ask "can this row receive children or host IPs", which is what the flag actually means and is the same sound direction used here. `AzureReconciler` was the only place the flag or a bare containment test stood in for "not offered as free", and it no longer does._

_**Strings checked** (step 6). The item's own text — "…owns 10.70.1.0/25, which no BASTET subnet records. BASTET is reporting that range as free space." — was previously the wrong thing to say in the tiled case and is now true by construction, because the free-space computation is what decides whether it is emitted. The trailing remedy sentence appended to it is untouched here and is **P10**'s subject: it omits the host-IP arm, so it still tells the operator to import a target the wizard refuses._

_**Two behaviour changes recorded rather than buried.** (1) An Azure range outside the VNet's declared address space, reachable when a credential sees only part of a subscription, now produces an item where it was silent. It is clearable, and the old fallback's justification does not survive the new predicate. (2) `VNetPrefixContaining` became dead with the `vnetPrefix` parameter and was deleted — the "is the containing row inside the VNet prefix" question no longer decides anything, because the deepest containing row is used instead of any containing row, which was the real fix for the ancestor case._

_Host IP assignments are deliberately not fed to `CalculateUnallocatedRanges` here: the snapshot carries `HasHostIpAssignments` as a bool, not the addresses. Omitting them makes the computed free space **larger**, so the reconciler reports where it might otherwise stay silent. That is the safe direction for this check and is why the approximation is acceptable._

_Tests: 894 → 897. Three inverted as above, three added: the tiled counter-test, a half-covered case that must still report (one /25 inside the Azure /24 leaves the other half free), and the fully-allocated silencer._

---

# High

## P3 — The bulk import wizard states *"All subnets in this prefix are already imported"* over Azure subnets that were never imported, hiding the only text that says why they cannot be `[x1]` — FIXED

_P3 is fixed and committed. The branch now counts what the filter actually hid, using `statusName`, which is already on the wire. Three outcomes: all hidden rows `AlreadyImported` gives "All 2 subnet(s) in this prefix are already imported."; none of them gives "2 subnet(s) in this prefix cannot be imported, and are hidden. Untick \"Hide unavailable\" to see why."; a mix gives both counts in one sentence. The wizard can no longer claim an import that did not happen._

_The verifier's two notes were both taken: the imported clause is emitted only when the count is above zero, and the `<div class="ms-4 mt-1 text-muted small"><em>…</em></div>` wrapper is kept so the two empty-state messages still match. The text is set with `.text()` rather than interpolated into markup, matching the sibling branch below it._

_The finding's cheaper interim ("All subnets in this prefix are unavailable…") was **not** taken. It removes the false claim but throws away the counts, and the sibling branch eighteen lines below already prints counts for suppressed prefixes — one screen would have said "N VNet prefix(es) … cannot be selected" and the other "all unavailable". The full fix costs nine lines and makes the two consistent._

_**Driven in headless Chromium against live Azure and real SQL Server, all three branches, no hand-built posts.** Fixture `rig-p3-vnet` 10.171.0.0/16 in `bastet-visible`. (1) **Blocked only** — subnets `rig-p3-s1` 10.171.1.0/24 and `rig-p3-s3` 10.171.3.0/24 with BASTET rows `handmade-row` 10.171.1.0/24 and `partial-overlap-row` 10.171.3.128/25 created through the ordinary Create form, so neither Azure subnet was ever imported. Switch off renders both with the red **Cannot import** badge and their reasons; switch on now renders "2 subnet(s) in this prefix cannot be imported, and are hidden…" where HEAD rendered "All subnets in this prefix are already imported." (2) **All genuinely imported** — fresh catalog, both subnets imported through `BulkCreateFromAzurePlan`, annotation returns `AlreadyImported` for both, switch on renders "All 2 subnet(s) in this prefix are already imported." (3) **Mixed** — a third Azure subnet `rig-p3-s5` 10.171.5.0/24 blocked by a hand row, switch on renders "1 subnet(s) in this prefix cannot be imported and 2 are already imported, and are hidden…"._

_One fact worth recording because it is what makes the distinction real: **`AnnotateSubnet` genuinely does return `AlreadyImported`**, confirmed above with `sameAzureResource` firing on a child whose stored `AzureResourceId` matches the Azure subnet. That is unlike `AnnotatePrefix`, which never returns it — the comment at `_BulkScripts.cshtml:325` records that, and it is why the prefix-level message had to be worded without the word "imported" at all. The subnet-level branch can use the status because the status is trustworthy here._

_**Sibling sweep** — grepped every `already imported` / `AlreadyImported` literal in `src/` and every `isSelectable` filter in the views. Two places hide rows behind that flag: this subnet-level filter, and the prefix-level one at `:201`. **The prefix-level twin was already fixed** — its empty-tree message counts suppressed prefixes and deliberately avoids claiming they were imported, with a comment describing this exact defect. This was the last instance of the pattern; the single-VNet wizard's `_ImportScripts.cshtml` has no notion of selectability at all, which is **P5** and **P14**, not this. One adjacent silence is left intact and is not a false statement: when some prefixes are hidden but others still render, no message names the hidden ones, because `suppressedPrefixes` is only read on the empty-tree path._

_**Strings checked** (step 6). The three new sentences are the only text this fix produces, and each was read against the state that renders it, above. `Untick "Hide unavailable"` names the switch by its own label at `_StepSelection.cshtml:46` and matches the wording already used at `:336`. The two adjacent messages the finding lists as still silent are unchanged and remain true: the preview's "No child subnets selected." and the commit's success banner over zero writes both report zero accurately; they simply do not mention the refusals._

_No unit test ships with this. The change is jQuery inside a `.cshtml`, and there is no JavaScript test infrastructure in the repository; the three-branch measurement above is the verification, recorded here as the durable record. Tests: 897, unchanged._

---

# Medium

## P4 — A pause of the holder makes BASTET report a durable subnet write as failed: a /8 was resized to /9 while the page said *"Error updating subnet"* `[x1]` — FIXED

_P4 is fixed and committed. `SqlSaveOutcome` (`src/Bastet/Services/Data/SqlSaveOutcome.cs`) answers one question - **did this failure leave the outcome unknown?** - and seven catch sites now branch on it before asserting anything. They say "BASTET could not confirm whether this change was applied. Reload ... before retrying." None claims the write happened; none claims it did not._

_**All four verifier corrections taken.** (1) The predicate is not `-2`. It is timeout **or** transport failure - `-2, -1, 20, 64, 121, 233, 10053, 10054, 10060` - because a connection reset after the server committed is exactly as ambiguous. The watch list said this predicate "has not been written down anywhere"; it is written down now, once. (2) The stale-token trap is closed. (3) Create paths get wording only, and the code says why: no key to re-read by. (4) `SQL_TIMEOUT_ERROR_NUMBER` was a `private const` in the locking service so "reuse it" would not compile; it is now `SqlSaveOutcome.CommandTimeout`, consumed by that service, so the copies cannot drift._

_**The stale-token fix is placed better than proposed.** The verifier wanted the re-read and `ModelState.Remove` added to the generic catch. But the block that runs last on every failure path already re-reads with `AsNoTracking` and assigns `viewModel.RowVersion`; its own comment calls it "what actually reaches the view". Only the POSTED token needed clearing from `ModelState`, which the tag helper prefers. One line there covers every failure path, including ones no catch covers._

_**Sibling sweep, and the three siblings were FIXED rather than deferred.** Sweeping every `catch (Exception` on a guarded write turned up three more that assert something stronger than the four filed: `BulkAzure.cs` and `AzureReconcile.cs` both ended "**and no changes were saved**", and `Azure.cs` reported an unexpected error the same way. Those wrap their work in a transaction, so the ambiguous moment is `CommitAsync`, which throws a bare `SqlException` rather than a `DbUpdateException` - which is why the first predicate did not see them. That is a reason the fix needs a second entry point, not a reason to skip it, and skipping it would have handed round 17 three findings of the shape this round exists to stop. `IsIndeterminateTransaction` accepts both shapes and is used at those three sites. It is deliberately wider: those blocks wrap reads too, so a read timeout also lands there. That direction is safe - nothing was written and "could not confirm" is merely cautious - whereas asserting nothing was saved when it was is the defect. `HostIpController`'s delete says only "Error deleting host IP" and asserts nothing about what was saved, so it is left alone._

_**Driven against real SQL Server, both halves, no fault injection.** A sqlcmd session held `BEGIN TRAN; SELECT ... WITH (UPDLOCK, HOLDLOCK); WAITFOR DELAY`, so guarded reads ran and only the UPDATE queued past the 30s command timeout. (1) The branch fires on the real shape: `POST /Subnet/Edit/1` Cidr 8 → 9 returned HTTP 200 in **30.1s** carrying "could not confirm", where HEAD said "Error updating subnet." That is the proof EF surfaces this as `DbUpdateException` wrapping `SqlException` -2; any other shape would have left the branch dead. (2) With the row moved underneath between GET and POST, the form posted `RowVersion=AAAAAAAAB9E=` and the redisplayed form carried `AAAAAAAAB9I=`, the current value. On HEAD it redisplayed the stale one, so Save-again answered "modified by another user" about the operator's own write._

_**Strings checked** (step 6). Seven messages replaced, each read against both states it can appear in - rolled back, and committed. `TimeoutException` above still says "Please try again", which stays correct: a lock-acquisition timeout writes nothing._

_Tests: 897 → 915. `SqlSaveOutcomeTests` covers the nine indeterminate numbers and - the counter-test that matters - six determinate ones that must **not** be swallowed: PK violation, duplicate key, FK violation, deadlock victim, permission denied and zero all mean the write did not happen. `SqlException` has no public constructor, so the number test is split into `IsIndeterminateErrorNumber` and the exception-shape half is covered by the live run above._

---

## P5 — The single-VNet import wizard's subnet list filters on exact prefix only, so it offers Azure subnets the commit refuses — and one such row discards every other selection in the same batch `[x1]` — FIXED

_P5 is fixed and committed, **and it closes P14 in the same change** because both are the same endpoint and the finding says the decision should be taken once. `GetSubnets` now **annotates instead of filtering**: every Azure subnet comes back carrying `status`, `reason` and `isSelectable`, using the same `BulkImportAvailability` enum the bulk wizard uses so one Azure subnet cannot be described in two vocabularies. `_ImportScripts.cshtml` renders a refused row disabled, with a red **Cannot import** badge and the reason, instead of dropping it._

_**The owner-decision question is answered by taking the full fix, not the interim.** The wizard now has the per-row status/reason contract the finding asked about._

_**Both defective proposals were avoided, and the verifier was right about both.** (1) The "cheaper interim" - drop rows overlapping an existing BASTET subnet in either direction - would have emptied the list on **every** VNet, because every offered row is inside the target by construction and the target's own row is always in the table. `GetSubnets_ARowThatWouldContainAnExistingSubnet_IsBlockedWithAReason` pins that a clean row survives, which is the assertion that would fail if anyone reintroduces it. (2) Mirroring `AnnotateSubnet` verbatim would have left the more-specific-parent case unhandled, because the bulk annotator leaves that test "to the plan" for want of a target. **This endpoint has a bound target, so the test is well defined here** - an existing row blocks only when it strictly contains the range and is itself strictly inside the target - which is exactly the case the bulk annotator cannot decide and this one can. The rule is written here rather than shared verbatim, and the code says why._

_The new predicate sits **behind** the `FullyEncompassesVNetPrefix` short-circuit, not beside it as a separate `.Where`, which would have dropped the encompassing row again and re-broken the import O6 restored._

_**Driven end to end in headless Chromium against live Azure**, fixture `rig-p5-vnet` 10.94.0.0/16 with s1-s4, s1 imported and hand rows `hand-2-128` 10.94.2.128/25 and `hand-4-23` 10.94.4.0/23 created through the ordinary form. The subnet step now renders: **s1** "Already imported" disabled; **s2** "Cannot import - Would contain existing Bastet subnet 'hand-2-128' (10.94.2.128/25)..."; **s3** plain and selectable; **s4** "Cannot import - A more specific Bastet parent subnet exists: 'hand-4-23' (10.94.4.0/23)...". On HEAD s2, s3 and s4 were three plain enabled checkboxes with no status and s1 was simply absent. Clicking **Select All** then ticked **only s3** - the three refused rows stayed unchecked and disabled - and Import returned "Successfully imported 1 child subnets." with row 5 written. On HEAD the same three-row selection returned one banner naming `hand-2-128`, a row the operator never selected, and wrote **nothing**: s3 was lost purely for sharing the batch._

_**Sibling sweep, and it found a defect this fix would otherwise have introduced.** Rendering rows disabled makes the wizard's own Select All dangerous: jQuery's `:checked` matches a disabled input, so `$(".subnet-checkbox").prop("checked", true)` would have ticked refused rows, counted them and enabled Import over them - the exact trap `_BulkScripts.cshtml` documents and guards with `:not(:disabled)`. Both the master handler and `syncSelectAllSubnets` are now scoped to `:not(:disabled)`, and the browser run above is the proof. The third limb of the filing - allocated space shown as free after a refused import - was already refuted by the verifier as a property of the hierarchy model rather than this line, and is not addressed here._

_**Strings checked** (step 6). Three reasons are new and each was read against the state that produces it, above. The "No compatible subnets found in this VNet" message now fires only when Azure genuinely returns nothing, because rows are no longer removed - which also retires the misreport the finding named._

_Tests: 915 → 918. `GetSubnets_TheRowEncompassingTheTargetPrefix_SurvivesTheAlreadyRecordedFilter` asserted the old drop-based contract ("the already-imported child is still dropped") and was rewritten for the annotate contract. Three added: the would-contain case with a clean-row counter-test, the more-specific-parent case, and P14's control that an **empty** target keeps the encompassing row selectable._

---

# Low

## P6 — Reconcile Re-link has no outcome surface of its own: when the automatic re-scan fails, a successful re-link is reported as a failed scan, and a failed re-link asserts a re-scan that never happened `[x1]` — FIXED

_P6 is fixed and committed. Re-link now has its own success panel, `#rec-relink-success`, a sibling of `#rec-relink-error` and like it **outside** `#rec-scan-content` so the automatic re-scan cannot wipe it. It is filled from the 200 body's `azureSubnetName` and shown **before** `runScan()`, so the statement that the write happened does not depend on a second request succeeding. It is cleared by the explicit scan button and by `showRelinkError`, so only one outcome is ever on screen._

_**Part (2) was taken in the corrected form, not as filed.** The hard-coded sentence at `_StepReview.cshtml` moved into `#rec-relink-rescan-note`, and it is written in **both** directions - "could not be re-scanned" by `showScanError`, "have been re-scanned" by `renderPlan` where the content is revealed. The filing and its interim both mutated it one way only with nothing to put it back, which the verifier showed would convert the false sentence into its mirror image: any scan failure, including the operator's first, would have latched "could not be re-scanned" for ever, so a later successful re-scan would assert the results were stale while they demonstrably were not. `runScan`'s two terminal handlers are the sole writers, which is the single-writer-per-outcome discipline the rest of the file uses. The TempData banner O16 removed was **not** restored, for the reason O16 recorded._

_**Driven in headless Chromium against live Azure and real SQL Server.** Fixture `rig-p6-vnet` 10.179.0.0/16, subnet imported then deleted and recreated on the same prefix so the row carries `RangeStillAllocatedInAzure` with a Re-link suggestion. **Limb (a), the case the fix exists for:** with `**/ReconcileScan` aborted at the transport layer from the moment Re-link is clicked - the app-recycle-between-the-two-POSTs case - the page now shows the success panel "Re-link saved. This subnet is now linked to Azure subnet 'rig-p6-s1-again'." **alongside** the scan error "Nothing was checked ... Because Azure could not be read ...", and `sqlcmd` confirms the write landed. On HEAD the same run showed only the scan error and a `document.body` scan for "Re-link" returned nothing: a committed repair reported as a failure. **The note, both directions:** after an aborted scan it reads "The results below could not be re-scanned, so they may not reflect Azure as it is now."; after a successful scan on the same page it reads "The results below have been re-scanned, so they reflect Azure as it is now." It flips rather than latching, which is the whole point of the correction._

_**Sibling sweep.** Grepped every panel toggled from more than one outcome in the three wizards. `#rec-scan-error`, `#rec-scan-warnings` and `#rec-scan-content` each have a single writer already. The watch list's wider question - other hard-coded sentences in Razor that the surrounding JavaScript can falsify - was checked in `_StepReview.cshtml`, `_StepSelection.cshtml` and `_BulkScripts.cshtml`: this was the only sentence asserting a fact about something that had not happened yet. The bulk wizard's static text describes what a control does, not what has occurred._

_**Strings checked** (step 6). Two new sentences, both read against the state that produces them, above. The success message degrades to "This subnet is now linked to its current Azure resource." when the response carries no name, so it can never render an empty quotation._

_No unit test ships with this: it is jQuery and Razor markup, and the repository has no JavaScript test infrastructure. The browser measurements above are the record. Tests: 918, unchanged._

---

## P7 — Sign-out no longer clears the TempData cookie, so an unread success banner naming another operator's subnet renders to the next principal on that browser `[x1]` — FIXED

_P7 is fixed and committed. `TempData.Clear()` runs in `Logout` before the environment branch, which is where the removed loop used to sit. `Clear()` forces the lazy `Load()`, so the globally registered `SaveTempDataFilter` reaches the provider with an empty dictionary and `CookieTempDataProvider` deletes **its own** cookie using its own name, path and SameSite options. Placement before the branch matters: the filter registers its `Response.OnStarting` callback in `OnResourceExecuting`, so the delete is appended on all three exits including the Production OIDC branch that returns `EmptyResult`._

_Neither rejected alternative was taken, and both are recorded so they are not tried later. Rendering `_TempDataAlerts` on `SignedOut.cshtml` does not work: a federated sign-out redirects to the identity provider and never loads that page. `Response.Cookies.Delete(".AspNetCore.Mvc.CookieTempDataProvider")` hard-codes the name and omits the provider's path and SameSite options, emitting a `Set-Cookie` the browser may not match._

_**O13's comment was corrected, which matters more than the line of code.** Removing the cookie loop was right - it expired every cookie the browser sent, including ones Bastet never issued, on an anonymous cross-site-navigable GET. But its justification said the TempData cookie "is re-minted on the next request", and that is false: ASP.NET Core removes a TempData entry only on **read**, which this codebase documents at `ErrorPageMessages.cs:37-42`. The loop had been the only thing deleting it. The comment now says so and points at the explicit `Clear()`, so the next person to tidy this does not remove the replacement for the same wrong reason._

_**Driven against a real server.** Posted `/Subnet/Edit/1` renaming a subnet and deliberately did not follow the redirect, leaving the entry unread with the TempData cookie held in the jar. `GET /Account/Logout` now answers with exactly **one** `Set-Cookie`: `.AspNetCore.Mvc.CookieTempDataProvider=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/; samesite=lax` - produced by the provider itself, not by a hard-coded string. The following `GET /Subnet` on the same jar rendered **0** `alert-success` elements. On HEAD that logout carried no `Set-Cookie` at all and the next page rendered the green banner naming the subnet._

_**Sibling sweep.** Twelve `TempData["SuccessMessage"]` writers across the controllers. Eleven set it immediately before a redirect the browser follows, so the entry is read on the next request. The twelfth, `SubnetController.AzureReconcile.cs:253`, sets it from a **JSON** endpoint whose client navigates after a 2000ms `setTimeout` - which is what makes an unread entry ordinary rather than exotic, and it is the reconcile delete, so the queued sentence asserts a completed destructive action. It is not itself a defect and is left as it is; clearing at sign-out is what stops it reaching the wrong principal, and that is now unconditional._

_**Strings checked** (step 6). No message text changed. The banner that used to leak is unchanged - it simply no longer survives a sign-out._

_Two consequences accepted deliberately: the delete header is emitted even when no TempData cookie was presented, which is a no-op at the browser; and since `/Account/Logout` is `[AllowAnonymous]` and cross-site navigable, a forged logout now also drops the victim's TempData cookie - Bastet's own cookie holding a message pending display, which is exactly what a sign-out should discard. Tests: 918, unchanged - the mechanism is cookie lifetime across a redirect the suite cannot exercise, so the live measurement above is the record._

---

## P8 — The aggregate withheld-from-deletion warning still asserts O1's exact-match sentence for an overlapping owner, on the panel headed *"Check this before deleting anything"* `[x1]` — FIXED

_P8 is fixed and committed. The warning now reads: "N subnet(s) were withheld from deletion because a live Azure resource still overlaps the range they record, so the recorded range and the live range are not the same. Archiving them would make BASTET report an allocated range as free space: ..." It asserts neither a rename nor a whole range._

_**The finding's cheaper interim was taken over its primary fix, because the verifier showed the primary's replacement wording was itself wrong** - which is fatal for a finding about wording precision. Three things had to be true at once and the offered text was not. (1) **Direction.** The overlap test is bidirectional: BASTET can record a /25 that Azure re-created as the containing /24, so "part of the range they record is still assigned" understates exactly as badly as the old sentence overstated. (2) **Cause.** "The Azure subnet was re-carved rather than removed" invents a cause the reconciler never established - the non-exact branch is reachable for a **VNet-level** row, where no Azure subnet was re-carved at all. (3) **Mechanics.** The `List<LiveRangeOwner>` type change the primary fix wanted needs an overload or a projection at `OwnerList`, which the "no new data, roughly fifteen lines" estimate omitted. The single direction-neutral, cause-free sentence is true for all three shapes and needs no type change._

_**This is P1's residue and was always going to be.** P1 widened the overlap arm to include the row's own resource, so it pushes strictly more rows into this warning - including same-resource re-carves, where the old sentence's "under a different resource" is false on its face. P1's entry recorded the dependency rather than pre-empting it; this is the commit that discharges it._

_**Sibling sweep** - every `plan.Warnings.Add` in the reconciler, six in all. The other five each state a fact the code actually established before writing it: a direct read returned 403 ("Azure denied access"), the read failed ("could not be asked"), the direct read succeeded ("still exist in Azure"), the cascade reason is passed in by the caller, and the empty-subscription warning reports counts. **None asserts an unverified cause**, which is the specific defect here. `NameList` and `OwnerList` were checked for the same class of problem and carry no causal claim._

_**Strings checked** (step 6) - this finding is a string. The new sentence was read against all three shapes it covers: rename (different resource, exact range), re-carve (same or different resource, partial or wider range), and VNet-level. Tests: 918 → 920. `AWithheldRange_ProducesAWarningNamingTheAzureSubnetThatHoldsIt` pinned the discredited phrase "still assigned in Azure" and now asserts the durable part - that the warning names the Azure subnet and says the rows were withheld. Two added: the widening re-carve, which asserts the two false clauses are **absent**, and a VNet-level row._

---

## P9 — The two reason strings O1 and O2 added to the reconcile review screen tell the operator to *"correct the recorded range"*, which `SubnetController.Edit` refuses for every row that can carry those statuses `[x1]` — FIXED

_P9 is fixed and committed. Both strings now name only routes that exist. The VNet-level arm reads "Either restore the VNet's original address prefix in Azure, or delete this BASTET subnet and import the current prefix again - its recorded range cannot be edited while it is linked to Azure." The subnet-level overlap arm reads the same shape but names the **subnet**: "either restore the Azure subnet at the recorded prefix, or ...". The XML doc on `VNetPrefixStillCovered` was updated to match._

_**The verifier's correction is the whole point of the fix and was taken.** The finding said to give `:232` the "same shape" as `:820`, but `:820`'s Azure-side action - restore the VNet's address prefix - is the **wrong** action for the subnet-level arm, whose live owner is a subnet, not the VNet address space. Copying it literally would have swapped one false instruction for another, in a finding about false instructions. The two arms now differ in exactly the noun that differs._

_The impossible clause was impossible by construction, not by accident: both statuses are only ever produced for a row with a non-empty `AzureResourceId`, and `SubnetController.Edit.cs:91-95` refuses a CIDR change on exactly that population, unconditionally. The XML doc now records that reasoning next to the enum member, so the next person to write a remedy for this status sees why "correct the recorded range" is not one._

_**Driven live**, and the same scan discharges three other findings at once - which is the composition check step 5 asks for rather than four separate runs. Fixture `rig-p6-vnet` 10.179.0.0/16, its subnet narrowed **in place** to 10.179.1.0/25 (`az network vnet subnet update`, ARM id preserved). One `POST /Azure/ReconcileScan` returned: `canCommit: false`, `items: 0`, the row withheld as `RangeStillAllocatedInAzure` (**P1**), carrying the new reason text with no impossible remedy (**P9**), under the new direction-neutral warning "a live Azure resource still overlaps the range they record" (**P8**), alongside a correct `AzureRangeNotImported` for the 10.179.1.128/25 the re-carve released, which BASTET now shows as free (**P2**). On HEAD that row was offered for deletion with `canCommit: true` and no warning at all._

_**Sibling sweep** - grepped `src/` for every operator-facing string advising an edit of a recorded range ("correct the recorded", "correct this subnet", "edit the recorded", "change the prefix"). After this change there are **no** others, and the remaining match is `Edit`'s own refusal message, which says "Change the prefix in Azure and re-import, or delete the subnet and recreate it" - the same two routes. The reconcile screen and the editor now give the same advice instead of contradicting each other._

_**Strings checked** (step 6): both replacements were read against the live output above, and each names an action that exists - restoring the Azure resource, or delete-and-re-import, which round 15 measured as working. Tests: 920, unchanged. No test asserted any substring of either reason (checked before and after), so there was no test churn either way; the strings are pinned by the live scan recorded here._

---

## P10 — O5's new whole-prefix remedy omits the host-IP arm, so the reconcile screen says *"Import it to mark that subnet fully allocated"* about a target the import wizard refuses and the Details page renders no import link for `[x1]` — FIXED

_P10 is fixed and committed. The remedy is now three-way: host IPs, then children, then the actionable "Import it to mark that subnet fully allocated." `HasHostIpAssignments` was already on `ExistingSubnetSnapshot`, populated by `AzureSubnetSnapshotService`, and simply unread here - while every downstream gate refuses on it. The new sentence says "which is refused while it has host IP assignments, so remove those first."_

_The verifier judged the fix sound and its one nuance is recorded in the code rather than left as a surprise: when a target has **both** host IPs and children, this branch names the host IPs while the commit-time gate in `HostIpValidationService` happens to test children first. Both are true blockers, so the message stays honest; only the clearing order differs. The interim (drop the trailing sentence entirely) was not taken - it leaves the operator with a true statement and no next step, and the full version is five lines._

_**Sibling sweep** - the question this branch answers is "why can this target not be imported", and the authority is the set of gates that refuse. All four were checked and all four refuse on host IPs: `AzureController.Import` redirects with "Subnet must not have any host IP assignments"; `ViewBag.CanImportFromAzure` requires `HostIpAssignments.Count == 0`, so the Details page renders no `/Azure/Import/{id}` link at all; `ValidateSubnetCanBeFullyAllocated` refuses; and `AzureBulkImportPlanner` blocks the prefix with its own reason. The reconciler was the only one of the five surfaces that did not know, and it is the one that tells the operator what to do. One case remains uncovered by any of them and is out of scope here, recorded so it is not lost: a whole-prefix target linked to a **different** VNet, where the planner blocks with "already linked to Azure VNet ...". No third consumer of `HasHostIpAssignments` was found that needs the same arm._

_**Strings checked** (step 6): the new sentence names an action that is reachable - removing host IPs - and the verifier's control confirmed the remedy works once they are gone (`POST /HostIp/Delete` then the identical bulk commit returned `fullyAllocatedTargets: 1`). Tests: 920 → 923. One for the host-IP arm asserting the old sentence is **absent**, one that the children arm still reports children, and the over-blocking counter-test that a clean target is still told to import._

---

## P11 — O8's rename guard covers only one arm of the flash ternary: the fully-encompassing import still announces *"Successfully renamed parent subnet to 'X'"* when no rename happened `[x1]` — FIXED

_P11 is fixed and committed. The encompassing arm now consults `parentRenamed` like the middle arm does, and when no rename occurred it reads "Marked 'X' as fully allocated by Azure subnet 'Y'."_

_**Both of the verifier's corrections were taken, including the one the finding omitted.** Kept: the four-line ternary change - at that point `parentSubnet.Name` already holds the post-write value, so the else-branch text is accurate. Added: **`parentSubnet.Name` now replaces `vnetName` in both rename arms**, because the true branch interpolated the raw posted value, so a VNet name over 100 characters would have reported a rename to a string that was truncated before it was stored. Dropped: the fix's second half, which wanted the empty-`vnetName` leg refused - that guard already exists at `SubnetController.Azure.cs:193-201` and cannot be bypassed, since `:389`'s condition is the precise negation of `:193`'s, so adding another would have been unreachable code with a weaker message. The finding's own "second, API-reachable leg" sub-claim was driven and struck by the verifier for the same reason._

_**The comment was rewritten, and that is the part that matters.** Its second sentence - "the parent was always renamed on the path that reaches it" - is the false premise that produced the bug, and leaving it beside a now-conditional arm would invite the guard being deleted again. Its first sentence is true and is kept: the validator refuses a parent with children, so this arm cannot fire on a top-up. The comment now states exactly why `parentRenamed` can be false here: the row's name already equals the VNet name, which is the steady state after any earlier import of the same VNet._

_**Driven in headless Chromium through the real wizard.** Fixture `rig-p11-vnet` 10.61.0.0/24 whose single subnet covers the whole prefix, with the BASTET row created **already named** `rig-p11-vnet` - no crafted request, just the ordinary habit of naming a placeholder row after the VNet it is about to hold. Subscription → VNet → tick → Import landed on Details with the banner **"Marked 'rig-p11-vnet' as fully allocated by Azure subnet 'rig-p11-sub'."** and `sqlcmd` showing `Name=rig-p11-vnet | fully=1` - the name unchanged before and after, only `IsFullyAllocated` written. On HEAD the same run announced "Successfully renamed parent subnet to 'rig-p11-vnet' and marked it as fully allocated..."._

_**Sibling sweep** - every claim of a rename in the codebase. The bulk path is sound and needed no change: `AzureBulkImportPlanner` sets `WillRename` only when the proposed name actually differs from the current one, and `BulkCreateFromAzurePlanCore` increments `totalTargetsRenamed` only inside a second equality check, so the bulk banner's "renamed N target(s)" counts real renames. This ternary was the only place asserting a rename it had not performed._

_**Strings checked** (step 6): three arms, each read against the state that produces it. Tests: 923, unchanged - the behaviour is a TempData string on a path with no controller-level seam, and the browser run above is the record._

---

## P12 — `HostIp/SetAllocationStatus` and `RelinkAzureSubnet` catch only `TimeoutException`, so a failing guarded save escapes as an unhandled 500 `[x1]` — FIXED

_P12 is fixed and committed for `SetAllocationStatus` only. It now has the handler its siblings already have - log, `TempData["ErrorMessage"]`, redirect to Details - plus an indeterminate branch above it, so a save whose outcome is unknown is not reported as a failure. `HostIpController` already had a logger in its primary constructor, so the injection the finding warned about was not needed._

_**Three of the fix's four claims were dropped, all on the verifier's measurements.** (1) **The `RelinkAzureSubnet` arm is not taken.** Its justification - that the wizard would otherwise show "Server error: 500" - is false: driven in a real browser with the blocker running, the wizard rendered "The re-link did not happen. | The re-link failed. | Nothing was changed." and re-enabled the button, because `_ReconcileScripts.cshtml:435-441` falls back to a generic message when `xhr.responseJSON` is absent. There is nothing for that catch to repair, and the "Server error: " shape quoted belongs to the bulk-delete handler, which already has its catch-all. (2) **The prioritisation was inverted** - the finding said do Relink first because `SetAllocationStatus` "at least reaches an error page"; it is the other way round, and `SetAllocationStatus` is the one that loses its modelled message. (3) **A bare `catch (Exception)` does not address the title's own claim** - under an acked-but-unread timeout the row really does commit, so "the operation failed" merely rewords the lie. That case is **P4**'s, which is why the indeterminate branch is here too and why this handler's own message never says "nothing was changed"._

_The finding's three refuted sub-claims stay struck and are not re-derived: the write **rolled back** rather than succeeding (that limb borrowed P4's SIGSTOP apparatus, which is not this finding's mechanism), no application lock is stranded (`sys.dm_tran_locks` was empty; the locking service releases in a `finally` before the exception propagates), and the Relink escalation above._

_**Driven against real SQL Server.** A sqlcmd session held `UPDLOCK, HOLDLOCK` on the row so the action's reads succeeded and only its UPDATE queued past the 30s command timeout. `POST /HostIp/SetAllocationStatus {SubnetId:1, IsFullyAllocated:true}` now returns **HTTP 302 in 30.2s** to `/Subnet/Details/1`, and that page renders the `alert-danger` "BASTET could not confirm whether the allocation status was changed. Reload the subnet to see its current state before retrying." On HEAD the same request returned **HTTP 500 `text/plain`** opening `Microsoft.EntityFrameworkCore.DbUpdateException`, with the modelled error contract never running._

_**Sibling sweep** - all eight lock-guarded write sites, which is how the finding itself was framed. Seven now have a generic handler: `HostIpController` Create `:167`, Edit `:318`, Delete `:437`, this one, and the three transactional paths in `SubnetController` (`Azure`, `BulkAzure`, `AzureReconcile`). The eighth, `RelinkAzureSubnet`, is deliberately left with none for the measured reason above - the client degrades correctly and a catch there would repair nothing. That is a decision with evidence, not an omission._

_**Strings checked** (step 6): two messages, and the indeterminate one deliberately avoids asserting that nothing was written, which is the trap P4 documents. Tests: 923, unchanged - the mechanism is a real SQL Server lock timeout that the SQLite suite cannot produce, so the live run is the record._

---

## P13 — The bulk selection screen marks a row `Available` and selectable with no reason when the preview will refuse the whole multi-VNet run for it — O11 mirrored only half of its own rule, on a justification that does not hold `[x1]` — FIXED

_P13 is fixed and committed. `AnnotateSubnet` now mirrors the more-specific-parent test as well as the would-contain one, through a `FindMoreSpecificParent` helper that resolves the VNet prefix containing the Azure subnet and blocks on any existing row strictly inside that prefix which strictly contains the range. The row is greyed with "Has a more specific existing Bastet parent 'X' (n/c), so it cannot be imported into this VNet prefix." - the plan-side wording, so the two screens agree._

_**The stated impossibility was simply false and the fix removes it.** O11's comment said the test "has no well-defined answer here" because the annotation pass has no target. But the plan-side test never uses the target either: it is scoped by `p.PrefixNetwork`/`p.PrefixCidr`, the VNet prefix, which `AnnotateSubnet` already holds as `vnet.Ipv4AddressPrefixes` - the same value its own encompassing test reads. That comment is deleted rather than left to justify the next half-mirror._

_**Both of the verifier's corrections were taken, and each has its own counter-test.** Containment is **strict** in both positions: strict when resolving the owning prefix, so an Azure subnet equal to a VNet prefix still hits the `encompassesAPrefix` early return; and strict when testing the existing parent, so the exact-match target itself is not treated as a more specific parent, which would refuse every ordinary top-up. `IsSubnetContainedInParent` is strict (`childCidr <= parentCidr` is false), so both fall out of using it in both positions._

_**The offered interim was not taken and must not be.** Moving the two O11 messages from `GlobalErrors` to the owning item's `Errors` does not make "a bad row only block its own prefix": `CanCommit` is `GlobalErrors.Count == 0 && Items.All(i => i.Errors.Count == 0)`, and `BulkCreateFromAzurePlanCore` returns a flat `BadRequest` on `!plan.CanCommit` before opening the transaction. There is no per-item commit, so it would relabel the message and block exactly as much as before._

_**Driven live against Azure.** Fixture `rig-p13-vnet` 10.63.0.0/16 with `rig-p13-recarved` 10.63.1.0/25, BASTET holding `PROD-CORE-63` 10.63.0.0/16 linked to that VNet and child `rig-p13-a` 10.63.1.0/24 - the ordinary "Azure re-carved a subnet after import" shape. `GET /Azure/BulkGetVNets` now returns `Blocked`, `isSelectable: false` with the reason naming `rig-p13-a`. On HEAD the same call returned `Available`, `isSelectable: true`, `reason: null`, and `POST /Azure/BulkImportPreview` over that VNet plus a clean one returned `canCommit: false` with a plan-wide error, refusing the clean VNet too._

_**Sibling sweep** - both commit-side tests in `DetectExistingBastetSubnetConflicts` are now mirrored in `AnnotateSubnet`, and the same pair is mirrored in `AzureController.AnnotateImportCandidate` for the single-VNet wizard by **P5**. Three copies of the rule, and after this round all three know both halves. That family - a predicate updated on the planner side only - is the watch list's highest-yield sweep, and P5, P13, P14 and P15 are all instances of it closed this round._

_Tests: 923 → 926. The mirror itself, plus the two over-blocking counter-tests the corrections exist for: an existing row **above** the VNet prefix (a 172.16/12 supernet) stays `Available`, and the exact-match target does not block its own children._

---

## P14 — O6 restored the fully-encompassing row to the single-VNet wizard but did not take O12's population guard, so on a populated top-up target the wizard offers exactly one selectable row whose only outcome is an error the bulk wizard greys out with a reason `[x2]` — FIXED

_P14 is fixed and committed **inside P5's commit**, deliberately. Both findings are the same endpoint and P14's own text says "It is the same decision as P5, on the same endpoint, and should be taken once" - and O6-and-O12 not composing is precisely what produced this finding, so splitting the fix across two commits would have repeated the mistake it records. `AnnotateImportCandidate` blocks the encompassing row when the target already holds children, with the bulk screen's own wording: "Covers the whole VNet prefix, which would mark Bastet subnet 'X' fully allocated, but it already has child subnets."_

_**The full form was taken rather than the one-conjunct filter, which answers the owner-decision question P14 raised.** The finding offered two shapes: filter the row out again (about four lines, leaving the wizard printing "No compatible subnets found in this VNet" on a VNet that visibly holds one subnet - correctly scoped but mute), or give the endpoint the same status/reason contract as the bulk one. P5 took the contract, so this row is now **shown, disabled, with the reason** instead of vanishing. That also closes O6's declared residue, which the finding named: a non-encompassing Azure subnet colliding with an unrelated BASTET row is no longer silently dropped either - it is greyed with its own reason._

_**Both over-blocking routes the verifier checked are covered by tests.** (a) An **empty** target keeps the encompassing row selectable, which is O6's restored mark-fully-allocated path: `GetSubnets_TheEncompassingRow_StaysSelectableWhenTheTargetIsEmpty` asserts `isSelectable: true` and no reason. (b) The predicate is "any row inside the target", which is right rather than a fuller mirror of `ValidateSubnetCanBeFullyAllocated`: that validator's other arm is host IPs, and `AzureController.Import` already redirects a target carrying host IPs away before the wizard renders, so children is the only reachable arm here._

_**Proved by the same browser run recorded under P5**, which is the composition check rather than a second isolated one. In the rewritten `GetSubnets_TheRowEncompassingTheTargetPrefix_SurvivesTheAlreadyRecordedFilter`, a target holding a child now returns the encompassing row disabled with "already has child subnets"; on HEAD it was the only selectable row on the screen and clicking Import rolled back with "Cannot mark a subnet as fully allocated if it has child subnets."_

_**Sibling sweep** - the population rule now exists in three places and all three agree: `AzureBulkImportPlanner.AnnotateSubnet` (O12), `AzureController.AnnotateImportCandidate` (this), and `ValidateSubnetCanBeFullyAllocated` at commit. That was the divergence: two of three knew it. See P13's entry for the wider sweep of planner-versus-controller predicates, which this is one of four instances of._

_Tests: counted under P5 (915 → 918); the empty-target control and the rewritten encompassing test are P14's._

---

## P15 — The single-VNet wizard creates two BASTET rows with the identical name and identical `AzureResourceId` when a multi-prefix Azure subnet is imported one prefix at a time `[x1]` — FIXED

_P15 is fixed and committed. `ResolveImportNames` takes the persisted tree as a second parameter and ORs in `HasPersistedSiblingFromSameAzureSubnet` - the bulk planner's predicate verbatim: qualify when any persisted row carries the same `AzureResourceId` but a different `{NetworkAddress, Cidr}`. The caller already had `treeCache`, the whole table read once for the batch, so no new query was added._

_The batch-only test could **never** fire on the shape that matters, which is why this was unreachable rather than rare: `AzureController.GetSubnets` does not offer prefixes BASTET already records, so a top-up of a multi-prefix Azure subnet always posts exactly one row. An Azure subnet that gains a second prefix after its first import can only ever be topped up one row at a time._

_**Driven twice through the real wizard in headless Chromium**, fixture `rig-p15-s` carrying 10.88.20.0/24 and 10.88.21.0/24 on one Azure subnet. Pass 1 offered both prefixes and imported .20 as `rig-p15-s`. Pass 2 offered both again with the .20 row now **disabled** - P5's annotation contract, where HEAD simply dropped it - and importing .21 persisted **`rig-p15-s (10.88.21.0-24)`**. Final rows: `2|rig-p15-s|10.88.20.0/24` and `3|rig-p15-s (10.88.21.0-24)|10.88.21.0/24`. On HEAD both rows were `rig-p15-s` with the identical `AzureResourceId`._

_Stated rather than left as a surprise, and it is deliberate: the fix qualifies only the newly-added row and leaves the already-persisted first row bare, so the pair reads `rig-p15-s` / `rig-p15-s (10.88.21.0-24)`. That is exactly what the bulk planner produces for the same input, and it resolves the harm because the names now differ._

_**The interim was corrected before being considered and then not needed.** Making `NameList` print `name (network/cidr)` would have closed the one surface where the rows are indistinguishable, but the verifier's correction stands: amending `OwnerList` as well is pointless, since it lists Azure-side owners (`ResourceId`, `SubnetName`, `VNetName`) which have no network or CIDR and are not the duplicated BASTET rows. With the primary fix the names differ everywhere, so neither is needed._

_**Sibling sweep** - the qualification rule now exists in two places, `AzureBulkImportPlanner` and `ResolveImportNames`, and both halves are the same predicate. The durable form is one shared helper called from both, which is **not** done here and is a recommendation to the owner: the two live in different assemblies-worth of concern (a service versus a controller static test seam) and merging them means moving one of them, which is a refactor rather than this fix. They are now identical in behaviour and each names the other. This is the fourth instance this round of the planner-versus-controller divergence the watch list flags - with P5, P13 and P14 - and all four are closed._

_Tests: 926 → 929. The persisted-sibling case, which fails against the unfixed code by returning the bare name; plus two counter-tests - an ordinary single-prefix import with an unrelated persisted row keeps its bare name, and a row's **own** persisted record is not treated as a sibling, so re-posting the same prefix does not qualify it against itself._

---

# Re-audit of the reconciled tree — findings P16 onward

**Target branch:** `audit/round-16` · **HEAD:** `7c29c32` · **Baseline:** 929 tests passing · **Findings P16–P35**

**Twenty findings survived this pass, and twelve of them are residue of round 16's own fixes.** That is 60% against round 16's own 73% (11 of 15), which is not an improvement worth claiming: **the fix process is still the problem.** The sibling sweep and the string-truth step did stop some of it — where a round-16 fix was swept, this pass found the sweep's *boundary* rather than the same defect again (P2's sweep enumerated the free-space consumers and missed the arm that returns before the computation runs; P5's sweep scoped the master checkbox to `:not(:disabled)` and did not consider the empty set that scoping creates). But three fixes leaked three findings each and one leaked two, and two of them shipped a new defect outright: P4's one-line `ModelState.Remove` defeats optimistic concurrency on the subnet Edit form (**P18**), and P2's new consumer routes a zero-address free range onto the reconcile screen (**P28**).

`P2←P17,P28,P29 · P4←P18,P19 · P5←P27,P32,P34 · P8←P23 · P15←P25` — ten against P1–P15. Two more were **filed** as round-16 residue and corrected by verifiers to earlier rounds: **P26** is round 14's N10 (the bulk planner's prefix-qualified target name, never mirrored into the single-VNet parent rename) and **P22** is round 15's O5 (the exact-match arm of the recorded-by-BASTET predicate). Twelve is the number the funnel carries; ten is the number after correction. Both are too high.

**Read P16 first, then P17.** They are the same wrong answer — an Azure-held range presented as free, with the control the product points operators at reporting clean — arriving by two routes the reconciler never looks down. P16 also archives, terminally, and then hands the range out. **Then read P18**, because it is round 16's own fix silently destroying another operator's write on the primary editing surface, with the concurrency warning that exists for exactly that case never rendered.

Eight of the twenty proposed fixes were judged unsound or incomplete and corrected inline. Four of those would have shipped a new defect rather than a wrong-but-harmless one: P17's predicate reports every correctly imported VNet prefix as unrecorded; P16's flat index omits VNet address prefixes and leaves the same archive open one CLI flag away; P22's predicate change silences a real hand-out; and P18's remedy re-creates the exact false sentence P4 was committed to remove.

## Decisions — this needs a decision from you and here is what it costs

| Finding | The decision | What it costs |
|---|---|---|
| **P17** (critical) — VNet address prefixes are never checked, so an Azure-held prefix produces a clean scan | Report unrecorded VNet address prefixes on the inbound walk, and if so, how to deduplicate against the per-subnet items sitting inside them? | One review item per VNet address prefix with no BASTET record, on a large estate's first scan, clearing for good on import — the same trade already accepted for P2 at subnet level. Without dedup the multiprefix fixture produced six items where four ranges exist: `10.102.0.0/16` alongside its own subnet `10.102.1.0/24`, and `192.168.10.0/24` alongside `192.168.10.0/25`. Two rows describing one gap on a screen headed *"Check this before deleting anything"*. |
| **P16** (critical) — reconcile archives a row whose range a *different* VNet in the same subscription still holds | The outbound guard closes the reproduced case. Should the inbound direction also report the recreated VNet's ranges, which means relaxing the `importedVNetIds` gate at `AzureReconciler.cs:349`? | Relaxing the gate costs one review item per unrecorded range on **every** VNet in the subscription, not only imported ones. The outbound guard alone is enough to stop the archive, so this is a separate scope call and can be deferred without leaving the defect open. |
| **P20** (medium) — reconcile step 1 promises hand-created subnets are never touched; the cascade archives them | Correct the sentence only, or also name the hand-created (`AzureResourceId`-empty) descendants in the confirm list instead of only counting them? | The sentence is four lines of Razor and closes the contradiction. Naming the descendants means carrying descendant names on `AzureReconcileItem`, widening the `#rec-go-confirm-btn` handler and one test — roughly one reconciler field plus a JS block. Step 3 already reports the correct cascade *count*; what it does not report is *which* rows. |
| **P21** (medium) — `DeleteConfirmed` binds no scope, so it archives whatever subtree exists at commit time | Bind the reviewed scope, and with what: reviewed-descendant identity, or the max-identity bound H4 already established for the purge paths? | Counts do not bind scope (a compensating delete-and-create leaves them equal) and refusing on any difference over-blocks a concurrent shrink — round 8 rejected that shape in writing. The sound forms are a hidden digest of the reviewed descendant ids, or two `int?` max-identity bounds mirroring `confirmedMaxId`. Either is two hidden fields, one recount inside the existing lock, one refusal branch. The same guard is missing at `SubnetController.AzureReconcile.cs:143`, so fixing only `Delete` closes half the class. |
| **P22** (medium) — the exact-match arm reports a fully-tiled VNet row with two false sentences and a destructive remedy | Change the predicate so a fully-tiled row goes silent, or leave the gate and fix only the false text? | The predicate change is about five lines plus two tests — but it silences the only signal that BASTET's model diverges from Azure's, and the verifier measured that BASTET **still hands out** `10.160.0.0/26` from inside those tiled children while Azure's `whole` subnet holds it. The string-only change removes the two measurably false sentences and keeps the warning. Take the string change unless you are content to lose the divergence signal. |
| **P24** (medium) — the migration lock's master fallback leaves EF's 30s lock as the only serialiser | Two lines (`SetCommandTimeout(330)` before each `Migrate()`), or restructure the bootstrap so both replicas contend in the same scope? | The two-liner was built and measured: the replica waits instead of crashing, then applies all six migrations. It does **not** restore the purpose-written *"Another replica appears to be stuck applying migrations"* message, so pair it with a catch around `Migrate()`. The restructure means creating the empty catalog explicitly under the master lock (`IRelationalDatabaseCreator.Create()`), releasing it, then taking `Bastet:Migration` on the now-existing catalog before migrating. The regression test can only ever be an integration test — lock scoping is a server property. |
| **P28** (low) — a free range of zero addresses at the parent's own network address | Fix only the reconcile consumer, or make `StartIp`/`EndIp`/`AddressCount` mutually consistent everywhere? | The consumer fix is one predicate at `AzureReconciler.cs:449` and was measured 929/929 green with the false item gone and the true one preserved. The display change touches every subnet in the product: a childless `/24` prints `10.0.0.0 - 10.0.0.255  254 IP addresses` today and would print `10.0.0.1 - 10.0.0.254  254`. It also **breaks the Create Subnet button**, which posts `StartIp` verbatim — measured: *"Network address 10.170.0.1 is not valid for CIDR /22."* Separately, the create path has no network/broadcast reservation at all, so a `/32` at a reserved address stays creatable; that is a third, larger decision. |

## Funnel — re-audit pass

| Stage | Count |
|---|---|
| Beats | 8 |
| Finders dispatched / returned | 20 / 20 |
| Raw findings | 44 |
| Dropped at merge (duplicates / out of scope) | 4 |
| Candidates carried to verification | 27 |
| — found by both passes `[x2]` | 6 |
| — found by one pass `[x1]` | 21 |
| Survived | 20 |
| — critical / high / medium / low / info | 2 / 2 / 5 / 9 / 2 |
| Refuted | 7 |
| Reproduced live | 19 of 20 |
| Not runnable | 1 |
| Residue of round-16 fixes | 12 |
| Browser-driven verification | yes |

The one **not runnable** entry is **P35**: the defect *is* the unreachability, so there is no state to drive into. It was still driven to the extent that matters — the endpoint was called and the channel confirmed empty on the wire while its sibling error channel populated on the same item.

**Discrimination re-verified.** Role assignments were read live, one each, both at **resource-group** scope, neither at subscription scope, so nothing inherits across groups: SP1 = Owner on `…/resourcegroups/bastet-visible`, SP2 = Owner on `…/resourcegroups/bastet-hidden`. The direct `az` matrix covers reads **and** writes in both directions — SP1→visible `READ 200 / WRITE 201`, SP1→hidden `AuthorizationFailed` on both; SP2 the mirror image (the write probes created and then deleted `rig-probe-write`). Through the application, two instances with per-principal credentials and `AZURE_TOKEN_CREDENTIALS` unset: SP1 on 21836 returned 6 VNets, every `resourceId` in `bastet-visible`, prefixes `10.100–10.106.0.0/16` + `192.168.10.0/24`; SP2 on 21837 returned 6 VNets, every `resourceId` in `bastet-hidden`, prefixes `172.16–172.22.0.0/16` + `192.168.20.0/24`. Zero cross-visibility in either direction. **Caveat for anyone reusing this rig:** VNet *names* are deliberately identical across the two groups, so names discriminate nothing — assert on the address-prefix family (`10.x` = visible, `172.x` = hidden) or on the resource group inside `resourceId`.

---

# Critical

## P16 — Reconcile archives a row whose recorded range is still allocated to a *different* VNet in the same subscription, then BASTET hands that range out `[x1]` — FIXED

_Fixed. `FindLiveOwnerOfRange` gains a subscription-wide arm after both VNet-scoped arms miss: a flat index carrying every live subnet prefix **and** every VNet address prefix, walked when the row's own VNet is absent from the inventory and the overlapping VNet is not itself recorded by a BASTET row, preferring a subnet match over a VNet-address-space match._
_Swept: every `livePrefixesByVNet` / `livePrefixOwners` / `PrefixKey` use — both VNet-keyed lookups live only in this method and both now fall through. Two existing counter-tests (`TheSameRangeAllocatedInADifferentVNet`, `AnOverlappingPrefixOnAnotherVNet`) forced the first gate; the cascade tests forced the second. `OverlapsRange` gained an equality arm because `IsSubnetContainedInParent` is strict and a recreated VNet's prefix is exactly equal._
_Verified: 933 tests. Live on Azure — `rig-p16-doomed` 10.198.0.0/16 + s1 imported, VNet deleted and recreated as `rig-p16-renamed` with identical space and subnet: scan now `canCommit false`, **0 items offered**, both rows `RangeStillAllocatedInAzure` naming `s1` and `10.198.1.0/24`. On HEAD: `canCommit true`, 2 items, empty reviewItems, delete committed._
_Not done: the filed fix's flat index omitted VNet address prefixes and would have left the no-subnets case open; the corrected form carries both. A separate VNet-address-space reason sentence was added because reusing the subnet sentence named a VNet as a subnet._

## P17 — Azure reconcile never checks VNet address prefixes, so a VNet prefix BASTET shows as free space produces a clean scan `[x1]` — FIXED

_Fixed. `ReportAzureRangesNoBastetSubnetRecords` now walks `vnet.Ipv4AddressPrefixes` as well as `vnet.Subnets`, emitting `AzureRangeNotImported` with `IsVNetLevel = true`, `AzureResourceId = vnet.ResourceId` and an address-space worded reason._
_Swept: the filed guard was unsound — `IsRangeRecordedByBastet`'s exact arm returns false for a VNet-level row that is not fully allocated, so an imported prefix would report itself. Guarded with an exact-row short-circuit before falling through, per the verifier. Per-subnet items falling inside a prefix already reported are suppressed so a range is never named twice._
_Verified: 938 tests. Live — `rig-p17-gap` declaring 10.196.0.0/16 + 10.197.0.0/16 with 10.196.0.0/16 imported: scan reports only `10.197.0.0/16` and is silent on the imported prefix. `az network vnet update --add addressSpace.addressPrefixes 10.198.0.0/16` on the already-imported VNet is now caught. Creating a BASTET row for 10.197.0.0/16 clears that item permanently. On HEAD all three cases returned `items=0 reviewItems=0`._
_Not done: `AMultiPrefixVNet_ScopesTheContainmentTestToThePrefixHoldingTheRange` asserted a total item count while being about subnet scoping; rescoped to assert its own subject. The "reporting as free space" wording is false when no BASTET row contains the range at all — that is **P31** and is fixed there._

## P18 — P4's `ModelState.Remove` refreshes the RowVersion token on every Edit re-display, so a second operator's concurrent change is silently overwritten with no concurrency warning `[x1]` — FIXED

_Fixed. The posted token is captured before the try; the tail block compares it against the re-read row and announces the change when they differ. `ownWriteMayHaveLanded` is set in the indeterminate catch only, so that path keeps its own "could not confirm" message instead of two._
_Swept: all three `ModelState.Remove(RowVersion)` sites. `SubnetController.Edit:189` is inside the concurrency catch, which already announces; `HostIpController:249` is likewise inside its catch and safe by construction. Only the shared tail block at `:257` — the one P4 added — refreshed the token silently on every failure path._
_Verified on real SQL Server, both directions: two operators → the notice renders before X can overwrite Y; one operator's own Back-and-resubmit → no "another user". On HEAD the two-operator case warned nothing and X's second save returned 302 with Y's Description and Tags gone._
_Not done: the filed fix reused the concurrency catch's "modified by another user" sentence, which the verifier showed mis-attributes a single operator's own stale re-post. The message is attribution-free instead — the server cannot know who moved the row. No unit test ships: SQLite does not persist `RowVersion` (the file's own `Assert.Null(stored)` pins that), so a passing test here would pass for the wrong reason._

## P19 — The subnet archive path asserts *"Error deleting subnet"* on an outcome it cannot know — the one lock-guarded transactional write P4's sweep missed `[x1]` — FIXED

_Fixed. `SqlSaveOutcome` now treats a `SqlException` carrying `Number == 0` as indeterminate when it has a transport inner exception (`IOException`/`SocketException`) or `Class >= 20`; `DeleteConfirmedCore` gains the indeterminate arm and redirects to `Index` rather than back to `Delete`, which rendered a 404 with no banner at all._
_Swept: all nine `IsIndeterminate` / `IsIndeterminateTransaction` sites share the predicate, so widening it closes the same hole at the three transactional sites P4 patched — they were protected against `-2` timeouts only. `git show --stat 931c5fb` confirms `SubnetController.Delete.cs` was never in P4's commit._
_Verified: 938 tests, 0 warnings._
_Not done: the filed arm alone would not have fired — the verifier proved the severed-commit shape is `Number == 0` on Linux managed SNI, so the predicate had to be corrected first. Round 16's `SqlSaveOutcomeTests` pinned `0` in the **determinate** set on the ground that it "means the write did not happen"; that counter-test asserted something false and is replaced by `AZeroErrorNumber_IsNotDecidedByTheNumberAlone`. `SqlException` has no public constructor, so the exception-shape half remains covered by live measurement rather than a unit test._

## P20 — Reconcile step 1 promises hand-created subnets are *"never touched"*; the wizard irreversibly archives them, and step 2 says the opposite `[x1]` — FIXED

_Fixed. Step 1 now states that deleting a flagged subnet archives everything beneath it, including subnets and host IPs created by hand or imported from another subscription, and that the scan also reports Azure ranges BASTET does not record._
_Swept: grepped the views and controllers for any other "never touched" / "not touched" / "untouched" promise — this was the only one. Step 2's banner already stated the truth, so the two panels now agree._
_Verified: 938 tests, 0 warnings. Static copy only; `grep "never touched" test/` returns nothing, so no test churn._
_Not done: the cheaper interim (deleting only the final clause) was rejected — the remaining text would still under-describe what the scan reports now that P2 added `AzureRangeNotImported` items. Naming hand-created descendants in the confirmation list rather than only counting them is a separate enhancement, not taken here._

## P21 — `DeleteConfirmed` archives whatever subtree exists at commit time, not the one the confirmation page showed, and the typed confirmation binds no scope `[x1]`

**Citation:** [`src/Bastet/Controllers/SubnetController.Delete.cs:85`](../src/Bastet/Controllers/SubnetController.Delete.cs#L85) · **Confidence:** confirmed · **Residue of:** none

**Scenario.** Operator A opens `/Subnet/Delete/{id}` for a leaf. The page renders *Child Subnets: 0*, *Host IP Assignments: 0*, and because `ChildSubnetCount` is 0 it omits the bold *"This will also delete N child subnet(s)!"* warning entirely. While that page is open, operator B creates a child and assigns a host IP. A types `approved` and submits. The POST carries only `id` and `confirmation`; `DeleteConfirmedCore` re-reads the tree inside the lock and archives the subtree **as it now is**. Every sibling destructive action binds reviewed state — `PurgeAllDeletedSubnetsConfirmed` and `PurgeAllDeletedHostIpsConfirmed` refuse without `confirmedMaxId` (`Delete.cs:271-276`, `HostIpController.cs:587-592`) and `BulkDeleteStaleAzureSubnets` 409s on a changed verdict. This one binds nothing.

**Reproduction.** Own port 22311, catalog `vfydel22311`, two independent sessions against one app. `PAGE Child Subnets = 0  Host IP Assignments = 0` / `PAGE warns about children: False`; then B: `child create -> 302 /Subnet/Details/2`, `hostip create -> 302`; then A posts the antiforgery token and form it was shown → `302 /Subnet`, banner *"Subnet 'vfy-root' and 1 child subnet(s) were deleted successfully. 1 host IP assignment(s) were archived."* `Subnets`: no rows. `DeletedSubnets`: `vfy-child 10.70.1.0 24` alongside the root. `DeletedHostIpAssignments`: `10.70.1.5`. The request A sent is byte-identical to the one it would have sent had nothing changed, so nothing on the server can tell the two apart. Two supporting measurements: `Subnet.RowVersion` would **not** close this — `CONVERT(BIGINT, RowVersion)` on the parent read `2004` before the child insert and `2004` after; and no re-parenting path exists (`EditSubnetViewModel` has no `ParentSubnetId`), so rows only enter a subtree by being created.

**Fix — unsound as filed.** Posting `confirmedChildSubnetCount`/`confirmedHostIpCount` and refusing on any difference is simultaneously under- and over-blocking. Under: a same-cardinality swap (the reviewed child deleted, a different one created) passes, and the finding's explicitly preferred *"refuse only when the recomputed counts are larger"* variant **licenses** that case. Over: refusing on a concurrent shrink refuses a delete strictly smaller than the one approved — round 8 rejected exactly that shape in writing (*"It refuses work the operator can legitimately do"*). It also omits the presence check that does the work in `confirmedMaxId` (`if (confirmedMaxId is null or <= 0)` refuses), so two `int?` parameters binding `null`/`0` on a replayed POST reopen the hole. **Correction — bind identity, not cardinality**, in one of two equivalent forms. Either a hidden digest over the sorted reviewed descendant subnet ids plus host-IP ids (a hash, not the raw list, so a large subtree does not blow up the form), recomputed inside the existing lock before `ArchiveSubnetSubtreeAsync` and refused only when the recomputed set contains an unreviewed id; or H4's own idiom — carry the maximum descendant `Subnet.Id` and maximum subtree `HostIpAssignment.Id` as `int?`, refuse on `null`, and refuse when anything in the recomputed subtree exceeds them (sound because `Id` is IDENTITY, `DELETE` never reseeds, and nothing re-parents; state that assumption where the guard lands). Either passes a concurrent shrink. Keep the proposed message and redirect, and add the counter-test the filing omits: **a subtree that shrank must still commit.** The same guard is missing at `SubnetController.AzureReconcile.cs:143`.

---

## P22 — `IsRangeRecordedByBastet`'s exact-match arm never consults the free-space computation, so a fully-tiled VNet-level row is reported with two false sentences and a remedy that costs correct child subnets `[x1]`

**Citation:** [`src/Bastet/Services/Azure/AzureReconciler.cs:425`](../src/Bastet/Services/Azure/AzureReconciler.cs#L425) · **Confidence:** confirmed · **Residue of:** O5 (round 15) — filed as P2 and corrected; the identical item with identical text was emitted by the pre-P2 `AccountsFor`, and `:404`/`:388-390` blame to `65d1fc6`

**Scenario.** A VNet whose address prefix is fully covered by one Azure subnet is imported prefix-only, producing a VNet-linked row at exactly that prefix with `IsFullyAllocated = 0`. The operator then carves it by hand into two `/25`s. The `/24` is now fully tiled and its Details page renders **no** "Unallocated IP Ranges" card. The scan still emits a review item because the exact arm returns at `:425` before `CalculateUnallocatedRanges` at `:449` is ever reached. Three things wrong at once: *"which no BASTET subnet records"* is contradicted by the item's own next sentence, which names the row; *"BASTET is reporting that range as free space"* is false; and the only remedy on a screen headed *"Needs review"* is *"remove the children that conflict with it first"*, and `DeleteConfirmed` archives to `DeletedSubnets`, which has no restore.

**Reproduction.** Own port 22779, fresh catalog `advp16x9q7_db`, own fixture `rig-vfyp16-whole` `10.171.0.0/24` with subnet `whole` `10.171.0.0/24`. Prefix-only import → `{"success":true,"createdTargets":1,"createdChildSubnets":0}`; row 1 `IsFullyAllocated=0`, VNet-linked. First scan: item reading *"…It covers the whole of BASTET subnet 'rig-vfyp16-whole'. Import it to mark that subnet fully allocated."* — **true** in that state. Then two `POST /Subnet/Create` for `10.171.0.0/25` and `10.171.0.128/25` → 302 each. `grep -ci 'Unallocated IP Ranges' Details/1` → **0**; independently, `POST /Subnet/Create 10.171.0.64/26` under row 1 → 200 with no row created, *"A more specific parent subnet exists: lower (10.171.0.0/25)."* Second scan: **still one item**, now reading *"…which no BASTET subnet records. BASTET is reporting that range as free space. It covers the whole of BASTET subnet 'rig-vfyp16-whole'. Importing it would mark that subnet fully allocated, which is refused while it still has child subnets, so remove the children that conflict with it first."* Unclearability driven, not asserted: `BulkGetVNets` now returns the subnet `status 3 Blocked`, and committing it returns `400 {"success":false,"itemErrors":[…"already has child subnets"]}`. Only deleting rows 2/3 silences it.

**Fix — unsound as filed.** Falling the exact arm through to `CalculateUnallocatedRanges` suppresses the item — and a verifier measured what that suppression hides: with the `/24` tiled, `Details/2` still offers `10.160.0.0-10.160.0.127` as free and `POST /Subnet/Create {10.160.0.0/26, parent 2}` **succeeded**, while Azure's `whole` subnet holds it. That is verbatim P2's own reproduction, re-created by the fix. Neither existing test catches it (`AnUnaccountedWholePrefixRangeOnAPopulatedTarget_SaysTheChildMustGoFirst` uses a half-tiling child; `AWholePrefixTargetWithChildren_StillReportsTheChildrenBlocker` sets `HasChildSubnets = true` with no child rows in the snapshot). The filed mechanics are also wrong — a bare fall-through hits the `deepest is null` arm at `:433-437` and returns `false` anyway. **Correction — take the finding's own "cheaper interim" as the fix, and treat it as first choice, not fallback.** Keep the gate at `:425`. In `ReportAzureRangesNoBastetSubnetRecords` (`:394-407`) compute `CalculateUnallocatedRanges(parts[0], cidr, rowsInsideTheRange)` once for the range being reported and split the leading sentence on the result: when it returns something, today's text unchanged (true, and the common case); when it returns nothing, a sentence of fact with no free-space claim and no self-contradiction. Leave the three-way remedy at `:381-392` alone — it is word-for-word what the planner returns as its Blocked reason and its 400 error, so it is an accurate statement of what the application requires, not false advice. Two tests in `AzureReconcilerInboundTests`: a fully-tiled VNet-level target is still reported but its reason must **not** contain *"reporting that range as free space"*; a childless one is reported and must.

---

## P23 — The withheld-from-deletion warning asserts the recorded and live ranges *"are not the same"* on the branch where they are identical `[x2]`

**Citation:** [`src/Bastet/Services/Azure/AzureReconciler.cs:195`](../src/Bastet/Services/Azure/AzureReconciler.cs#L195) · **Confidence:** confirmed · **Residue of:** P8

**Scenario.** P8 replaced the old rename wording with one direction-neutral sentence for every overlap shape. It is false for the **exact** shape — the commonest, and the only one that offers Re-link. `FindLiveOwnerOfRange`'s exact arm (`:495-504`) returns `Exact = true` with `LivePrefix` literally the recorded string. A row recorded `10.160.1.0/24` and linked to Azure subnet `s1`; `s1` deleted and `s1b` created at exactly `10.160.1.0/24` in the same VNet. The item's own reason reads *"The range 10.160.1.0/24 is still assigned in Azure to subnet 's1b' … Re-link it to that Azure subnet."* The aggregate warning rendered directly above it, under *"Check this before deleting anything"* (`_StepReview.cshtml:38-41`), reads *"…because a live Azure resource still overlaps the range they record, **so the recorded range and the live range are not the same**."* They are the same — `10.160.1.0/24` both. On a linked row the recorded range cannot be edited, so the false sentence steers toward delete-and-reimport (losing history, children, host IPs) when **Re-link** is sitting on the row beneath.

**Reproduction.** Own port 21993, catalog `verifp8x`, own fixture, SP1. Imported `rig-verifp8x-vn` through the real bulk path (`createdTargets 1, createdChildSubnets 1`), then `az network vnet subnet delete … -n s1` + `create … -n s1b --address-prefixes 10.160.1.0/24`. `POST /Azure/ReconcileScan` returned items: **none**; `reviewItems[0]`: `6 s1 10.160.1.0/24 RangeStillAllocatedInAzure`; `warnings[0]` verbatim as above. Render path confirmed without a browser: `_ReconcileScripts.cshtml:256-258` appends each warning with `.text(w)` into `#rec-scan-warnings-list`, which `_StepReview.cshtml:38-41` places under *"Check this before deleting anything"*. Corroborated independently on four other rigs, including one where `POST /Subnet/RelinkAzureSubnet` then returned 200 with `azureSubnetName "sn-carve2"` and the next scan was clean, confirming the exact arm was the one that fired.

**Fix — sound, with one omission.** Take the **cheaper interim**: delete the eleven words *", so the recorded range and the live range are not the same"* from `:195`, leaving *"…still overlaps the range they record. Archiving them would…"*, which is accurate in both overlap directions, for VNet-level rows, and for the exact arm (identical ranges do overlap). Measured on a patched copy: **929 total, 0 failed**, no test edits. The **primary** wording (*"still holds or overlaps the range they record"*) is also true of all three shapes but the filing omits that two existing assertions pin the old substring — measured **929 total, 2 failed**: `TheWithheldWarning_AssertsNeitherARenameNorAWholeRange` (`:106`) and `TheWithheldWarning_IsTrueForAVNetLevelRowToo` (`:120`), each asserting `Contains("still overlaps the range they record")`. That second test's *name* is itself wrong for its exact-arm fixture and should be renamed or re-fixtured to a genuine non-exact VNet-level overlap. **Do not take the two-warning alternative as filed** — carrying `Exact` out means changing `rangeStillAllocated` from `List<AzurePrefixOwner>` to `List<LiveRangeOwner>`, and `OwnerList` (`:520`) takes the former, so it needs an overload or a projection at both call sites; round 16 already judged that same mechanical gap underspecified when P8's original fix proposed it.

---

## P24 — The migration lock's master fallback leaves EF Core's own 30s lock as the only serialiser, and exceeding it kills the replica at startup with a raw stack trace `[x2]`

**Citation:** [`src/Bastet/Program.cs:244`](../src/Bastet/Program.cs#L244) · **Confidence:** confirmed · **Residue of:** none

**Scenario.** Two replicas, `BASTET_AUTO_MIGRATE=true`, empty server. Replica A finds the catalog absent, gets SQL 4060, and takes `Bastet:Migration` on **master** (`:244`, then `:306-325`). Its `Migrate()` at `:331` issues `CREATE DATABASE` and applies migrations. Replica B starts after the catalog exists and takes `Bastet:Migration` on **the catalog**. Application locks are database-scoped, so the two never exclude each other. The only remaining serialiser is EF's `__EFMigrationsLock`, which `SqlServerHistoryRepository` acquires with **no `@LockTimeout`**, so the 30s client `CommandTimeout` is the only ceiling. Exceed it and the `SqlException` is unhandled: the process exits before serving a request and the operator gets *"Execution Timeout Expired … Error Number:-2"* with a stack trace at `Program.cs:331` instead of the purpose-written *"Another replica appears to be stuck applying migrations. Startup was aborted rather than risking a concurrent migration."* at `:320-323`. BASTET's own lock would have waited 300000 ms; the substitute waits 30000 ms and crashes.

**Reproduction.** Full two-replica scenario, own ports 22771–22774, own catalogs. **The scope split, driven by the app itself:** instance A launched against a non-existent catalog and SIGSTOPped the moment it held the EF lock; `sys.dm_tran_locks` (`resource_type='APPLICATION'`): `master | 0:[Bastet:Migration] | spid=104 | GRANT` and `vrf16q_race | 0:[__EFMigrationsLock] | spid=108 | GRANT`. **B is waved through and dies:** instance B against the same catalog took `Bastet:Migration` in `vrf16q_race` (`spid=78 GRANT`, never waited) and then waited on `__EFMigrationsLock`; its log: *"Failed executing DbCommand (30,008ms) [CommandTimeout='30'] … sp_getapplock @Resource = '__EFMigrationsLock'"* then *"Unhandled exception … Program.cs:line 331 … Error Number:-2"*. Process dead 36s after launch, HTTP 000. The 300000 ms `@LockTimeout` at `:313` is never reached because B was granted its lock instantly in a different scope. **Control on the causal chain:** without the master fallback, two replicas against an existing catalog both contend on `Bastet:Migration` in that catalog, the loser waits up to 300s and enters `Migrate()` only after the winner releases. The 30s death is specific to the scope split.

**Fix — sound, with two corrections.** The interim two-liner (`dbContext.Database.SetCommandTimeout(330)` before each `Migrate()` at `:331/:334`) was built and measured: log line 2 shows `CommandTimeout='330'`, the process was alive and still waiting at 55s, and after the holder released it applied all six migrations and served the app with zero unhandled exceptions. It lengthens a timeout rather than refusing anything, so there is no over-block. (1) It does **not** restore the written diagnostic — a peer stuck past 330s still yields the raw `SqlException` — so pair it with a catch around the `Migrate()` calls that rethrows the `:320-323` wording on a lock-acquisition failure. (2) The *"thorough half"* as worded is not implementable: `Migrate()` cannot be interrupted between `CREATE DATABASE` and applying migrations, so master's lock cannot be "closed after the database is created". The correct restructure is to create the empty catalog explicitly under the master lock (`IRelationalDatabaseCreator.Create()`, or `CREATE DATABASE` on the master connection), release the master lock, then take `Bastet:Migration` on the now-existing catalog before calling `Migrate()`. The suggested regression test must be an integration test against a real SQL Server — lock scoping is a server property, out of reach of `MigrationLockConnectionStringTests`.

---

# Low

## P25 — Reconcile warnings identify withheld subnets by bare `Name` on the destructive screen, where those rows appear nowhere else at all `[x1]`

**Citation:** [`src/Bastet/Services/Azure/AzureReconciler.cs:530`](../src/Bastet/Services/Azure/AzureReconciler.cs#L530) · **Confidence:** confirmed · **Residue of:** P15 — the line predates round 16 (`bf120d66`), but round 16 considered this exact one-line change as P15's interim and struck it on the premise *"with the primary fix the names differ everywhere"*. P15's fix only qualifies a row whose persisted sibling shares its `AzureResourceId`; two rows imported from two different Azure VNets are untouched by it, so the premise does not hold

**Scenario.** `NameList` (`:527-532`) renders `'{i.Name}'` and nothing else, and is used by all four withholding warnings (`:275` not-visible, `:283` unknown, `:291` still-live, `:327` cascade-blocked), all of which render into `#rec-scan-warnings` under *"Check this before deleting anything"*. `Subnet.Name` is indexed but not unique, and every import path derives it from the Azure subnet name, so `default`, `app`, `gateway`, `AzureBastionSubnet` repeat across every VNet in an estate. Every other list on this path disambiguates by network/CIDR — the deletable table (`_ReconcileScripts.cshtml:272-273`), the review table (`:293-294`) and the confirm list (`:448`, `name (network/cidr)`). The withheld rows are stripped from `plan.Items` at `:268` and never enter `ReviewItems`, so **the warning string is the only place they appear at all**: told that `'app'` lost visibility, the operator cannot tell which resource group to go and check.

**Reproduction.** Own catalog `vfy16nlx`, ports 21993 (SP1) / 21994 (SP2) / 21995 (patched). Two ordinary bulk imports produced the collision with no hand editing: `2|default|172.16.1.0/24|…/bastet-hidden/…`, `3|app|172.16.2.0/24|…/bastet-hidden/…`, `5|default|10.113.1.0/24|…/bastet-visible/…`, `6|app|10.113.2.0/24|…/bastet-visible/…`. `az network vnet subnet delete … -n app`, then `POST /Azure/ReconcileScan` as SP1: items `[id=6 name='app' 10.113.2.0/24 SubnetDeleted]`; warnings `["3 Azure-linked subnet(s) were missing from the subscription listing … withheld from deletion: 'rig-vnet-simple', 'default', 'app'."]`. Driven in headless Chromium through the real wizard: `#rec-scan-warnings` visible ending *"…'rig-vnet-simple', 'default', 'app'."*, `#rec-stale-section` visible with `app  10.113.2.0/24  Subnet deleted`, `#rec-confirm-list`: `app (10.113.2.0/24) - Subnet deleted`. Both panels on one screen, and the warning is the only surface there that names a subnet without its range. **No wrong archive is reachable from the ambiguity** — the withheld rows have no checkbox and the commit is id-keyed — which is why this is low.

**Fix — sound.** `$"'{i.Name}' ({i.NetworkAddress}/{i.Cidr})"` at `:530`. Built clean, **929/929 unchanged**, and re-driven on the patched binary against the same catalog and Azure state: *"…withheld from deletion: 'rig-vnet-simple' (172.16.0.0/16), 'default' (172.16.1.0/24), 'app' (172.16.2.0/24)."* Three checks a reviewer should not take on trust: every item in all four lists comes from the single `Item` factory (`:606-624`), which sets `NetworkAddress` and `Cidr` for every status including VNet-level ones; it is display text on an already-computed plan, so nothing is refused that works today; and the existing assertions survive because the quoted-name substring is unchanged (`AzureReconcilerTests.cs:575, :674-675, :737`, `AzureReconcilerRangeStillAllocatedTests.cs:93`). **Do not drop `Max` from 10 to 5** as the filing offers — that silences five subnets to shorten a sentence, on the one screen whose complaint is that subnets cannot be identified; every warning already opens with the true total. **Leave `OwnerList` (`:520-525`) alone** — it names Azure-side owners, which carry no network or CIDR and are not BASTET rows. The pinning test must use a **cross-VNet** duplicate; a multi-prefix fixture would pass without the fix, since P15 already separates that shape.

---

## P26 — The single-VNet import's parent rename does not apply the prefix-qualification rule, so a multi-prefix VNet imported one prefix at a time produces two rows with identical `Name` and identical `AzureResourceId` `[x1]`

**Citation:** [`src/Bastet/Controllers/SubnetController.Azure.cs:292`](../src/Bastet/Controllers/SubnetController.Azure.cs#L292) · **Confidence:** confirmed · **Residue of:** N10 (round 14) — filed as P15 and corrected. `git log -S multiPrefixVNetIds` returns exactly `8afa2df` (Audit 14 Cleanup); N10 introduced the prefix-qualified target name in `AzureBulkImportPlanner.TargetName` and never mirrored it into `BatchCreateChildSubnetsCore`

**Scenario.** A Bastet row holds one CIDR, so a VNet with two address prefixes **must** be imported into one row per prefix — there is no other way. `GetCompatibleVNets` matches on address prefix alone, so the VNet is legitimately offered at each of its prefix rows. Each import takes the `!targetIsPopulated` branch and overwrites the target's name with the raw VNet name and sets `AzureResourceId` to the VNet id. Result: two rows at different prefixes, both named after the VNet, both carrying the same `AzureResourceId`. `AzureBulkImportPlanner.TargetName` (`:752-763`) appends ` ({network}-{cidr})` whenever the VNet contributes more than one distinct selected prefix; `SubnetController.Azure.cs:284-293` applies no such rule. The consequence lands on P25's surface: a warning naming `'rig-vnet-multiprefix'` cannot be matched to a row.

**Reproduction.** Own port 23411, catalog `vfyp16b_mp`, SP1, shared read-only fixture `rig-vnet-multiprefix`. Two ordinary `POST /Subnet/Create` rows (`planA 10.101.0.0/16`, `planB 10.102.0.0/16`), then the single-VNet wizard driven **twice in headless Chromium** — no hand-built post: `/Azure/Import/1` → subscription → `#vnet-select` offered exactly `['rig-vnet-multiprefix']` → `#subnet-list` showed `sub-a 10.101.1.0/24` → tick → import; banner *"Successfully renamed parent subnet to 'rig-vnet-multiprefix' and imported 1 child subnets."* Same against `/Azure/Import/2` for `sub-b`. `sqlcmd`: rows `1|rig-vnet-multiprefix|10.101.0.0|16|…/virtualNetworks/rig-vnet-multiprefix` and `2|rig-vnet-multiprefix|10.102.0.0|16|` (same id). Consequence driven: a second instance as **SP2** against the same catalog returned `warnings[0] = "4 Azure-linked subnet(s) were missing … withheld from deletion: 'rig-vnet-multiprefix', 'rig-vnet-multiprefix', 'sub-a', 'sub-b'."` The archive **table** is not ambiguous (network/cidr in its own column, commit id-keyed), so no wrong row can be archived — the ambiguity is confined to the four `NameList` strings.

**Fix — sound in substance, three corrections.** (1) `HasPersistedSiblingFromSameAzureSubnet` cannot be reused as named: it is typed on `AzureImportSubnetViewModel` and self-excludes by `{NetworkAddress, Cidr}`; the parent is a `Subnet` and must self-exclude by **identity**. Add `HasPersistedSiblingHoldingSameVNet(target, vnetResourceId, persistedSubnets)` testing `e.Id != target.Id && Equals(e.AzureResourceId, vnetResourceId, OrdinalIgnoreCase)`. Taken literally, *"if any other persisted row already carries this vnetResourceId"* over `treeCache` — which contains `parentSubnet` itself — would spuriously rename an already-linked empty target on a repeat import and re-fire the false flash round 16 closed as P11. (2) Apply the suffix **before** `:291` so `parentRenamed` compares against the qualified string. Measured on a patched copy: build 0 warnings, **929/929**, and the same browser run produced *"…renamed parent subnet to 'rig-vnet-multiprefix (10.102.0.0-16)'"* with the P11 flash still accurate; the over-block counter-test (single-prefix `rig-vnet-simple` into a fresh row) kept the bare name. (3) **Do not take the "cheaper interim"** (skip the rename when a sibling holds the VNet id): it leaves the second target under the operator's placeholder while the first carries the VNet name, so the estate no longer identifies that VNet's rows by name at all. Record separately, because it is measured and is *not* this finding: `TargetName`'s `multiPrefixVNetIds` is computed from the **current selection only** (`AzureBulkImportPlanner.cs:134-141`), so the bulk wizard driven one prefix at a time produces the same bare-name collision. Counter-test: a single-prefix VNet imported into an empty target keeps the bare name, and a repeat import into an already-linked empty target does not rename at all.

---

## P27 — The single-VNet wizard offers a VNet, and every subnet in it, that its own commit refuses outright because the target is already linked to a different Azure VNet `[x1]`

**Citation:** [`src/Bastet/Controllers/AzureController.cs:184`](../src/Bastet/Controllers/AzureController.cs#L184) · **Confidence:** confirmed · **Residue of:** P5 — `git blame -L 184,200` gives `4f71e245`, P5's commit; `AnnotateImportCandidate`'s four rules contain no test of the target's own `AzureResourceId`

**Scenario.** BASTET row 1 = `10.191.0.0/16` was imported from VNet `rig-vfy16b-va`, so it carries that VNet's `AzureResourceId`. Re-opening `/Azure/Import/1` (permitted as a top-up by `AzureController.cs:38`), step 2 offers **both** `rig-vfy16b-va` and `rig-vfy16b-vb` — `GetCompatibleVNets` matches on address prefix alone and knows nothing about `subnet.AzureResourceId`. Picking `vb`, step 3 returns its subnet as `status 0 / "Available" / isSelectable true / reason null` — a green, tickable row. Submitting hits `SubnetController.Azure.cs:268-280`, which rolls the transaction back with *"Bastet subnet 'rig-vfy16b-va' (10.191.0.0/16) is already linked to Azure VNet '…va' and cannot be re-linked to '…vb' by an import."* The rule lives in the bulk annotator (`AzureBulkImportPlanner.AnnotatePrefix:198-209`) and in the commit path, and is missing from the annotation path P5 built. **A stronger path removes the duplicate-address-space precondition entirely:** delete the linked VNet in Azure — the *"renamed or moved"* case the guard's own remedy names — and the wizard offers exactly **one** VNet, all green, that can never commit, with no in-band remedy at all (`Subnet.AzureResourceId` has no editor anywhere).

**Reproduction.** Own port 22983, catalog `vfy16bxlink`, own fixtures (two VNets deliberately sharing `10.191.0.0/16`; ARM accepted both in one resource group, which settles the premise as a supported Azure state). `GET /Azure/GetVNets?subnetId=1` → `['rig-vfy16b-va','rig-vfy16b-vb']` — no status field exists on `AzureVNetViewModel`. `GET /Azure/GetSubnets?vnetResourceId=…/vb&subnetId=1` → `{"name":"b1","addressPrefix":"10.191.2.0/24","status":0,"statusName":"Available","reason":null,"isSelectable":true}`. `POST /Subnet/BatchCreateChildSubnets` → 302 with the alert-danger above; `SELECT … FROM Subnets` unchanged at two rows. Same instant, same DB: `GET /Azure/BulkGetVNets` returns `rig-vfy16b-vb` `{"status":3,"statusName":"Blocked","isSelectable":false,"reason":"Bastet subnet 'rig-vfy16b-va' is already linked …"}`. Two screens, one Azure state, opposite vocabularies. Driven in headless Chromium on the deleted-VNet variant: subnet row `{'label': 'sb 10.171.2.0/24', 'disabled': False}` — no badge, no reason, checkbox enabled, Import enabled, click → the rollback banner. Nothing written, no partial state.

**Fix — unsound as prioritised.** The filed **primary** (annotate `AzureVNetViewModel` in `GetVNets` only) does not fix the cited line: `GET /Azure/GetSubnets` would still answer `status: 0 / "Available" / isSelectable: true` for the conflicting VNet, which is the defect. It also creates an unspecified empty state — `GetCompatibleVNets` matches on prefix, so a target whose linked VNet has since had its prefix widened is absent from the list and every remaining option would be disabled, with no rendering for "nothing selectable"; the bulk screen's checkbox tree gives no pattern to copy into a `<select>`. **Correction — the filed "cheaper interim" is the fix.** In `GetSubnets` (`:146-176`), before the per-row loop: if `!string.IsNullOrEmpty(subnet.AzureResourceId) && !string.Equals(subnet.AzureResourceId, vnetResourceId, OrdinalIgnoreCase)`, call the existing `Block(azureSubnet, …)` on every returned row. The render already exists — red *"Cannot import"* badge and reason div at `_ImportScripts.cshtml:274-281`, checkbox disabled at `:286`, and Select All already scoped `:not(:disabled)` by P5's sweep. Mirror the guard's **full** antecedent (`vnetResourceId` non-empty too, as at `Azure.cs:268-270` and `AnnotatePrefix:200-209`) or an ARM response with an empty id would be blocked where the commit lets it through. Word it from the commit's sentence, not the planner's shorter one: the planner's ends *"…would replace that link, so it is refused"*, the commit's ends *"If the VNet was renamed or moved, delete the Bastet subnet and import it again"* — the second is the only remedy the application offers and is what the deleted-VNet operator needs. The VNet-level annotation is worth adding **after** this arm, with an explicit "no VNet in this subscription can be imported into this target" state. Two tests: every row `Blocked`/`IsSelectable=false` for a target linked elsewhere, and the counter-test that a same-VNet top-up still returns `Available` with `reason: null`.

---

## P28 — `CalculateUnallocatedRanges` emits a free range of **zero** addresses at the parent's own network address, and the reconciler counts it as free space `[x1]`

**Citation:** [`src/Bastet/Services/IpUtilityService.cs:313`](../src/Bastet/Services/IpUtilityService.cs#L313) (emitter) and [`src/Bastet/Services/Azure/AzureReconciler.cs:449`](../src/Bastet/Services/Azure/AzureReconciler.cs#L449) (the consumer P2 added) · **Confidence:** confirmed · **Residue of:** P2

**Scenario.** When the first merged allocation starts at `startIp + 1` — a `/32` child at network+1, or a host IP at network+1, the most ordinary assignment there is — the `currentPosition == startIp && cidr < 31` branch (`:306-315`) emits `StartIp = EndIp =` the parent's network address with `AddressCount = Start - currentPosition - 1 = 0`, and adds it unconditionally. `AzureReconciler.IsRangeRecordedByBastet` at `:449` decides "is this Azure range recorded" with `!CalculateUnallocatedRanges(...).Any()`, and a zero-address range makes `Any()` true. Azure subnet `default` = `10.100.1.0/24` with BASTET rows `.1/32, .2/31, .4/30, .8/29, .16/28, .32/27, .64/26, .128/25` — **every usable address recorded**, only the reserved network address uncovered — still produces *"Azure subnet 'default' … owns 10.100.1.0/24, which no BASTET subnet records. BASTET is reporting that range as free space."* The only way to clear it is to create `10.100.1.0/32`: to allocate the reserved network address as a subnet.

**Reproduction.** Own ports 24771 / 22883, own catalogs, live Azure. Emitter: parent `10.105.0.0/16` with child `10.105.0.1/32` → free ranges `('10.105.0.0','10.105.0.0','0 IP addresses')` and `('10.105.0.2','10.105.255.254','65,533')`, with **Create Subnet buttons on both**; `POST /Subnet/Create 10.105.0.0/32` → 302, while the same address posted to `/HostIp/Create` → 200 *"Cannot assign the network address as a host IP"*. Driven in headless Chromium: the modal for the zero-address row claims **"1"** usable address and lands on `/Subnet/Create?networkAddress=10.180.0.0&cidr=32&parentId=20` prefilled. Consumer, end to end against live ARM: `[baseline] 0 items` → `delete the /24 row` → `1 item AzureRangeNotImported 10.100.1.0/24` → `tile .1/32 … .128/25` → **same item, unchanged** → `create 10.100.1.0/32` → `0 items`. That last step is the control: the item survives 255 of 256 addresses being recorded and clears only when the reserved network address is allocated. Line 313 is the **only** zero-count producer — the else arm (`:318-323`) has `Start > currentPosition`, the tail (`:342-347`) is guarded by `currentPosition <= lastIp`, and the childless arms are `subnetSize` or `subnetSize - 2`.

**Fix — unsound as filed, both variants.** The *"thorough"* variant (`StartIp = currentPosition + 1`) is refuted by measurement: the free-space **Create Subnet** button copies `range.StartIp` verbatim into `#networkAddressDisplay` (`_SubnetCalculationScripts.cshtml:27-28, 127-133`) and navigates to `/Subnet/Create?networkAddress=<StartIp>`, and the server refuses a non-boundary address — driven on the ordinary shape (parent `/16`, first child `10.170.4.0/24`, optimal `/22`): `POST NetworkAddress=10.170.0.1 Cidr=22` → 200, *"Network address 10.170.0.1 is not valid for CIDR /22. The network address must align with the subnet boundary."*, while the control `10.170.0.0/22` → 302. It also breaks four `IpUtilityServiceTests` cases. The *"cheaper interim"* (filter `AddressCount == 0` out of `CalculateUnallocatedRanges`) reverses the decision recorded at `docs/AUDIT-FINDINGS-4.md:127` and fails the test round 4 shipped to pin it — measured **928/929**, `CalculateUnallocatedRanges_LeadingGapOfOne_ReportsNoUsableAddresses` expected `"10.0.0.0"`, got `"10.0.0.2"`. **Correction — fix the consumer P2 added, not the emitter:** `AzureReconciler.cs:449` → `return !ipUtilityService.CalculateUnallocatedRanges(network, cidr, rowsInsideTheRange).Any(r => r.AddressCount > 0);`. Measured in a copy: `dotnet test` **929/929**; driven against live Azure, the false item disappears once the eight covering rows exist, the true positive (the `/24` row deleted and nothing covering it) is still reported, and round 4's Details-page affordance is unchanged. Add a test beside P2's existing counter-tests: a `/24` whose usable addresses are wholly covered by rows inside it, with only the reserved network address uncovered, must not be reported. The Details-page half is round 4's recorded decision, not a defect; the display/count inconsistency and the create path's lack of any network/broadcast reservation are in the decisions table above.

---

## P29 — The merge loop in `CalculateUnallocatedRanges` overflows `uint` at `255.255.255.255`, so it returns free ranges that lie **inside** allocations it was given `[x2]`

**Citation:** [`src/Bastet/Services/IpUtilityService.cs:284`](../src/Bastet/Services/IpUtilityService.cs#L284) · **Confidence:** confirmed · **Residue of:** P2 — the arithmetic is old (`841c272`, 2025-04-06) and was **unreachable** in the shipped application until P2's commit `0c7a73a` routed the reconciler through the same function with a nested row set; `git grep CalculateUnallocatedRanges 0c7a73a^ -- src/` shows the only prior caller was `SubnetController.Read`, which cannot construct a reaching input

**Scenario.** `next.Start <= current.End + 1` at `:284` is `uint` arithmetic. When a merged allocation ends at `255.255.255.255`, `current.End + 1` wraps to 0, every later allocation fails the merge test, and `currentPosition` at `:327` is reset **backwards** to that later allocation's end. Everything after it is emitted as free even though it sits inside an allocation the caller passed in. The Details page cannot reach it (it is passed direct children — disjoint, so the one ending at `.255` sorts last — or host IPs, never both; three separate validators enforce that). `AzureReconciler.IsRangeRecordedByBastet` (`:445-449`) can: it passes every BASTET row at any depth inside the Azure range, which is a **nested** set by construction. ARM accepts a VNet at `224.0.0.0/3` (`240.0.0.0/4` and `255.0.0.0/8` are refused), so a subnet at `255.255.254.0/23` is reachable.

**Reproduction.** Against the shipped service, `ProjectReference` to `Bastet.csproj`, nothing patched: parent `255.255.254.0/23`, kids `[255.255.254.0/24; 255.255.255.0/24; 255.255.255.0/26]` → `FREE 255.255.255.64 - 255.255.255.254 count=191`, inside a `/24` the caller handed it; the identical shape at `10.0.0.0/23` returns empty; brute force over every address of the `/23` gives **0** uncovered. Property fuzz over 200,000 random cases, parent CIDRs `{0,1,2,3,7,8,15,16,22,23,24,29,30,31,32}`: `violations=15288 (withTopAlloc=15288, other=0)` — every violation has an allocation ending at `255.255.255.255`, none without. End to end on own port 21993, catalog `ver16lgi_az`: imported the `224.0.0.0/3` prefix through the app's own endpoints, created the two `/24`s, `[CONTROL: no nested row] items about 255.255.x: 0`, created `255.255.255.0/26`, `[BUG] 1 item: AzureRangeNotImported 255.255.254.0/23 … "which no BASTET subnet records. BASTET is reporting that range as free space."` Coverage of the `/23` is identical in both states; adding a row **inside** an already-covered range flips the verdict.

**Fix — sound.** `if ((long)next.Start <= (long)current.End + 1)` at `:284`. Nothing else changes — `currentPosition` is already `long` and `current.End = Math.Max(...)` is correct. There is no cheaper interim; this is already one cast. Measured on a patched copy: the filed case returns empty; the same 200k fuzz at the same seed gives `violations=0`; with top-of-space inputs excluded both builds produce a **byte-identical output digest** (`-456531515881380100`), so the cast cannot over-block; `dotnet test` 929/929. Two additions to what ships with it: take the **general property test** as well as the single case — *no returned range may intersect any allocation the caller passed in*, over random inputs, is what actually pins this and costs nothing; and the specific test must use a parent that touches `255.255.255.255`, since the identical shape at `10.0.0.0/23` passes on HEAD.

---

## P30 — The inbound reconcile item states no BASTET subnet records the range and then names the BASTET subnet that **is** exactly that range `[x1]`

**Citation:** [`src/Bastet/Services/Azure/AzureReconciler.cs:404`](../src/Bastet/Services/Azure/AzureReconciler.cs#L404) · **Confidence:** confirmed · **Residue of:** none — `:404` and the remedy arms blame to `65d1fc6` (round 15); P10 (`973b89c`) added a third arm carrying the same pairing without catching it

**Scenario.** `wholePrefixTarget` (`:375-379`) can only match a row whose `NetworkAddress` **and** `Cidr` equal the reported range, so whenever any remedy sentence is appended the leading clause is false by construction. A VNet whose address prefix is fully covered by one Azure subnet is imported prefix-only; the item reads *"Azure subnet 'whole' in VNet 'rig-vrf16-whole' owns 10.134.0.0/24, which no BASTET subnet records. BASTET is reporting that range as free space. It covers the whole of BASTET subnet 'rig-vrf16-whole'. Import it to mark that subnet fully allocated."* The first clause and the third contradict each other inside one string. The second clause is true in this state — the Details page does print the whole `/24` free with a Create Subnet button — so the item is worth raising; only the leading clause is wrong.

**Reproduction.** Own port 21967, catalog `vrf16whole`, SP1. **Run A:** `POST /Subnet/BulkCreateFromAzurePlan` with `subnets:[]` → `createdTargets 1`; row `1 rig-vrf16-whole 10.134.0.0 24 0`; scan returned the reason verbatim above; `GET /Subnet/Details/1` renders `10.134.0.0 | 10.134.255.255 | 254 IP addresses` with a Create Subnet button. **Run B kills the "crafted selection" objection:** a VNet created with **no subnets** was imported (`subnets:[]` is then the only selection the wizard can send), and Azure grew a subnet covering the prefix afterwards — the ordinary drift the reconciler exists to catch — producing the identical sentence. **Run C**, the P10 children arm: adding `kid 10.134.0.0/25` gives *"…so remove the children that conflict with it first."* with the same false lead. The string is rendered verbatim to the operator (`_ReconcileScripts.cshtml:295`).

**Fix — unsound as filed, in its tail.** The proposed replacement ends *"…records that range but is not marked fully allocated, **so** BASTET is reporting it as free space."* That consequent was measured **false** in the children arm: with the `/24` tiled by two `/25`s the item still fires and `Details/1` renders no unallocated section at all. Today's text asserts that falsehood flatly; the fix promotes it to an asserted consequence. It also asserts *"BASTET subnet 'X' records that range"*, which is the opposite of the item's own point, and its *"followed by the existing remedy"* leaves `X` named twice in consecutive sentences with `It` losing its antecedent. **Correction:** keep the two-arm shape but build both sentences from the lookup and drop the free-space claim from the whole-prefix arm — when `wholePrefixTarget` is null keep today's text unchanged (it is true there, and `ARangeAzureAssignedThatNoBastetSubnetRecords_IsReported` pins it); when it is non-null emit *"Azure subnet 'S' in VNet 'V' owns P. BASTET subnet 'X' holds exactly that range but is not marked fully allocated, so BASTET does not record it as allocated."* followed by the existing remedy with its leading *"It covers the whole of BASTET subnet 'X'."* removed. **Cheaper interim, one phrase, true in every arm:** `:404` → *"which no BASTET subnet records **as allocated**."* It leaves the free-space sentence false in the tiled-children case, which is **P22**'s subject. The filing's third sub-claim — that *"the children that conflict with it"* is inaccurate — does **not** stand: the row's range equals the Azure range, so every child of it is inside it and every child conflicts.

---

## P31 — Reconcile tells the operator *"BASTET is reporting that range as free space"* for Azure ranges no BASTET subnet contains, where BASTET reports nothing at all `[x1]`

**Citation:** [`src/Bastet/Services/Azure/AzureReconciler.cs:404`](../src/Bastet/Services/Azure/AzureReconciler.cs#L404) · **Confidence:** confirmed · **Residue of:** none — `git show 1a31228` shows the pre-round-16 `AccountsFor` returning false for the same input, so P2 neither introduced nor widened this shape

**Scenario.** `IsRangeRecordedByBastet` returns false at `:433-437` when **no** BASTET row contains the Azure range (`deepest is null`), and the method returns at `:436` before `CalculateUnallocatedRanges` at `:449` is ever called. The item then reads *"…which no BASTET subnet records. BASTET is reporting that range as free space."* and, because `wholePrefixTarget` cannot match either, carries **no remedy**. The second sentence is false: with no containing row, no Details page, no free-space table and no tree node mentions that range — BASTET is not reporting it as anything. This is not an edge: the gate at `:349` is per-**VNet**, so importing any one prefix of a multi-prefix VNet arms the report for every subnet under every other prefix.

**Reproduction.** Own port 21987, catalog `ver16bfsp`, SP1, shared `rig-vnet-multiprefix`. `POST /Subnet/BulkCreateFromAzurePlan` selecting **only** `10.101.0.0/16` with `sub-a` → `createdTargets 1, createdChildSubnets 1`; `sqlcmd` shows exactly two rows, neither containing `10.102.1.0/24` or `192.168.10.0/25`. `POST /Azure/ReconcileScan` → `items: 0, reviewItems: 2`, both ending at *"free space."* with no remedy. Falsified against the application itself: `GET /Subnet/Details/1` → `grep -c "10\.102"` = **0**, `grep -c "192\.168"` = **0**, and its unallocated table contains neither range; `GET /Subnet` lists only `10.101.0.0/16` and `10.101.1.0/24`. `sub-a` is correctly silent, so the check itself works.

**Fix — unsound as filed.** The replacement remedy *"Import the VNet prefix that contains it to bring it under BASTET's control"* must not ship. `AzureReconcilerInboundTests.cs:310` pins a range outside every VNet prefix, where no containing prefix exists to import. Worse, where one does exist the advice is actively harmful: importing the prefix alone creates a row that **contains but does not record** the range, putting it straight into that row's Unallocated IP Ranges table with a Create Subnet button — the operator would be told to perform the one action that manufactures the state the first sentence warns about. Name no remedy here; the `wholePrefixTarget` block at `:375-392` is the only place that may, because it is the only one that checks what the wizard will accept. The *"cheaper interim"* is incomplete on its own terms: its *"or an exact row exists"* clause keeps the false sentence on the exact-match arm (`:418-426`), where a VNet-linked, not-fully-allocated row whose children fully tile it renders no free-space table at all — that is **P22**. **Correction:** derive the sentence from the same computation P2 chose, uniformly. Have `IsRangeRecordedByBastet` also report whether any part of the Azure range is genuinely offered as unallocated — take the exact or deepest containing row, run `CalculateUnallocatedRanges` on it, intersect with `[network, cidr]` — and emit the free-space sentence only when that intersection is non-empty; with no containing row, stop at *"which no BASTET subnet records."* Two tests: a range no row contains (no free-space sentence, no remedy) and an exact VNet-linked row fully tiled by children (no free-space sentence).

---

## P32 — The single-VNet wizard still asserts *"Only compatible subnets are shown"* over a list that now deliberately shows incompatible subnets `[x1]`

**Citation:** [`src/Bastet/Views/Azure/Import/_SubnetList.cshtml:5`](../src/Bastet/Views/Azure/Import/_SubnetList.cshtml#L5) · **Confidence:** confirmed · **Residue of:** P5 — the line predates round 16 (`9f220e0`), but before `4f71e24` the wizard filtered refused rows out and rendered no badge and no reason, so no row on screen declared itself un-importable; P5 created the state and its own "Strings checked" step read only the strings the commit added

**Scenario.** P5 changed `AzureController.GetSubnets` from filtering incompatible rows out to annotating and returning them, which is the opposite of what this panel header promises. Target 1 = `10.100.0.0/16` linked to `rig-vnet-simple` with child `default` already imported. Step 3 renders the sentence and directly beneath it `default 10.100.1.0/24` with a grey **Already imported** badge, a disabled checkbox and the reason *"Already imported as Bastet subnet 'default'."* The introductory sentence tells the operator anything on screen is importable, so the disabled row reads as a UI fault rather than a refusal with a stated reason. The bulk wizard's sibling header (`_StepSelection.cshtml:3-13`) already spells the badges out; the two wizards now render the same badge vocabulary under headers that contradict each other.

**Reproduction.** Own port 23411/22617, catalog `vfy16p5res`, SP1, driven twice in headless Chromium against live ARM. Pass 1 imported both subnets through the real wizard. Pass 2, `inner_text("#step3")`: *"Select subnets to import from Azure. Only compatible subnets are shown. / Select All Subnets / default **Already imported** / 10.100.1.0/24 / Already imported as Bastet subnet 'default'. / app **Already imported** / …"*; checkboxes `[{'subnet-0', disabled: True}, {'subnet-1', disabled: True}]`. Pass 3, the Blocked limb: with a hand row `hand-2-128 10.100.2.128/25`, the list renders `app **Cannot import** … "Would contain existing Bastet subnet 'hand-2-128' (10.100.2.128/25), which would create an invalid hierarchy."` — **every** row on screen refused, under a header promising only compatible ones are shown. `inner_text("#step3 .alert-info")` confirms the sentence is in the always-visible info alert, not a collapsed element.

**Fix — unsound in its "durable" half.** The reword is right and is what should ship: replace `:5` with *"Select subnets to import from Azure. Subnets Bastet cannot take are shown greyed out with the reason."* — or mirror `_StepSelection.cshtml:3-13`, naming only the two badges this wizard emits (`Already imported`, `Cannot import`; it never emits `Will update existing`). Nothing in `src/`, `test/` or `.claude/` asserts on the literal. **Reject the "durable version"** (port `_BulkScripts.cshtml:239-262`'s offered/withheld count sentence): that sentence exists to account for rows the bulk screen **hides** behind its *"Hide unavailable"* toggle, two of its three arms end with *'Untick "Hide unavailable" to see why'* — a control this wizard does not have — and since P5 this wizard hides nothing, so the withheld count is always zero and the generated sentence is vacuous. **Leave `_SubnetList.cshtml:54` alone** — *"No compatible subnets found in this Virtual Network."* now fires only when ARM genuinely returns nothing, and is accurate post-P5.

---

## P33 — The bulk selection legend tells the operator that *"Will update existing"* rows are greyed out and cannot be selected; they are enabled, tickable and the main top-up route `[x2]`

**Citation:** [`src/Bastet/Views/Azure/BulkImport/_StepSelection.cshtml:8`](../src/Bastet/Views/Azure/BulkImport/_StepSelection.cshtml#L8) · **Confidence:** confirmed · **Residue of:** none — `git blame` puts the legend and `result.IsSelectable = true` in the same commit `aedd0bd1` (2026-07-16); the contradiction shipped together

**Scenario.** `AzureBulkImportPlanner.AnnotatePrefix` is the only producer of `WillUpdateExisting` (`:227`) and **always** pairs it with `result.IsSelectable = true` (`:232`); `_BulkScripts.cshtml:190` disables a checkbox only on `!prefixInfo.isSelectable`, and `:178` does not hide it under *"Hide unavailable"*. So there is no state in which such a row is greyed out. The legend at `:8-11` reads *"Anything BASTET already has is greyed out and cannot be selected: **Already imported** …, **Cannot import** …, and **Will update existing** imports into a subnet you already have."* — false for one of the three badges it enumerates, and false about the one badge whose whole purpose is to be selected. An operator who reads it concludes the top-up path is unavailable.

**Reproduction.** Own port 22537, catalog `advvlegend16`, SP1, live Azure, one hand-created row `topup-target 10.100.0.0/16`. `GET /Azure/BulkGetVNets` returned exactly one non-Available row: `rig-vnet-simple 10.100.0.0/16 WillUpdateExisting selectable=True | Will import into existing Bastet subnet 'topup-target'.` In headless Chromium the legend read back verbatim, and the row dumped as `('bulk-prefix-25-0', '10.100.0.0/16', is_disabled=False, 'rig-vnet-simple 10.100.0.0/16Will update existing')`. Ticking it: `checked=True, preview-btn disabled=False`. Toggling *"Hide unavailable"* ON: row still present and visible. One consequence beyond the filing: `_BulkScripts.cshtml:316` scopes **Select all** to `:not(:disabled)`, so Select all ticks these rows — an operator who believes the legend believes Select all cannot touch anything BASTET already has; it does, and the run writes into existing BASTET subnets. The mandatory Preview step is what caps this at low.

**Fix — unsound in its "durable" half.** Take the cheapest variant and extend it: rewrite `:8` so the premise is about what BASTET can **accept** rather than what it already has — *"Rows BASTET cannot take are greyed out: `Already imported` …, `Cannot import` …, `Blocked by VNet prefix` its VNet prefix cannot be imported. `Will update existing` **can** be selected — it imports into a Bastet subnet you already have."* That also covers the fourth badge the filing missed (`_BulkScripts.cshtml:226`), which has no enum value behind it. **Reject the "durable" generate-one-line-per-enum-value form**, on three measured grounds: the rendered disabled state is not a function of the row's own status (`:210`/`:219` disable an `Available`, `isSelectable: true` subnet whenever its VNet prefix is refused, so a status→selectable table would state the opposite); `Blocked by VNet prefix` has no enum member, so the least guessable badge would stay undocumented; and `AnnotatePrefix` never returns `AlreadyImported`, so a per-value legend would advertise a prefix-level badge that cannot occur. Merely re-partitioning the list under the unchanged lead clause is not enough either — *"Anything BASTET already has is greyed out"* is the false universal, and a `WillUpdateExisting` row falsifies it.

---

# Info

## P34 — *"Select All Subnets"* is live but inert when every offered row is non-selectable: the master ticks itself and nothing is selected `[x1]`

**Citation:** [`src/Bastet/Views/Azure/Import/_ImportScripts.cshtml:109`](../src/Bastet/Views/Azure/Import/_ImportScripts.cshtml#L109) · **Confidence:** confirmed · **Residue of:** P5 — `git blame` puts `:112`/`:118`'s `:not(:disabled)` scoping on `4f71e245`; before P5 an all-refused VNet produced zero rows and `#no-subnets`, so the all-disabled non-empty list is new

**Scenario.** A top-up re-entry into the wizard on a linked target whose every Azure subnet is already imported renders one row, disabled. Clicking *"Select All Subnets"* leaves the master checkbox visibly **ticked** while zero subnets are selected and *"Import Selected Subnets"* stays greyed out. The handler at `:112` selects `.subnet-checkbox:not(:disabled)` — an empty set — so `.trigger("change")` fires on nothing, `syncSelectAllSubnets` (`:116-123`) never runs, and the master is never corrected back. This is the state round 12's L3 explicitly outlawed: its `checked > 0 &&` conjunct exists precisely so an empty list cannot tick the master.

**Reproduction.** Own port 22171, catalog `vfy16b3`, SP1, live Azure. Imported `rig-vnet-onesubnet`'s only subnet through the wizard, then re-entered: `BEFORE master={checked:false, disabled:false}; boxes=[{subnet-0, disabled:true, checked:false}]; import-disabled=true` → click `#select-all-subnets` → `AFTER master={checked:TRUE}; boxes unchanged; import-disabled=true`. Confirmed twice on two rigs. **The filing's second limb is refuted and struck:** *"nothing says why"* is false — `inner_text("#step3")` reads *"…only **Already imported** 10.106.0.0/20 Already imported as Bastet subnet 'only'."*, i.e. the badge and the reason are on screen. Nothing wrong can be submitted: a second click undoes the tick, `:244` clears it in `beforeSend` on re-entry, and `#import-form.submit` at `:355` blocks a zero-selection post independently.

**Fix — unsound in its second limb.** Take either `$("#select-all-subnets").prop("disabled", $(".subnet-checkbox:not(:disabled)").length === 0)` in `loadSubnets`' success handler (two-way on every render, so a later VNet with selectable rows re-enables it; the paths that skip it leave `#subnet-selection` hidden by `beforeSend`) or — better, and one line — append `syncSelectAllSubnets();` to the master's own handler after `:112`, which drives the master to unchecked through the very `checked > 0 &&` conjunct round 12 wrote for this and is a no-op in both directions on a non-empty set. **Reject the second limb** (*"plus the three-way count sentence `_BulkScripts.cshtml:239-262` already computes"*): two of that block's arms end with *'Untick "Hide unavailable" to see why'*, a control this wizard does not have, and every arm says the rows *are hidden* — here they are on screen with their own badge and reason. It would replace a silent-but-harmless control with a false statement about what the operator is looking at.

---

## P35 — `BulkImportPlanItem.Warnings` is never written, and the bulk preview renders a per-item Warnings panel that can never appear `[x1]`

**Citation:** [`src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs:193`](../src/Bastet/Models/ViewModels/AzureBulkImportViewModels.cs#L193) · **Confidence:** confirmed · **Residue of:** none — `git blame` puts both the property and the render block on `73fc76f2` ("Azure Bulk Import (#108)", 2026-05-07); original dead code, not residue of any P-fix

**Scenario.** The property is initialised to `[]` and nothing in `src` or `test` ever adds to it. It is serialised into every `/Azure/BulkImportPreview` response, and `_BulkScripts.cshtml:549-555` builds a whole `alert alert-warning` block titled *"Warnings"* from it, guarded on `item.warnings && item.warnings.length > 0`. The plan has a per-item **error** channel and a plan-wide `GlobalErrors` channel that are used, and a warning channel that is not: a display surface with no producer.

**Reproduction — not runnable in the usual sense.** The defect *is* the unreachability, so there is no state to drive into; this is the narrow dead-code exception. The endpoint was nevertheless driven to confirm the channel exists on the wire and is always empty while its sibling populates. Own port 24117, catalog `vwarn16`, SP1: `POST /Azure/BulkImportPreview` for five VNet prefixes (including a `willMarkFullyAllocated` item and two `willRename` items) → every item `"errors": [], "warnings": []`. The discriminating run: a target with a child subnet → same item, `"errors": ["Cannot import VNet prefix 10.105.0.0/16: matched Bastet subnet 'emptyvnet' … already has child subnets …"], "warnings": []`. Exhaustive producer check: `grep -rn "Warnings" --include=*.cs --include=*.cshtml` yields exactly one hit for this type — the declaration; all six `.Warnings.Add` sites (`AzureReconciler.cs:193,211,272,280,289,326`) write `AzureReconcilePlan.Warnings`, a different type. The bulk planner writes `item.Errors.Add` at 8 sites and `plan.GlobalErrors.Add` at 13, and `item.Warnings` at none. No client-injectable path: `BulkImportPlanItem` is never model-bound; the plan is constructed server-side at `AzureController.cs:345`.

**Fix — take the delete arm only.** Remove the property at `:193` **together with** the render block at `_BulkScripts.cshtml:549-555`; leaving the block behind just relocates the dead code. Measured on a patched copy: `dotnet build --no-incremental` 0 warnings 0 errors, `dotnet test` **929/929**, no test asserts the JSON shape of the preview response. **Reject the "give it its first producer" arm.** `WillRename` and `WillMarkFullyAllocated` are **already rendered**, on the same card, by the same file — `_BulkScripts.cshtml:517-519` prints *"→ will rename to **X**"* and `:532-534` prints *"— will be marked **fully allocated** …"*. Seeding them into `Warnings` would print each fact twice and put an amber panel headed *"Warnings"* on every ordinary item, since `WillRename` fires on renames the operator asked for by ticking the box and `WillMarkFullyAllocated` on the encompassing import O6/P14 deliberately restored as a supported outcome. If a non-fatal per-item channel is wanted later, reintroduce it with a real producer and a message not already in the header.

---

# Refuted — reported by a finder, killed by the verifier

| id | Title | file:line | Why it was killed |
|---|---|---|---|
| — | *(none)* | — | **No candidate was refuted in the original round-16 pass.** All fifteen candidates carried to verification survived, and all fifteen reproduced on a running instance. One raw finding was dropped at merge before verification as a duplicate. |
| R1 | An `AzureRangeNotImported` item raised under a host-IP-bearing container is unclearable and names no remedy, while the exact-prefix arm beside it got one from P10 `[x1]` | `AzureReconciler.cs:381` | Consequence refuted by control run. The reason string is **byte-identical with and without host IP assignments** on the containing row — `wholePrefixTarget` is null either way — so the host IP causes nothing in the message and P10 never touched this arm. "Unclearable" is false and the clearance was measured: delete the host IP and the wizard flips `Blocked → WillUpdateExisting` in the same request cycle, commit, review items 2 → 0. The blocker is named on four other surfaces (`Subnet/Create`, the import TempData, `BulkGetVNets`, `BulkImportPreview`) plus the "Has Host IPs" badge. Unlike P10, which was a **false** instruction, this arm asserts nothing. Attribution wrong too: `:381` blames to `65d1fc6` (round 15). Its "cheaper interim" — relax `wholePrefixTarget` to the deepest containing row — would ship the exact false statement P10 existed to remove (*"It covers the whole of BASTET subnet 'X'"* about a strict subset). |
| R2 | `SubnetViewModelValidationParityTests` certifies Create/Edit validation parity while the one place parity fails is uncovered — Edit accepts a name Create refuses `[x1]` | `test/Bastet.Tests/Security/SubnetViewModelValidationParityTests.cs:46` | Re-files an explicit owner decision, twice recorded: `AUDIT-FINDINGS-5.md:158` (*"`[SafeText]` was deliberately not added … adding it to the edit model would make those rows uneditable until renamed"*) and `AUDIT-FINDINGS-12.md:314` (refuted as R1), with a standing carry-forward *"must not be 'restored'"*. The only novel element — a bidi override on the delete-confirmation screen — was obtained **through Create**: `Description` carries no `[SafeText]` on either view model, and `POST /Subnet/Create` with U+202E in `Description` returned 302 with zero validation errors and rendered on the same page. A harm that survives the proposed fix is not evidence for the finding. Rendered rather than byte-inspected, the operator is not misled: the mangled sentence sits above an identity card rendering `Name` and `10.77.1.0/24` correctly in separate bidi paragraphs, the confirm token is the literal word `approved`, and the row is chosen by URL id. |
| R3 | The Create-Subnet modal's parent-network lookup can never match, and would pick a child's network address if it ever did `[x1]` | `_SubnetCalculationScripts.cshtml:224` | The `find` is genuinely dead; the consequence is impossible. `parentId` is `Model.Id` and `childSubnets` holds that subnet's **children's** ids, which can never equal their own parent's id — so the "if anyone fixes it to `==`" harm the severity rested on cannot materialise, and was measured not to: `loose find: NO MATCH`. The fallback is not a happy accident but an identity — `startingAddress` is always a `range.StartIp` inside the parent, and `getSubnetBoundaries(startingAddress, parentCidr)` masks it to the parent's own network address, measured `10.50.0.0-10.50.255.255` for parent `10.50.0.0/16`. Zero runtime effect, and none after the specific edit the finding warns about. |
| R4 | Two arms of the reconcile wizard's `statusLabel` switch are unreachable, so two review statuses have no label anywhere in the UI `[x1]` | `_ReconcileScripts.cshtml:225` | The arms are dead; the stated consequence is not caused by them. The review table has **no status column at all**, by an explicit decision recorded at `AUDIT-FINDINGS-6.md:330-336` (*"No badge case was added to the client-side `statusLabel` switch, deliberately … the review table renders each row's reason instead"*). A/B with both arms deleted: the operator's screen is byte-identical. The finder's own evidence contradicts the claim — it rendered an `AzureRangeNotImported` row, a status with **no arm at all**, and got the same "no badge". Three of the five review statuses have never had an arm. `VNetPrefixStillCovered`'s label was added deliberately in `65d1fc6` alongside its routing (`AUDIT-FINDINGS-15.md:122`). |
| R5 | Unreachable same-resource-and-same-prefix exclusion retained in `FindLiveOwnerOfRange`'s overlap arm `[x1]` | `AzureReconciler.cs:512` | A round-16 **struck** item re-filed verbatim: `AUDIT-FINDINGS-16.md:99` records the verifier raising unreachability and the owner declining, in the same words. Re-filing needs a new measured consequence; the finder measured none (929/929 unchanged), and an instrumented run confirmed 22 evaluations across the suite and two live scans with **zero fires**. Its only novel fact is that `4c0ec5e` deleted the explaining comment — inadmissible: comment findings are out of scope, and that commit is the owner's whole-file comment ban. Its own proposed substitute already exists (`AzureReconcilerRangeStillAllocatedTests.cs:187, :203, :291`). |
| R6 | `AzureBulkImportPlanner.TruncateForName` is dead — no caller anywhere in src or test `[x2]` | `AzureBulkImportPlanner.cs:809` | Every fact is right and there is no defect: unreachable in every execution, so no input produces a wrong output. The finding concedes it (*"No wrong output today"*). `AUDIT-FINDINGS-11.md` lists this exact method under **"Dead but deliberate — do not tidy"**, and rounds 5, 12 and 14 each record it killed; `x2` proves nothing, since a private method with zero callers is trivially rediscoverable by grep and round 14's filing was also `x2`. The deletion is safe (verified clean rebuild, 929/929) and belongs in a cleanup sweep, not a findings file. |
| R7 | `AzureService.ExtractIpv4Prefix` has had no callers since round 13 `[x2]` | `AzureService.cs:350` | Verbatim re-raise of round 14's refuted R4 (`AUDIT-FINDINGS-14.md:273`), same method, same `[x2]`, same first-prefix-wins-beside-the-plural argument, and the same disposition applies: *"True observation, absent consequence … the sole stated consequence is conditional on a future edit that does not exist."* The owner's standing answer is visible in the tree — round 14's sibling R5 (`TruncateForName`) is still present two rounds later. The one thing that changed since is that `4c0ec5e` stripped the distinguishing XML docs from the two similarly-named helpers, which is a documentation argument and inadmissible. |

**Rows R1–R7 come from the re-audit pass**; the *(none)* row is the original round-16 result and is kept as filed.

The original round's zero-refutation result should be read as a statement about the **funnel**, not about the code: 28 raw findings were compressed to 15 candidates before any verifier saw them, so the merge stage absorbed the noise that Refuted has absorbed in previous rounds. What verification did kill this round was distributed **inside** the surviving entries rather than concentrated here — eight fixes judged unsound or incomplete, and three sub-claims driven and struck: P11's *"second, API-reachable leg"* (already guarded at `SubnetController.Azure.cs:193`), P12's *"on a write that succeeded"* and its entire `RelinkAzureSubnet` escalation, and P5's third limb (the free-space consequence, which survives the fix and is therefore not caused by the cited line). Each is recorded in the finding it belongs to so round 17 does not re-derive it.

---

# Watch list

Not findings. Only items a verifier could not **settle** — thin evidence, unproven reachability, or patterns worth grepping next round. Nothing reproduced is parked here; every reproduced defect above is filed at the severity its consequence warrants, regardless of trigger frequency or fix cost.

- **The acked-write family is wider than the one error number anyone has looked at.** P4 is `-2`. A transport-level failure *after* the server committed — `10054`, `-1`, "A transport-level error has occurred when receiving results", a reset connection, a KILLed SPID — is exactly as ambiguous, lands in the same catch, and produces the same categorical "Error X-ing" message. Nobody swept the six controller catch sites for it, and nobody established which of them can be reached with the write already durable. The correct predicate is "the save's outcome is unknown", and it has not been written down anywhere.
- **`Subnet.AzureResourceId` still has no editor and `DeletedSubnets` still has no restore.** Third round running. This round they are why P1's archive is terminal, why P6's successful repair is unverifiable by the operator, and why P9's printed remedy has to be a lie. What remains unsettled is the same thing round 15 left: whether any other flow silently depends on that column being immutable, and whether a one-off admin repair action would have a second consumer.
- **Round 15's `AccountsFor` sweep was asked for and only half-done.** It asked for a sweep for a *third* class of row that could satisfy `AccountsFor` without recording the range. This round found the containment arm wrong again (P2) — that is arm one revisited, not the sweep. The remaining question is sharper now that P2's correct predicate is known: **is any other consumer of "BASTET records this range" using a proxy rather than the free-space computation itself?** That is a grep for `IsFullyAllocated` and `IsSubnetContainedInParent` used as a stand-in for "not offered as free".
- **Duplicated predicates keep producing findings, and the enumeration round 15 asked for still has not happened.** Four more this round: the population guard in the planner vs. `GetSubnets` (P14), the containment rules in `ValidateSubnetCreation` vs. the wizard's list (P5), the more-specific-parent test in the plan vs. `AnnotateSubnet` (P13), and the sibling-qualification rule in the bulk planner vs. `ResolveImportNames` (P15). All four are one side of a rule updated on the other side only, and in three of them the stale side is a **controller or view** while the current side is a planner. The availability enum's own contract names this failure mode, which makes each instance self-documented rather than discovered. Grep next round for every predicate that appears in both a planner/annotation and a controller/commit path — this is now the single highest-yield sweep available.
- **Two Production-only legs are read, not driven, because the rig demands OIDC.** P7's non-Development logout branch (its only cookie write is `SignOutAsync` on the cookie scheme, so the TempData cookie is untouched for the same reason) and P12's `UseExceptionHandler("/Error")` rendering of the escaped 500. Neither defect depends on which page renders, but the exact Production bodies are unverified. This is the same gap round 15 recorded against O14 and it has not closed.
- **Hard-coded assertions in Razor markup that the client never reconciles with what happened.** P6 found one (`_StepReview.cshtml:33` asserting a re-scan occurred). Nobody swept the other two wizards' static markup for sentences that state a fact the surrounding JavaScript can falsify. The pattern is a literal sentence in a `.cshtml` inside a panel whose visibility is toggled from more than one outcome.
- **The reconcile wizard prints bare BASTET subnet names in four warning strings.** P15 is the case where two rows genuinely collide, and it is filed. What is not settled is whether any *other* surface identifies a row by name alone on a screen that authorises a destructive action — `NameList`/`OwnerList` were the only two examined.
- **`EditSubnetViewModel.Name` still has no `[SafeText]` while `CreateSubnetViewModel.Name` does** (verified against the tree at `1a31228`: the attribute appears on `CreateSubnetViewModel` and on both host-IP view models, and nowhere in `EditSubnetViewModel`). No path that round-trips a name through Edit was swept this round either. Carried unexamined from rounds 14 and 15 — the third time.
- **Port collisions on the rig are now frequent enough to corrupt a measurement.** Three agents hit an occupied port this round, and one read another agent's application for several steps before noticing, leaving two rows in a catalog it did not own. Anyone running the rig should verify the port is free **and** assert the app's own identity (its catalog name) before trusting a measurement, not just that something answered on the port.

**Added by the re-audit pass (P16–P35).**

- **A severed commit strands the session-scoped `sp_getapplock`, which contradicts what round 16 recorded.** While driving P19, `sys.dm_tran_locks` showed an `APPLICATION X` lock held by a sleeping session after the client connection was reset; `sp_releaseapplock` then ran on a *different* pooled `ClientConnectionId` and failed with error 1223, and every subsequent subnet operation took the 30s `TimeoutException` path until the session was KILLed. Round 16's P4 entry states *"no application lock is stranded … the locking service releases in a `finally`"*. That holds when the connection survives; it was measured not to hold when the connection does not. Nobody has established which paths can reach it or whether the pool returns the same connection often enough to self-heal.
- **`SqlSaveOutcome`'s determinate set encodes a statement that was measured false.** `SqlSaveOutcomeTests` pins `Number == 0` among the six determinate numbers that "must not be swallowed" because they "mean the write did not happen". On Linux managed SNI a post-commit reset arrives as `SqlException.Number == 0` with the write durable — driven, twice, on two independent rigs. P19 names the fix; what is not settled is whether the rest of that list is Windows-native-SNI numbering too, i.e. whether the three sites P4 patched are protected against anything but `-2` on this platform.
- **The "imported from another subscription" limb of the reconcile cascade is read, not driven.** P20 measured the hand-created limb. `AzureReconciler.cs:120` skips linked snapshots failing `BelongsToSubscription`, so such a row is not in `plan.Items` and cannot be in `withheld` either, and the provenance-blind subtree walk would archive it silently. The rig has one subscription, so this is code-read only. It is the same sentence and the same fix, but the second half of the claim is unmeasured.
- **The create path has no network/broadcast reservation at all.** Scoped out of P28 deliberately: `CalculateUnallocatedRanges` never offers them, but `POST /Subnet/Create` accepts `10.105.0.0/32` and `10.105.255.255/32` as child subnets, while `/HostIp/Create` refuses the same addresses with *"Cannot assign the network address as a host IP"*. Whether a `/32` at a reserved address is a legitimate bookkeeping row is an owner question nobody has been asked.
- **`TargetName`'s multi-prefix qualification is computed from the current selection only.** `AzureBulkImportPlanner.cs:134-141` builds `multiPrefixVNetIds` from `parsed`, i.e. the prefixes ticked in **this** run — so the bulk wizard driven one prefix at a time produces the same bare-name collision P26 closes on the single-VNet side. Measured while verifying P26, not filed as its own finding because the fix is the same rule in a third place. This is the fifth instance of the planner-versus-controller duplication the list above already flags, and the first where both sides are the planner.
- **Round 16's `[SafeText]` carry-forward is now swept, and the answer is "no defect from the Edit round-trip".** See R2: the asymmetry is a decision recorded twice (rounds 5 and 12) and the Edit path produces nothing Create does not. What the sweep *did* surface, and what is not settled, is that `Description` carries `[NoHtml]` and no `[SafeText]` on **both** view models, so a bidi override reaches the delete-confirmation page through `POST /Subnet/Create` with zero validation errors. Rendered, it garbles one sentence beside a correct identity card; nobody has swept the other free-text columns or the other destructive confirmation pages for the same shape.
- **Stray files at the repo root, not just stray rows in catalogs.** Four agents reported an untracked `proxy.pid` at `/home/anuj/code/Bastet` during this pass (contents `189326`, a dead PID, dropped by another agent's TDS relay whose `cd` did not take). Each correctly declined to delete a file it had not created, and it had to be cleared by hand before the tree was clean. Anything that writes a pid or log file must be given an explicit working directory under the scratch rig; a single untracked file at the root refuses the final commit.
