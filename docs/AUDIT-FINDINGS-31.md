# Bastet — Round-31 Audit Findings
branch: audit/round-31 / HEAD: 8e10c72 / test baseline: 971 passed / date: 2026-09-15 / scale: Standard — beats 1, 2, 3, 4, 5, 7 x 2 independent passes (12 finders); beat 6 skipped, 8e10c72..HEAD -- src/ is empty; verification interrupted by a machine reboot and completed on a rebuilt rig
Round 31 filed 16 findings, of which 0 are residue of round 30's own fixes.
Five findings are residue of an earlier round: 18-R4 / 18-R5 (L3), 23-L4 (L12), 25-L3 and 28-L8 (L15), and two at round granularity with no per-finding ledger id, both round 17 / f4a0a87 (L2, L8).

# Critical

# High

# Medium

## M1 — Replicas cold-starting on an empty DataProtectionKeys table each persist their own key with no coordination, so cross-replica form POSTs answer 400 and TempData banners are dropped `[x1]`
**Where:** src/Bastet/Program.cs:89 (the only DataProtection wiring, L85-89), src/Bastet/Program.cs:217-371 (the only cross-replica startup serialization, released L346-352 before `app.Run()` at L442 — verified not to cover the key-ring load in **either** BASTET_AUTO_MIGRATE mode; with `false` even the incidental stagger is gone). Consumers, not defect sites: the 13 `[ValidateAntiForgeryToken]` POSTs in SubnetController.Create/Edit/Delete/BulkAzure/AzureReconcile, AzureController and HostIpController; Views/Shared/_TempDataAlerts.cshtml (rendered by 8 pages incl. Subnet/Details.cshtml); Views/HostIp/Delete/_Header.cshtml; src/Bastet/Controllers/ErrorPageMessages.cs (a third TempData channel the finder missed — on the blind replica `Take` returns null and ErrorController substitutes generic text, losing the specific sentence at SubnetController.Delete.cs:23,128, Edit.cs:23,57,170,202, Read.cs:42, AzureController.cs:40,135); Views/Azure/BulkImport/_BulkScripts.cshtml:473 and :644 and Views/Azure/Reconcile/_ReconcileScripts.cshtml:135 and :371 (the wizard partials send the token in the RequestVerificationToken header and render their own failure text instead of the 400 page).
**Breaks:** Two replicas share a catalog whose DataProtectionKeys table exists but is empty (first boot after the shipped src/Bastet/Database/2.5.sql, which creates it empty at L196-201, or after a clear). Each runs an unserialized SELECT → "Creating key" → INSERT → re-SELECT with no lock of any kind held; whichever replica's post-persist re-read precedes the other's INSERT never learns that key. From 2 minutes after startup (the framework only auto-refreshes an unknown key id inside a window set once in the KeyRingProvider constructor) until that replica's next scheduled ring refresh (~24 h) or a restart, it answers any POST whose form the other replica served with HTTP 400 "Bad Request — BASTET / The request could not be understood by the server." offering only "Return to Home" and "Go Back", and silently drops any TempData banner the other wrote: the subnet or host IP really was created or deleted, the Details page renders 200, and the confirmation is simply gone — including the indeterminate-outcome warnings at SubnetController.Delete.cs:168 and HostIpController.cs:416 ("BASTET could not confirm whether this subnet/host IP was deleted. Check … before retrying."), which are delivered only through TempData across a redirect. Same-replica traffic works, so the failure looks random. BASTET_AUTO_MIGRATE defaults to false (README:125 — the documented db_datareader/db_datawriter SQL-script deployment) and Program.cs L375-378 tells operators that this SQL key table is what makes "multiple replicas without session affinity" work, so affinity cannot be assumed (§6). One verifier's rate correction, which changes nothing about severity: with two replicas the failing combination is "form rendered by the replica whose key X lacks" AND "POST lands on X", about one submit in four rather than one in two. A second nuance the scenario understates: a double-key cold start is necessary but not sufficient — the key one replica missed must also be the other's resolved default, i.e. the two INSERTs must be separated by more than one re-read round trip while both opening SELECTs still saw the table empty.
**Repro:** `$RIG/start-app.sh v_m9_truth 57097 bastet_r31_v_m9_truth A` plus a second labelled instance on the same catalog, with an exclusive `TABLOCKX,HOLDLOCK` held on DataProtectionKeys (plain sqlcmd contention, no code or config change) until both replicas blocked on their opening SELECT. Five consecutive two-replica cold starts with BASTET_AUTO_MIGRATE=**true** produced two keys every time ("race attempt=1..6 createdA=1 createdB=1 rows=2"), the log showing `sp_releaseapplock` for the migration lock and only then `SELECT … FROM [DataProtectionKeys]` → "Creating key {…}" → INSERT → a second SELECT → "Now listening" (measured stagger ~470 ms from the stored activation dates). Consequence half, two replicas holding disjoint rings past the 2-minute window (divergence confirmed non-destructively by decoding the key id out of each antiforgery cookie, `diverged=YES`): A→A and B→B control POSTs 200; "A serves → B posts" and "B serves → A posts" both 400, title "Bad Request - BASTET", logs carrying "Antiforgery token validation failed. The antiforgery token could not be decrypted. ---> System.Security.Cryptography.CryptographicException: The key {…} was not found in the key ring"; cross-replica TempData reads 302 → 200 with `banner=MISSING` while the subnet existed, log "The temp data cookie .AspNetCore.Mvc.CookieTempDataProvider could not be loaded." The second verifier reproduced the same end state on one instance (stash and clear the table, restart so it mints its own key, re-insert the other key) and pinned the time gate in both directions: identical foreign-key state at t+0m20s → POST 302 /Subnet/Details/3 with the banner shown; at t+2m48s → POST 400 and the banner absent.
**Fix:** Serialize the first key-ring load across replicas. Before `app.Run()`, take an Exclusive Session `sp_getapplock` on a **dedicated** resource `bastet:dataprotection:{catalog}` and, while holding it, force the ring once with `app.Services.GetRequiredService<IDataProtectionProvider>().CreateProtector("Bastet.KeyRingStartup").Protect(new byte[1])`, then release. Do **not** reuse `Bastet:Migration:<catalog>`: that queues every replica's warm-up behind a migration that may hold the lock for its full 300 s timeout and changes what that lock means. It must run in both BASTET_AUTO_MIGRATE modes and must sit **after** the L217-371 block, because in auto-migrate mode `dataProtectionTableExists` is set true at L64 without probing and the table only exists once `dpContext.Database.Migrate()` has run. One verifier judged the filed fix **unsound as written** on two points. (1) As proposed it introduces a new defect in exactly the deployment it targets: today, with BASTET_AUTO_MIGRATE=false (the default), startup never fails on a database problem — the only DB touch is the INFORMATION_SCHEMA probe at L66-82 inside the §7-accepted blind catch, which degrades to ephemeral keys and lets the app start — while sharing `OpenMigrationLockConnection` (L226-299) imports code that throws on a failed open, throws "could not open master" on the 4060 fallback and throws "Startup was aborted" on any negative `sp_getapplock` result, so a replica deployed the documented script way would start failing to boot on contention or a transient connect failure. Make the warm-up lock strictly best-effort in that path: wrap the open/acquire in a try that logs and falls through to an unserialized load, and treat a negative lock result as "proceed unserialized", never as an abort. Wrap the `Protect()` call the same way — `DataProtectionHostedService.StartAsync` swallows a startup ring-load failure today, so an unguarded call would turn a degraded start into a crash loop. (2) The auto-migrate half has a genuinely one-line narrow form that should be stated separately: do the same `Protect()` warm-up inside the existing try at L323-341 while the migration lock is already held, adding no connection, no lock and no new failure mode; only the BASTET_AUTO_MIGRATE=false path (the reproduced one) needs the new best-effort lock. Restructure note, stated separately as the rules require: sharing the open/acquire/release helper across both paths is modest but more than a one-line change. Mechanism check: `IDataProtectionProvider` is a root singleton and `EntityFrameworkCoreXmlRepository` creates its own scope, so resolving it from `app.Services` before `app.Run()` is correct and the framework's later hosted-service load hits the cached ring. Product question: should the startup application lock be taken regardless of BASTET_AUTO_MIGRATE, since it is a cross-replica coordination lock rather than part of migrating? Interim: bring one replica up and wait until it is ready before starting the others, or restart every replica once after a simultaneous cold start; session affinity at the load balancer also masks it. Extra key rows do not need deleting.
**Residue of:** none

# Low

## L1 — [NoHtml] refuses ordinary operator text (angle-bracketed e-mail addresses, placeholders, generic notation) as "HTML tags" on every Name/Description write path, while the bulk-commit sibling silently strips the same input `[x2]`
**Where:** src/Bastet/Services/Security/ValidationAttributes.cs:5-25 (NoHtmlAttribute — refuses when `StripHtml(value) != value.Trim()`), src/Bastet/Services/Security/InputSanitizationService.cs:10-11 (HtmlTagPattern `</?[A-Za-z][^>]*>`), src/Bastet/Services/Security/InputSanitizationService.cs:18-28 (StripHtml), src/Bastet/Services/Security/InputSanitizationService.cs:86 (StripHtml inside SanitizeName), src/Bastet/Services/Security/InputSanitizationService.cs:104 (StripHtml inside SanitizeDescription), src/Bastet/Services/Security/IInputSanitizationService.cs:6 (the StripHtml member — added by both verifiers), src/Bastet/Models/ViewModels/SubnetViewModels.cs:10 and :27, src/Bastet/Models/ViewModels/EditSubnetViewModel.cs:23 and :30, src/Bastet/Models/ViewModels/HostIpViewModels.cs:24 and :44, src/Bastet/Controllers/SubnetController.BulkAzure.cs:231 (SanitizeName on the target rename), :279 (SanitizeName on AutoCreateTargetName — **omitted by the finder**, and exercised live), :338 (SanitizeName on child.Name), src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:882-890 (TruncateAndSanitizeName → SanitizeName, feeding the preview's proposed names at :437 and :470 and the fully-allocating name at :619/:630/:637 — **omitted by the finder**; no code change needed there once the strip leaves SanitizeName). Behaviourally inert, listed so the diff is not a surprise: src/Bastet/Controllers/SubnetController.BulkAzure.cs:199-201 and :340-342 call SanitizeDescription on ARM resource ids, which can never contain "<".
**Breaks:** An Edit-role operator typing "Contact: NOC <noc@example.com>", "Naming: <site>-<role>-<nn>", "List<string> of hosts; Map<Key, Value>", "x<y and y>z", "see </details> section" or "Owner: Jane Doe <jane@corp.example>" into a subnet Description, a subnet Name or a host IP Name is refused on all four ordinary forms (Subnet Create, Subnet Edit, HostIp Create, HostIp Edit — all RequireEditRole) with "HTML tags are not allowed in descriptions" / "…in subnet names" / "…in host names". The text is not markup and the refusal message is untrue for it. The rule is also unpredictable from the operator's seat: "temp < 5 and load > 3", "I <3 subnets", "a <b c" and "1 << 3 and 8 >> 2" all save, because the pattern only needs a letter after the bracket. The same Create and Edit forms accept the identical string in Tags and render it Razor-encoded (since 18-R7 / 24-L1), so the contradiction sits on one screen. What the operator loses is the ability to record an RFC 5322 name-addr contact or an angle-bracket placeholder anywhere in the product. Meanwhile the bulk-import commit — the §5 sibling write path — runs no DataAnnotations at all (ValidateSubnetCreation never calls Validator.TryValidateObject; grep of src/ finds no caller) and strips instead: a posted child name "<b>s1</b> web<nn> Owner <jane@example.com>" commits 200 and is stored as "s1 web Owner". One decision, two implementations, already disagreeing. Output encoding is what keeps the app safe here and it does: every Razor sink attribute/text-encodes operator strings, and both wizard clients escapeHtml/.text() every server string.
**Repro:** `$RIG/start-app.sh v_m1_truth 60913 bastet_r31_v_m1_truth A`, then probe_forms.py and probe_bulk.py (requests + the form's own antiforgery token). Fresh catalog, root 10.200.0.0/16 = id 1. Every text above posted as Name and as Description to POST /Subnet/Create, POST /Subnet/Edit/10, POST /HostIp/Create and POST /HostIp/Edit → HTTP 200 redisplay carrying the refusal; the four controls → 302 to Details, the value round-tripping verbatim and appearing HTML-encoded exactly once; the identical text posted as Tags on the same Create and Edit forms → 302, rendered encoded in the Tags dd. Bulk: POST /Azure/BulkImportPreview with vNetName `<i>rig-r31-a1</i> lab <noc@example.com>` and child name `<b>s1</b> web<nn> Owner <jane@example.com>` → canCommit true with planned names "rig-r31-a1 lab" and "s1 web Owner"; POST /Subnet/BulkCreateFromAzurePlan → 200 "created 1 VNet target subnet(s), created 1 Azure child subnet(s)"; the tree then lists exactly those stripped names. Encoding proof for the deletion's precondition: a subnet named `x" onmouseover="alert(1)` with description `d'-alert(2)-' & <3 "q"`, tags `<script>alert(3)</script>,t" onmouseover="alert(4)` and a host IP named `h" onmouseover="alert(5)`, fetched across Details, the Index tree, Edit, Delete, Create GET, HostIp Index/Create/Edit/Delete, AllHostIps, the archives and both purge pages — zero pages carried any payload unencoded, and the TempData success banner was encoded. The bulk divergence is reachable only by a hand-built Admin request (Azure names cannot contain "<"), which is why it counts under §5's explicit "any API caller" sibling clause rather than as an operator flow.
**Fix:** Delete the filter rather than widen it, exactly as 24-L1 did for Tags: remove `[NoHtml]` from the six properties, delete NoHtmlAttribute, delete the StripHtml step from SanitizeName and SanitizeDescription, then the now-callerless StripHtml and HtmlTagPattern and the interface member, keeping trim and the 100/1000 length cut so the forms and the bulk commit accept and refuse exactly the same input (§5 sibling rule). Two constraints from the verifiers. (1) The attribute and the strip must go **together**: deleting only `[NoHtml]` while SanitizeName still strips would let a name such as "<b>" be silently emptied past `[Required]` and stored blank — that half-fix is unsound. (2) StripHtml currently supplies a `Trim()` after the length cut, so keep a trailing `.Trim()` (or accept the unreachable-in-practice trailing-space case: `[StringLength]` refuses over-length form input before sanitisation and Azure names are at most 80 chars). Do not take the finder's own "narrower" option of dropping only the six attribute uses — it leaves HtmlTagPattern/StripHtml with no caller, i.e. dead machinery. Do not take the element-name-allowlist alternative: it keeps a filter §5 calls a usability rule wearing a security badge, still misfires on ordinary text ("<b>" or "<i>" as note markers, "<a>"), and re-creates the two-implementation drift. After the deletion `[StringLength]` still refuses over-length form input before the filter would have run, so form and bulk paths accept the same set. No client-side validator exists for NoHtml (the rendered forms carry only length/required data-val attributes), so nothing on the client changes. The 18-R21 ledger row ("tests re-pinned to NoHtml") means reconcile will find pins on the deleted attribute to re-point, as 24-L1 did. Product question: none blocking — the only text that becomes storable is text Razor already encodes. Interim: write the contact without the angle brackets, or use parentheses for placeholders.
**Residue of:** none (origin 3940c98 "Input Sanitization (#43)", pre-loop; f4a0a87 / round 17 only narrowed the pattern from `<[^>]*>` and has no per-finding ledger id, and 18-R7 / 24-L1 closed the Tags instance of the same class)

## L2 — After an Azure subnet is re-carved, the wizard describes that subnet's own stale linked Bastet row as a hand-made middle-man "Azure has no such subnet" and names "re-carve", a remedy Edit refuses for every linked row `[x2]` strings
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:373-385 (AnnotateSubnet middle-man branch, sentence at L378-382), src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:802-809 (DetectExistingBastetSubnetConflicts middle-man global error, sentence at L804-808). Both verifiers **struck** the finder's other two sites: AnnotateSubnet L361-371 and the plan-level twin L795-801 ("Would contain existing Bastet subnet 's1' (10.x.1.0/25), which would create an invalid hierarchy.") are true, name the row as §4 requires and name no remedy at all — a fact-only refusal is an accepted shape (24-L3, 28-L10) — so there is nothing untrue and nothing refused there. Leave both untouched; "never names reconcile" is not a rule.
**Breaks:** A VNet was imported and its subnet s1 is later re-ranged in place in Azure (/24 → /25), a re-carve §5 puts in scope. Wizard step 2 and the step-3 global error then say of the stale row that carries that very Azure subnet's resource id: "Bastet subnet 's1' (10.x.1.0/24) sits between this VNet and 10.x.1.0/25, and Azure has no such subnet. Imported subnets are placed directly under their VNet, so delete or re-carve 's1' before importing this one." Two untruths for a linked row. Azure *has* that subnet, under the id the row carries — reconcile, on the same state, says "The Azure subnet still exists but its address prefix is now 10.x.1.0/25, not 10.x.1.0/24. Delete it here if you want to, then use the Azure import wizard to bring in its current prefix." And "re-carve" is refused for every Azure-linked row: the Edit page renders the CIDR input readonly and disabled with "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it.", and the POST is refused with "This subnet is linked to an Azure resource, so its CIDR cannot be changed here." — which sends the operator back to the wizard that sent them there. Rule 3 plus a false fact, on two screens of one wizard. The "delete" half is true and actionable (the plain Delete page deletes a linked row and the import then succeeds), so the consequence is confusion, not lost space. Mechanism: FindMoreSpecificParent (L268-292) matches any existing row strictly inside the VNet prefix that strictly contains the Azure subnet, with no look at AzureResourceId, so a stale linked row is indistinguishable from a hand-made one at the sentence.
**Repro:** `$RIG/start-app.sh v_m6_truth 56499 bastet_r31_v_m6_truth A`; own fixture `az network vnet create -n rig-r31-v_m6_truth-v1 --address-prefixes 10.140.0.0/16 --subnet-name s1 --subnet-prefixes 10.140.1.0/24` plus s2, imported through the wizard as rows 1/2/3 (row 2 carrying AzureResourceId …/rig-r31-v_m6_truth-v1/subnets/s1); then `az network vnet subnet update -n s1 --address-prefixes 10.140.1.0/25`. GET /Azure/BulkGetVNets → s1 10.140.1.0/25 status=Blocked with that sentence verbatim and a resourceId tail matching row 2's; POST /Azure/BulkImportPreview → canCommit False with the plan-level twin ("…sits between that VNet and this subnet, and Azure has no such subnet…"); POST /Azure/ReconcileScan → item id=2 SubnetPrefixChanged "The Azure subnet still exists but its address prefix is now 10.140.1.0/25, not 10.140.1.0/24."; GET /Subnet/Edit/2 → CIDR readonly+disabled, POST Cidr=25 → 200 redisplay with the linked-row refusal and the DB row still /24. Playwright step 2 renders "s1 10.140.1.0/25 Cannot import" plus the sentence. Loop control: BulkDeleteStaleAzureSubnets [2] → targetsDeleted 1, s1 then Available, commit "created 1 Azure child subnet(s)". Grown direction (Azure back to /24 while the row is /25) draws the would-contain sentence, and the same-shape gone-resource variant (Azure s1 deleted, a new s3 10.140.2.0/25 created inside stale linked row 's2') still says "delete or re-carve 's2'". Held variant (one host IP on the row): reconcile answers HeldByManualContent "BASTET will not delete it … Delete it here first, then run the scan again." while the wizard sentence is unchanged.
**Fix:** Both verifiers judged the filed fix **unsound**. Keep the refusal — it is right and serves §4 — and change only the two middle-man sentences, but branch **first** on `!string.IsNullOrEmpty(row.AzureResourceId)`, the same predicate SubnetController.Edit.cs:82 refuses the CIDR change on, rather than on `AzureResourceIdentity.IsSameResourceId` alone: the reproduced gone-resource variant is a linked row in the way that is linked to a *different* (here deleted) Azure subnet, where Edit refuses the re-carve identically, so a same-resource-only branch would leave the refused remedy standing there. Inside that branch `IsSameResourceId(row.AzureResourceId, subnet.ResourceId)` may refine the fact — the planner already computes exactly that at this call site via HasPersistedSiblingFromSameAzureSubnet (L20-29), which is why the same preview names the planned child "s1 (10.140.1.0-25)". Linked and same resource: "Bastet subnet 's1' (10.140.1.0/24) was imported from this Azure subnet, whose prefix is now 10.140.1.0/25. Delete 's1' before importing this one." Linked to another resource: "Bastet subnet 's2' (10.140.2.0/24) is linked to an Azure subnet and sits between this VNet and 10.140.2.0/25. Delete 's2' before importing this one." Unlinked: keep the current §4 sentence verbatim. Do **not** write "Azure Reconcile offers to delete it": reconcile withholds a row holding manual content ("BASTET will not delete it … Delete it here first") while the plain Delete page accepts every row, so that sentence would plant a fresh rule-3 defect of the kind 24-L3 removed; "Azure Reconcile also reports it" is the most that is true, and "delete" is reachable by anyone who sees the wizard (Admin ⊇ Delete, and SubnetController.Delete.cs has no Azure-link handling at all). Two string sites, no behaviour change, no status, selectability or planner-logic change, no new predicate beyond the link check. Interim: none in the product; operators who run the reconcile scan first get the correct instruction there.
**Residue of:** round 17 (f4a0a87 "Audit 17 Cleanup (#170)", which reworded round 16's d18327e "Has a more specific existing Bastet parent" refusal into this sentence) — round granularity only, no per-finding ledger id exists before round 18. The two struck would-contain sites are moot; their blame is 65d1fc6 (round 15) and the non-audit aedd0bd / 73fc76f, not aedd0bd alone as the finder wrote.

## L3 — Wizard refuses link-only adoption of an unlinked hand-made target that has host IPs, while offering it for the identical fully-allocated shape `[x2]`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:204-211 (AnnotatePrefix: an exact-range row with host IPs that is not a top-up → Blocked "Bastet subnet '{n}' already has host IP assignments."; the fully-allocated sibling at L212-226 offers WillUpdateExisting link-only adoption), src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:548-554 (BuildPlanItem: the `!IsSameVNet(exact, p.Source.VNetResourceId) ||` term at L549 makes the plan refuse the prefix for an unlinked host-IP target even when no subnet is selected, where the fully-allocated error at L555-560 fires only when a selected subnet lacks its linked row). A corroborating finder's citation of "AnnotatePrefix (L165-172)" is stale against HEAD — those lines are AnnotateAvailability; the merged L204-211 is correct. The commit path re-runs the planner and carries no separate host-IP check, and the client consumes IsSelectable/CanCarrySubnetWork, so the two sites are the complete set.
**Breaks:** An operator carved a range by hand and assigned a host IP inside it; an Azure VNet has that exact prefix. Step 2 renders "Cannot import — Bastet subnet '…' already has host IP assignments." with the prefix checkbox disabled, a prefix-only preview answers canCommit=false with "Cannot import VNet prefix 10.31.0.0/16: matched Bastet subnet '…' (10.31.0.0/16) already has host IP assignments.", and the commit answers HTTP 400 with the same sentence. Remove the host IP and mark the same row fully allocated — a row that equally cannot hold child subnets — and the identical prefix becomes "Will update existing — Will link existing Bastet subnet '…' to this VNet. It stays marked fully allocated, so no subnets will be created inside it.", selectable, previewing canCommit=true and committing 200 "linked 1 existing target(s) to Azure". A link creates nothing inside the target, which is the model's own reasoning for allowing it on a fully-allocated row, so the host-IP row has no conflict either. The refusal names no remedy, and the operator's only route to having Bastet track that VNet is to archive their host IP records or the row. The decisive control: adding just the AzureResourceId flips the identical host-IP row to AlreadyImported with rename offered and a clean plan — §4's "linking a target that is not yet linked still counts as work and must stay offered", "a row the operator built by hand can be adopted by the VNet it matches", and "refusing on what the row already holds keys on provenance, which is never a reason".
**Repro:** `$RIG/start-app.sh v_m7_truth 33987 bastet_r31_v_m7_truth A` against shared fixtures rig-r31-a1 (10.31.0.0/16) and rig-r31-a3 (10.34.0.0/24), with hand-made rows carrying host IPs 10.31.0.5 and 10.34.0.5. GET /Azure/BulkGetVNets → both prefixes statusName=Blocked, isSelectable=False, canCarrySubnetWork=False with that sentence (their child subnets Blocked with "Containing Bastet subnet … has host IP assignments and cannot have child subnets."); prefix-only previews canCommit=False with the item error; POST /Subnet/BulkCreateFromAzurePlan → HTTP 400 carrying it. Playwright step 2: the a1 card reads "Cannot import", checkbox disabled=True. Then POST /HostIp/Delete 10.34.0.5 and POST /HostIp/SetAllocationStatus id 2 true: the same row → WillUpdateExisting, isSelectable=True, checkbox enabled **on the same screen as the other card's "Cannot import"**, preview canCommit=True, commit 200 "linked 1 existing target(s) to Azure", AzureResourceId then set. Provenance control: cleared the flag on the now-linked row and re-added host IP 10.34.0.5 by hand (accepted, 302) → the identical row is AlreadyImported, renameOnlyCandidate=True, "Already imported as Bastet subnet '…', which has host IP assignments, so no subnets can be added inside it.", its prefix-only plan error-free and rename=true planning willRename=True; ReconcileScan on that state is clean. Same row, same host IP, same VNet: the only variable between Blocked/HTTP 400 and AlreadyImported/canCommit True is the presence of the AzureResourceId.
**Fix:** Mirror the fully-allocated branch. AnnotatePrefix L204-211: when `exact.HasHostIpAssignments && !isTopUp`, return WillUpdateExisting with IsSelectable=true and the reason "Will link existing Bastet subnet '{n}' to this VNet. It has host IP assignments, so no subnets will be created inside it." (keep the isTopUp AlreadyImported wording unchanged). BuildPlanItem L549: drop **only** the `!IsSameVNet(exact, p.Source.VNetResourceId) ||` term, keeping `p.Subnets.Any(s => LinkedRowForSameAzureSubnet(s, existingSubnets) is null)`. Do **not** read the finder's prose "fires only when a selected non-encompassing subnet lacks its linked row, exactly as the fully-allocated error" literally, and do **not** copy the fully-allocated branch's `!s.FullyEncompasses &&` exclusion (L556) into the host-IP branch: the encompassing subnet never has a linked row, and WillMarkFullyAllocated (L626-638) checks only `exact.HasChildSubnets`, so excluding it would let a selection of the encompassing subnet mark a host-IP row fully allocated with no plan error and without the base CanMarkFullyAllocated rule — a §5 unvalidated Azure-driven write. With the term dropped, a selection including the encompassing subnet still errors, any non-encompassing subnet under a hand-made tree still errors (matching the per-subnet Blocked annotations at L389-396, which stay disabled in the client), the encompassing-subnet branch at AnnotateSubnet L322-329 stays Blocked, and a prefix-only selection reaches the commit's ExactMatch path (SubnetController.BulkAzure.cs L240-266), which has no host-IP gate and links exactly as it does today for the fully-allocated twin. WouldRenameTarget is computed before the branch (L201-202), so rename-on-link behaves like the fully-allocated branch; the client needs no change. Product question to settle in the fix: if the owner instead wants link-only adoption closed for both shapes, that is a §4 change and the 18-R4 branch at L212-226 is the one to revert — §4's current text points the other way, and §8 carries no ruling on it. Interim: none in the product; adoption today costs the host IP records (archive them, link, re-enter).
**Residue of:** 18-R4 / 18-R5 (round 20, 6fb8557: 18-R4 added link-only adoption for the fully-allocated shape only, and the 18-R5 whole-diff-gate repair "restored the unlinked-target plan refusal" by adding the `!IsSameVNet ||` term in the same round; the bare Blocked sentence itself is original, 73fc76f #108 / aedd0bd #133)

## L4 — AllDeletedHostIps re-derives "original subnet unresolved" from the display string, so a live subnet named "Unknown" is shown as gone `[x2]` — FIXED
_Fixed in <sha>. HostIpController writes the whole unresolved string (`Unknown (Original Subnet ID: N)`) and the view renders `@hostIp.SubnetName` with no branch, so a live subnet named "Unknown" is reported by its name._
_Swept: grep of src/ for `== "Unknown"` / `!= "Unknown"` returns nothing; the write-only AllDeletedHostIpItemViewModel.OriginalSubnetId and its mapping deleted so no 23-I10-class orphan is left._
_Verified: live rig — before, a live "Unknown" subnet and a purged one rendered byte-identically; after, they are 'Unknown' and 'Unknown (Original Subnet ID: 2)' with 'Gone (deleted)' beside them. 5 pins; reverting the controller sentinel reds 2, restoring the view branch reds 1._
_Reviewed: verdict (c) — reviewer reproduced all three cases on both builds, confirmed no remaining consumer of the deleted property and that paging is unaffected._

## L5 — HostIp Create breadcrumb rebuilds the subnet name by splitting the composite SubnetInfo string, so names containing "(" are truncated or blank `[x2]` — FIXED
_Fixed in <sha>. CreateHostIpViewModel carries SubnetName, set at the three Create render paths and round-tripped through a hidden input; the breadcrumb renders it and the `SubnetInfo.Split('(')` is gone._
_Swept: grep of src/ for `Split('('` returns nothing; a test now fails if any view reintroduces it. HostIp Index's breadcrumb already rendered the bare name and is unchanged._
_Verified: live rig — 'Lab (test)' rendered 'Lab' and '(alpha)' rendered a 0px empty anchor before; both render whole after, including the POST-redisplay and subnet-gone branches. 9 pins; restoring the Split reds 1, dropping the hidden input reds 1, deleting the redisplay restore reds 1._
_Reviewed: verdict (c) — reviewer measured the anchor widths on both builds, confirmed the hidden input is inert to GlobalSanitizationFilter and never persisted, and that a crafted script payload renders encoded. Its correction was adopted: the subnet-exists redisplay path is now pinned behaviourally, not by source text alone._

## L6 — Deleted Subnets page offers "Purge All" on an empty archive and the click lands on a refusal; the sibling deleted-host-IP page gates the same control on count `[x2]` — FIXED
_Fixed in <sha>. The Deleted Subnets header declares `@model DeletedSubnetListViewModel` and gates Purge All on `Model.TotalCount > 0 && UserHasRole(Admin)`, the host-IP archive's existing gate._
_Swept: both Purge All entry points in the product now carry the same gate, and a test fails if any view adds an ungated one. The GET's count refusal is kept as the server-side guard._
_Verified: live rig — empty archive now renders zero purge links (it offered one that bounced off a refusal before); with two archived records the link is present and reaches the confirmation page. 6 pins; dropping the count term reds 2._
_Reviewed: verdict (c) — reviewer confirmed the model flows through the no-argument PartialAsync overload and rendered the page as Admin, Delete, Edit and View without a throw._

## L7 — Host IP Edit sidebar tells Edit-only operators to delete the host IP, a remedy their role cannot reach `[x2]` strings — FIXED
_Fixed in <sha>. The Edit sidebar injects IUserContextService and branches on `UserHasRole(Delete)`: that reader keeps the original sentence byte-identically, everyone else is told to ask someone with the Delete role._
_Swept: a test fails if any view carries the delete-remedy sentence without the role gate; the two other remedy sentences in Views already route around the role._
_Verified: live rig — the Admin rendering is byte-identical to before. The reviewer drove the branch itself in a throwaway copy with a role-switchable DevAuthHandler: Edit-only reads the ask-someone wording and is refused 403 at /HostIp/Delete, Delete-only reads the direct sentence and gets 200. 6 pins; removing the branch reds 2, gating on Edit instead of Delete reds 2._
_Reviewed: verdict (c) — reviewer confirmed UserHasRole(Delete) is byte-for-byte the RequireDeleteRole membership and that the @inject reaches only this one partial._

## L8 — Create form seeds the child name through SubnetNaming.ToSafeText, which strips accented and non-Latin letters, quotes, slashes, colons and brackets from the parent's own valid name `[x1]`
**Where:** src/Bastet/Controllers/SubnetController.Create.cs:56-61 (`ToSafeText(parentSubnet.Name)` then WithSuffix, or the bare "{networkAddress}-{cidr}" when the filtered name comes back empty), src/Bastet/Services/SubnetNaming.cs:8-14 (the OutsideSafeText regex `[^a-zA-Z0-9\s\-_.,!?@#$%&()+=]` and ToSafeText — the sole caller in the product is the Create GET). Sites complete: grep of src/ finds no other character-class filter of this shape, ToSafeText has one caller, and the planner uses only WithSuffix.
**Breaks:** An operator opens Details of a subnet they named "Réseau Zürich \"Ost\" <3" — a name every rule accepted, stored verbatim and rendered verbatim by the Details h1 and every other screen — clicks Create Subnet on an unallocated range, confirms the CIDR modal and lands on the Create form pre-filled "Rseau Zrich Ost 3-10.200.41.0-25": the é, ü, the quotes and the "<" were silently deleted from their own parent's name. Clicking straight through writes that mangled name into the tree with a success banner naming it. "東京" seeds no name at all ("10.200.36.0-26"); "São Paulo/DC1" → "So PauloDC1-…"; "O'Brien's net" → "OBriens net-…"; "Zone \"A\"" → "Zone A-…"; "Lab: east <3" → "Lab east 3-…"; and the §5 example "temp < 5 and load > 3" → "temp  5 and load  3-…" with doubled spaces. The seed is what an operator who clicks straight through creates, so the tree fills with mangled names derived from correct ones. The filter serves no rule the product has: it exists only to satisfy the `[SafeText]` validation that round 17 removed from CreateSubnetViewModel.Name, and nothing on the Create POST refuses any character the parent's stored name can contain — the unfiltered seed posts and stores verbatim (proved live for the quote, angle-bracket, accent, CJK and slash cases). The client validates `#Name` with data-val-required and data-val-length only, no regex.
**Repro:** `$RIG/start-app.sh v_m2_truth 34615 bastet_r31_v_m2_truth A`. repro_http.py created root 10.200.0.0/16 and nine /24 parents with those names through POST /Subnet/Create (every one accepted, every Details h1 verbatim), then read `input#Name` on GET /Subnet/Create?networkAddress=<net>&cidr=26&parentId=<id>, producing every row above. POSTing the seeded name unchanged created id 11 with the mangled name; POSTing the **unfiltered** "Réseau Zürich \"Ost\" <3-10.200.40.128-25" was accepted (id 12, stored and rendered verbatim) — no rule refused it. repro_browser.py drove the real operator path in headless Chromium: Details/13 h1 "Réseau Zürich \"Ost\" <3" → click .create-subnet-btn (data-network 10.200.41.0) → modal → #createSubnetBtn → landed on /Subnet/Create?networkAddress=10.200.41.0&cidr=25&parentId=13 with `#Name` = "Rseau Zrich Ost 3-10.200.41.0-25" → submitted unchanged → Details/14 h1 that name, banner "Subnet 'Rseau Zrich Ost 3-10.200.41.0-25' was created successfully." (screenshot create_seeded_name.png). Razor encodes the same value on redisplay (`value="R&#xE9;seau Z&#xFC;rich &quot;Ost&quot; &lt;3"`), so the sink is safe. Control "plain-ascii_name (ok)" seeds unchanged.
**Fix:** Seed from the parent's stored name: at SubnetController.Create.cs:56-61 replace `ToSafeText(parentSubnet.Name)` with `parentSubnet.Name`, keep `WithSuffix(name, "-{networkAddress}-{cidr}", MaxSubnetNameLength)`, and delete SubnetNaming.ToSafeText and the OutsideSafeText regex (no other caller). **Drop** the empty-name fallback at Create.cs:58-59 rather than leaving it "if desired": once the filter is gone it is unreachable — Name is `[Required]` on Create and Edit (RequiredAttribute rejects whitespace-only), the bulk commit stores SanitizeName of a non-empty ARM name, and the DB column is NOT NULL. The unfiltered seed passes the POST's own rules by construction: model validation runs on the raw post before GlobalSanitizationFilter, NoHtml's tag regex needs "<" + letter + ">" and a stored name never contains a match, truncation cannot create one, and the suffix adds no ">". One latent edge to record rather than widen for: with non-ASCII names now reaching WithSuffix, `baseName[..room]` (SubnetNaming.cs:24, room = 100 minus the suffix length, about 85) can split a UTF-16 surrogate pair when a parent name of 86+ characters carries an astral character across the cut, and the HtmlEncoder renders the lone surrogate as U+FFFD — either cut on a text-element boundary inside WithSuffix or accept it as a corner of an already lossy truncation; a rune-aware cut is a widening. **Triage must see this:** the round-17 squash commit f4a0a87 records verbatim, in its sub-commit "Apply the same name rule to host IPs": "Not changed, and deliberately: SubnetController.Create.cs:56 still runs a parent's name through SubnetNaming.ToSafeText when suggesting a child name, so that suggestion still strips characters the form now accepts. The owner chose to leave it." The finder omitted this. That choice was never promoted to PRODUCT-MODEL §7 (a closed list created the very next round that does not include the seed filter) nor to §8, and no ledger row records it, so the verifier did not refute on it — it reads as a declined sibling under the round-16 "declining a sibling is the owner's call and goes in the record" rule, made while the SafeText concept still existed in code. If the owner still wants the filter left, the right outcome is a §7 entry so it stops being re-found; otherwise the fix is a pure deletion. Azure-imported parents are unaffected either way (ARM names are restricted to [A-Za-z0-9_.-]). Interim: retype the name in the Create form before saving.
**Residue of:** round 17 (f4a0a87 "Audit 17 Cleanup (#170)", which removed `[SafeText]` from CreateSubnetViewModel.Name and left the seed-side mirror that round 7 / ff285cf had introduced as its exact complement, with the commit line "Filter the parent name so the Create prefill passes its own validation") — round granularity only, no per-finding ledger id survived the squash. **Not** 18-R21 / 23-I5: those deleted the other SafeText remnants (SafeTextAttribute; IsSafeText/SafeTextPattern) and changed no seed behaviour; the divergence between what the form accepts and what the seed keeps began at f4a0a87.

## L9 — Subnet Create refuses a child that equals its parent's range with "must be contained within the parent subnet range"; the CIDR-must-be-larger refusal written for that case is unreachable `[x1]` strings
**Where:** src/Bastet/Controllers/SubnetController.Helpers.cs:168-184 (ValidateSubnetCreation: the strict containment check at L168 — via src/Bastet/Services/IpUtilityService.cs:136-139, which returns false for `childCidr <= parentCidr` before any address comparison — emits the NetworkAddress-keyed containment sentence at L172-175 for every `Cidr <= parent.Cidr` input including the identical range, so the Cidr-keyed block at L178-184, "Child subnet CIDR must be larger than parent subnet CIDR. Parent subnet CIDR is {n}", can never execute), src/Bastet/Services/Validation/SubnetValidationService.cs:22-27 (ValidateSubnetContainment emits the same first sentence for the equal-range case on the Edit CIDR-change path, where it is joined by the true INVALID_CIDR_HIERARCHY sentence at L29-33, so only the wording of the first sentence is wrong there — secondary), src/Bastet/Services/Validation/SubnetValidationService.cs:150-152 (the INVALID_CIDR_CHANGE sentence "Decreasing CIDR to /{n} would make this subnet too large to fit within its parent subnet (…)" appended on the same Edit path carries the same imprecision for an exactly-equal range — **named by a verifier** so the Edit-path part of any fix is taken whole or not at all). A grep of src/ finds exactly these emitters of the two sentences; AzureBulkImportPlanner has no copy.
**Breaks:** Parent R = 10.50.0.0/24 exists. On /Subnet/Create the operator enters a name, Network Address 10.50.0.0, CIDR 24 and selects R as the parent, and is refused with "Child subnet must be contained within the parent subnet range. Parent subnet is 10.50.0.0/24" — rendered in the span for the Network Address input, whose value is correct, while the Cidr span, the field that must change, shows nothing. The range entered *is* the parent's own range; it does not lie outside it, so the sentence states a false fact, and at /24 no other address inside 10.50.0.0/24 is aligned while every other /24 is outside it, so the plain reading names a remedy the app refuses in every form. The product carries its own sentence for exactly this input one check later, and that check is dead. The same wording appears for 0.0.0.0/0 entered under 0.0.0.0/0. The refusal itself is correct (§1: a parent and child with the same CIDR are the collision the product exists to prevent) and nothing here should be removed — the finding makes a dead refusal live. Reach is the Create form only (typed by hand or via a crafted Create GET query): the Details CIDR modal enforces min = parentCidr+1 client-side and its server suggestion table has no equal-CIDR entry, and the bulk import path can never hand this validator an equal-or-larger child (FindDeepestContainer uses the same strict containment, an equal-range Azure subnet becomes WillMarkFullyAllocated with children skipped, and an equal-range existing row is an ExactMatch that never calls the validator) — so the finder's "shared by every bulk-import creation" clause is true of the code but adds no reach. On the Edit path the same first sentence is produced but accompanied by "Child subnet CIDR must be larger than parent subnet CIDR (representing a smaller subnet)", which is the correct reason.
**Repro:** `$RIG/start-app.sh v_m3_truth 50145 bastet_r31_v_m3_truth A`; requests through the real /Subnet/Create and /Subnet/Edit forms with the antiforgery token from each GET, plus a headless-Chromium submit so no client-side gate intervenes. Built Z 0.0.0.0/0 (id 1), R 10.50.0.0/24 under Z (id 2), C1 10.50.0.0/26 under R (id 3). CREATE X {10.50.0.0, Cidr 24, parent 2} → HTTP 200 redisplay, `[NetworkAddress]` = "Child subnet must be contained within the parent subnet range. Parent subnet is 10.50.0.0/24", Cidr span empty; {0.0.0.0, Cidr 0, parent 1} → the same sentence for 0.0.0.0/0; contrast inputs 10.50.0.0/16, 10.60.0.0/24 and 10.50.1.0/25 under R → the identical containment sentence (true for the last), 10.50.0.64/25 → the alignment sentence. The Chromium submit of X under R: url back at /Subnet/Create, visible errors = ['Child subnet must be contained within the parent subnet range. Parent subnet is 10.50.0.0/24'], `contains 'CIDR must be larger': False`. EDIT id 3 Cidr→24 → 200 with `[Cidr]` = "CIDR validation failed: Child subnet must be contained within the parent subnet range; Child subnet CIDR must be larger than parent subnet CIDR (representing a smaller subnet); Decreasing CIDR to /24 would make this subnet too large to fit within its parent subnet (10.50.0.0/24)". The Cidr-keyed Create sentence never appeared on any attempt. Details of R shows the modal's suggestion table with keys 25..32 only, so the modal cannot submit an equal CIDR.
**Fix:** In ValidateSubnetCreation move the existing `if (viewModel.Cidr <= parentSubnet.Cidr)` block (L178-184) **above** the containment check at L168, so a range equal to or larger than the parent's is refused with the CIDR sentence and the containment sentence is reserved for ranges that actually lie outside the parent. Two existing blocks swap order; no new rule, no new string, no guard widened. Both blocks return false, so no accept/refuse outcome changes on Create or on either bulk-import call (Helpers.cs is reached from SubnetController.Create.cs:85 and SubnetController.BulkAzure.cs:292 and :393), the re-keyed error renders on the form via `asp-validation-for="Cidr"` at Views/Subnet/Create/_SubnetForm.cshtml:32 and joins the bulk path's ModelStateMessage, and for a disjoint input with an equal-or-larger block (10.60.0.0/24 under 10.50.0.0/24) the first refusal becomes the CIDR sentence with the containment sentence following once the CIDR is raised — both true and actionable, and this validator already returns on the first failing rule everywhere. A verifier narrowed the optional half: either **drop** the Edit-path change (the Edit operator already receives the true reason in the same line) or take SubnetValidationService.cs L22-27 **and** L150-152 together — changing L22-27 alone would leave "would make this subnet too large to fit within its parent subnet" printing for the exactly-equal range, the same imprecision one clause later. If the Edit path is touched, apply it as an `else` branch of the existing `childCidr <= parentCidr` test in ValidateSubnetContainment (its only caller is ValidateSubnetCidrChange at SubnetValidationService.cs:139) so IsValid is unchanged and only the untrue NOT_IN_PARENT_RANGE sentence drops out. Interim: none needed; the refusal itself is correct, only its reason is wrong.
**Residue of:** none (blame on Helpers.cs L168-184 lands on 9f220e0 "Add admin role and Azure import feature (#32)" and `git log -S` on a single-line substring of each sentence traces both to e05c3b1 "Add subnet cidr editing and tests (#11)", which landed the strict predicate and the dead block together — the block was dead from birth; neither is an audit merge, and the ledger has no prior row for these sentences. The finder's claim that `git log -S` on the sentences returns nothing is wrong but immaterial.)

## L10 — Delete-confirm on a row that no longer exists stashes a TempData error for a page that answers 404, so the alert surfaces on the next unrelated page `[x1]`
**Where:** src/Bastet/Controllers/HostIpController.cs:367-370 (HOST_IP_NOT_FOUND from ValidateHostIpDeletion — its only possible error → `TempData["ErrorMessage"]` → RedirectToAction(Delete) → the Delete GET's bare `NotFound()`; always leaks), src/Bastet/Controllers/HostIpController.cs:353-354 (confirmation gate), src/Bastet/Controllers/HostIpController.cs:436-437 (TimeoutException), src/Bastet/Controllers/HostIpController.cs:714-715 (SetAllocationStatus TimeoutException → Subnet Details, which answers RedirectToErrorPage(404) for a gone subnet via SubnetController.Read.cs:42 — **named by neither finder**, reproduced by both verifiers), src/Bastet/Controllers/SubnetController.Delete.cs:93-94 (confirmation gate), src/Bastet/Controllers/SubnetController.Delete.cs:99-101 (missing-scope gate), src/Bastet/Controllers/SubnetController.Delete.cs:112-113 (TimeoutException) — all seven reproduced; the three subnet sites redirect to the Delete GET, which answers RedirectToErrorPage(404), whose page never reads `TempData["ErrorMessage"]`. Same-shape siblings that are race-only and were not separately driven (the row was just loaded under the lock, so they leak only if it is deleted between the redirect and the following GET): src/Bastet/Controllers/HostIpController.cs:429-430, :721-724, :730-731 and src/Bastet/Controllers/SubnetController.Delete.cs:134-137, :143-146, :178-179. One verifier **drops** src/Bastet/Controllers/HostIpController.cs:416-422 (the `subnetIdForRedirect is null` arm redirecting to AllHostIps) as unreachable — HostIpValidationService and the controller share the scoped BastetDbContext, so the validator's tracking query leaves the row tracked and FindAsync answers from the change tracker without a DB call, BeginTransactionAsync is outside that try, and every DB call after FindAsync runs once subnetIdForRedirect is set; the other verifier agrees it needs an indeterminate SqlException inside FindAsync and did not drive it. Treat it as not a site. Not sites, checked: the purge gates (their GET overwrites a pending gate message with its own and lands on a page that renders it) and HostIp Index (:41-42) / Create GET (:80-81), which redirect to Details of a subnet just loaded.
**Breaks:** Two operators, or two tabs. B deletes host IP 10.200.0.9; A still has the Delete page open and submits it. A's POST is redirected to /HostIp/Delete?ip=10.200.0.9, which answers a bare 404 ("The resource you requested could not be found. This may have happened because: The resource may have been deleted …"), while `TempData["ErrorMessage"] = "Host IP assignment not found"` stays pending — neither the re-executed /Error/404 (Program.cs uses UseStatusCodePagesWithReExecute in both environments and ErrorController only pops its own ErrorPageMessages token) nor _ErrorLayout nor AllHostIps.cshtml reads that key. The next page A opens that includes _TempDataAlerts — the Subnet Hierarchy, any subnet Details page — shows a red "Host IP assignment not found" with nothing on that page to act on, gone on reload. The confirmation gates have the same shape ("You must type 'approved' to confirm deletion.", "The deletion scope was missing from the form. Review the subnet and confirm again."), as do the lock-timeout catches ("The operation timed out due to high concurrency. Please try again." / "…because another subnet operation is in progress…"). The trigger is ordinary: a Delete page left open while another operator, another tab, a subnet delete cascade or a reconcile delete removes the row. A single-tab double-click is **not** a trigger — Chromium issues one POST and drops the second while the first navigation is pending — so the scenario must not rest on double-clicking. The Edit sibling already answers 404 on the identical gone-row event (28-L2), so Delete diverges from its sibling on one event. Rule 3 and §5's "check who can reach the remedy a message names".
**Repro:** `$RIG/start-app.sh v_m4_truth 43353 bastet_r31_v_m4_truth A`. HTTP drive: second POST of the Delete form after the row was deleted → chain [(302,'/HostIp/Delete?ip=10.200.0.9'),(404,None)] title 'Not Found - BASTET' alerts []; next GET /Subnet → 'Subnet Hierarchy - BASTET' alerts ['Host IP assignment not found']; the GET after that → []. Wrong-word POST for the gone row → 302 → 404, then GET /Subnet/Details/1 → ["You must type 'approved' to confirm deletion."] beside "No host IP assignments have been added to this subnet yet." A gate message stashed for an existing row then GET /HostIp/AllDeletedHostIps's sibling /HostIp/AllHostIps → alerts [] (that page has no TempData rendering), and the message then surfaces on the next /Subnet. Subnet side: stale wrong-word and missing-scope POSTs on gone subnet 2 → [(302,'/Subnet/Delete/2'),(302,'/Error/404?m=…'),(404,None)] with no alert markup, the message on the next /Subnet; the approved full-scope POST for a gone subnet goes straight to /Error/404 and does **not** leak (control). Two tabs in one Chromium context: B deletes 10.200.0.21, A's stale submit lands on the 404, then A's Subnets menu shows the red alert above the tree (screenshot a_subnets_leak.png), gone on reload. Timeout sites driven deterministically by holding `sp_getapplock 'Bastet:SubnetOperations'` Exclusive/Session in my own catalog via sqlcmd: each POST returned after 30.0 s → redirect → 404, with the timeout sentence on the next /Subnet; the same for POST /HostIp/SetAllocationStatus SubnetId=2 on a gone subnet → [(302,'/Subnet/Details/2'),(302,'/Error/404?m=…'),(404,None)] then the timeout alert on /Subnet.
**Fix:** Answer 404 where the row is found gone and stash nothing — the 28-L2 shape. (1) Resolve existence **before** the pre-lock gates in both DeleteConfirmed actions, reusing the existing one-line helpers rather than adding a second query or an Include reload: `HostIpController.HostIpExists(ip)` (HostIpController.cs:735, the helper 28-L2's Edit path uses) and `SubnetController.SubnetExists(id)` (SubnetController.Helpers.cs:100), answering `NotFound()` and `RedirectToErrorPage(404, "The subnet with ID {id} could not be found or may have been deleted.")` exactly as DeleteConfirmedCore already does — and give that subnet sentence one private home shared by the Delete GET, DeleteConfirmed and DeleteConfirmedCore instead of a third copy. (2) Inside the lock, since ValidateHostIpDeletion can produce only HOST_IP_NOT_FOUND, replace the whole foreach/TempData/redirect block at L363-371 with `return NotFound();` — or delete the validator call outright, since the FindAsync four lines later under the same lock already answers it, which is the §5-preferred shape; do not add a code switch. (3) Add the missed site: HostIpController.SetAllocationStatus must resolve the subnet before the lock, or answer `NotFound()` in its TimeoutException catch when `context.Subnets` has no such id. (4) **Drop** the finder's third bullet about :416-422 entirely — the arm is unreachable, so re-targeting it to Subnet Index fixes no reproducible behaviour and is widening. (The other verifier would instead take the one-line change of adding _TempDataAlerts to AllHostIps.cshtml as AllDeletedHostIps.cshtml already does, on the grounds that the message tells the operator to check the host IP lists and AllHostIps is one; that is optional polish, not part of closing this defect.) (5) State the residual honestly rather than adding mechanism: after these changes the only remaining leaks are race-only — a row deleted by the lock holder *during* the 30 s wait before the TimeoutException, or deleted between a within-lock redirect and the following GET at HostIpController.cs:429-430 and SubnetController.Delete.cs:134-137/:143-146/:178-179; closing the timeout half means repeating the existence check inside each `TimeoutException` catch, one line each. Do **not** close this by having the Delete or Details GETs discard a pending `TempData["ErrorMessage"]` when they answer 404: that would swallow a message another tab stashed for a different page, and it is the wider, less obvious change. Interim: none needed; the stray alert is dismissible and is consumed by the next request.
**Residue of:** none for the defect and for the always-leaking site (the stash-then-redirect-to-Delete shape is 841c272 #18 / 9f220e0 #32 / 78ba9dc #112 / 6edef5c #134, none of them audit merges; `git log -S 'Host IP assignment not found' -- src/` returns 841c272 only, and 28-L2's 5ea2eb5 touched only the Edit path). Two named sites were added by audit merges copying that pre-existing shape and are residue of those rounds only, neither with a per-finding ledger id: SubnetController.Delete.cs:97-101, the missing-scope gate, from d18327e (Audit 16) with its null check extended by 18-R2 (f2050fa); and the dropped, unreachable HostIpController.cs:412-422 from f4a0a87 (Audit 17).

## L11 — CIDR modal's "Valid range: {parent+1} - 32" label and the input's min are parent-derived, so the modal calls a CIDR valid that it then refuses for the clicked range `[x1]` strings
**Where:** src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:57 (the `Valid range: ${minCidr} - ${maxCidr} (recommended: …)` label), :27 (`const minCidr = parentCidr + 1`), :59 (`$('#cidrInput').attr('min', minCidr)` — reachable by the spinner: ArrowDown from the recommended value lands on the refused cidr), :19 (`const parentCidr`, whose only reader is L27 and which goes dead with the fix), :7-13 (the per-suggestion serialiser that must carry the new field — fix site), src/Bastet/Services/IpUtilityService.cs:321-342 (the ascending cidr loop of SuggestChildSubnets, which fills NetworkAddressByCidr with null for every cidr that has no free aligned block at or after the range start and is where the per-range lower bound is to be stamped — fix site; L310-351 is the whole method), src/Bastet/Models/IPCalculationModels.cs:12-17 (ChildSubnetSuggestion, which has no MinAvailableCidr — fix site).
**Breaks:** Subnet X 10.200.9.0/24 holds children 10.200.9.0/26 and 10.200.9.128/25, leaving one free range 10.200.9.64-10.200.9.127 (64 addresses). Details → Create Subnet on that range opens the modal with "Valid range: 25 - 32 (recommended: 26)" and min=25 on the number input. Typing 25 — a value inside the range the label calls valid, and exactly one ArrowDown from the recommended value — is refused on the same screen: "No free /25 block starts at or after 10.200.9.64.", size "Invalid - none free at or after this range", input `is-invalid`, Create disabled. The label is `parentCidr + 1` for every range, while acceptance is decided solely by indexing the per-range server table (the input handler at L75-87). The accepted set is provably the contiguous suffix [smallest cidr with a non-null NetworkAddressByCidr entry, 32] — if a /N block fits at an aligned address at or after the range start then a /N+1 does too, and /32 always fits a non-empty range — so the true lower bound is knowable from the very table the modal indexes and already serves. A displayed bound contradicting the table printed beside it (rule 3; §5's "a displayed count must match the range it is printed beside"). The refusal sentence itself is accurate and the Create button is disabled, so no address space is at stake. Any Edit-or-higher operator on a subnet whose free space is fragmented reaches it — a single /32 child in a /24 suffices.
**Repro:** `$RIG/start-app.sh v_m5_truth 35729 bastet_r31_v_m5_truth A`; created X 10.200.9.0/24 with children X-a 10.200.9.0/26 and X-b 10.200.9.128/25 through the Create form. Details/1 shows one free range "10.200.9.64 | 10.200.9.127 | 64 IP addresses | 62 | Create Subnet", serialised as `{"startIp":"10.200.9.64","recommendedCidr":26,"networkAddressByCidr":{"25":null,"26":"10.200.9.64",…,"32":"10.200.9.64"}}`. Clicking Create Subnet opened the modal with `#validCidrRange` = "25 - 32 (recommended: 26)" and `#cidrInput min='25'`. Typing 25 → feedback "No free /25 block starts at or after 10.200.9.64.", size "Invalid - none free at or after this range", class `form-control is-invalid`, Create disabled; ArrowDown from 26 stepped the spinner to 25 (allowed by the min attribute) and produced the same refusal. Sweep /24../33: /24 refused ("Please enter a valid CIDR value within the allowed range."), /25 refused (the null-entry sentence), /26../32 accepted at 10.200.9.64, /33 refused — the accepted set is exactly the table's first non-null key onward while the label prints 25, and `Object.keys(byCidr).find(c => byCidr[c] !== null)` evaluated in-page returns 26. Control Y 10.200.10.0/24 with only .0/26 (free from .64): same label "25 - 32 (recommended: 26)", and 25 **is** accepted with the address adjusted to 10.200.10.128 — so the true bound is per range, not per parent. Second shape Z with .0/26 and .128/26: both free ranges label "25 - 32" and both refuse 25.
**Fix:** Stamp the per-range lower bound on the server. Inside the existing ascending cidr loop of SuggestChildSubnets, when the entry just written for range i is non-null and that range's bound is not yet set, record it (first non-null in ascending order is the minimum; resolve it in the same final loop that resolves RecommendedCidr, e.g. a second `int?[]` beside `recommended`) — no second pass, and a bound always exists for every returned suggestion because /32 fits any non-empty range and empty ranges already throw. Declare the property on ChildSubnetSuggestion, serialise it beside `recommendedCidr` at _SubnetCalculationScripts.cshtml:7-13, and use `activeSuggestion.minAvailableCidr` in place of `parentCidr + 1` at L57 and L59. `MinAvailableCidr <= RecommendedCidr` by construction, so the label "Valid range: {min} - 32 (recommended: {rec})" stays coherent, and both existing refusal sentences remain true below the new bound: a hand-typed value below it still hits the table's null entry and gets the 28-L10 sentence, and a value below parentCidr+1 still hits `undefined` and gets the "within the allowed range" sentence. Delete the now-dead `const minCidr` at L27 and `const parentCidr` at L19 rather than leaving them; keep `maxCidr = 32`, which is always valid. The client-only variant (take the first key of `networkAddressByCidr` whose value is not null — JS enumerates integer-like keys ascending, and it returned 26 in-page) is equally correct and touches one file, but it re-derives on the client what the server can stamp, against the 23-L2-remainder / 24-L2 direction that the modal only indexes the server table; the server stamp is the right primary. Tag caveat recorded by a verifier: `strings:true` is loose — one of the two operator-visible sites is the input's `min` attribute, a spinner bound, and the fix is a computed per-range value rather than a text edit; severity is Low either way. Interim: none; the refusal sentence already states the fact for the typed value.
**Residue of:** none (`git log -S 'Valid range'` and `-S 'const minCidr = parentCidr + 1'` -- src/ land on e05c3b1 #11 and 46b3e69 #17, both pre-audit; blame shows all three lines on 8891320 only because that commit rewrote the file. The finder's clause "the contradiction became visible when 23-L2-remainder introduced per-range null entries" is **inaccurate** — the pre-8891320 engine printed the same parentCidr+1 label and refused the same input on the same screen with "No compatible network address found for this CIDR size."; 23-L2-remainder changed the refusal's mechanism and 28-L10 its sentence, and neither touched the label.)

## L12 — Held-by-manual-content rows are never confirmed against Azure, so a row whose resource the credential merely cannot see tells the operator to delete their manual content and rescan; they archive it irreversibly and the rescan withholds the row anyway `[x1]`
**Where:** src/Bastet/Services/Azure/AzureReconciler.cs:88-99 (BuildPlan: rows with manual content are moved to ReviewItems with the absence lead and the remedy, assembled at L94-96, before any confirmation), src/Bastet/Controllers/AzureController.cs:198-200 (ConfirmProposedDeletionsAsync collects absenceClaims from `plan.Items` only, so ReviewItems are never sent to ConfirmResourcesAsync), src/Bastet/Services/Azure/AzureReconciler.cs:122-214 (ApplyConfirmations applies the NotVisible/Unknown/Live verdict wording to plan.Items only, and its early return at L129-132 on `plan.Items.Count == 0` skips confirmation entirely when only held rows exist), src/Bastet/Services/Azure/AzureReconciler.cs:197-202 (the cascade-withheld set, which lists notVisible, unknown and ReviewItems and deliberately excludes stillLive ids), src/Bastet/Views/Azure/Reconcile/_StepReview.cshtml:73-76 (the static Needs-review panel prose repeating the same remedy for every held row), src/Bastet/Controllers/SubnetController.AzureReconcile.cs:84-95 (the held-row 409 re-emits `held[id].Reason` including the remedy, and builds `held` from ReviewItems only), src/Bastet/Models/ViewModels/AzureReconcileViewModels.cs:60-76 (AzureReconcileItem has no field for the pre-held status the fix needs).
**Breaks:** A catalog holds a VNet-linked row with hand-made children; Bastet is then run under a principal with no RBAC on that resource group, so the VNet is live but invisible to the listing. The reconcile scan lists the row under Needs review: "The VNet this subnet was imported from could not be found in this subscription's listing. It holds 2 subnets created here. BASTET will not delete it, because Azure has no record of that and it would be destroyed with no way to restore it. Delete it here first, then run the scan again." The offered, non-held rows on the same screen **are** GET-confirmed by resource id and correctly reported as "Azure denied access when asked about them directly — the credential may have lost access to their resource group"; the held row is classified from the subscription listing alone and is never asked. An operator who follows its instruction deletes the hand-made children — archived irreversibly, the product having DeletedSubnets and Purge but no restore control — and rescans: the same row now appears only in the denied-access warning ("…have been withheld from deletion: '…' (10.31.0.0/16), 's1' …, 's2' …"), and posting its id answers 409 "1 of the selected subnet(s) are no longer offered for deletion by the latest re-check, so nothing was deleted." The named remedy led to a withhold and cost two operator-owned records; the VNet existed throughout, confirmed by a counter-scan under the original principal. Rule 3 (a message naming a remedy the app then refuses) compounded by rule 2 (the app induced the destruction of operator-owned records on information it had the means to complete, and does complete for every other absence claim on the same screen). 24-L3 already removed a refused remedy from held prefix-changed rows on this exact principle. Reachability nuance: the 409 itself needs a crafted POST — after the remedy the real page renders no row and no checkbox for the target — so what the operator sees is the row vanishing from Needs review into the denied-access warning. Severity nuance: the finder filed medium and one verifier held medium (irreversible loss of allocation records, not closable by one string); the other corrected it to low on the 23-L4 precedent and the "a contradiction the operator can see on one screen = Low" rule, noting the remedy is destructive but not signal-free (typed-confirmation Delete page, the same-screen warning, the archive listing) and that no address space is misreported since the parent row still holds its range as allocated. The correction is applied, so it is filed Low.
**Repro:** `$RIG/start-app.sh v_m8_truth 39949 bastet_r31_v_m8_truth A`; imported the shared fixture rig-r31-a1 10.31.0.0/16 with s1/s2 through the bulk wizard (rows 1/2/3, the fixture itself never modified in Azure) and added hand-made children C1-hand 10.31.3.0/24 and C2-hand 10.31.4.0/25 under the target (rows 4/5, AzureResourceId null). Control scan under A: items [], reviewItems [], warnings [] — clean, the VNet is live. Restarted under principal B: POST /Azure/ReconcileScan → items []; reviewItems [row 1 HeldByManualContent with that exact sentence]; warnings "1 subnet(s) were withheld from deletion because they hold subnets or host IP assignments that were created here rather than imported from Azure: 'rig-r31-a1' (10.31.0.0/16)." **and** "2 Azure-linked subnet(s) were missing from the subscription listing, and Azure denied access when asked about them directly … 's1' (10.31.1.0/24), 's2' (10.31.2.0/24)." The instance log carries "Azure denied access to …/rig-r31-a1/subnets/s1 (403)" and "…/s2 (403)" only — the VNet id was never asked. The real Reconcile page (Playwright) shows the review row plus the panel prose "For rows holding content created here, delete that content first, then scan again.", stale section hidden, 0 checkboxes. Followed the remedy: POST /Subnet/Delete/4 and /5 with confirmation=approved → 302 each, "…were deleted successfully"; /Subnet/DeletedSubnets lists both with only "Purge All" as a control. Rescan under B: items [], reviewItems [], a single warning naming all three rows as denied-access withholds; the page shows review and stale sections both hidden, 0 checkboxes. POST /Subnet/BulkDeleteStaleAzureSubnets [1] → HTTP 409 "…no longer offered for deletion by the latest re-check…" (same 409 with the reviewed held verdict). Restarted under A: scan clean — the VNet was live all along and the operator's two hand-made records are gone for nothing. Raw ARM confirms the mechanism: a GET on a VNet in an inaccessible group answers 403 AuthorizationFailed (mapped to NotVisible by AzureService ConfirmOneAsync L266-272) while a missing VNet in a visible group answers 404 → Deleted.
**Fix:** Confirm absence-led held rows the way offered rows are confirmed, then let the existing verdict buckets describe them — no new withhold and no new wording, and no range reasoning (reconcile decides on the linked resource id, §3). Carry the pre-held status on AzureReconcileItem (set at AzureReconciler.cs:93); in ConfirmProposedDeletionsAsync (AzureController.cs:198-200) include ReviewItems whose pre-held status `IsAbsenceStatus` in the absenceClaims — restricted to those, because a prefix-changed held row *was* found in the listing and must not be re-bucketed as missing from it; in ApplyConfirmations, for those held rows: Deleted → stay in ReviewItems (the lead may then state the plain fact instead of the 23-L4 listing clause), NotVisible / Unknown / Live → out of ReviewItems and into the existing notVisible / unknown / stillLive lists so the existing warning sentences describe them, keeping their ids in the cascade-withheld set. One verifier judged the filed fix **unsound without three details**, and both name them, so they are part of the fix. (1) ApplyConfirmations returns early at L129-132 whenever `plan.Items` is empty — exactly the reproduced state (0 offered rows, 1 held row, which is also the commonest held shape: a VNet row whose only children are hand-made) — so the guard must become "nothing to confirm", i.e. no absence-led rows in Items **or** ReviewItems; as written, the held row would still never be re-bucketed. (2) The held warning at L105-111 is emitted in BuildPlan *before* confirmation, so a row moved to the notVisible/unknown/stillLive bucket would still be named as "withheld … because they hold subnets or host IP assignments created here" while no longer appearing under Needs review — one screen naming one row in two different withhold reasons; emit that warning after confirmation, over the rows that remain held (or build it from ReviewItems at the end of ApplyConfirmations). (3) The cascade-withheld set at L197-202 excludes stillLive ids on purpose, so a held-origin row confirmed Live must be added to that set explicitly, for all three buckets, or its manual content stops protecting an offered ancestor. No change is needed in SubnetController.AzureReconcile.cs: its `held` dictionary keys on ReviewItems, so a moved row correctly falls to the generic "no longer offered" 409. Do **not** take the wording-only alternative of dropping "Delete it here first, then run the scan again." from absence-led held rows: it removes the only path for genuinely deleted resources and would additionally have to touch _StepReview.cshtml:73-76 and the bulk-delete 409 echo. Interim: before acting on a Held row that says "could not be found in this subscription's listing", check whether other rows from the same resource group appear in the "Azure denied access" warning on the same screen — if they do, the resource is hidden, not gone. Note the limit of that workaround: in the finder's original shape, where the row's only descendants are hand-made, no such sibling hint exists on the screen at all.
**Residue of:** 23-L4 (338e8e0 — the round that handled exactly this RBAC-hidden held-row case and softened the lead while leaving the remedy in place). Layers under it, none with a per-finding ledger id: the remedy sentence itself is round 17 / f4a0a87, absenceClaims-over-Items-only is round 5 / 8cefc64, the held path is round 14 / 8afa2df surviving the 23233f2 mass revert, and 24-L3's fact lead is 9d3c7b2.

## L13 — After a 409 that says the reviewed state is stale, both wizard clients keep the superseded plan/snapshot actionable and re-offer the identical submit `[x1]`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:704 (showCommitError re-enables `#bulk-confirm-commit-btn` for every failure, including the plan-changed 409 from SubnetController.BulkAzure.cs L149-162; lastSelection and its stale `expected` stamps are kept — `invalidatePlan()` is reached only from the selection/preview paths at L96/L118/L361/L407 — so the next click re-posts a byte-identical request), :611 (`$("#bulk-go-commit-btn").prop("disabled", !plan.canCommit)` — the button and the #step3-tab/#step4-tab pills keep their enabled state after the 409, so Preview still presents the superseded plan as committable), :618-625 (the go-commit click handler hides `#bulk-commit-error`, so re-entering Commit from the stale Preview erases the refusal and shows a clean Confirm Import; the re-enable inside that range is L624), src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:434 (showCommitError ends in refreshDeleteButton() while confirmedIds/confirmedVerdicts are kept after the 409s of SubnetController.AzureReconcile.cs L84-121), :271 (updateGoConfirmBtn enables off a lastPlan that is never cleared, so Review still shows the refused row ticked with "Next: Confirm deletion" enabled), and two sites **both verifiers add**: :347 (the go-confirm handler hides `#rec-commit-error` and re-snapshots confirmedIds/confirmedVerdicts at L306-310 from the superseded lastPlan — the refusal-erasure is not bulk-only) and :388 (refreshDeleteButton() inside the delete request's `complete` handler, which jQuery runs *after* success/error and is therefore the last word on `#rec-confirm-delete-btn`, so removing the L434 call alone would change nothing).
**Breaks:** Bulk: the operator previews a VNet with "Rename matched…" on; before Confirm Import another operator renames the Bastet target; Confirm Import → 409 "The plan changed since it was previewed, so nothing was imported. Re-run the preview, review what it now says, and confirm again." with a differences list naming the change. Confirm Import is left enabled; the second click posts a byte-identical body and gets the identical 409. "Back to Preview" does not re-run the preview and shows the superseded plan — asserting a Bastet subnet name Bastet no longer holds, the very fact the server had just named in the differences list — with "Continue to Commit" enabled and both pills live; that click **erases the refusal banner** and presents a clean Confirm Import over the plan the server just refused. Reconcile is the same shape and worse: after the 409 "1 of the selected subnet(s) are no longer offered for deletion by the latest re-check, so nothing was deleted. Re-run the scan…", the Delete button is re-enabled with the same snapshot, a second click re-posts the identical ids, the Review tab still lists and ticks a Bastet row that has already been deleted with "Next: Confirm deletion" live, and re-entering Confirm rebuilds the list from the superseded lastPlan and names that gone row again. Nothing is written — the server refuses every stale resubmission. Two framing corrections reconcile must not re-import from the finder's text: (1) the remedy the message names **is** reachable (bulk: Back to Preview → Back to Selection → Next: Preview; reconcile: the Subscription pill → Next: Scan, both verified working with every control enabled), so this is **not** a rule-3 "message naming a remedy the app refuses" and must not be graded as one — the defect is the refused action being offered alongside the message; (2) the repeated submit is not a submit that can never work — the server re-derives the plan on every POST and commits only what matches the operator's approved expectations, so when the divergence was reverted the identical stale payload committed correctly (200, the child created, no preview in between), which means the finding cannot rest on "it offers work that is not work" either. What is left, and what makes it a finding rather than polish, is that the client keeps a snapshot its own server has just declared superseded and goes on presenting it as current and committable, and that two ordinary clicks erase the refusal — the class the loop has repeatedly accepted as Low (28-L9, 28-L2, 27-L1).
**Repro:** `cd $RIG/app && $RIG/start-app.sh v_m10_truth 40403 bastet_r31_v_m10_truth A` (**must** be started with cwd = $RIG/app, or ASP.NET Core takes the caller's cwd as content root, every static asset 404s, jQuery never loads and no wizard XHR is ever sent — the shared reference instance on 5310 is in that state, so no wizard drive against it is trustworthy); own Azure fixture rig-r31-v_m10_truth-v1 10.147.0.0/16 with s1 and s3. Playwright headless chromium with every POST body and status captured. Bulk: imported the prefix + s1, previewed with rename ON ticking s3 → plan card 'Exact match Bastet subnet "rig-r31-v_m10_truth-v1" (10.147.0.0/16) / Child Azure subnets: Create s3 10.147.3.0/24'; a second page renamed Bastet subnet 1 to "v1-renamed-by-other"; Confirm Import → 409 with `differences:["10.147.0.0/16: renaming the target changed to True."]`; screen after: commitErrorHidden=false, confirmDisabled=**FALSE**, confirmVisible=true, step3TabDisabled=false, step4TabDisabled=false, goCommitDisabled=false, plan card unchanged; second click → request bodies byte-identical (`req2==req3` True, 795 bytes each) and identical 409s; "Back to Preview" → plan card still names the old name, goCommitDisabled=false; "Continue to Commit" → commitErrorHidden=**TRUE**, confirmDisabled=false, confirmVisible=true. Recovery only via Back to Preview → Back to Selection → Next: Preview, which re-previews with the rename and then commits 200. Reconcile: deleted Azure subnet s1, scanned (row id 2 "s1 … Subnet deleted"), ticked, Next: Confirm deletion, typed approved; a second page deleted Bastet subnet 2; Delete Stale Subnets → 409 with `subnetIds:[2]`; deleteDisabled=**FALSE**, the confirmation box still reads "approved", second click posts the byte-identical body for the identical 409; the Review pill → row 2 still listed and still checked, goConfirmDisabled=false, both pills enabled; "Next: Confirm deletion" from that stale Review → errorHidden=TRUE and the confirm list rebuilt from lastPlan naming the already-deleted row. The only re-scan control is `#rec-scan-btn` on the Subscription step; running it reports "Everything imported from this subscription still exists in Azure. There is nothing to clean up." Three 409s, zero rows changed; instance log 0 'fail:' lines.
**Fix:** Narrow the existing re-enable to the failures it serves, keeping it for the 503 lock timeout ("…another subnet operation is in progress. Please try again.") and the `xhr.status === 0` outcome-unknown branch, both of which legitimately want the identical retry. Discriminate on the **status**: thread `xhr.status` from each error handler into showCommitError and branch on 409. Do **not** use the finder's offered alternative of sniffing `payload.differences` (bulk) or `payload.subnetIds` (reconcile): the bulk endpoint returns three further stale-state 409s that carry no differences array and are equally unrepeatable — SubnetController.BulkAzure.cs:219 "Matched Bastet subnet (id=…) for VNet … was not found. Another user may have deleted it.", :248 the already-linked re-link refusal, and :362 "… is no longer linked to the Azure subnet it was previewed against. Run the preview again." — so the payload sniff would leave those three still re-offering a plan the server has already rolled back. Both showCommitError functions are also called from the success handler (`result.success === false`), where no xhr exists, so pass the status explicitly (0/undefined from that path) rather than reading a global. Bulk: on the 409 call `invalidatePlan()` and leave `#bulk-confirm-commit-btn` disabled — invalidatePlan nulls lastSelection (so the Confirm handler returns early even if something re-enabled the button), disables the go-commit button and the step3/step4 pills while activateTab keeps the step4 pane and its banner on screen, and leaves Back to Preview → Back to Selection → Next: Preview reachable with `#bulk-go-preview-btn` enabled. Reconcile: set `confirmedIds = confirmedVerdicts = null` and `lastPlan = null`, call `updateGoConfirmBtn()`, and keep the banner on screen; both refreshDeleteButton() calls (L434 and the `complete` handler at L388, which fires last) then keep Delete disabled, and the L347 re-entry is closed because the go-confirm handler guards on `!lastPlan`. Void the snapshot rather than poke the button, precisely because L388 runs last. One verifier judged the filed fix **unsound on two further points**. (a) The bulk arm trades one silent screen for another: the refusal banner lives inside the Commit pane, so the moment the operator clicks "Back to Preview" (activateTab re-enables that pill) they are on a Preview step showing the superseded plan beside a mute, dead "Continue to Commit" with nothing on screen saying why — close that in the same change by rendering the server's refusal text through the Preview step's existing `#bulk-global-errors` list (the channel renderPlan already uses for "this plan cannot be committed"), or, smaller still, keep the retry (it is provably safe, since the server re-derives and commits only what the operator approved) and simply stop the go-commit handler from hiding a refusal that still stands, which alone closes the strongest half of the defect. (b) The reconcile arm as written inlines a third copy of "the confirmation is void" instead of reusing `invalidateConfirmation()`, which is exactly the §5 duplication invariant — split invalidateConfirmation() into a named state half plus the step-2 bounce and call the state half from the 409 branch so the banner (which lives in `#rec-step3`) survives, keeping the proposed `lastPlan = null` + `updateGoConfirmBtn()`. Reconcile already receives the server's `subnetIds` in the 409 body, and unticking exactly the rows the server named is on the correct axis (consuming the server's decision rather than re-deriving it) and is a cleaner narrow alternative to voiding the whole snapshot. Cosmetic, not a defect: on the bulk 409 the active step4 pill will carry both `active` and `disabled`; the pane and its banner stay visible, which is what matters. No server change, no new guard. Interim: none required — the server refuses the stale resubmission every time (409, nothing written); operators recover by Back to Preview → Back to Selection → Next: Preview (bulk) or the Subscription pill → Next: Scan for deleted resources (reconcile).
**Residue of:** none. Round-granularity provenance for the fixer: bulk L704 and the L618-625 handler are 73fc76f "Azure Bulk Import (#108)" (the original feature, non-audit) with L624 touched by 78fc4c9 (round 12); the plan-changed 409 and attachApprovedOutcomes are 09cee3d (round 10) with the differences list e03ae51 (round 11); reconcile's showCommitError frame is the non-audit aedd0bd (#133), L434 is bf120d6 (round 4), L388 is 6d1a4cb (round 13), L347 is aedd0bd, L271 is 65d1fc6 (round 15). Every contributing commit predates round 18, so no per-finding ledger id exists; the only round ≥ 18 commit touching any of it is 338e8e0 (23-L1), which reworded the noLongerStale 409 on the server and changed no client line.

## L14 — "Collapse All" re-expands a root the operator collapsed by hand and never collapses the first level (no-op on a two-level tree) `[x1]` — FIXED
_Fixed in <sha>. The #collapse-all handler is now the single statement `$('.subnet-children').slideUp(200, updateToggleIcons);`, the mirror of #expand-all, so every level collapses and a hand-collapsed root stays collapsed._
_Swept: #collapse-all/#expand-all/.subnet-tree/.subnet-children across src/ — the only other implementation is the sibling #expand-all, which repainted icons synchronously instead of in the slideDown callback (the same defect class as the per-row toggle's documented callback pattern); fixed in the same commit._
_Verified: browser drive on a 3-level tree — before: Collapse All left all three roots display:block/dash and re-opened a hand-collapsed root; after: all roots and the nested row go display:none/plus and the hand-collapsed root stays collapsed. Expand-inside-collapse-window race probed at +60/120/199ms: 0 icon mismatches (it reproduced on HEAD). New SubnetTreeScriptTests: 4/4 pass, full revert reds 4/4, re-adding .show() reds 2, narrowing the selector reds 3, dropping the callback reds 1. Suite 975 passed._
_Reviewed: verdict (c) — no demonstrated failure, no model violation, 60 randomized settled actions on 3- and 4-level trees with 0 mismatches and 0 console errors. Two corrections adopted: the pins were token pins (a differently-spelled first-level re-show passed all four) and are now an exact single-statement match, which reds that evader 3/4; the mirror assertion was relaxed so a future expand-all change does not red it._

## L15 — Wizard XHRs answered by the sign-in redirect after cookie expiry are reported as "The server could not be reached." `[x1]` strings
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:31 (**citation corrected from :30 to :31** by a verifier: L30 is the `return xhr.status === 0` test, L31 is the sentence; readErrorMessage runs L29-33), src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:668-672 (the commit status-0 arm, sentence at L670), src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:20-24 (readErrorMessage), src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:410-414 (the delete status-0 arm, sentence at L412). These four are the complete set — a grep over .cs/.cshtml/site.js finds exactly those four "could not be reached" strings, and all seven `$.ajax` error handlers in the product route through them. The two readErrorMessage sites feed five banners, so one string edit changes five: _BulkScripts.cshtml:83 (subscriptions), :147 (VNets), :498 (preview) and _ReconcileScripts.cshtml:108 (subscriptions), :165 (scan, via showScanError). Context the strings fix does not touch: src/Bastet/Program.cs:139-194 (the non-Development else block — DefaultScheme = Cookies, DefaultChallengeScheme = OpenIdConnect at L145, ExpireTimeSpan 1 h + SlidingExpiration at L153-154, and the only OIDC events registered are OnTicketReceived at L173 and OnRemoteFailure at L181, neither touching the challenge), and the endpoints behind it: AzureController.cs:8 (class-level RequireAdminRole covering GetSubscriptions/BulkGetVNets/BulkImportPreview/ReconcileScan), SubnetController.BulkAzure.cs:16, SubnetController.AzureReconcile.cs:16.
**Breaks:** In any non-Development deployment — cookie + OIDC is the only non-Development auth the product has, so this is the normal shape, not an exotic configuration — an admin opens Bulk Azure Import or Azure Reconcile, is interrupted, and comes back after the session has lapsed, then clicks Next: Select VNets / Next: Preview / Next: Scan / Confirm Import / Delete Stale Subnets. All seven wizard XHRs are `[Authorize(Policy = "RequireAdminRole")]`, so an expired cookie makes the request anonymous, the policy evaluator returns Challenged rather than Forbidden, and the OIDC handler answers with a 302 to the identity provider's authorize endpoint; the browser follows it cross-origin, the CORS check fails, and jQuery's error callback fires with `xhr.status === 0`. The wizard then prints "The server could not be reached." — the server *was* reached and refused for lack of a session — or, on commit/delete, "The server could not be reached, so it is unknown whether the change was applied. Check the subnet list before retrying." for a request that certainly never reached the action. Nothing on screen mentions signing in (wwwroot/js/site.js has no global ajax handler), so the only remedy the sentence implies — wait and retry — repeats the identical banner until the operator happens to reload the page. Rule 3: the operator cannot act on what they are told. Reachability is stronger than an idle hour: Program.cs L85-95 falls back to ephemeral keys when the DataProtectionKeys table is absent, so an app restart (or a second instance without shared keys) invalidates every issued auth cookie at once and a wizard page left open across a restart hits this on the next click; and `UseTokenLifetime = true` at L166 makes the effective window the id_token's rather than exactly the configured hour (the quoted hour is literally what L153-154 configures and is accurate as a citation, but the window is deployment-dependent).
**Repro:** `cd $RIG/app && $RIG/start-app.sh v_m12_truth 58467 bastet_r31_v_m12_truth A` (cwd must be $RIG/app or wwwroot resolves against the caller's directory, every static asset 404s, jQuery never loads and no wizard XHR is ever sent), then a local pass-through proxy in front of the unmodified instance answering the named wizard XHR with exactly what ASP.NET Core's OIDC challenge emits — `302 Location: http://…/connect/authorize?client_id=mvc_client&response_type=code&scope=openid%20profile%20email%20roles&code_challenge_method=S256&redirect_uri=…%2Fsignin-oidc&state=…`, the authorize endpoint being a real listening cross-origin server. The app was not rebuilt, mutated or patched, and the redirect injected is the exact shape Program.cs L156-193 configures. Control, no challenge: `#bulk-subscription-error` stays `d-none` with empty text, the dropdown populates, raw XHR probe `onload status=200`, the captured wizard request carrying `X-Requested-With: XMLHttpRequest` and `Sec-Fetch-Mode: cors`. Challenged, driven through the real wizard in headless Chromium: `#bulk-subscription-error-message`, `#rec-subscription-error-message`, `#bulk-vnet-error-message`, `#rec-scan-error-message` and `#bulk-preview-error-message` all read "The server could not be reached."; `#bulk-commit-error-message` reads the "…so it is unknown whether the change was applied. Check the subnet list before retrying." sentence; console in every phase "Access to XMLHttpRequest at 'http://…/connect/authorize?…' (redirected from 'http://…/Azure/GetSubscriptions') … blocked by CORS policy" plus `net::ERR_FAILED`, raw probe `onerror status=0 responseURL=''`, so `JSON.parse("")` throws, payload is null and the status-0 arm is taken. The POST arm was driven too (POST /Subnet/BulkDeleteStaleAzureSubnets → `onerror status=0`). **Plausibility gap, declared and carried into the fix:** Bastet issuing the 302 itself cannot be driven on this rig — start-app.sh pins ASPNETCORE_ENVIRONMENT=Development, DevAuthHandler authenticates every request as Admin, and there is no identity provider — so the server half was read, which RIG.md §11 makes the sanctioned method for exactly this question: Program.cs L139-194, the RequireAdminRole attributes above, and a grep of src/ for XMLHttpRequest / X-Requested-With / OnRedirectToIdentityProvider / OnRedirectToLogin / OnRedirectToAccessDenied / "StatusCode = 401" returning nothing but the unrelated OnRemoteFailure HandleResponse at Program.cs:190. Robustness checks against the verifier's own refutation: the status-0 outcome does not depend on the fake IdP being unhelpful (it answered the preflight 200 and simply carried no Access-Control-Allow-Origin, as every real authorize endpoint does), nor on X-Requested-With triggering a preflight (stripping it, the redirected cross-origin GET still fails the plain CORS check). Confidence stays **plausible** for that one read half; everything downstream of it is live.
**Fix:** Take the narrow (strings) half and **only** that half. The two readErrorMessage sentences become a true disjunction carrying a remedy that serves both causes: "The server could not be reached, or your sign-in has expired. Reload this page and try again." — the reload is reachable by anyone who sees the banner (the page GET is the challenged path and comes back signed in, confirmed not to be an AccessDenied), and a genuinely dead server fails the reload visibly; a disjunction is not a false claim, and in a Development deployment the sign-in clause simply cannot fire. Write each commit/delete site's **own** remedy rather than the finder's one slashed sentence ("repeat the import/scan") pasted into two screens: bulk import "…Check the subnet list before retrying; if you are asked to sign in, sign in and run the import again."; reconcile "…Check the subnet list before retrying; if you are asked to sign in, sign in and scan again." — §3 says reconcile never re-adds anything, so the delete banner must never name an import. Keep the "it is unknown whether the change was applied" hedge unchanged: under the challenge cause the change definitely was not applied (authorization rejects before the action runs), so the hedge over-warns rather than under-warns, which is the safe direction and the narrower edit. Keep one readErrorMessage per script, as 28-L8 left it — a per-handler variant for preview or VNets would be a second implementation of one sentence, the drift §5 warns about. The structural alternative the finder names — an OIDC `Events.OnRedirectToIdentityProvider` that sets 401 and calls HandleResponse() when the request carries `X-Requested-With: XMLHttpRequest`, plus a `xhr.status === 401` arm in both scripts — is machinery, not a string, and must **not** ride this finding: §5 says a fix is the minimal change that makes the wrong behaviour right with no hardening and no widening, and §3 prefers deleting machinery to adding a case to it. The finder was right to name it separately; leave it named and unbuilt. One deployment shape is closed by the same strings fix and is not worth filing separately: if the OIDC authority shares the app's origin (or a deployment runs response_mode=form_post), the XHR reads a 200 HTML sign-in page instead and jQuery's json parse fails, printing "The server returned status 200." — also untrue and also unactionable. Interim: reload the wizard page when the banner appears; the page GET is redirected to sign-in and the wizard works again.
**Residue of:** 28-L8 (5ea2eb5 "Audit 28 Cleanup (#190)" — introduced readErrorMessage and chose the status-0 sentence) and 25-L3 (f8bd697 "Audit 25 Cleanup (#185)" — chose the commit/delete status-0 sentence); both rounds picked a single named cause for status 0 without the challenge-redirect cause. The absence of XHR-aware challenge handling itself is original code, not residue (blame on Program.cs L142-146 is 83cd0c1, 2025-04-01, before the audit loop).

# Info

# Refuted — reported by a finder, killed by the verifier

| id | title | file:line | tag | reason |
| --- | --- | --- | --- | --- |
