# Bastet — Round-31 Audit Findings
branch: audit/round-31 / HEAD: 8e10c72 / test baseline: 971 passed / date: 2026-09-15 / scale: Standard — beats 1, 2, 3, 4, 5, 7 x 2 independent passes (12 finders); beat 6 skipped, 8e10c72..HEAD -- src/ is empty; verification interrupted by a machine reboot and completed on a rebuilt rig
Round 31 filed 16 findings, of which 0 are residue of round 30's own fixes.
Five findings are residue of an earlier round: 18-R4 / 18-R5 (L3), 23-L4 (L12), 25-L3 and 28-L8 (L15), and two at round granularity with no per-finding ledger id, both round 17 / f4a0a87 (L2, L8).

# Critical

# High

# Medium

## M1 — Replicas cold-starting on an empty DataProtectionKeys table each persist their own key with no coordination, so cross-replica form POSTs answer 400 and TempData banners are dropped `[x1]` — FIXED
_Fixed in <sha>. After migrations and guarded on dataProtectionTableExists, startup takes an Exclusive Session sp_getapplock on a dedicated resource `Bastet:DataProtection:{catalog}`, forces the key ring once via CreateProtector("Bastet.KeyRingStartup").Protect(), and releases in a finally. The whole block is best-effort: any failure logs a warning and startup continues unserialised._
_Both verifier soundness requirements met: it shares nothing with the migration lock (no OpenMigrationLockConnection, no master fallback, no "Startup was aborted"), so the BASTET_AUTO_MIGRATE=false default still degrades rather than failing; and the Protect() call cannot crash-loop a degraded start._
_Verified: forced race (TABLOCKX held until both replicas block on their opening SELECT) — unfixed minted 2 keys every run in both migrate modes, fixed minted 1 every run. Consequence half: with divergent rings a cross-replica POST answers 400 and the TempData banner vanishes; fixed, the same POST is 302 and the banner arrives. Six injected failures all still start the app (sp_getapplock EXECUTE denied, connection refused, lock held past the timeout, db_datawriter revoked, no key table, dead database). Cost 25-40ms. Suite 1026._
_Reviewed: two independent reviews. Both found the production change sound and unrefuted; both returned (a) against the PINS only. Round one: a guard assertion matched an unrelated guard 40 lines earlier, and deleting `.Protect(...)` reinstated the defect green. Round two found three more evasions — releasing the lock before forcing the ring, a zero lock timeout, and a per-machine lock resource — each reinstating the defect with every pin green._
_Not done the skill's way, by owner instruction: two demonstrated failures normally mean revert-and-defer, and the fix was reverted before the owner overrode it ("fucking fix it"). The pins were rewritten a second time to bind the ordering rather than the tokens — the Protect() call must sit between the acquire and the release, the timeout must be >= 1s, and the resource must carry no per-machine component — and all five evasions plus the original two now red, verified directly rather than by a third review._

# Low

## L1 — [NoHtml] refuses ordinary operator text (angle-bracketed e-mail addresses, placeholders, generic notation) as "HTML tags" on every Name/Description write path, while the bulk-commit sibling silently strips the same input `[x2]` — FIXED (inverted by owner ruling)
_Fixed in <sha>. The owner overruled the finding's premise: the HTML-tag refusal **stays** (PRODUCT-MODEL §8 "31-L1", §5 exemption, §7 closed list). Only the parity half was taken — `ContainsHtmlTags` is now one predicate on IInputSanitizationService, asked by both NoHtmlAttribute and a single up-front guard in BulkCreateFromAzurePlan, so the bulk import refuses the names the form refuses instead of silently stripping them._
_Swept: the guard sits on the raw posted selection (VNetName, Expected.NewName, every subnet Name) because the planner's TruncateAndSanitizeName strips before the commit loop sees anything — a first attempt inside the loop provably did nothing. Left alone by owner decision: the name-length asymmetry (form refuses >100, bulk truncates) and SanitizeDescription being called on Azure resource ids. Description takes no operator or Azure text on the import path (null, or an app-generated fully-allocated note), so it has no parity gap._
_Verified: before, a posted child name `<b>s1</b> web` committed 200 and stored `s1 web`; after, it is refused 400 with nothing written, while clean selections against rig-r31-a1 and the multi-prefix a2 still preview and commit end to end. Azure itself rejects a name containing `<`, so the guard is unreachable from real Azure data. 12 pins; removing the guard reds 4, dropping the subnet-name loop reds 4, checking the sanitized name instead of the raw one reds 4, inverting the predicate reds 36._
_Reviewed: verdict (c) — reviewer drove 144 form outcomes across both builds and found the form's behaviour byte-identical, 14 near-miss strings (`temp < 5`, `I <3 subnets`, `1 << 3`) refused by neither path, and 16 malformed payloads returning clean 400s with no unhandled exception. Its correction was adopted: the VNetName and Expected.NewName arms survived deletion green and now each red a test._

## L2 — After an Azure subnet is re-carved, the wizard describes that subnet's own stale linked Bastet row as a hand-made middle-man "Azure has no such subnet" and names "re-carve", a remedy Edit refuses for every linked row `[x2]` strings — STRUCK
_Struck on the owner's ruling. The finding's premise is wrong: Bastet tracks ranges, so when an Azure subnet's prefix is edited the range Bastet recorded no longer exists. "Azure has no such subnet" is true of 10.140.1.0/24; the surviving resource id and name do not make it the same subnet._
_Owner, verbatim: "THAT SUBNET DOES NOT EXIST ANYMORE. THE FUCKTARD EDITED IT AND NOW ITS SOMETHING ELSE"; "IF A FUCKFACE EDITS THE FUCKING SUBNET ADDRESS, THE BASTET OPERATION SHOULD BE DELETE WHAT WE HAVE AND RE FUCKING IMPORT"; "WE ARENT GETTING INTO COMPLICATED BULLSHIT OF FIXING"; "STOP BEING OPPORTUNISTIC IT LEADS TO FEATURE BLOAT"._
_The sentence already names delete, which is that operation. The only surviving sliver was the word "re-carve", which Edit refuses on a linked row; not worth a branch. A fix was written and driven live, then reverted; AzureBulkImportPlanner.cs carries no change from this round._

## L3 — Wizard refuses link-only adoption of an unlinked hand-made target that has host IPs, while offering it for the identical fully-allocated shape `[x2]` — STRUCK
_Struck on the owner's ruling (PRODUCT-MODEL §8 "31-L3", §4, §7). A Bastet row holding host IP assignments can never hold child subnets, and a VNet exists to hold subnets, so linking the two asserts a containment the model forbids and leaves the next import with nowhere to place the Azure subnets. The refusal is deliberate._
_The fix was implemented and driven live before being reverted: the planner offered WillUpdateExisting for the host-IP shape and the plan refusal dropped its cross-VNet term. Both are reverted; the working tree carries no L3 change._
_Not the reason: the reconciler first argued the danger was that linking is a one-way door with no unlink control. The owner corrected that — the model violation is the point, not the reversibility._
_The asymmetry the finding reported is real and is now recorded as intended: the fully-allocated branch stays adoptable because that flag can be cleared, while host IP records cannot._

## L4 — AllDeletedHostIps re-derives "original subnet unresolved" from the display string, so a live subnet named "Unknown" is shown as gone `[x2]` — FIXED
_Fixed in <sha>. HostIpController writes the whole unresolved string (`Unknown (Original Subnet ID: N)`) and the view renders `@hostIp.SubnetName` with no branch, so a live subnet named "Unknown" is reported by its name._
_Swept: grep of src/ for `== "Unknown"` / `!= "Unknown"` returns nothing; the write-only AllDeletedHostIpItemViewModel.OriginalSubnetId and its mapping deleted so no 23-I10-class orphan is left._
_Verified: live rig — before, a live "Unknown" subnet and a purged one rendered byte-identically; after, they are 'Unknown' and 'Unknown (Original Subnet ID: 2)' with 'Gone (deleted)' beside them. 5 pins; reverting the controller sentinel reds 2, restoring the view branch reds 1._
_Reviewed: verdict (c) — reviewer reproduced all three cases on both builds, confirmed no remaining consumer of the deleted property and that paging is unaffected._

## L5 — HostIp Create breadcrumb rebuilds the subnet name by splitting the composite SubnetInfo string, so names containing "(" are truncated or blank `[x2]` — FIXED
_Fixed in <sha>. CreateHostIpViewModel carries SubnetName, set at the three Create render paths and round-tripped through a hidden input; the breadcrumb renders it and the `SubnetInfo.Split('(')` is gone._
_Swept: grep of src/ for `Split('('` returns nothing; a test now fails if any view reintroduces it. HostIp Index's breadcrumb already rendered the bare name and is unchanged._
_Verified: live rig — 'Lab (test)' rendered 'Lab' and '(alpha)' rendered a 0px empty anchor before; both render whole after, including the POST-redisplay and subnet-gone branches. 9 pins; restoring the Split reds 1, dropping the hidden input reds 1, deleting the redisplay restore reds 1._
_Reviewed: verdict (c) — reviewer measured the anchor widths on both builds, confirmed the hidden input is inert to GlobalSanitizationFilter and never persisted, and that a crafted script payload renders encoded. Its correction was adopted: the subnet-exists redisplay path is now pinned behaviourally, not by source text alone._

## L6 — Deleted Subnets page offers "Purge All" on an empty archive and the click lands on a refusal; the sibling deleted-host-IP page gates the same control on count `[x2]` — FIXED
_Fixed in <sha>. The Deleted Subnets header declares `@model DeletedSubnetListViewModel` and gates Purge All on `Model.TotalCount > 0 && UserHasRole(Admin)`, the host-IP archive's existing gate._
_Swept: both Purge All entry points in the product now carry the same gate, and a test fails if any view adds an ungated one. The GET's count refusal is kept as the server-side guard._
_Verified: live rig — empty archive now renders zero purge links (it offered one that bounced off a refusal before); with two archived records the link is present and reaches the confirmation page. 6 pins; dropping the count term reds 2._
_Reviewed: verdict (c) — reviewer confirmed the model flows through the no-argument PartialAsync overload and rendered the page as Admin, Delete, Edit and View without a throw._

## L7 — Host IP Edit sidebar tells Edit-only operators to delete the host IP, a remedy their role cannot reach `[x2]` strings — FIXED
_Fixed in <sha>. The Edit sidebar injects IUserContextService and branches on `UserHasRole(Delete)`: that reader keeps the original sentence byte-identically, everyone else is told to ask someone with the Delete role._
_Swept: a test fails if any view carries the delete-remedy sentence without the role gate; the two other remedy sentences in Views already route around the role._
_Verified: live rig — the Admin rendering is byte-identical to before. The reviewer drove the branch itself in a throwaway copy with a role-switchable DevAuthHandler: Edit-only reads the ask-someone wording and is refused 403 at /HostIp/Delete, Delete-only reads the direct sentence and gets 200. 6 pins; removing the branch reds 2, gating on Edit instead of Delete reds 2._
_Reviewed: verdict (c) — reviewer confirmed UserHasRole(Delete) is byte-for-byte the RequireDeleteRole membership and that the @inject reaches only this one partial._

## L8 — Create form seeds the child name through SubnetNaming.ToSafeText, which strips accented and non-Latin letters, quotes, slashes, colons and brackets from the parent's own valid name `[x1]` — FIXED
_Fixed in <sha>. The Create form seeds the parent's stored name through WithSuffix; SubnetNaming.ToSafeText, its regex and the unreachable empty-name fallback are deleted (the prefill was the only caller)._
_Swept: no ToSafeText/OutsideSafeText caller remains in src/ or test/; the orphaned `using System.Text.RegularExpressions` and `partial` on SubnetNaming went with it. Validation is untouched — [NoHtml] still refuses tags on save per the owner's 31-L1 ruling, so a stored name can never carry one and the seed passes its own POST by construction._
_Verified: live browser drive of the whole path (Details, Create Subnet, CIDR modal, submit unchanged) — before, 'Réseau Zürich "Ost"' seeded 'Rseau Zrich Ost-…' and '東京' seeded a bare address; after, both seed verbatim and post successfully. Suite 1005. Reinstating the filter reds 6 tests._
_Reviewed: verdict (c) — reviewer drove 12 accepted parent names end to end with zero dialogs, zero page errors and every sink encoded, confirmed the form still refuses `<b>x</b>` and `<script>`, and confirmed the dropped fallback is unreachable (whitespace names fail [Required]). Known corner it re-measured and the finding already recorded: an emoji straddling the 100-char truncation now yields one U+FFFD. Its correction was adopted: the replacement forward-slash test was vacuous and now exercises the range-qualifying path, where the mutation it names reds it._
_Not done: the round-17 note "The owner chose to leave it" was overridden, put to the owner in this round and confirmed — that choice was made while [SafeText] validation still existed, and rounds 18 and 23 deleted it._

## L9 — Subnet Create refuses a child that equals its parent's range with "must be contained within the parent subnet range"; the CIDR-must-be-larger refusal written for that case is unreachable `[x1]` strings — FIXED
_Fixed in <sha>. ValidateSubnetCreation runs the `Cidr <= parentSubnet.Cidr` check before the containment check, so an equal-or-larger range is refused with the CIDR sentence against the Cidr field and containment is reserved for ranges actually outside the parent. Two existing blocks swapped; no new rule or string._
_Swept: the only other emitters are on the Edit path (SubnetValidationService), deliberately untouched on the audit verifier's narrower option since that operator already receives the true reason in the same line._
_Verified: live — an equal range now answers 'Child subnet CIDR must be larger than parent subnet CIDR. Parent subnet CIDR is 24' on the Cidr field with the NetworkAddress span empty; a genuinely outside range still answers containment. 5 pins; swapping the order back reds 2._
_Reviewed: verdict (c) — reviewer ran a 9-case matrix on both builds: 8 refused, 1 created, identical on each, with only the sentence and field changing on three. Bulk import drove byte-identically across all four rig prefixes, since ModelStateMessage aggregates key-agnostically._

## L10 — Delete-confirm on a row that no longer exists stashes a TempData error for a page that answers 404, so the alert surfaces on the next unrelated page `[x1]` — FIXED
_Fixed in <sha>. Both DeleteConfirmed actions resolve existence before their pre-lock gates and answer 404 (HostIpExists / SubnetExists, both pre-existing); the within-lock ValidateHostIpDeletion block, whose only error was HOST_IP_NOT_FOUND, is deleted since the FindAsync under the same lock already answers it; SetAllocationStatus answers 404 in its timeout catch when the subnet is gone. Net deletion in the host-IP path._
_Swept: the subnet not-found sentence now has one home in Delete.cs instead of two copies. The unreachable HostIpController.cs:416-422 arm was left alone per the verifier. The residual race-only sites are left open as the finding states; no guard was added for them._
_Verified: two-tab races on all five sites, fixed against unfixed — every one leaked its message onto the next unrelated page before and leaks nothing now. Live-row controls identical on both builds: wrong word, missing scope, rowVersion mismatch, cascade delete of a subnet with a child and two host IPs, and all three lock-timeout paths. Suite 1018; full revert reds 4 of 8._
_Reviewed: verdict (c) — reviewer confirmed the deleted validator has no other caller and only ever produced HOST_IP_NOT_FOUND, that the within-lock checks still stand behind the new pre-lock ones, and that the timeout catch opens a fresh connection so it cannot throw. Mutants proved the pins bind the ORDERING (guard before gate), not the text._
_Not done: the SetAllocationStatus timeout arm has no unit seam (the mock locking service never throws TimeoutException), so it is covered by the live drive only and recorded here and in the ledger row rather than pinned._

## L11 — CIDR modal's "Valid range: {parent+1} - 32" label and the input's min are parent-derived, so the modal calls a CIDR valid that it then refuses for the clicked range `[x1]` strings — FIXED
_Fixed in <sha>. The modal takes its lower bound from the first CIDR the server's own per-range table has a block for, and uses it for both the label and the input's min; the unread parentCidr and minCidr consts are deleted. One file, net one line._
_Deliberately NOT the audit's preferred fix: stamping MinAvailableCidr on ChildSubnetSuggestion would grow IpUtilityService, the single home of the product's IP arithmetic, against the owner's standing instruction not to bloat core logic. The client already receives this table and already indexes it to decide acceptance, so label, min and acceptance are now one expression and cannot drift._
_Verified: the defect range (10.200.9.64-.127, 64 addresses) labelled '25 - 32' with min=25 and refused 25 on the same screen; it now labels '26 - 32' and accepts 26. Control: a range where a /25 genuinely fits still labels 25 and accepts it, adjusting the address. Suite 1020; full revert reds both pins._
_Reviewed: verdict (c) — reviewer proved the accepted set is upward-closed (a non-null at /N implies one at /N+1, and /32 always fits a non-empty range) across 131,070 exhaustive shapes plus 4,000 random fragmentations, with zero holes; confirmed integer-like key ordering holds on both sides; and drove seven shapes including a /31 parent that correctly labels '32 - 32'. Its correction was adopted: a reversed-find mutant survived the first pin and now reds._

## L12 — Held-by-manual-content rows are never confirmed against Azure, so a row whose resource the credential merely cannot see tells the operator to delete their manual content and rescan; they archive it irreversibly and the rescan withholds the row anyway `[x1]` — STRUCK
_Struck on the owner's ruling. The defect is real and reproduced, but the fix adds a status field to AzureReconcileItem and changes ApplyConfirmations' bucketing, its early return and where one warning is emitted — mechanism in the reconciler, the file this loop has grown hardest (peaked at 911 lines against a 203-line baseline before the round-16 revert)._
_Owner's standing instruction this round: "DO NOT BLOAT THE CORE LOGIC", "NO OVERCOMPLICATED FIXING BULLSHIT", "STOP BEING OPPORTUNISTIC IT LEADS TO FEATURE BLOAT"._
_Operator workaround, unchanged: before acting on a held row that says "could not be found in this subscription's listing", check whether other rows from the same resource group appear in the "Azure denied access" warning on the same screen. If they do, the resource is hidden, not gone. The limit stands — where a row's only descendants are hand-made, no sibling hint appears._

## L13 — After a 409 that says the reviewed state is stale, both wizard clients keep the superseded plan/snapshot actionable and re-offer the identical submit `[x1]`
**Where:** src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:704 (showCommitError re-enables `#bulk-confirm-commit-btn` for every failure, including the plan-changed 409 from SubnetController.BulkAzure.cs L149-162; lastSelection and its stale `expected` stamps are kept — `invalidatePlan()` is reached only from the selection/preview paths at L96/L118/L361/L407 — so the next click re-posts a byte-identical request), :611 (`$("#bulk-go-commit-btn").prop("disabled", !plan.canCommit)` — the button and the #step3-tab/#step4-tab pills keep their enabled state after the 409, so Preview still presents the superseded plan as committable), :618-625 (the go-commit click handler hides `#bulk-commit-error`, so re-entering Commit from the stale Preview erases the refusal and shows a clean Confirm Import; the re-enable inside that range is L624), src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:434 (showCommitError ends in refreshDeleteButton() while confirmedIds/confirmedVerdicts are kept after the 409s of SubnetController.AzureReconcile.cs L84-121), :271 (updateGoConfirmBtn enables off a lastPlan that is never cleared, so Review still shows the refused row ticked with "Next: Confirm deletion" enabled), and two sites **both verifiers add**: :347 (the go-confirm handler hides `#rec-commit-error` and re-snapshots confirmedIds/confirmedVerdicts at L306-310 from the superseded lastPlan — the refusal-erasure is not bulk-only) and :388 (refreshDeleteButton() inside the delete request's `complete` handler, which jQuery runs *after* success/error and is therefore the last word on `#rec-confirm-delete-btn`, so removing the L434 call alone would change nothing).
**Breaks:** Bulk: the operator previews a VNet with "Rename matched…" on; before Confirm Import another operator renames the Bastet target; Confirm Import → 409 "The plan changed since it was previewed, so nothing was imported. Re-run the preview, review what it now says, and confirm again." with a differences list naming the change. Confirm Import is left enabled; the second click posts a byte-identical body and gets the identical 409. "Back to Preview" does not re-run the preview and shows the superseded plan — asserting a Bastet subnet name Bastet no longer holds, the very fact the server had just named in the differences list — with "Continue to Commit" enabled and both pills live; that click **erases the refusal banner** and presents a clean Confirm Import over the plan the server just refused. Reconcile is the same shape and worse: after the 409 "1 of the selected subnet(s) are no longer offered for deletion by the latest re-check, so nothing was deleted. Re-run the scan…", the Delete button is re-enabled with the same snapshot, a second click re-posts the identical ids, the Review tab still lists and ticks a Bastet row that has already been deleted with "Next: Confirm deletion" live, and re-entering Confirm rebuilds the list from the superseded lastPlan and names that gone row again. Nothing is written — the server refuses every stale resubmission. Two framing corrections reconcile must not re-import from the finder's text: (1) the remedy the message names **is** reachable (bulk: Back to Preview → Back to Selection → Next: Preview; reconcile: the Subscription pill → Next: Scan, both verified working with every control enabled), so this is **not** a rule-3 "message naming a remedy the app refuses" and must not be graded as one — the defect is the refused action being offered alongside the message; (2) the repeated submit is not a submit that can never work — the server re-derives the plan on every POST and commits only what matches the operator's approved expectations, so when the divergence was reverted the identical stale payload committed correctly (200, the child created, no preview in between), which means the finding cannot rest on "it offers work that is not work" either. What is left, and what makes it a finding rather than polish, is that the client keeps a snapshot its own server has just declared superseded and goes on presenting it as current and committable, and that two ordinary clicks erase the refusal — the class the loop has repeatedly accepted as Low (28-L9, 28-L2, 27-L1).
**Repro:** `cd $RIG/app && $RIG/start-app.sh v_m10_truth 40403 bastet_r31_v_m10_truth A` (**must** be started with cwd = $RIG/app, or ASP.NET Core takes the caller's cwd as content root, every static asset 404s, jQuery never loads and no wizard XHR is ever sent — the shared reference instance on 5310 is in that state, so no wizard drive against it is trustworthy); own Azure fixture rig-r31-v_m10_truth-v1 10.147.0.0/16 with s1 and s3. Playwright headless chromium with every POST body and status captured. Bulk: imported the prefix + s1, previewed with rename ON ticking s3 → plan card 'Exact match Bastet subnet "rig-r31-v_m10_truth-v1" (10.147.0.0/16) / Child Azure subnets: Create s3 10.147.3.0/24'; a second page renamed Bastet subnet 1 to "v1-renamed-by-other"; Confirm Import → 409 with `differences:["10.147.0.0/16: renaming the target changed to True."]`; screen after: commitErrorHidden=false, confirmDisabled=**FALSE**, confirmVisible=true, step3TabDisabled=false, step4TabDisabled=false, goCommitDisabled=false, plan card unchanged; second click → request bodies byte-identical (`req2==req3` True, 795 bytes each) and identical 409s; "Back to Preview" → plan card still names the old name, goCommitDisabled=false; "Continue to Commit" → commitErrorHidden=**TRUE**, confirmDisabled=false, confirmVisible=true. Recovery only via Back to Preview → Back to Selection → Next: Preview, which re-previews with the rename and then commits 200. Reconcile: deleted Azure subnet s1, scanned (row id 2 "s1 … Subnet deleted"), ticked, Next: Confirm deletion, typed approved; a second page deleted Bastet subnet 2; Delete Stale Subnets → 409 with `subnetIds:[2]`; deleteDisabled=**FALSE**, the confirmation box still reads "approved", second click posts the byte-identical body for the identical 409; the Review pill → row 2 still listed and still checked, goConfirmDisabled=false, both pills enabled; "Next: Confirm deletion" from that stale Review → errorHidden=TRUE and the confirm list rebuilt from lastPlan naming the already-deleted row. The only re-scan control is `#rec-scan-btn` on the Subscription step; running it reports "Everything imported from this subscription still exists in Azure. There is nothing to clean up." Three 409s, zero rows changed; instance log 0 'fail:' lines.
**Fix:** Narrow the existing re-enable to the failures it serves, keeping it for the 503 lock timeout ("…another subnet operation is in progress. Please try again.") and the `xhr.status === 0` outcome-unknown branch, both of which legitimately want the identical retry. Discriminate on the **status**: thread `xhr.status` from each error handler into showCommitError and branch on 409. Do **not** use the finder's offered alternative of sniffing `payload.differences` (bulk) or `payload.subnetIds` (reconcile): the bulk endpoint returns three further stale-state 409s that carry no differences array and are equally unrepeatable — SubnetController.BulkAzure.cs:219 "Matched Bastet subnet (id=…) for VNet … was not found. Another user may have deleted it.", :248 the already-linked re-link refusal, and :362 "… is no longer linked to the Azure subnet it was previewed against. Run the preview again." — so the payload sniff would leave those three still re-offering a plan the server has already rolled back. Both showCommitError functions are also called from the success handler (`result.success === false`), where no xhr exists, so pass the status explicitly (0/undefined from that path) rather than reading a global. Bulk: on the 409 call `invalidatePlan()` and leave `#bulk-confirm-commit-btn` disabled — invalidatePlan nulls lastSelection (so the Confirm handler returns early even if something re-enabled the button), disables the go-commit button and the step3/step4 pills while activateTab keeps the step4 pane and its banner on screen, and leaves Back to Preview → Back to Selection → Next: Preview reachable with `#bulk-go-preview-btn` enabled. Reconcile: set `confirmedIds = confirmedVerdicts = null` and `lastPlan = null`, call `updateGoConfirmBtn()`, and keep the banner on screen; both refreshDeleteButton() calls (L434 and the `complete` handler at L388, which fires last) then keep Delete disabled, and the L347 re-entry is closed because the go-confirm handler guards on `!lastPlan`. Void the snapshot rather than poke the button, precisely because L388 runs last. One verifier judged the filed fix **unsound on two further points**. (a) The bulk arm trades one silent screen for another: the refusal banner lives inside the Commit pane, so the moment the operator clicks "Back to Preview" (activateTab re-enables that pill) they are on a Preview step showing the superseded plan beside a mute, dead "Continue to Commit" with nothing on screen saying why — close that in the same change by rendering the server's refusal text through the Preview step's existing `#bulk-global-errors` list (the channel renderPlan already uses for "this plan cannot be committed"), or, smaller still, keep the retry (it is provably safe, since the server re-derives and commits only what the operator approved) and simply stop the go-commit handler from hiding a refusal that still stands, which alone closes the strongest half of the defect. (b) The reconcile arm as written inlines a third copy of "the confirmation is void" instead of reusing `invalidateConfirmation()`, which is exactly the §5 duplication invariant — split invalidateConfirmation() into a named state half plus the step-2 bounce and call the state half from the 409 branch so the banner (which lives in `#rec-step3`) survives, keeping the proposed `lastPlan = null` + `updateGoConfirmBtn()`. Reconcile already receives the server's `subnetIds` in the 409 body, and unticking exactly the rows the server named is on the correct axis (consuming the server's decision rather than re-deriving it) and is a cleaner narrow alternative to voiding the whole snapshot. Cosmetic, not a defect: on the bulk 409 the active step4 pill will carry both `active` and `disabled`; the pane and its banner stay visible, which is what matters. No server change, no new guard. Interim: none required — the server refuses the stale resubmission every time (409, nothing written); operators recover by Back to Preview → Back to Selection → Next: Preview (bulk) or the Subscription pill → Next: Scan for deleted resources (reconcile).
**Residue of:** none. Round-granularity provenance for the fixer: bulk L704 and the L618-625 handler are 73fc76f "Azure Bulk Import (#108)" (the original feature, non-audit) with L624 touched by 78fc4c9 (round 12); the plan-changed 409 and attachApprovedOutcomes are 09cee3d (round 10) with the differences list e03ae51 (round 11); reconcile's showCommitError frame is the non-audit aedd0bd (#133), L434 is bf120d6 (round 4), L388 is 6d1a4cb (round 13), L347 is aedd0bd, L271 is 65d1fc6 (round 15). Every contributing commit predates round 18, so no per-finding ledger id exists; the only round ≥ 18 commit touching any of it is 338e8e0 (23-L1), which reworded the noLongerStale 409 on the server and changed no client line.

## L14 — "Collapse All" re-expands a root the operator collapsed by hand and never collapses the first level (no-op on a two-level tree) `[x1]` — FIXED
_Fixed in <sha>. The #collapse-all handler is now the single statement `$('.subnet-children').slideUp(200, updateToggleIcons);`, the mirror of #expand-all, so every level collapses and a hand-collapsed root stays collapsed._
_Swept: #collapse-all/#expand-all/.subnet-tree/.subnet-children across src/ — the only other implementation is the sibling #expand-all, which repainted icons synchronously instead of in the slideDown callback (the same defect class as the per-row toggle's documented callback pattern); fixed in the same commit._
_Verified: browser drive on a 3-level tree — before: Collapse All left all three roots display:block/dash and re-opened a hand-collapsed root; after: all roots and the nested row go display:none/plus and the hand-collapsed root stays collapsed. Expand-inside-collapse-window race probed at +60/120/199ms: 0 icon mismatches (it reproduced on HEAD). New SubnetTreeScriptTests: 4/4 pass, full revert reds 4/4, re-adding .show() reds 2, narrowing the selector reds 3, dropping the callback reds 1. Suite 975 passed._
_Reviewed: verdict (c) — no demonstrated failure, no model violation, 60 randomized settled actions on 3- and 4-level trees with 0 mismatches and 0 console errors. Two corrections adopted: the pins were token pins (a differently-spelled first-level re-show passed all four) and are now an exact single-statement match, which reds that evader 3/4; the mirror assertion was relaxed so a future expand-all change does not red it._

## L15 — Wizard XHRs answered by the sign-in redirect after cookie expiry are reported as "The server could not be reached." `[x1]` strings — FIXED
_Fixed in <sha>. Four string literals, no logic: the status-0 arm of both readErrorMessage functions names the sign-in cause and a reachable remedy, and each wizard's commit/delete arm keeps its hedge and adds its own next step (import again / scan again)._
_Swept: all four "could not be reached" sites in src/ changed, none left. One readErrorMessage per script kept, as 28-L8 left it. The OIDC 401 machinery the finding named separately was deliberately not built._
_Verified: all seven banners driven in headless Chromium with the XHR aborted; text renders escaped. Suite 1010; full revert of the four strings reds 9 of 13 wording pins._
_Reviewed: verdict (c) — reviewer confirmed the disjunction is true under both causes, that reloading the wizard page is challenged as a navigation and returns 200 rather than AccessDenied, and that the over-warning hedge cannot cause a double-apply because the planner re-offers no completed work. Nine single-edit regressions each red the shape assertions the two re-pointed pins carry._

# Info

# Refuted — reported by a finder, killed by the verifier

| id | title | file:line | tag | reason |
| --- | --- | --- | --- | --- |
