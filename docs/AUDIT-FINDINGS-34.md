# Bastet — Round-34 Audit Findings

branch `audit/round-34` · HEAD `b17adb8` · test baseline 1047/1047 passing · 2026-09-23 · residue rate 9/11

> Round 34 filed 11 findings, of which 0 are residue of round 33's own fixes. Round 33 made no fixes (it filed 0 findings, and `3af5afd..HEAD -- src/` is empty), and 9 of the 11 are residue of an earlier round's fix: 18-R15 (L1), 18-R6 (L3), 25-L2 (L4), 18-R5 (L5) and 18-R3 (L9), plus four at round granularity with no per-finding ledger id, round 16 (M2), round 6 (L6) and round 17 (L7, L8).

Growth: src/Bastet/Controllers/HostIpController.cs 743 lines at HEAD, 743 at the previous audit commit (3af5afd).
Growth: src/Bastet/Controllers/SubnetController.Edit.cs 280 lines at HEAD, 280 at the previous audit commit (3af5afd).
Growth: src/Bastet/Services/Azure/AzureBulkImportPlanner.cs 942 lines at HEAD, 942 at the previous audit commit (3af5afd).
Growth: src/Bastet/Controllers/SubnetController.BulkAzure.cs 511 lines at HEAD, 511 at the previous audit commit (3af5afd).

# Critical

None.

# High

None.

# Medium

## M1 — A host-IP delete confirmation archives whichever record holds that address when it is submitted, even one renamed or re-created after the operator reviewed it `[x1]` — FIXED
_Fixed in "Audit 34 M1: bind the host-IP delete confirmation to the reviewed row". The Delete page posts the reviewed row's RowVersion and DeleteConfirmed refuses inside the lock, back to the Delete page, when it is missing or differs (round 17's subnet pattern, f4a0a87)._
_Swept: every irreversible single-record path: the subnet delete was already bound, purges act on immutable archive rows, the subnet cascade keeps §8 18-R2's count; no other unbound path; the e2e route note now names the new form field._
_Verified: HostIpDeleteStaleRowTests (10 tests), 4 red on the unfixed code; each mutant red (full revert, check dropped or inverted, null token accepted, GET stamp or form input dropped, input outside the form, check hoisted before the lock, word gate bypassed); live on two replicas: stale rename and delete-and-re-create refused with the current name shown, re-confirm archives, controls unchanged; suite 1079/1079, 0 warnings._
_Reviewed: (c) preference, no failure found; its three optional corrections adopted (form-scoped pin, in-lock pin, e2e note)._
_Not done: product question for the owner, does §8 18-R2's count tolerance extend to the single host-IP delete page? The rename half is bound either way._

## M2 — Subnet delete archives a child created while the confirmation page was loading: the page leaves it out of the child count, but the scope guard already covers it `[x1]` — FIXED
_Fixed in "Audit 34 M2: take the subnet delete page's count and scope from one walk". The Delete GET takes the shown child count and the posted scope bound from one subtree walk; CountAllDescendants, whose only caller that was, is deleted._
_Swept: no other caller; the POST's in-lock check and §8 18-R2's host-IP count are unchanged; every subnet insert runs under the global lock and nothing re-parents a row, so the max-id bound stays valid._
_Verified: SubnetDeletePageSnapshotTests (14 tests: a child inserted at each GET command boundary, plus a deep-tree round trip), 4 red on the unfixed code; mutants red (full revert, count or bound from a second walk or from the Include, a Min() bound; the walk-order swap stays green as an equivalent); live race, 41 attempts each: HEAD 6 over-deletions, fix 0; suite green, 0 warnings._
_Reviewed: (c) preference, 0 over-deletions in 164 unpadded and 30 padded races; its two test-hardening notes adopted, its one-helper preference declined._

# Low

## L1 — Edit page and Edit refusal tell the operator to change an Azure-linked prefix in Azure and re-import it, but the import wizard refuses the re-import until the old row is deleted `[x2]` `strings`
**Where:** src/Bastet/Views/Subnet/Edit/_EditForm.cshtml:30, src/Bastet/Controllers/SubnetController.Edit.cs:87
**Breaks:** With the Azure import flag on, Edit on every Azure-linked row, a linked subnet (snet-grow 10.140.2.0/24 of VNet rig-34-v3t-a, snet-x 10.136.1.0/24 of rig-34-v3r-a) and a linked VNet row alike, shows "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it." (_EditForm.cshtml:30), and an Edit form harvested before the wizard link-adopted the row, posted afterwards with a new CIDR, gets the same remedy as its Cidr field error: "This subnet is linked to an Azure resource, so its CIDR cannot be changed here. Change the prefix in Azure, then ask an administrator to re-import it." (SubnetController.Edit.cs:87; reachable without crafted input). The operator does what it says: in Azure snet-grow becomes 10.140.2.0/23 and snet-shrink 10.140.4.0/24 becomes 10.140.4.0/25 (and snet-y 10.136.2.0/24 becomes 10.136.2.0/23, snet-x becomes 10.136.1.0/25). The administrator's Bulk Azure Import then marks both "Cannot import": the grown one reads "Would contain existing Bastet subnet 'snet-grow' (10.140.2.0/24), which would create an invalid hierarchy." and names no remedy at all, the shrunk one gets the middle-man sentence ("... delete or re-carve ... before importing this one"), a forced preview returns canCommit=false, and the prefix ends up "Already imported ... Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add.", hidden entirely under "Only show what would change". Because the network address is fixed, every resize the Edit page's CIDR field is about either contains the old prefix or sits inside it, so the wizard refuses the re-import for all of them. If the subnet is moved to a disjoint range instead (10.140.6.0/24 to 10.140.9.0/24, or 10.136.4.0/24 to 10.136.8.0/24), the re-import is accepted but adds a second row, "snet-move (10.140.9.0-24)", linked to the same Azure subnet, and the old range stays allocated. Only delete-then-import brings the new prefix in cleanly, and the Edit page never names the delete: Reconcile names it for the rows it offers ("Delete it here if you want to, then use the Azure import wizard to bring in its current prefix."), and /Subnet/Delete does the delete for rows Reconcile holds. The message names a remedy the app refuses: PRODUCT-MODEL §1 rule 3 ("A message naming a remedy the app refuses is a defect."), §3 ("After deleting a row whose range changed, the operator goes back through the bulk import wizard - a message about a changed range must point there") and §8 31-L2 ("IF A FUCKFACE EDITS THE FUCKING SUBNET ADDRESS, THE BASTET OPERATION SHOULD BE DELETE WHAT WE HAVE AND RE FUCKING IMPORT"). No address space is lost, and reconcile reports the stale rows.
**Repro:** Truth verifier ran: live rig, real tree, visible principal, app on port 22120, catalog a34_v3t_1, private VNet rig-34-v3t-a 10.140.0.0/16 with snet-grow 10.140.2.0/24, snet-shrink 10.140.4.0/24, snet-move 10.140.6.0/24 and snet-held 10.140.12.0/24 (built with build-fixtures.py; ids in inventory/v3t.txt). (1) Created hand-built row hand-vnet 10.140.0.0/16 with the harvested Create form, then saved its Edit form while the CIDR could still be edited. (2) Imported the VNet through the wizard in Chromium (wiz.py import); hand-vnet was adopted and 4 linked children were created. (3) POSTed the saved pre-adoption Edit form with Cidr=17; GET /Subnet/Edit/2; POSTed a harvested /Subnet/Edit/2 form with Cidr=23. (4) Added host IP 10.140.12.10 to snet-held with the HostIp Create form. (5) `az-as.sh visible network vnet subnet update`: snet-grow to 10.140.2.0/23, snet-shrink to 10.140.4.0/25, snet-move to 10.140.9.0/24, snet-held to 10.140.12.0/25. (6) GET /Azure/BulkGetVNets, the wizard card in Chromium with the filter off and on, and a forced POST /Azure/BulkImportPreview with all four subnets. (7) POST /Azure/ReconcileScan. (8) Positive control: deleted snet-grow and snet-shrink through BulkDeleteStaleAzureSubnets, deleted snet-held through the harvested /Subnet/Delete/5 form, then re-ran the wizard import in Chromium. Came back: (3) stale form: HTTP 200; the Cidr error reads "This subnet is linked to an Azure resource, so its CIDR cannot be changed here. Change the prefix in Azure, then ask an administrator to re-import it."; the row stays /16; GET Edit/2 shows a read-only /24 and "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it."; the Cidr=23 POST gets the same refusal. (6) snet-grow 10.140.2.0/23 is "Cannot import": "Would contain existing Bastet subnet 'snet-grow' (10.140.2.0/24), which would create an invalid hierarchy.", naming no remedy; snet-shrink and snet-held are "Cannot import" with the middle-man sentence ("delete or re-carve ..."); "Only show what would change" hides all three; the forced preview returns canCommit=false with three global errors; snet-move is Available, so the bare re-import adds a second row. (7) Reconcile offers snet-grow, snet-shrink and snet-move as SubnetPrefixChanged ("Delete it here if you want to, then use the Azure import wizard to bring in its current prefix.") and lists snet-held only as HeldByManualContent: not offered, "BASTET will not delete it ... Delete it here first, then run the scan again." (8) After the deletes, the wizard commits snet-grow 10.140.2.0/23, snet-shrink 10.140.4.0/25 and snet-held 10.140.12.0/25; snet-move, re-imported without a delete, becomes the new row "snet-move (10.140.9.0-24)" 10.140.9.0/24, and the old snet-move 10.140.6.0/24 stays allocated, linked to the same Azure subnet. Reach verifier ran: real tree, visible principal, own fixtures (rig-34-v3r-a 10.136.0.0/16 with snet-x 10.136.1.0/24, snet-y 10.136.2.0/24, snet-z 10.136.4.0/24, snet-w 10.136.6.0/24; rig-34-v3r-b 10.136.64.0/18 with snet-b1), built with build-fixtures.py and inventoried in inventory/v3r.txt. (1) `start-app.sh v3r 22141 a34_v3r_2 real visible`; imported rig-34-v3r-a (target plus 4 subnets) with POST /Azure/BulkImportPreview and POST /Subnet/BulkCreateFromAzurePlan; added host IP 10.136.6.10 to snet-w through the harvested /HostIp/Create form; GET /Subnet/Edit/2 (snet-x) and /Subnet/Edit/1 (VNet row). (2) `az-as.sh visible network vnet subnet update`: snet-x to 10.136.1.0/25, snet-y to 10.136.2.0/23, snet-z to 10.136.8.0/24, snet-w to 10.136.6.0/25. (3) GET /Azure/BulkGetVNets, then a forced BulkImportPreview for each subnet on its own, then POST /Azure/ReconcileScan; rendered /Azure/BulkImport in Chromium (pw.browser_page), with and without "Only show what would change". (4) Committed the only subnet the wizard would select. (5) Positive control: POST /Subnet/BulkDeleteStaleAzureSubnets for the offered rows, then import; for the held snet-w, harvested /Subnet/Delete/5 with confirmation=approved, then import. (6) Second site, on port 22140 with catalog a34_v3r_1: created unlinked hand-b 10.136.64.0/18 through /Subnet/Create and harvested /Subnet/Edit/1 while its CIDR was editable; the wizard then link-adopted it (ExactMatch, linkedTargets 1); posted the harvested form with Cidr=19. Came back: Edit page: both the linked child (snet-x) and the linked VNet row showed "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it." Wizard after the Azure change (what the admin sees): snet-x 10.136.1.0/25 Cannot import, with the middle-man sentence ("...delete or re-carve 'snet-x' before importing this one"); snet-y 10.136.2.0/23 Cannot import: "Would contain existing Bastet subnet 'snet-y' (10.136.2.0/24), which would create an invalid hierarchy."; snet-w 10.136.6.0/25 Cannot import, with the middle-man sentence; a forced preview answered canCommit=false for each of the three. Disjoint move: snet-z 10.136.8.0/24 was Available; committing it created a second row, 'snet-z (10.136.8.0-24)', with the same resource id, and the old 'snet-z' 10.136.4.0/24 stayed allocated. After that commit the prefix read "Already imported ... Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add."; ticking "Only show what would change" hid the whole VNet card (seen in Chromium). Reconcile offered snet-x, snet-y and the old snet-z as SubnetPrefixChanged ("...Delete it here if you want to, then use the Azure import wizard to bring in its current prefix.") and held snet-w as HeldByManualContent ("It holds 1 host IP assignment. BASTET will not delete it..."). Positive control: after the reconcile delete, snet-x and snet-y were Available and imported (created 2); snet-w could only be deleted through /Subnet/Delete, which archived 10.136.6.10, after which it imported; a final scan was clean. Race site (second catalog): POSTing the pre-adoption Edit form with Cidr=19 gave HTTP 200 with the Cidr field error "This subnet is linked to an Azure resource, so its CIDR cannot be changed here. Change the prefix in Azure, then ask an administrator to re-import it.", and the page also showed the "modified by another user" summary.
**Fix:** Both verifiers judged the filed fix unsound. The filed fix (b5p2-3) would make _EditForm.cshtml:30 read "Change it in Azure, then ask an administrator to delete this subnet (Azure Reconcile offers it once the prefix has changed) and re-import it." with the same wording at Edit.cs:87; its parenthetical is false for any linked row whose subtree holds host IPs or hand-made subnets, because Reconcile withholds such a row as HeldByManualContent and never offers it for deletion (reproduced by both: snet-held with one host IP, and snet-w holding 10.136.6.10, were listed only as "BASTET will not delete it ... Delete it here first"), and recording host IPs in an imported subnet is ordinary use, so the case is common; shipping it would ship a new message naming a remedy the app refuses, the defect being fixed. The sibling finder b3p2-2's "delete this subnet from Azure Reconcile" is false for the same rows. The corrected fix is the sibling finder b2p1-2's wording, a pure string swap at the two sites that keeps the existing IsAzureImportEnabled gating and the "ask an administrator" routing (both correct under §5's reachable-remedy rule: Reconcile and the wizard are Admin-only, and Admin includes Delete) and does not mention Reconcile: src/Bastet/Views/Subnet/Edit/_EditForm.cshtml:30 becomes `<text>Change it in Azure, then ask an administrator to delete this subnet and import it again.</text>`, and src/Bastet/Controllers/SubnetController.Edit.cs:87 becomes `" Change the prefix in Azure, then ask an administrator to delete this subnet and import it again."`. With the flag off both sentences still end at the fact, as they do now. No shared constant or other code change is needed (b2p1-2's optional shared-constant suggestion is not required for the fix). This wording is true in every case driven, grow, shrink, held and disjoint move: an administrator reaches the offered rows through Reconcile, and the held row can be deleted through the ordinary /Subnet/Delete page, which an administrator can always reach and which shows its counts and requires the typed confirmation (deleting snet-held there, then importing, committed 10.140.12.0/25; deleting snet-w there archived 10.136.6.10, after which it imported). It adds, widens and branches nothing and serves §1 rule 3 and the §3 / §8 31-L2 delete-then-import loop. The site list is complete: the only other "import it again" text in src/ (SubnetController.BulkAzure.cs:301) already names the delete, and the Edit sidebar ends at the fact. No interim; no product question.
**Residue of:** 18-R15 (git log -S and git blame put both lines on f2050fa, Audit 18 Cleanup; 18-R15 "CIDR refusal names one remedy, gated on the import feature flag" replaced "re-import, or delete the subnet and recreate it" with a single "re-import" remedy that dropped the delete step)

## L2 — With 'Rename matched Bastet subnets' ticked, adopting an unlinked hand-built row renames it to the VNet name on range match alone `[x2]` — FIXED
_Fixed in "Audit 34 L2: rename a matched target only when it already carries this VNet's link". BuildPlanItem renames an exact target only when the row is already linked to this VNet, the one condition §4 names; the annotation's WouldRenameTarget, the same decision asked a second time, is gated the same way so it agrees with the plan._
_Swept: child renames were already link-gated; the commit acts only on the plan's WillRename, re-planned under the lock with its Expected check; RenameOnlyCandidate, the flag's only reader, already required a linked row, so nothing on screen changes but the rename; the toggle's label claims nothing about unlinked rows._
_Verified: new and corrected tests (unlinked adoption with and without children, fully allocated, a row linked to a different VNet, a differently cased id, annotation-plan agreement, two tests that assumed an unlinked row would be renamed given its link), red on the unfixed code; mutants red (full revert, inverted gate, any link counts, ordinal id comparison, annotation flag ungated, always rename); live: HEAD renamed two hand-built rows on adoption, the fix linked them under their own names and a second pass offered Rename only and renamed; suite green, 0 warnings._
_Reviewed: (c) preference, including a nine-prefix mixed browser run and a preview-to-commit race answered by a precise 409; its three notes adopted (case test, the equal-names test given its link, the annotation flag)._

## L3 — The bulk import wizard keeps offering a 'Rename only' for a child subnet whose name it chose itself, and the rename changes nothing `[x2]` — FIXED
_Fixed in "Audit 34 L3: stop suffixing children that share their target's name". BuildPlanItem no longer seeds its child-name collision set with the target's own names, so a child named like its VNet or its adopted target keeps its Azure name and an existing 'X (X)' row gets one real rename._
_Swept: the annotation (ProposedChildName) and the plan now agree for every Azure-producible selection; DisambiguateName stays for sibling collisions, reached only by a renames-off selection holding a linked sibling hand-renamed to a new subnet's Azure name (e.g. a stale second wizard tab), unchanged from HEAD, whose later offer is a real rename._
_Verified: six new AzureBulkImportChildNamingTests, red on the unfixed code with the audit's exact names; each seed restored alone (existing, renamed, created, children-only, inside-a-container) reds its test; live: HEAD made 'X (X)' rows and a no-op rename, the fix renamed both for real with nothing offered after, and a fresh import used the Azure names; suite green, 0 warnings._
_Reviewed: (c) preference; its rationale correction (stale-tab reachability) and both test additions adopted._
_Not done: product question for the owner, may an imported child carry the same name as its VNet target row? §4 and the schema read yes and the fix relies on it._

## L4 — ARM throttling or a server error on the subscription list is shown as 'Could not authenticate with Azure' `[x2]` `strings`
**Where:** src/Bastet/Controllers/AzureController.cs:48, src/Bastet/Controllers/AzureController.cs:143, src/Bastet/Services/Azure/AzureService.cs:37
**Breaks:** An Admin opens /Azure/BulkImport or /Azure/Reconcile with a valid Azure service principal (the visible principal: a valid secret, Owner on bastet-visible) that lists its subscription "Main" normally, with no banner, moments before and after on the same instance. Meanwhile ARM answers GET https://management.azure.com/subscriptions?api-version=2022-12-01 with 429 (Retry-After: 1), 500 or 503 on all four of the SDK's attempts, after the token has already been obtained from Entra ID. CheckCredential (AzureService.cs:24-41) wraps the first GET /subscriptions page in `catch (Exception)` → CredentialCheckResult.Failed (:37-40), so the RequestFailedException becomes Failed, and both page actions map Failed to the same fixed string at AzureController.cs:48 and :143: the page renders HTTP 200 with the red banner "Could not authenticate with Azure. Check the credentials and that Azure is reachable from this host." above the client panel "Failed to load subscriptions from Azure. Details have been logged.", and the subscription picker stays hidden. Authentication succeeded and Azure answered, so it was reachable; the log records Status: 429 (Too Many Requests), 500 (Internal Server Error) or 503 (Service Unavailable). The banner states a false cause and sends the operator to two checks that both pass, the operator may rotate a working secret for nothing, and the remedy that works, reloading the page once ARM recovers, is never named (PRODUCT-MODEL §1 rule 3: "The operator must be able to act on what they are told."). The identical banner appears for a genuinely bad secret (AADSTS7000215), so the page gives the operator no way to tell the two apart. The step-1 panel beneath the banner is true, and the log records the real status. Sustained ARM throttling and 5xx incidents are real; a single transient failure is absorbed by the SDK retry without a banner, so only a fault that outlasts the SDK's four attempts reaches it.
**Repro:** Truth verifier ran, with `RIG=/tmp/claude-1000/-home-anuj-code-Bastet/3b511843-ebe5-46df-a19d-01e22a4bcd59/scratchpad/rig; W=$RIG/work/v7t; echo '[]' > $W/faults.json; $RIG/bin/start-app.sh v7t 22360 a34_v7t_1 fault visible BASTET_AUDIT_FAULTS=$W/faults.json`, then `$RIG/azcli/bin/python $W/drive.py 22360 <ruleset>` for rulesets empty, s500, s429, s503, s500first, empty. drive.py writes the rules file atomically, e.g. `[{"match":"^GET https://management\\.azure\\.com/subscriptions\\?","nth":"all","action":"status","status":500}]` (429 with headers {"Retry-After":"1"}; 503; nth [1] for the transient case), then GETs /Azure/BulkImport and /Azure/Reconcile, parses div.alert-danger li with BeautifulSoup+lxml, GETs /Azure/GetSubscriptions, and greps the new app-log lines. Browser: `$RIG/azcli/bin/python $W/browser.py` (Chromium via pw.browser_page, 429 rule; waits for the client error panel, then lists the visible .alert-danger texts). Sign-in control: `$RIG/bin/start-app.sh v7t 22361 a34_v7t_2 fault visible BASTET_AUDIT_FAULTS=$W/faults2.json AZURE_CLIENT_SECRET=<deliberately bogus>` with `[{"match":".","nth":"all","action":"status","status":500}]`, then GET /Azure/BulkImport and grep that app log. Came back: rules empty (before and after): /Azure/BulkImport 200 "Bulk Azure Import - BASTET" and /Azure/Reconcile 200 "Azure Reconcile - BASTET", no .alert-danger li; GetSubscriptions success, 1 subscription; 0 injections. Persistent 500 on GET /subscriptions: both pages 200 (5.6 s / 5.8 s), each with the banner li "Could not authenticate with Azure. Check the credentials and that Azure is reachable from this host."; GetSubscriptions {success:false, error:"Failed to load subscriptions from Azure. Details have been logged."}; 12 injections (4 SDK attempts per call); log: "Azure credential validation failed / Azure.RequestFailedException ... Status: 500 (Internal Server Error) / ErrorCode: InternalServerError". Persistent 429 + Retry-After 1: the same banner on both pages; log "Status: 429 (Too Many Requests) / ErrorCode: TooManyRequests". Persistent 503: the same banner on both pages; log "Status: 503 (Service Unavailable)". Transient 500 (nth [1] only): 1 injection, the SDK retry passed, no banner, 1 subscription. Chromium under 429: both pages show two visible red alerts, "Could not authenticate with Azure. Check the credentials and that Azure is reachable from this host." and "Failed to load subscriptions from Azure. Details have been logged.", and the subscription picker stays hidden. Discriminating control (bogus client secret, match-everything 500 rule): the same banner, the log shows AuthenticationFailedException from ClientSecretCredential (AADSTS7000215 invalid client secret), and there are 0 [AUDIT-FAULT] injections, so the fault policy is reached only after a token has been issued: in the 500/429/503 drives sign-in had really succeeded, and a genuine sign-in failure and ARM throttling or server errors render the identical banner. Reach verifier ran, with the same RIG and `W=$RIG/work/v7r`, `$RIG/bin/start-app.sh v7r 22380 a34_v7r_1 fault visible BASTET_AUDIT_FAULTS=$W/faults.json` (faults.json = [] to start); rules written atomically (tmp file, then mv), with no restart between them: `[{"match": "^GET https://management\\.azure\\.com/subscriptions\\?", "nth": "all", "action": "status", "status": 500}]`, then the same rule with status 429 plus `"headers": {"Retry-After": "1"}`, then 503; controls: [] before and after; nth [1] with 500 (transient); 401 with an InvalidAuthenticationToken body, and "throw" (the two causes the banner is true for). `$RIG/azcli/bin/python $W/probe.py http://127.0.0.1:22380 <tag>` (requests + BeautifulSoup/lxml: GET /Azure/BulkImport and /Azure/Reconcile, collect the li items of the visible div.alert-danger ul.mb-0; GET /Azure/GetSubscriptions); `$RIG/azcli/bin/python $W/browser.py http://127.0.0.1:22380 b500` (headless Chromium through pw.browser_page: waits for the step-1 terminal state, lists the visible .alert texts, takes a screenshot). Token-ordering proof: `$RIG/bin/start-app.sh v7r 22381 a34_v7r_2 fault none BASTET_AUDIT_FAULTS=$W/faults-none.json` with the same 500 rule, then `curl --max-time 300 /Azure/BulkImport` and count the AUDIT-FAULT injections in the log. Teardown: stop-app.sh 22380 and 22381, both catalogs dropped, no Azure resources created. Came back: control with rules []: both pages 200 with no banner, and GetSubscriptions answers success with 1 subscription ("Main"). 500 on every attempt: /Azure/BulkImport returns 200 in 6.2 s and /Azure/Reconcile 200 in 5.7 s, both showing the banner li "Could not authenticate with Azure. Check the credentials and that Azure is reachable from this host."; GetSubscriptions returns {success:false, error:"Failed to load subscriptions from Azure. Details have been logged."}; 12 injections (4 SDK attempts x 3 calls); the app log shows "Azure credential validation failed / Azure.RequestFailedException ... Status: 500 (Internal Server Error) ErrorCode: InternalServerError". 429 with Retry-After 1: both pages show the same banner (6.0 s and 6.6 s), 12 injections, log "Status: 429 (Too Many Requests)". 503: both pages show the same banner, 12 injections. Chromium: the visible alerts on both pages are exactly that banner, the step's info text, and the step-1 panel "Failed to load subscriptions from Azure. Details have been logged." (screenshot shot-b500-Azure-BulkImport.png). Transient 500 (nth [1]): 1 injection; the SDK retry succeeded, there was no banner, and the subscription was listed, so only a fault that outlasts the SDK's 4 attempts reaches the banner. True-cause controls: 401 gives the same banner, which is true there; "throw" gives AggregateException "Retry failed after 4 tries (Connection refused)" and the same banner, which is also true there. Token-ordering proof: the principal "none" with the identical 500 rule produced 0 injections and a logged CredentialUnavailableException, with the page showing the same banner; the fault policy only runs after the bearer-token policy has obtained a token, so in the visible runs Entra ID issued a token and then ARM answered with the error: authentication really succeeded. Rules emptied again: no banner, and the subscription was listed.
**Fix:** Both verifiers judged the filed fix sound: one string at two sites that adds, widens and branches nothing and serves §1 rule 3. Replace the one literal used identically at src/Bastet/Controllers/AzureController.cs:48 and :143 with a sentence that is true on every path CheckCredential maps to CredentialCheckResult.Failed (a null ArmClient, credential unavailable, a sign-in/authentication failure, an ARM 401/403, a transport or network failure, a timeout, an ARM 429/5xx after the SDK's retries), that phrases the causes as alternatives, and that names the log and a remedy that works for each cause. Truth verifier's wording: "Could not list subscriptions from Azure: the sign-in failed, Azure was unreachable from this host, or Azure refused, throttled or failed the request. Details have been logged; fix the cause they name, then reload this page." Reach verifier's wording: "Could not list subscriptions from Azure. The sign-in may have failed, Azure may be unreachable from this host, or Azure may have refused or failed the request. Details have been logged; check them, then reload this page." The finder's string ("Could not read subscriptions from Azure: the sign-in failed, Azure was unreachable from this host, or Azure refused or failed the request. Details have been logged.") needs two refinements: its colon list reads as if all three happened, so phrase them as alternatives ("may have"), and it drops every remedy except "Details have been logged", so add "then reload this page", the only remedy that works for 429/5xx. "Details have been logged" stays true on the null-client path, since AzureArmClientProvider has already logged that failure once. A cause-neutral sentence also ends this rewording series (24-L4, then 25-L2): no newly found exception class can make it false again. Leave src/Bastet/Services/Azure/AzureService.cs:37-40, the tri-state enum and the controller branches exactly as they are: add no fourth credential outcome and no exception-type filter (the round-25 audit verifier rejected one, ledger 25-L2, and §8 31-L2 presumes such a branch wrong). Rejected: the sibling finder b3p2's variant, which fixes only the headline and keeps "Check the credentials and that Azure is reachable from this host" as the first instruction for a 429 or 5xx, the misdirection this finding reports, so it only half-closes the defect; and letting non-authentication exceptions escape CheckCredential into the controller's existing catch, which branches the mechanism and whose "Error connecting to Azure. Details have been logged." is just as untrue for an ARM error response, since the connection succeeded. No interim needed: the fix is already a single string. Product questions: (a) each load of /Azure/BulkImport and /Azure/Reconcile lists subscriptions twice, once in the server-side CheckCredential banner (24-L4) and once in the client's /Azure/GetSubscriptions panel, which classify the same three outcomes and on failure both appear one above the other; should the server banner be reworded (the narrow fix) or deleted (CheckCredential, CredentialCheckResult and both controller blocks) so the client's already-true message is the only one? §5's two-implementations rule points at deletion, but that deletes 24-L4's deliberate banner and loses the "check the credentials" hint for the common bad-secret case, and §8 31-L2 prefers leaving deliberate behaviour alone; the choice is the owner's. (b) Should the Failed banner keep naming causes, or become cause-neutral like the VNet-list error ("Azure could not be read for this subscription. Details have been logged."), which closes the series of rewordings of this banner (24-L4, 25-L2, now this one) for good?
**Residue of:** 25-L2 (f8bd697, Audit 25, wrote the current string at both sites; correction to the finder's note: the catch-all at AzureService.cs:37-40 predates the audit (aedd0bd #133, originally 9f220e09), and 24-L4 (9d3c7b2) only changed its return from false to CredentialCheckResult.Failed)

## L5 — The bulk import commit marks a subnet that holds host IPs fully allocated when the posted encompassing subnet carries the VNet's own resource id `[x1]` — DEFERRED
_Deferred: its fix would be the fourth fix-commit this round in AzureBulkImportPlanner.cs (L3, L2 and the strings batch with L8 took the three the cap allows); of the four Low findings in that file it is the one only a hand-built request reaches._
_Transplanted whole, repro included, to docs/DEFERRED-FINDINGS.md; nothing in this round's diff touches the waiver it describes._

## L6 — When a confirming read fails, reconcile says 'Nothing is wrong with the subnet itself' about rows whose Azure resource is deleted `[x1]` `strings`
**Where:** src/Bastet/Services/Azure/AzureReconciler.cs:185, src/Bastet/Services/Azure/AzureReconciler.cs:186
**Breaks:** VNet rig-34-v6t-gone (10.139.16.0/20) and its subnet snet-a (10.139.17.0/24) were bulk-imported as rows 1 and 2, then deleted in Azure (in the reach verifier's run: rig-34-v6r-gone 10.138.16.0/20 with snet-g 10.138.17.0/24, plus snet-b 10.138.34.0/24 of the surviving VNet rig-34-v6r-keep); a direct ARM GET returns 404 for each, and an unfaulted scan offers them as "VNet deleted" / "Subnet deleted". When ARM answers their confirmation GETs with 503 or 429 on all 4 SDK attempts, or the connection fails, ConfirmOneAsync (AzureService.cs:249-276) sends the failure to Unknown and reconcile correctly withholds the rows, but ApplyConfirmations (AzureReconciler.cs:181-187) adds a fixed sentence that knows nothing about the resource. The operator sees only this warning, under "Check this before deleting anything": "2 Azure-linked subnet(s) were missing from the subscription listing, and Azure could not be asked about them - the read failed rather than answering. Nothing is wrong with the subnet itself; try the scan again. They have been withheld from deletion: 'rig-34-v6t-gone' (10.139.16.0/20), 'snet-a' (10.139.17.0/24)." (the reach run's identical sentence names 'rig-34-v6r-gone' (10.138.16.0/20), 'snet-g' (10.138.17.0/24) and 'snet-b' (10.138.34.0/24)). Withholding them is correct, but the reassurance is a claim about Azure state that Azure never made, and here it is false, since all of them are deleted; it also contradicts "were missing from the subscription listing" in the same sentence. The same sentence comes back in the warnings list of the reconcile delete's 409 ("... no longer offered for deletion by the latest re-check ...", SubnetController.AzureReconcile.cs:106, rendered by showCommitError), straight after the operator approved those rows as deleted. With the VNet list emptied, live rows get the identical sentence, so it reflects nothing Bastet knows. The operator is reassured about stale allocations instead of being told the state is unknown, which discourages the rescan that is the only thing that unpins them: this breaks PRODUCT-MODEL §3 ("It reports; the operator decides") and Rule 0 / the symmetric half of rule 1 (Azure never decides anything, and Bastet must not claim what Azure did not say). Same untrue-claim-about-Azure class as 23-L1 and 23-L4. The situation is ordinary: an imported VNet or subnet deleted in Azure, with a 5xx, 429 or transport failure on its confirmation GET (ConfirmResourcesAsync fires up to 8 concurrent GETs per scan, so throttling is plausible).
**Repro:** Truth verifier ran, with `RIG=/tmp/claude-1000/-home-anuj-code-Bastet/3b511843-ebe5-46df-a19d-01e22a4bcd59/scratchpad/rig; W=$RIG/work/v6t` (block 11, NET4 10.139.0.0/16): (1) `$RIG/bin/build-fixtures.py $W/fixtures-v6t.json $RIG/inventory/v6t.txt`: bastet-visible rig-34-v6t-gone 10.139.16.0/20 + snet-a 10.139.17.0/24, and rig-34-v6t-live 10.139.32.0/20 + snet-b 10.139.33.0/24; (2) `start-app.sh v6t 22300 a34_v6t_1 real visible` and `start-app.sh v6t 22302 a34_v6t_1 fault visible BASTET_AUDIT_FAULTS=$W/faults.json` (the fault tree's src equals HEAD except AuditRig/ and AzureArmClientProvider.cs; AzureReconciler.cs and AzureService.cs are identical, checked with diff); (3) `drv.py import 22300 rig-34-v6t-gone rig-34-v6t-live`: /Azure/BulkImportPreview, then /Subnet/BulkCreateFromAzurePlan, result 200, rows 1-4 with AzureResourceIds; (4) `drv.py scan 22300` as a pre-delete control, then `az-as.sh visible network vnet delete -g bastet-visible -n rig-34-v6t-gone`; ground truth: `curl -H @$(arm-token.sh visible)` GET .../virtualNetworks/rig-34-v6t-gone and .../subnets/snet-a, with GET .../rig-34-v6t-live/subnets/snet-b as a control; (5) positive control: `drv.py scan 22300` (real tree, no faults); (6) faults.json (swapped in with mv) = `[{"match":"^GET https://management\\.azure\\.com/subscriptions/[^/]+/resourceGroups/bastet-visible/providers/Microsoft\\.Network/virtualNetworks/rig-34-v6t-gone","nth":"all","action":"status","status":503}]`, then `drv.py scan 22302`, and `bw_rec.py 22302` in Chromium (/Azure/Reconcile, select the subscription, click #rec-scan-btn, read #rec-scan-warnings); (7) `del409.py`: a clean scan on 22302 offers rows 1 and 2; swap in the 503 rule, then POST /Subnet/BulkDeleteStaleAzureSubnets with confirmation "approved" and the offered statuses; (8) variants: action "throw" (network failure) on the same match, and emptyPage on the VNet list plus 503 on the rig-34-v6t-(gone|live) GETs. Came back: import 200, "created 2 VNet target subnet(s), created 2 Azure child subnet(s)"; pre-delete scan: nothing flagged; after the Azure delete, ARM GET of the VNet and of snet-a both return 404 ResourceNotFound, snet-b returns 200; real-tree control scan: row 1 'rig-34-v6t-gone' 10.139.16.0/20 VNetDeleted and row 2 'snet-a' 10.139.17.0/24 SubnetDeleted are both offered, with no warnings. Fault tree, 503 rule: 8 injections in the app log (2 resources x 4 SDK attempts); items and reviewItems are empty; both over HTTP and in Chromium, #rec-scan-warnings ("Check this before deleting anything") shows "2 Azure-linked subnet(s) were missing from the subscription listing, and Azure could not be asked about them - the read failed rather than answering. Nothing is wrong with the subnet itself; try the scan again. They have been withheld from deletion: 'rig-34-v6t-gone' (10.139.16.0/20), 'snet-a' (10.139.17.0/24)." (a screenshot confirms it). Delete path: a clean scan offered VNetDeleted/SubnetDeleted; under the 503 rule the delete answers 409 "2 of the selected subnet(s) are no longer offered for deletion by the latest re-check..." with warnings = [the same sentence]; nothing was archived (4 live rows, 0 in DeletedSubnets). "throw" variant: 8 injections and the identical sentence. emptyPage + 503 variant: one warning with the identical sentence for all 4 rows, the live pair (true there) and the deleted pair (false there), so the clause is emitted whatever the resource's real state is. Reach verifier ran, with the same RIG (block v6r, NET4 10.138.0.0/16, ports 22320-22321, catalog a34_v6r_1; every Azure id appended to inventory/v6r.txt before creation): (1) `az-as.sh visible network vnet create -g bastet-visible -n rig-34-v6r-gone --address-prefixes 10.138.16.0/20 --subnet-name snet-g --subnet-prefixes 10.138.17.0/24`; the same for rig-34-v6r-keep 10.138.32.0/20 with snet-a 10.138.33.0/24, then subnet create snet-b 10.138.34.0/24; (2) `start-app.sh v6r 22320 a34_v6r_1 real visible`; `work/v6r/import.py http://127.0.0.1:22320 rig-34-v6r-` (harvest the token from /Azure/BulkImport, BulkGetVNets, BulkImportPreview, attach expected, POST /Subnet/BulkCreateFromAzurePlan); (3) `work/v6r/scan.py http://127.0.0.1:22320` (baseline: nothing stale); (4) `az-as.sh visible network vnet delete -n rig-34-v6r-gone`; `az-as.sh visible network vnet subnet delete --vnet-name rig-34-v6r-keep -n snet-b`; (5) `curl -H @"$(arm-token.sh visible)"` .../resourceGroups/bastet-visible/providers/Microsoft.Network/virtualNetworks/{rig-34-v6r-gone, rig-34-v6r-gone/subnets/snet-g, rig-34-v6r-keep/subnets/snet-b, rig-34-v6r-keep/subnets/snet-a}?api-version=2024-05-01; (6) `work/v6r/scan.py http://127.0.0.1:22320` (positive control on unmodified HEAD); (7) `stop-app.sh 22320; start-app.sh v6r 22321 a34_v6r_1 fault visible BASTET_AUDIT_FAULTS=$RIG/work/v6r/faults.json`; a tracer rule first, to confirm the confirmation-GET URLs; then faults.json (written atomically) = `[{"match":"^GET https://management\\.azure\\.com/subscriptions/[^/]+/resourceGroups/bastet-visible/providers/Microsoft\\.Network/virtualNetworks/rig-34-v6r-(gone|keep/subnets/snet-b)","nth":"all","action":"status","status":503}]`; (8) `work/v6r/scan.py http://127.0.0.1:22321`, then `work/v6r/browser_scan.py http://127.0.0.1:22321` (Chromium: /Azure/Reconcile, select the subscription, click #rec-scan-btn, read #rec-scan-warnings-list li, screenshot); (9) the same rule with status 429 + Retry-After 1: scan.py, then `work/v6r/delete_try.py` (POST /Subnet/BulkDeleteStaleAzureSubnets, confirmation "approved", ids 1,2,5 with the clean scan's verdicts); (10) faults.json = [] then scan.py again (post-fault control). Came back: import HTTP 200, "created 2 VNet target subnet(s), created 3 Azure child subnet(s)"; rows 1 rig-34-v6r-gone 10.138.16.0/20, 2 snet-g 10.138.17.0/24, 3 rig-34-v6r-keep 10.138.32.0/20, 4 snet-a 10.138.33.0/24, 5 snet-b 10.138.34.0/24; baseline scan: no items and no warnings; ground truth after the Azure deletes: direct ARM GETs returned 404 ResourceNotFound for rig-34-v6r-gone, 404 for rig-34-v6r-gone/subnets/snet-g and 404 NotFound for rig-34-v6r-keep/subnets/snet-b, while snet-a returned 200; positive control on unmodified HEAD (port 22320): the scan offered id=1 VNetDeleted, id=2 SubnetDeleted and id=5 SubnetDeleted, with no warnings. Faulted scan, 503: HTTP 200 in 6.7 s, 12 injections (3 resources x 4 SDK attempts), items empty, and the only warning was "3 Azure-linked subnet(s) were missing from the subscription listing, and Azure could not be asked about them - the read failed rather than answering. Nothing is wrong with the subnet itself; try the scan again. They have been withheld from deletion: 'rig-34-v6r-gone' (10.138.16.0/20), 'snet-g' (10.138.17.0/24), 'snet-b' (10.138.34.0/24)." Browser: title "Azure Reconcile - BASTET"; #rec-scan-warnings visible with exactly that li; #rec-stale-section and #rec-nothing-stale hidden; the screenshot confirms the warning under "Check this before deleting anything". 429 throttling: 12 injections and the identical warning; the delete re-check under the same 429 returned HTTP 409 "3 of the selected subnet(s) are no longer offered for deletion by the latest re-check, so nothing was deleted. Re-run the scan and review the results and any warnings shown.", whose warnings[] carried the same sentence (showCommitError renders warnings as list items); the database still had 5 live rows and 0 archived. Post-fault control, rules []: ids 1, 2 and 5 were offered again as VNetDeleted/SubnetDeleted, with no warnings, so the sentence was false at the moment it was shown. Cleanup: both instances stopped by PID, all rig-34-v6r-* VNets deleted, catalog a34_v6r_1 dropped, inventory lines left for teardown; a backgrounded subshell wrote a one-line del-snetb.log into the repository root, which was gitignored and moved out within a minute (git status --porcelain empty).
**Fix:** Both verifiers judged the fix sound and narrow: it only deletes words and adds, widens or branches no mechanism. In the Unknown warning at src/Bastet/Services/Azure/AzureReconciler.cs:184-186, delete "Nothing is wrong with the subnet itself; " (line 185) and capitalise the next word, "try" to "Try" (line 186; the finder's quoted result already shows "Try" but cited only :185, so both lines change). The warning then reads: "{n} Azure-linked subnet(s) were missing from the subscription listing, and Azure could not be asked about them - the read failed rather than answering. Try the scan again. They have been withheld from deletion: {names}." Change nothing else: the Unknown bucket, the withhold, its cascade handling and the rescan remedy stay as they are. Nothing parses warnings: the scan renders them as li text, the delete 409 passes plan.Warnings through unchanged, and VerdictMatchesApproval compares item Reason, not Warnings; the string exists nowhere else in src/ (SignInFailed.cshtml's "Nothing is wrong with your account" is unrelated), so the one change covers both the scan panel and the delete-409 list (SubnetController.AzureReconcile.cs:106, rendered by showCommitError), which come from the same source line. The remaining text stays true on every path into the Unknown bucket that can reach ApplyConfirmations (5xx or 429 after retries, a transport failure, a timeout, or any other RequestFailedException status); the null-client Unknown and the unparsable-id Unknown cannot reach it (a null client makes GetVNetInventory fail, so BuildPlan returns with no items and the delete path returns 400 first; an id that is neither a VNet nor a subnet is filed as UnrecognisedResourceId before confirmation), and "Try the scan again" is a remedy the operator can reach from the same screen, so no conditional is needed, because the clause is never established by anything Bastet observed. It serves PRODUCT-MODEL §3 ("It reports; the operator decides. Silently withholding the report is its own defect") and Rule 0 / the symmetric half of rule 1: the message must not assert a resource state Azure did not report; the finder's §1 rule 3 contract is the weaker fit, since the named rescan remedy is real and reachable. This matches the precedents 23-L1 and 23-L4, which removed existence claims Azure had not made. No interim; no product question.
**Residue of:** round 6 (0de1293 "Audit Cleanup 6" (#143) introduced the clause when it split the Unknown warning out of the NotVisible one; git blame on AzureReconciler.cs:181-186 and `git log -S 'Nothing is wrong with the subnet itself' -- src/` both find only 0de1293; per-finding ledger ids begin at round 18, so this is cited by round)

## L7 — When the in-lock host IP lookup fails, host IP delete sends its 'could not confirm' banner to All Host IPs, which never shows it; it appears later on an unrelated page `[x1]` — FIXED
_Fixed in "Audit 34 L7: show the stranded host-IP delete banner on All Host IPs". All Host IPs now renders the shared TempData alerts partial above its list, as All Deleted Host IPs already does._
_Swept: every TempData write in the controllers lands on a page that renders it; the in-lock lookup fallback in HostIpController is the only redirect to All Host IPs; no controller change._
_Verified: AllHostIpsTempDataTests red on the unfixed view and for the line deleted, moved into either branch, Razor- or HTML-commented, or gated; live: a schema lock timing out the in-lock lookup lands on All Host IPs with the banner above the count and the next page clean; the archive-lock control unchanged; suite green, 0 warnings._
_Reviewed: (c) preference, no failure found (19 other redirect flows showed no stray or duplicate alert); its whole-line pin tightening adopted._
_Not done: when the IP sorts past row 50 the banner lands on page 1 without that row; the redirect-to-Delete alternative the finding weighed would avoid it and was not taken._

## L8 — Bulk import wizard describes adopting an unlinked Bastet row as adding or importing subnets and never says the row will be linked, which is the only change the commit makes `[x1]` `strings`
**Where:** src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:240, src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:241
**Breaks:** An operator builds hand-a 10.34.0.0/16 by hand, with hand-made children hand-x 10.34.1.0/24 and hand-y 10.34.2.0/24, all unlinked and matching Azure VNet rig-34-base-simple exactly (snet-a 10.34.1.0/24, snet-b 10.34.2.0/24): the standard Bastet-first adoption PRODUCT-MODEL §4 describes ("A row the operator built by hand can be adopted by the VNet it matches"). An Admin opens Bulk Azure Import. The prefix row is badged 'Will update existing' and its reason reads "Will add any missing subnets to existing Bastet subnet 'hand-a'. Subnets already imported are left untouched." (AzureBulkImportPlanner.cs:240-241), while both Azure subnets are "Cannot import" ("Bastet subnet 'hand-x' already uses 10.34.1.0/24.", the same for hand-y); with 'Only show what would change' ticked, the same sentence sits directly above "2 subnet(s) in this prefix cannot be imported, and are hidden. Untick "Only show what would change" to see why." Ticking the prefix previews only 'Exact match Bastet subnet "hand-a" (10.34.0.0/16) / No child subnets selected.' (Continue enabled), and the commit reports "Bulk import succeeded: created 0 VNet target subnet(s), created 0 Azure child subnet(s), renamed 0 target(s), renamed 0 child subnet(s), linked 1 existing target(s) to Azure, and marked 0 target(s) as fully allocated." Nothing was added: hand-a now carries rig-34-base-simple's resource id while hand-x and hand-y stay NULL (7 rows before and after), its Edit page now says "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it." with the CIDR no longer editable, and re-running the wizard on the same Azure data describes the prefix as "Already imported as Bastet subnet 'hand-a'. Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add." (the filter then hides the card). The reason described a top-up of an imported VNet for what was the first import (adoption) of an operator-built row: it promised subnet additions that could not happen, called the operator's hand-made children "already imported", and never said the row would be linked, the only change the commit makes. The Select-step legend ("Will update existing can be selected — it imports into a BASTET subnet you already have.") announces adoption only in general terms; the defect is that this row's own reason says something else. The same sentence misdescribes partial adoption: hand-m 10.43.0.0/16 with hand-made child hand-m5 10.43.5.0/24 (or hand-m0 10.43.0.0/24) against rig-34-base-many gets it too; ticking the prefix plus snet-00 previews 'Exact match ... / Create snet-00 10.43.0.0/24', and the commit "created 1 Azure child subnet(s) ... linked 1 existing target(s)", linking hand-m, again unannounced. Round 14 (8afa2df) wrote the with-children sentence only for linked top-ups, while an unlinked exact row with children was still Blocked; round 17 deleted that refusal and left the reason alone, so the unlinked row now gets the linked-row sentence. By contrast, the unlinked fully-allocated branch (:222) says "Will link existing Bastet subnet 'hand-fa' to this VNet. It stays marked fully allocated, so no subnets will be created inside it." This breaks §4: the wizard must not advertise work that is not work, the work actually offered for an unlinked target is "linking a target that is not yet linked", and §4 says the "Will add any missing subnets" sentence is how the identical shape with the link already present is advertised. Not part of the finding (both verifiers struck it as a site): the childless sentence at :242, "Will import into existing Bastet subnet 'X'." (e.g. 'Operator block DO NOT RENAME' over "No IPv4 subnets in this prefix." against rig-34-base-empty, or hand-enc against rig-34-base-encompass), is the pre-audit first-import adoption wording that round 14 deliberately kept for first imports, is defined by the Select legend, and is true, since the VNet prefix is imported into that row and the row is afterwards shown as "Imported from Azure"; and :238 (Status=WillUpdateExisting) is correct, because §4 counts linking as work. The consequence is an untrue message; no address space is lost.
**Repro:** Truth verifier ran, with `RIG=/tmp/claude-1000/-home-anuj-code-Bastet/3b511843-ebe5-46df-a19d-01e22a4bcd59/scratchpad/rig; SUB=$($RIG/bin/sub-id.sh)`: `$RIG/bin/start-app.sh v11t 22600 a34_v11t_1 real visible` (real HEAD tree, visible principal); `$RIG/azcli/bin/python $RIG/work/v11t/build_hand.py http://127.0.0.1:22600` (hand-built rows via the parser-harvested /Subnet/Create form: hand-a 10.34.0.0/16 with hand-x 10.34.1.0/24 and hand-y 10.34.2.0/24 under it; hand-m 10.43.0.0/16 with hand-m5 10.43.5.0/24; 'Operator block DO NOT RENAME' 10.42.0.0/16; hand-fa 10.44.0.0/16 marked fully allocated via the Details SetAllocationStatus form); `curl -s "http://127.0.0.1:22600/Azure/BulkGetVNets?subscriptionId=$SUB"` (the planner's statusName/reason per prefix and subnet); `$RIG/azcli/bin/python $RIG/work/v11t/drive_wizard.py http://127.0.0.1:22600 $SUB rig-34-base-simple,rig-34-base-many,rig-34-base-empty,rig-34-base-delegated` (Playwright Chromium: select the subscription, capture each card with 'Only show what would change' off, then on); `drive_wizard.py ... rig-34-base-simple 10.34.0.0/16 "" commit` (tick the prefix only, then preview, commit and read the banner); `drive_wizard.py ... rig-34-base-simple` (re-run after the link, the contrast); `curl -s http://127.0.0.1:22600/Subnet/Edit/1` (hand-a after the link); `drive_wizard.py ... rig-34-base-empty 10.42.0.0/16 "" commit`; `build_enc.py` (hand-enc 10.39.0.0/24, no children) then `drive_wizard.py ... rig-34-base-encompass 10.39.0.0/24 10.39.0.0/24 commit`; `drive_wizard.py ... rig-34-base-many 10.43.0.0/16 10.43.0.0/24 commit`; `$RIG/bin/sql.sh a34_v11t_1 "SELECT Id, Name, NetworkAddress, Cidr, ParentSubnetId, IsFullyAllocated, AzureResourceId FROM Subnets ORDER BY Id"`; teardown: stop-app.sh 22600, catalog dropped, no Azure resources created, repo `git status --porcelain` empty. Came back: before commit, all seven hand-made rows had AzureResourceId NULL (SQL). rig-34-base-simple, filter off: the prefix card shows the badge 'Will update existing' and the reason "Will add any missing subnets to existing Bastet subnet 'hand-a'. Subnets already imported are left untouched."; its subnets read "snet-a 10.34.1.0/24 Cannot import / Bastet subnet 'hand-x' already uses 10.34.1.0/24." and "snet-b ... 'hand-y' already uses 10.34.2.0/24.". Filter on: the same reason sits directly above "2 subnet(s) in this prefix cannot be imported, and are hidden. Untick "Only show what would change" to see why." Preview: 'VNet "rig-34-base-simple" — prefix 10.34.0.0/16 / Exact match Bastet subnet "hand-a" (10.34.0.0/16) / No child subnets selected.', Continue enabled. Commit: "Bulk import succeeded: created 0 VNet target subnet(s), created 0 Azure child subnet(s), renamed 0 target(s), renamed 0 child subnet(s), linked 1 existing target(s) to Azure, and marked 0 target(s) as fully allocated." SQL afterwards: hand-a AzureResourceId=.../virtualNetworks/rig-34-base-simple; hand-x and hand-y NULL; row count 7 before and 7 after. /Subnet/Edit/1: the CIDR input is now type=hidden and the page reads "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it." Re-run after the link: the same prefix is disabled, reading "Already imported as Bastet subnet 'hand-a'. Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add."; the filter hides the card. Contrast, delegated (hand-fa unlinked and fully allocated): "Will link existing Bastet subnet 'hand-fa' to this VNet. It stays marked fully allocated, so no subnets will be created inside it." Partial adoption, many (hand-m with hand-m5): the same with-children sentence; snet-05 is Cannot import and 9 subnets are Available; ticking the prefix plus snet-00 previews 'Exact match ... / Create snet-00 10.43.0.0/24'; commit: "created 1 Azure child subnet(s) ... linked 1 existing target(s)"; hand-m linked, hand-m5 NULL, snet-00 created under hand-m; the link was again never announced. Shape 2, empty: "Will import into existing Bastet subnet 'Operator block DO NOT RENAME'." over "No IPv4 subnets in this prefix."; commit: "linked 1". Encompass (hand-enc unlinked, no children): the prefix reads "Will import into existing Bastet subnet 'hand-enc'." and snet-all reads "Covers the whole VNet prefix, so it marks the target fully allocated instead of being created."; ticking both previews "— will be marked fully allocated"; commit "created 0 ... linked 1 ... marked 1 target(s) as fully allocated"; hand-enc IsFullyAllocated=1 and carries the note. Reach verifier ran, with the same RIG: (1) `$RIG/bin/start-app.sh v11r 22620 a34_v11r_1 real visible`; (2) `$RIG/work/v11r/create.py` submitted the harvested /Subnet/Create form for hand-a 10.34.0.0/16 (top level), hand-x 10.34.1.0/24 and hand-y 10.34.2.0/24 under hand-a, and 'Operator block DO NOT RENAME' 10.42.0.0/16, plus contrast rows hand-enc 10.39.0.0/24, hand-fa 10.44.0.0/16 (marked fully allocated via the harvested Details SetAllocationStatus form) and hand-m 10.43.0.0/16 with hand-m0 10.43.0.0/24 under it; all rows unlinked and matched against the read-only base fixtures rig-34-base-simple/-empty/-encompass/-delegated/-many, no Azure resources created; (3) `curl /Azure/BulkGetVNets?subscriptionId=$($RIG/bin/sub-id.sh)`; (4) `$RIG/work/v11r/wizard.py` (Playwright Chromium) against /Azure/BulkImport: picked the subscription, captured the Select-step legend and the 10.34.0.0/16 row with 'Only show what would change' off and then on, ticked only that prefix, previewed, clicked Continue to Commit and Confirm Import; (5) `sql.sh a34_v11r_1 "SELECT Id,Name,NetworkAddress,Cidr,ParentSubnetId,AzureResourceId,IsFullyAllocated FROM Subnets"`; (6) GET /Subnet/Edit/1; (7) re-ran BulkGetVNets; cleanup: stop-app.sh 22620, then DROP DATABASE a34_v11r_1. Came back: Select-step legend: "Will update existing can be selected — it imports into a BASTET subnet you already have." Row with the toggle off: "rig-34-base-simple 10.34.0.0/16 [Will update existing] Will add any missing subnets to existing Bastet subnet 'hand-a'. Subnets already imported are left untouched.", below it "snet-a 10.34.1.0/24 [Cannot import] Bastet subnet 'hand-x' already uses 10.34.1.0/24." and snet-b the same for hand-y. With the toggle on, the same reason sits directly above "2 subnet(s) in this prefix cannot be imported, and are hidden. Untick "Only show what would change" to see why." Preview: 'Exact match Bastet subnet "hand-a" (10.34.0.0/16) / No child subnets selected.' Commit banner: "Bulk import succeeded: created 0 VNet target subnet(s), created 0 Azure child subnet(s), renamed 0 target(s), renamed 0 child subnet(s), linked 1 existing target(s) to Azure, and marked 0 target(s) as fully allocated." SQL: hand-a AzureResourceId = /subscriptions/<sub>/resourceGroups/bastet-visible/providers/Microsoft.Network/virtualNetworks/rig-34-base-simple; hand-x and hand-y NULL. Edit/1 now reads "Imported from Azure, so the prefix is fixed here. Change it in Azure, then ask an administrator to re-import it." Rediscovery over the same Azure data: AlreadyImported, "Already imported as Bastet subnet 'hand-a'. Every Azure subnet in this prefix is either already recorded or cannot be imported, so there is nothing to add." Shape 2 (rig-34-base-empty): "Will import into existing Bastet subnet 'Operator block DO NOT RENAME'." over "No IPv4 subnets in this prefix." Contrasts: hand-fa (unlinked, fully allocated) reads "Will link existing Bastet subnet 'hand-fa' to this VNet. It stays marked fully allocated, so no subnets will be created inside it."; hand-enc (unlinked, no children) against rig-34-base-encompass: the prefix reads "Will import into existing Bastet subnet 'hand-enc'." and snet-all is Available with "Covers the whole VNet prefix, so it marks the target fully allocated instead of being created."; hand-m against rig-34-base-many: the same "Will add any missing subnets... Subnets already imported are left untouched." sentence, with snet-00 Blocked (hand-m0) and snet-01..09 Available.
**Fix:** Both verifiers judged the filed fix unsound. The filed fix (b5p2-1) branches the whole L238-242 reason on isTopUp, so for every unlinked row, including unlinked rows without children at :242, the reason would read "Will link existing Bastet subnet 'X' to this VNet. Its existing subnets are left as they are; any Azure subnet you tick is created under it."; that trailing clause is false for an unlinked childless row whose VNet has a subnet covering the whole prefix (driven live: hand-enc 10.39.0.0/24 against rig-34-base-encompass, where snet-all 10.39.0.0/24 is Available with "Covers the whole VNet prefix, so it marks the target fully allocated instead of being created." and committing it gave "created 0 ... linked 1 ... marked 1 target(s) as fully allocated"), so it would contradict that subnet reason on the same card, a new untrue message; "Its existing subnets are left as they are" is vacuous for a row with no children; and it rewrites the :242 sentence, which was never defective and which already fits (the fix is wider than the defect). The finder's premise that !isTopUp at this point means the row is unlinked is correct (VNet ids come from ARM vnet.Id, IsSameResourceId is false on null, and a row linked elsewhere has already returned Blocked at L190-197), and the finder's own interim (change only the with-children sentence for unlinked rows, drop "already imported", name the link) is the sound narrow fix. The verifiers' corrected fixes differ; both are message conditionals on the existing isTopUp that add no status, field, selectability, plan field or client change, and preview and commit are unaffected because DescribeApprovedPlanDivergences does not compare reasons. Corrected fix (truth verifier; names the link): in AnnotatePrefix keep the top-up sentence for linked rows only, name the link for unlinked rows that have children, and leave the childless arm (:242) unchanged: `result.Reason = !exact.HasChildSubnets ? $"Will import into existing Bastet subnet '{exact.Name}'." : isTopUp ? $"Will add any missing subnets to existing Bastet subnet '{exact.Name}'. Subnets already imported are left untouched." : $"Will link existing Bastet subnet '{exact.Name}' to this VNet. Its existing subnets are left untouched, and any Azure subnet you tick is created inside it.";`. Its trailing clause is true only in this arm: a subnet covering the whole prefix is Blocked when the row has children (L313-320), and middle-man and containing subnets are Blocked, so every tickable subnet is created directly under the row; it mirrors the existing link-only adoption sentence at :222 and serves §4. Cheaper interim, which the reach verifier proposes as the complete fix: a one-condition change at :240 that reuses an existing string and adds none, `result.Reason = isTopUp && exact.HasChildSubnets ? $"Will add any missing subnets to existing Bastet subnet '{exact.Name}'. Subnets already imported are left untouched." : $"Will import into existing Bastet subnet '{exact.Name}'.";` (isTopUp is already in scope at L199), so an unlinked row, with or without children, shows the wizard's existing adoption sentence, which already appears for unlinked childless rows, which the Select legend defines ("imports into a BASTET subnet you already have"), and which is true in every unlinked case, including an encompassing Azure subnet. It removes the false "add any missing subnets" and "already imported" claims; the truth verifier treats it as a fallback only, because it still leaves the link unnamed above "2 subnet(s) in this prefix cannot be imported", while the reach verifier prefers it for not adding a new string where an existing, legend-defined one already fits. Product question: the preview's Exact match line (src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:559-564) and BulkImportPlanItem carry no link flag, so the preview shows 'Exact match Bastet subnet "X"' the same way for an adoption and a top-up and never says a row will be linked; should it? Not proposed: it would need a new plan field, a mechanism the §8 31-L2 standing instruction presumes wrong.
**Residue of:** round 17 (f4a0a87, "Let a hand-built Bastet subnet be adopted by the VNet it matches", deleted the refusal that Blocked unlinked exact rows with children ("already has child subnets and is not linked to this VNet. Already imported?") and routed those rows to the with-children sentence round 14 (8afa2df) wrote only for linked top-ups, without rewording it; both verifiers corrected the finder's "round 14")

## L9 — Edit conflict message says 'Reload the page to see the current values', but reloading resubmits the stale form and returns the identical refusal; for host IPs the page's URL answers 404 `[x1]` `strings`
**Where:** src/Bastet/Controllers/SubnetController.Edit.cs:266, src/Bastet/Controllers/HostIpController.cs:240, src/Bastet/Controllers/HostIpController.cs:286, src/Bastet/Views/HostIp/Edit/_SubnetInfo.cshtml:37, src/Bastet/Views/HostIp/Edit/_EditForm.cshtml:8
**Breaks:** An Edit-role operator A opens /Subnet/Edit/1 ('reload-test', 10.220.66.0/24); another session, a second Edit-role operator or the same operator in a second tab, renames it to 'renamed-by-other'; A then saves a Description change. A gets 200 at /Subnet/Edit/1 with "This subnet was modified by another user while you were editing it, so it was not saved. Stored values that differ from this form: Name is now 'renamed-by-other'; Description is now empty. Reload the page to see the current values, then re-apply the changes that still make sense." The redisplay is a POST result that keeps the posted RowVersion, failing closed by design since 18-R3. Doing what it says (the browser's reload) re-sends POST /Subnet/Edit/1 with the same stale RowVersion (a headed browser asks to confirm the resubmission first; headless-shell and full Chromium re-POST silently, which is what pressing Continue does) and returns the identical refusal, with #Name still 'reload-test'; the operator can loop on this forever. The host IP case is the same (10.220.67.9 'original-name'; another session sets 'changed-by-other'; A saves 'my-name'): reload re-POSTs /HostIp/Edit and returns the identical "This host IP was modified by another user while you were editing it, so it was not saved. Reload the page to see the current values, then re-apply the changes that still make sense." with #Name still 'my-name', and the URL the page shows, /HostIp/Edit, answers 404 "Not Found - BASTET" ("Resource Not Found") when opened by GET, because the form at _EditForm.cshtml:8 posts without ?ip=. The sidebar's standing hint on that page, "Concurrent edits to this host IP by other users will be detected. If another user has modified this record, you'll need to reload and try again." (_SubnetInfo.cshtml:37), names the same step that does not work. What works is Cancel, then Edit, on both pages; for subnets only, re-entering /Subnet/Edit/1 in the address bar (a GET) also loads the current values, so the subnet half fails only when "reload" means the browser's reload control, while the host IP half fails both ways; browser Back re-fetches the GET page, but Chromium restores the operator's typed values over the fresh ones. The messages name neither working path. Nothing is written, because the redisplay fails closed; the operator simply cannot act on the remedy the message names. This breaks PRODUCT-MODEL §1 rule 3 ("The operator must be able to act on what they are told.") and the §5 bullet "Check who can reach the remedy a message names".
**Repro:** Truth verifier ran, with `RIG=/tmp/claude-1000/-home-anuj-code-Bastet/3b511843-ebe5-46df-a19d-01e22a4bcd59/scratchpad/rig`: `$RIG/bin/start-app.sh v12t 22660 a34_v12t_1 real none` (real tree, whose src/ is byte-identical to the repo's src/ at b17adb8); in $RIG/work/v12t, `$RIG/azcli/bin/python seed.py` (harvested forms: subnet 1 'reload-test' 10.220.66.0/24, subnet 2 'hostip-parent' 10.220.67.0/24, host IP 10.220.67.9 'original-name'); `$RIG/azcli/bin/python drive_subnet.py` (Chromium opens Edit; a second requests session saves a rename; the browser saves; then page.reload(), an address-bar GET, and Cancel -> Edit); `$RIG/azcli/bin/python drive_hostip.py` (the same for the host IP, plus a GET of the shown URL and a positive control with ?ip=); `$RIG/azcli/bin/python drive_back.py` (browser Back after a conflict, on both pages); `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:22660/HostIp/Edit`; `$RIG/bin/stop-app.sh 22660; $RIG/bin/sql.sh master "ALTER DATABASE [a34_v12t_1] SET SINGLE_USER WITH ROLLBACK IMMEDIATE; DROP DATABASE [a34_v12t_1];"`. Came back: SUBNET 1: the second session's POST /Subnet/Edit/1 returned 302 (Name='renamed-by-other'); the browser saved a Description change and got 200 from POST /Subnet/Edit/1 with the summary "This subnet was modified by another user while you were editing it, so it was not saved. Stored values that differ from this form: Name is now 'renamed-by-other'; Description is now empty. Reload the page to see the current values, then re-apply the changes that still make sense."; page.reload() sent ['POST http://127.0.0.1:22660/Subnet/Edit/1'] and got 200; the summary was identical (True), #Name was still 'reload-test', and the stored row did not change (Name renamed-by-other, RowVersion 0x...07D4 before and after); an address-bar GET of /Subnet/Edit/1 returned 200 with #Name 'renamed-by-other', so for subnets that route does work. HOST IP 10.220.67.9: the form's action attribute is '/HostIp/Edit' (no ?ip=); the second session's POST returned 302 (Name='changed-by-other'); the browser saved 'my-name' and got 200 from POST at URL /HostIp/Edit with the summary "This host IP was modified by another user while you were editing it, so it was not saved. Reload the page to see the current values, then re-apply the changes that still make sense."; page.reload() sent ['POST http://127.0.0.1:22660/HostIp/Edit'] and got 200; the summary was identical (True) and #Name was still 'my-name'; a GET of the shown URL returned 404, title 'Not Found - BASTET', body 'Resource Not Found', and curl got the same 404; the sidebar hint reads "If another user has modified this record, you'll need to reload and try again."; positive control: GET /HostIp/Edit?ip=10.220.67.9 returned 200 with #Name 'changed-by-other'. PROPOSED-FIX PATHS: subnet: Cancel went to GET /Subnet/Details/1, then Edit loaded #Name 'renamed-third-by-other', and the re-applied save returned 302 and the database holds Tags 'mytag'; host IP: Cancel went to GET /HostIp?subnetId=2, then the row's Edit loaded #Name 'changed-again-by-other', and the re-applied save returned 302 and the database holds Name 'my-name'. BACK: after a conflict, Back re-fetches the GET page, but Chromium puts the operator's typed values back over the fresh ones (subnet #Description and host IP #Name both showed 'back-test-mine'). Reach verifier ran, with the same RIG: `$RIG/bin/start-app.sh v12r 22680 a34_v12r_1 real none`; `$RIG/azcli/bin/python $RIG/work/v12r/drive.py http://127.0.0.1:22680` (sets up the subnet and host IP through the harvested Create forms; headless-shell Chromium is operator A, a second requests.Session is the other user; logs every document request the reload sends); `$RIG/azcli/bin/python $RIG/work/v12r/drive_full.py http://127.0.0.1:22680 1` (the same subnet conflict and reload in full Chromium 153, new headless); `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:22680/HostIp/Edit ; curl -s -o /dev/null -w '%{http_code}' 'http://127.0.0.1:22680/HostIp/Edit?ip=10.212.200.9'`; `$RIG/bin/stop-app.sh 22680 ; $RIG/bin/sql.sh master "ALTER DATABASE [a34_v12r_1] SET SINGLE_USER WITH ROLLBACK IMMEDIATE; DROP DATABASE [a34_v12r_1];"`. Came back: SUBNET (id 1, 'reload-test' 10.212.200.0/24): the other session sent POST /Subnet/Edit/1 with Name='renamed-by-other' and got 302 /Subnet/Details/1; the browser then changed Description and clicked Save: POST /Subnet/Edit/1 returned 200 'Edit Subnet - BASTET' with the alert "This subnet was modified by another user while you were editing it, so it was not saved. Stored values that differ from this form: Name is now 'renamed-by-other'; Description is now empty. Reload the page to see the current values, then re-apply the changes that still make sense."; page.reload() sent exactly [('POST', '/Subnet/Edit/1')], with RowVersion in the body, and returned 200 with the identical alert (compared equal) and #Name still 'reload-test'; full Chromium 153 did the same (its reload sent POST /Subnet/Edit/1 and got the identical alert); controls: a GET of /Subnet/Edit/1 (address-bar Enter) showed #Name 'renamed-by-other', and Cancel -> /Subnet/Details/1 -> Edit showed the current values. HOST IP 10.212.200.9: the GET form's action is '/HostIp/Edit', with no ?ip=; the other session sent POST /HostIp/Edit with Name='changed-by-other' and got 302; the browser saved Name='my-name': POST returned 200 at URL http://127.0.0.1:22680/HostIp/Edit with the alert "This host IP was modified by another user while you were editing it, so it was not saved. Reload the page to see the current values, then re-apply the changes that still make sense."; the sidebar on the same page read "Concurrent edits to this host IP by other users will be detected. If another user has modified this record, you'll need to reload and try again."; page.reload() sent [('POST', '/HostIp/Edit')] and got the identical alert, with #Name still 'my-name'; GET /HostIp/Edit returned 404 'Not Found - BASTET', h1 'Resource Not Found', in both the browser and curl; /HostIp/Edit?ip=10.212.200.9 returned 200; control: Cancel -> /HostIp?subnetId=1 -> Edit -> /HostIp/Edit?ip=10.212.200.9 showed #Name as the current stored value; conflict responses carry Cache-Control: no-store,no-cache.
**Fix:** Both verifiers judged the wording fix sound: only strings change, it adds, widens or branches no mechanism, and it leaves 18-R3's deliberate fail-closed redisplay, the posted RowVersion and the form action as they are. At each site replace the reload instruction with a step that loads a fresh form by GET: src/Bastet/Controllers/SubnetController.Edit.cs:266, replace "Reload the page to see the current values, then re-apply the changes that still make sense." with "Use Cancel, then Edit, to load the current values, then re-apply the changes that still make sense."; src/Bastet/Controllers/HostIpController.cs:240 and :286 (keep them one identical string), "Use Cancel, then Edit on this host IP, to load the current values, then re-apply the changes that still make sense."; src/Bastet/Views/HostIp/Edit/_SubnetInfo.cshtml:37, where the verifiers' wordings differ: the truth verifier replaces "you'll need to reload and try again" with "use Cancel, then Edit on this host IP, and try again.", the reach verifier changes the second sentence to "If another user has modified this record, use Cancel and open it again, then re-apply your change." or deletes that sentence, since the conflict message already covers it (the finder's "... use Cancel and open it again, then try again." is the same step). src/Bastet/Views/HostIp/Edit/_EditForm.cshtml:8 contributes the 404 but needs no change: once no message names a reload, nothing sends the operator to /HostIp/Edit. It serves PRODUCT-MODEL §1 rule 3 and the §5 bullet "Check who can reach the remedy a message names": both Edit actions require RequireEditRole, and the Edit controls the remedy uses, on Details (Shared/_RoleBasedActions.cshtml:8-13) and on the host IP list reached by Cancel (Views/HostIp/Index/_HostIpTable.cshtml:27-32), are gated on UserHasRole(Edit), the same hierarchy; both named paths were driven live to a successful re-applied save. Interim: none; both verifiers drop the finder's interim (`asp-route-ip="@Model.IP"` on the host IP form): it only makes an address-bar GET work, F5 still re-POSTs and the message would still say "Reload the page", so it closes nothing, it is no cheaper than the string swaps, it changes form routing the minimal fix does not need, and once the wording stops naming a reload it is "while we're here" work (§5). Leave HostIpValidationService.cs:109 ("Please reload and try again") alone: it never renders, because the controller replaces it on CONCURRENCY_CONFLICT (HostIpController.cs:234-241). HostIpController.cs:286 is the DbUpdateConcurrencyException catch, in practice unreachable (every host IP write holds the global app lock, and the save uses a freshly loaded token), but it carries the identical string, so change it with :240. Noted for whoever makes the fix, not verified and not sites of this finding (reaching them needs an indeterminate SQL failure the rig has no seam for): the indeterminate-outcome sentences at SubnetController.Edit.cs:186 and HostIpController.cs:294 ("Reload the subnet to see its current state before retrying" / "Reload the host IP to see its current state before retrying") sit on the same POST-result redisplay pages. No product question.
**Residue of:** 18-R3 (f2050fa, round 18, the 18-R3 fix, made the retry fail closed and replaced "Click 'Save Changes' again", which was true while the token was refreshed, with "Reload the page to see the current values" at both conflict messages; 6fb8557, round 20, 18-R3-rem, carried that clause into the unified Subnet message (:266) and the HostIp catch (:286); the sidebar line is pre-audit, 841c272)

# Info

None.

# Refuted — reported by a finder, killed by the verifier

| id | title | reported by | killed because |
|---|---|---|---|
| G1 | Another site can sign the operator out: /Account/Logout ends the session on a cross-site GET without an antiforgery token | b1p2-1 | Deliberate, owner-accepted behaviour, not a defect: the tokenless GET Logout is original (e05c3b1; the layout link from #35), the owner's #133 (aedd0bd) recorded logout CSRF as accepted in source ("the worst outcome is an unwanted sign-out"; a POST-only Logout would 405 external GET logout links), and rounds 10, 15 (65d1fc6, which fixed only the third-party harm and deliberately kept the signed-in self-sign-out) and 16 each edited the method and left it a tokenless GET; no PRODUCT-MODEL sentence covers session integrity and the fix adds an antiforgery guard, hardening that §5 excludes and the §8 31-L2 standing instruction presumes wrong (the truth verifier also judged the fix unsound); the operator-visible sign-out did not reproduce on the rig (truth: could not, the operator stayed authenticated and the sign-out path exists only in Production; reach: ran it, only a pending "created successfully" banner was dropped, the row still created), so Info at most |
| G13 | README still advertises the deleted per-target Azure import and other Azure UI that later fixes removed or renamed | b7p1-1, b7p2-1 | Out of scope, not a product defect: both verifiers ran it and every claim reproduces, but the only untrue text is README.md prose that the application never builds, publishes or renders (the Dockerfile copies only the publish output); at HEAD the product behaves as PRODUCT-MODEL §3 and ledger fixes 23-L5, 18-R20 and 18-R17 require (single-VNet wizard routes answer 404 and nothing offers them, the rename label reads "to their Azure names", the only cascade note counts child subnets, the unrecognised-link reason ends at what is true); a finding must be an operator-visible wrong behaviour with a src/ citation (§5, the audit skill, BRIEF §4), b7p2-1 has no src/ site and b7p1-1's src/ sites are places where the product is right; stale documentation is the §6 class; the beat wording that brought docs into scope is not in the audit skill and cannot widen the owner's definition; no ledger row has ever filed a README finding; the drift is real and cheap to fix as a README edit for the owner outside the audit loop (Low if ever ruled in scope) |
| G14 | README tells operators to 'correct or clear' an unrecognised Azure link, a remedy the product refuses | b7p2-2 | Out of scope and unreachable: every claim reproduces (truth), but the only site is README.md:220, not src/, and there is no src/ site to correct it to, because every in-product text for the state is already true (the reconcile reason at AzureReconciler.cs:68-70 fixed by 18-R17, the Needs-review explainer at Reconcile/_StepReview.cshtml:73-76, the Edit sidebar at Subnet/Edit/_InformationSidebar.cshtml:8-17) and the product does exactly what §3 prescribes (no re-link, nothing edits the Azure link); no real operator can reach the state (reach: could not), since the only writer of AzureResourceId is the bulk import commit and the wizard posts only ARM listing ids (AzureService.cs:101 and :119), so it takes a direct DB write or a hand-made Admin JSON body naming a non-VNet id; even then "clear" works (Details, then Delete, archived the rows) and "correct" works (delete, then re-import through the wizard, the §8 31-L2 operation); a Low strings one-sentence README edit if docs are ruled in scope |
