# Bastet — Round-36 Audit Findings
branch audit/round-36 / HEAD 0aa175d / test baseline 1093/1093 / 2026-09-24 / residue rate 4 of 8

> Round 36 filed 8 findings, of which 4 are residue of previous rounds' own fixes (L1 ← round 13, L4 ← 31-M1, L5 ← round 14, L7 ← round 9); 0 trace to the one fix since round 35 (34-L5, 0aa175d).

model gate (every finding re-read against docs/PRODUCT-MODEL.md §5 after verification): L6 refuted; L2's and L8's product questions, L4's and L5's alternative fixes, L1's note on an unfiled defect, and L9's SetAllocationStatus half with its new endpoint struck.

review (2026-09-25, every finding re-read against docs/PRODUCT-MODEL.md, the code at HEAD and git origins): L8 refuted; L4 regraded Medium as M2 (returned to L4, Low, at reconcile under the one-string rule); L1 residue round 13 and L5 residue round 14; L1 sites trimmed to FullyAllocatedNote.cs; L7 Wrong by re-cited to rule 3.

growth: src/Bastet/Program.cs 484 lines at 0aa175d vs 484 at 05f78d3
growth: src/Bastet/Controllers/AccountController.cs 83 lines at 0aa175d vs 83 at 05f78d3

# Critical

# High

# Medium

## M1 — After OIDC sign-in, Bastet redirects the browser to an off-site scheme-relative URL taken from the request path `[x1]` — FIXED
_Fixed in the M1 commit on audit/round-36 (residue: none): one rule, ReturnUrl.IsLocal (Url.IsLocalUrl semantics plus the header check), now decides both auth transitions: Logout applies it to returnUrl in place of its inline Url.IsLocalUrl + HttpHeaderValue.IsValid, and OidcSignIn.OnTicketReceived, the named method Program.cs now wires, keeps the id_token trimming and replaces a non-local return target with "/"._
_Swept: no IsLocalUrl call remains in src/; HttpHeaderValue.IsValid's only other caller is the frame-ancestors CSP value, a different decision; no controller issues its own Challenge with a RedirectUri; under a path base OriginalPathBase comes first, so the target stays local._
_Verified: build 0 warnings; suite 1123 → 1154 (ReturnUrlTests, OidcSignInTests); live against the stateless mock IdP over plain HTTP and, by the reviewer, over HTTPS in Chromium: the unfixed build sent //evil.example/phish (and the ///, /x/..// and /\ shapes) off-site after sign-in, the fixed build lands on / with the auth cookie; /Subnet, deep links with queries, encoded spaces and 7 KB paths are kept; Logout's 54-row shape table is identical on both builds; recorded in /e2e phase H._
_Reviewed: (c) PASS; mutations (a) to (d3) each caught by the pins that name them; the Program.cs wiring, having no unit seam, is held by the source pin and the /e2e record._
_Not done: an authenticated Logout with returnUrl "~/Subnet" answers a raw "~/Subnet" Location after the IdP callback on both builds, pre-existing and unreachable from sign-in; not part of this finding._
# Low

## L1 — Bulk import joins the fully-allocated note with a bare LF, so a description it fills to exactly 1000 characters is refused by the subnet's own Edit form `[x2]` — FIXED
_Fixed in the L1 commit on audit/round-36 (residue of round 13): FullyAllocatedNote.Append joins the note with "\r\n", the line break every form-written description already stores, and Strip trims '\r' as well as '\n', so a description the import fills to the cap is re-posted by the Edit form at the same length._
_Swept: Append and Strip are the only join and strip sites (callers SubnetController.Azure.cs:28 → SubnetController.BulkAzure.cs:374, and HostIpController.cs:706 on un-mark); Create is not a site (identical Description attribute set, never re-posts an import-written description); the Edit conflict message no longer names Description after a rename-only import over a fixed-shape row._
_Verified: build 0 warnings; suite 1093 → 1105; BulkImportDescriptionParityTests drives the real bulk commit and the real EditSubnetViewModel rule (red at HEAD: "stored 1000 characters that the Edit form re-posts as 1001") and FullyAllocatedNoteTests pins the CRLF join, the cap counted as the browser counts it, and the CR-free Strip; the reviewer's full revert and both one-edit regressions (CRLF join without the Trim; Trim without the join) go red._
_Reviewed: (c) PASS; two notes adopted: the boundary corpus no longer cuts inside a CRLF, and the legacy residual below goes into the ledger row._
_Not done: rows already stored at exactly 1000 with a bare LF are not repaired (re-import or un-mark heals them; until then the Edit conflict message may name Description for such a row); the browser's CRLF normalisation has no unit seam, so that surface is recorded in /e2e phase G, committed with L9._

## L2 — Signed-in user with no Bastet role: Access Denied's 'Return to Home', the error page's 'Return to Home' and the navbar links all lead back to a denial `[x2]` `strings` — FIXED
_Fixed in the strings batch commit on audit/round-36 (residue: none): one predicate on the existing user-context service, IsSignedInWithoutRole(role) = signed in and not UserHasRole(role), gates the links that lead to View-role pages in the three views (Access Denied's and the error layout's Return to Home, the layout's brand link and its Subnets and Host IPs menus); anonymous visitors and every role holder see exactly what they saw before._
_Swept: href="/" occurs only in the three views; SignedOut and SignInFailed link Home only for anonymous readers (the controller redirects signed-in readers); the predicate uses the same IsInRole primitive and role set as RequireViewRole, so no principal is gated one way and authorised the other; the finding's two per-view conditions were folded into the one predicate instead of shipping both._
_Verified: build 0 warnings; suite +11 (RoleGatedLinksViewSourceTests, UserContextServiceTests); live, mock IdP: the role-less reader is offered only My Roles, Logout and Go Back (0 of 5 offered links denied; brand a span), View, Edit, Delete and Admin readers' anchor lists are identical to the unfixed build, anonymous anchors identical on Access Denied, /Error/404 and Sign-in Not Completed; recorded in /e2e phase H._
_Reviewed: (c) PASS; gate-by-gate mutations M1–M7 each caught by exactly the pin that names them._

## L3 — Bulk import wizard says Azure failed when it was Bastet's own database that failed `[x1]` `strings` — FIXED
_Fixed in the strings batch commit on audit/round-36 (residue: none): the catch in BulkGetVNets, reachable only after Azure answered, now says "Failed to compare the Azure VNets with BASTET's subnets. Details have been logged."; one string._
_Swept: GetVNetInventory returns Success=false for the null client, the empty id and every ARM exception, so no Azure failure reaches the catch; the sibling catches (GetSubscriptions, BulkImportPreview, ReconcileScan, the commit, the reconcile delete's inventory gate) attribute correctly or are unreachable; the old string appears nowhere else._
_Verified: build 0 warnings; suite +1 (AzureControllerTests); live on both builds with the catalog taken offline after a real ARM read: the unfixed banner blamed Azure, the fixed one names the comparison, the log holds the SqlException at the snapshot read and no AzureService failure; under BASTET_LOG_LEVEL_DEFAULT=Error in Production the catch still logs at fail, so "Details have been logged" is true._
_Reviewed: (c) PASS; reverting the string reddens exactly the new pin._

## L4 — Key-ring startup lock misses replicas whose connection strings spell the catalog in a different case, so they mint separate keys and reject each other's forms (31-M1 back) `[x1]` — FIXED
_Fixed in the L4 commit on audit/round-36 (residue of 31-M1): the key-ring startup lock resource is the constant "Bastet:DataProtection" instead of "Bastet:DataProtection:{catalog}"; sp_getapplock scopes it to the database it is taken in, so every replica of one catalog meets on it however its connection string spells or omits the catalog, and two deployments in different catalogs never block each other (proven live, not assumed); one literal replaces an interpolation, deleting the second catalog-to-lock-name implementation._
_Swept: the same variable feeds acquire and release; the migration lock keeps its lower-cased catalog because it can fall back to master; no other applock user or document names the old resource; the 31-M1 ClearPool warm-up is untouched (a first boot against a catalog auto-migrate creates still takes the lock)._
_Verified: build 0 warnings; suite +1 (DataProtectionStartupTests); live: unfixed 2 keys in 3 of 3 mixed-case cold starts and, in a forced 87 ms window, a cross-replica POST answering 400 with the success banner dropped; fixed 1 key in 3 of 3, both replicas waiting on the one resource, every cross-post accepted; a connection string with no catalog also meets; recorded in /e2e phase H; no unit seam for top-level startup._
_Reviewed: (c) PASS; three notes adopted: the pin now binds both @Resource parameters to the variable (an acquire-only or release-only rename goes red), the /e2e record states that the 400 shows only after the two-minute post-startup key refresh window, and the severity returns to Low under the skill's one-string rule (the round-36 review had raised it to Medium as 31-M1's class)._

## L5 — Top-up prefix says already-imported subnets are left untouched while the same card offers to rename one `[x1]` `strings` — FIXED
_Fixed in the strings batch commit on audit/round-36 (residue of round 14): the top-up prefix reason in AnnotatePrefix drops its second sentence and now reads "Will add any missing subnets to existing Bastet subnet 'X'." alone, which is true whether the rename switch is on or off; no client branching on the switch._
_Swept: no other operator-facing text claims imported subnets are untouched or unchanged (src/ grep; the wizard legend and README agree with §4); the client renders the planner's reason verbatim for a top-up prefix and only concatenates on the rename-only branch; the unlinked exact-match branch still says "Will import into existing Bastet subnet 'X'.", so 34-L8's distinction stands._
_Verified: build 0 warnings; suite +1 (AzureBulkImportAdoptionReasonTests); live in the real wizard against a VNet created for it: the unfixed build showed the two-sentence reason directly above a child offering Rename only and then renamed it; the fixed build shows the one-sentence reason and renames identically (child and target halves both driven); with the switch off both builds read the same._
_Reviewed: (c) PASS; restoring the sentence reddens exactly the two pins that name it and nothing else in the 1122-test suite._

## L7 — Sign-in Not Completed offers no way out when the identity provider refuses Bastet itself: it lists user-side causes, says nothing is wrong, and its only remedy repeats the failure `[x1]` `strings` — FIXED
_Fixed in the strings batch commit on audit/round-36 (residue of round 9): SignInFailed.cshtml gains one sentence after "Nothing is wrong with your account, and nothing was changed.": "If this happens every time you try, contact your administrator.", the escalation Access Denied already uses; the cause list and the retry link stay; no branch on the failure type and no Program.cs change._
_Swept: OnRemoteFailure is the only route to the page and the controller sends authenticated readers Home, so every reader is anonymous and the sentence names nothing they cannot do; the page still claims nothing about logging; AccessDenied and the Server Error guidance keep their existing administrator wording._
_Verified: build 0 warnings; suite +1 (SignInFailedViewSourceTests); live: the fixed build renders the sentence once, in order, the unfixed build not at all; with a mock IdP refusing the token endpoint (invalid_client) three retries returned the identical page carrying the sentence, and with the refusal removed the same button signed in._
_Reviewed: (c) PASS; nine pin mutations (sentence removed, moved, duplicated, reworded, "system administrator", the log clause added, the retry link removed or moved) all red; one note not adopted: the pin's broad DoesNotContain("log") is test strictness only._

## L9 — After a lapsed sign-in, submitting a host-IP Create, Edit or Delete form lands on a 404 that says the host IP may have been deleted `[x1]` — FIXED
_Fixed in the L9 commit on audit/round-36 (residue: none): the three host-IP forms carry their identifier in the form URL (asp-route-ip on Delete and Edit, asp-route-subnetId on Create), as the subnet forms' URLs already do, so the GET that follows a re-sign-in lands on the form instead of the 404; no controller change._
_Swept: the POST actions bind the form field first (a body ip differing from the URL ip binds the body, on both builds); real delete, edit and create, every refusal and redisplay, and the stale-rowVersion refusal behave byte-identically to HEAD; the same attribute also fixes a plain GET of the URL shown after a refused redisplay; Mark as Fully Allocated (POST-only SetAllocationStatus, 405 with a working Go Back) is not a site._
_Verified: build 0 warnings; suite 1105 → 1108; live lapse drive (mock IdP, 20 s token, 35 s idle): unfixed build 404 on all three forms, fixed build 200 on each form's own page, subnet Edit control 200 on both, database unchanged on both; recorded in /e2e phase H._
_Reviewed: (c) PASS; the reviewer's M0–M9 mutations show each source pin catches a missing, moved or wrong-member route attribute and the Razor build catches a non-existent one._

# Info

# Refuted — reported by a finder, killed by the verifier

| candidate | where | killed by | reason |
|---|---|---|---|
| L6 (b2p2-c1) | src/Bastet/Services/IpUtilityService.cs:420 | model gate, after verification (truth refuted; reach and tiebreak survived) | §5: a candidate is refuted when "making it a defect needs a reading this file does not state, a convention this file does not choose, or a product question: the file has not decided it" — the finding's own product question offered three conventions for a non-block row, while §5 states the row as the block ("AddressCount == EndIp − StartIp + 1, always"; max usable "the block minus its own network and broadcast"); and when "it reads behaviour a previous round chose on purpose ... in a new way: a settled choice stands unless a sentence here contradicts it" — round 17 (f4a0a87) chose Size − 2 per row on a multi-block range. |
| L8 (b5p1-c1) | src/Bastet/Views/Azure/Reconcile/_ReconcileScripts.cshtml:352 | review against the product model, after verification (truth and reach both survived) | §5: a candidate is refuted when "making it a defect needs a reading this file does not state, a convention this file does not choose, or a product question: the file has not decided it" — the candidate's own product question asked whether the indeterminate notice ("could not confirm whether this delete was applied ... scan again") may be cleared by a new confirmation from the same scan before the operator re-scans, and 32-L1 recorded the same question open; and when "it reads behaviour a previous round chose on purpose ... in a new way: a settled choice stands unless a sentence here contradicts it" — f8d1d7a (31-L13) removed $("#rec-commit-error").addClass("d-none") from the go-confirm handler and put it in renderPlan, recorded in the ledger as "renderPlan clears the banner so only a genuinely fresh plan starts clean". No sentence contradicts it: the 503 banner's text is true and the operator can act on it. |
| b3p1-c1 | src/Bastet/Services/Azure/AzureBulkImportPlanner.cs:589 | verifiers (truth: refuted, reach: survived, tiebreak: refuted) | truth: Real behaviour but not a defect. Section 4 constrains only imported subnet rows. Placing a VNet target under the deepest container is the base IPAM rule that manual Create enforces the same way (sections 1 and 5). The in-batch overlap check is a guard between selections in one plan, not a second implementation. The fix adds a provenance-keyed refusal that 31-L2 presumes wrong. \| tiebreak: Real behaviour, not a defect. The placement is the base IPAM rule, which the Create form enforces the same way; the model's only allowed imported/manual difference is the middle-man rule, and the reproduced tree satisfies it. The re-carve case is section 3's delete-then-import loop and converges. The fix adds a refusal based on the container's origin, which sections 1 and 4 and 31-L2 rule out. |
| b5p2-c1 | src/Bastet/Views/Azure/BulkImport/_BulkScripts.cshtml:282 | verifiers (truth: refuted, reach: refuted) | truth: Not a defect: the row's 'Blocked by VNet prefix' badge, defined by the step's legend, and the 'Cannot import' reason on the prefix directly above already say why it is blocked. The planner's reason is a true description with no remedy, shown on a disabled row, and round 10 kept it on purpose. \| reach: Not a wrong behaviour. The row's "Blocked by VNet prefix" badge, the step legend that defines it, and the prefix's reason directly above all explain the block. The encompassing reason is true: driven, clearing the named conflict makes the preview mark the target fully allocated exactly as stated. No product rule is broken. |
