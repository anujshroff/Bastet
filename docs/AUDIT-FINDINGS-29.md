# Bastet — Round-29 Audit Findings

- Branch: `audit/round-29`
- HEAD: `c38d031405e9a75a53917eb9289914272cd920b9`
- Test baseline: 969 passed, 0 failed, 0 skipped, 0 build warnings
- Date: 2026-09-08
- Scope: Regression-only over 8891320..HEAD (round 28 residue 4 > 2)
- Beat structure: beats 6 and 7 × 2 passes
- Funnel: 7 raw → 5 merged (2 x2, 3 x1) → 5 survived, 0 refuted, 0 dropped at merge
- Round 29 filed 5 findings, of which 5 are residue of round 28's own fixes.
- `test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs` carries L3 and L5: 137 lines at HEAD, 101 lines at 8891320.

# Critical

None.

# High

None.

# Medium

None.

# Low

## L1 — HostIpViewSourceTests binds incidental tokens, not the rendering: a conditional wrapper on Edit/_Header brings back the empty 'Error' alert with the pin green (and a hand-rolled second rendering on Create also passes) `[x2]`

**Where:** test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:39; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:33; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:34; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:35; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:36; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:40; test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs:41

**Breaks:** PRODUCT-MODEL §5 test rule, shape "a test that stays green with the code it guards broken". For Edit the pin only requires exactly one `asp-validation-summary="ModelOnly"` in Edit/_Header.cshtml and the absence of the tokens `ModelState.ErrorCount` and `alert-heading`. Re-wrapping the summary (src/Bastet/Views/HostIp/Edit/_Header.cshtml:8) as `@if (!ViewData.ModelState.IsValid) { <div class="alert alert-danger mb-4" role="alert"><h4>...Error</h4><div asp-validation-summary="ModelOnly" class="mb-0"></div></div> }` uses neither token and re-creates 28-L6's Edit defect: submitting Name `<b>bold</b>` (a field-level [NoHtml] error, no model-level error, so the ModelOnly tag helper emits nothing) renders a red alert whose entire content is the heading "Error" above the form, while HEAD renders no alert at all. For Create the pin counts tag helpers per file and forbids one file name, so a new partial that hand-rolls `foreach (var error in ViewData.ModelState[""]!.Errors)` (the original 28-L6 shape under a different name) restores the double rendering of model-level errors and also passes; so does the codebase's own tag-helper line added to Create/_Header.cshtml or Edit/_EditForm.cshtml, files the pin never reads (every model-level error, e.g. "This IP address is already assigned" and the concurrency-conflict sentence, then renders twice). Each mutant leaves the whole suite green. Low: a contradiction visible on one screen, and 28-L6's own defects.

**Repro:** Verifier: fresh `git archive HEAD` (c38d031) copies under rig/v2-C1/{src,src-revert,src-v7,src-v19,src-v20,src-v21}, each built with `dotnet build Bastet.sln -c Debug -p:UseSharedCompilation=false` (0 warnings, 0 errors); tests via `dotnet test test/Bastet.Tests/Bastet.Tests.csproj --no-build -p:UseSharedCompilation=false [--filter "FullyQualifiedName~HostIpViewSourceTests"]`. (1) HEAD, filter: `Test run summary: Passed!  total: 1  failed: 0  succeeded: 1  skipped: 0`. (2) Full revert of Edit/_Header.cshtml, Create.cshtml and Create/_ErrorAlert.cshtml to 8891320, full suite: `Failed!  total: 969  failed: 1  succeeded: 968`; only red test `HostIpViewSourceTests.HostIpCreateAndEdit_RenderModelLevelErrorsExactlyOnce` (`Assert.Empty() Failure: Collection was not empty` at L33) — the candidate does not rest on the full revert. (3) V7, Edit/_Header.cshtml L8 `<div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>` replaced by `@if (!ViewData.ModelState.IsValid) { <div class="alert alert-danger mb-4" role="alert"> <h4><i class="bi bi-exclamation-triangle-fill me-2"></i>Error</h4> <div asp-validation-summary="ModelOnly" class="mb-0"></div> </div> }`: full suite `Passed!  total: 969  failed: 0  succeeded: 969`. V19, Create.cshtml gains `<partial name="Create/_ModelErrors" />` after the `Create/_Header` partial plus new Create/_ModelErrors.cshtml = `@if (ViewData.ModelState[""] != null && ViewData.ModelState[""]!.Errors.Count > 0) { <div class="alert alert-danger mb-4" role="alert"> @foreach (var error in ViewData.ModelState[""]!.Errors) { <div>@error.ErrorMessage</div> } </div> }`: full suite `Passed!  total: 969  failed: 0  succeeded: 969`. V20, the exact tag-helper line the codebase already uses appended to Create/_Header.cshtml and inserted directly after `<form asp-action="Edit" method="post">` in Edit/_EditForm.cshtml (mirrors _HostIpForm.cshtml:15): pin `Passed!  total: 1`, full suite `Passed!  total: 969  failed: 0`. V21, the Edit summary inside the deleted Create/_ErrorAlert's own dismissible `@if (!ViewData.ModelState.IsValid) { <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert"> ... <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> </div> }` (condition identical to Views/Azure/BulkImport.cshtml:14 and Reconcile.cshtml:14): pin green, full suite `Passed!  total: 969  failed: 0`. (4) Live, rig/v2-C1/run-app.sh (SP1, Development, BASTET_AZURE_IMPORT=true, AZURE_TOKEN_CREDENTIALS unset), ports 5340 (V7, pid 44207), 5341 (V19, pid 44211), 5342 (HEAD control, pid 44221), 5343 (V20, pid 56593), catalogs bastet_rig_v2c1_{v7,v19,head,v20}, playwright drivers rig/v2-C1/drive.py and drive2.py, JSON rig/v2-C1/drive*-*.json, screenshots rig/v2-C1/shots/. V7: POST /HostIp/Edit for 10.66.0.10 with Name `<b>bold</b>` (field-level [NoHtml], HostIpViewModels.cs:44; redisplay via HostIpController.cs:300 → RedisplayEditAsync) → `.alert-danger` = `[{"text": "Error", "html": "<div class=\"alert alert-danger mb-4\" role=\"alert\">\n        <h4><i class=\"bi bi-exclamation-triangle-fill me-2\"></i>Error</h4>\n        \n    </div>"}]`, field_errors `["HTML tags are not allowed in host names"]` (shots/v7-edit-fielderror.png: a red box reading only "Error" above the form); HEAD :5342 same POST → `.alert-danger` = `[]`, same field error (shots/head-edit-fielderror.png). V19: POST /HostIp/Create with already-assigned 10.66.0.10 (ValidateNewHostIp → AddModelError("") at HostIpController.cs:114) → two `.alert-danger`: `<div class="alert alert-danger mb-4" role="alert">\n            <div>This IP address is already assigned</div>\n    </div>` and `<div class="alert alert-danger validation-summary-errors" role="alert"><ul><li>This IP address is already assigned</li>\n</ul></div>` (shots/v19-create-modelerror.png); HEAD → exactly one (the tag helper's). V20 :5343 (drive2.py): Create with duplicate 10.67.0.10 → two identical "This IP address is already assigned" alerts (HEAD: one); Edit posted from a second tab holding a stale RowVersion (HostIpValidationService.cs:106-109 CONCURRENCY_CONFLICT → AddModelError("") at HostIpController.cs:235) → two identical "This host IP was modified by another user while you were editing it, so it was not saved. Reload the page to see the current values, then re-apply the changes that still make sense." alerts (HEAD: one); shots/v20-d2-*.png vs head-d2-*.png. All four PIDs killed afterwards, ports free. (5) Settled-gap check: docs/AUDIT-LEDGER.md:169 (28-L6) says "pinned by HostIpViewSourceTests"; brief row "none recorded; pinned by view source"; .claude/skills/e2e/SKILL.md has no record of this surface (only generic host IP create/edit request coverage ~L747-755). Not settled. Finder (b7B): the same V7 and V19 mutants (rig/b7B/mutant-V7-_Header.cshtml, mutant-V19-Create.cshtml + mutant-V19-_ModelErrors.cshtml) in rig/b7B/src: `dotnet test/Bastet.Tests/bin/Debug/net10.0/Bastet.Tests.dll --filter-class Bastet.Tests.HostIpManagement.HostIpViewSourceTests` → `Passed! total: 1 failed: 0 succeeded: 1` for each, whole dll 969/969; controls: Edit/_Header reverted to 8891320 → `Assert.DoesNotContain() Failure: Sub-string found`; Create.cshtml + Create/_ErrorAlert.cshtml restored → `Assert.Empty() Failure: Collection was not empty`; V7 live on :5370 → edit_redisplay.alerts_danger = `[{text: "Error", ...}]` with field_errors `["HTML tags are not allowed in host names"]`, HEAD on :5371 → `[]` (rig/b7B/shots/mut-edit-redisplay.png).

**Fix:** The finder's fix (pin the Edit line verbatim plus `Assert.DoesNotContain("@if", editHeader)`, and `Assert.DoesNotContain("ModelState", ...)` over every view under Views/HostIp) was judged unsound: it is still a token pin — V20, a second `asp-validation-summary="ModelOnly"` in Create/_Header.cshtml or Edit/_EditForm.cshtml, contains no `@if` and no `ModelState`, keeps the verbatim line, passes that fix entirely and doubles every model-level error live. Filed fix, test-only: pin the behaviour the row promises — exactly one model-level rendering across every view each page is composed from, delivered by one unwrapped tag helper. In test/Bastet.Tests/HostIpManagement/HostIpViewSourceTests.cs replace the Fact with this Theory (keep FindRepoRoot/ViewPath/ReadView; drop ModelOnlySummaries and the `_ErrorAlert` file-name assertions, which this subsumes):

```csharp
private static int ModelLevelRenderings(string view) =>
    Regex.Matches(view, @"asp-validation-summary=|Html\.ValidationSummary\(|ModelState").Count;

private static IEnumerable<string> PageViews(string page) =>
    Directory.GetFiles(ViewPath($"src/Bastet/Views/HostIp/{page}"), "*.cshtml")
        .Prepend(ViewPath($"src/Bastet/Views/HostIp/{page}.cshtml"));

[Theory]
[InlineData("Create", "Create/_HostIpForm.cshtml", "<div asp-validation-summary=\"ModelOnly\" class=\"alert alert-danger\" role=\"alert\"></div>")]
[InlineData("Edit", "Edit/_Header.cshtml", "<div asp-validation-summary=\"ModelOnly\" class=\"alert alert-danger mb-4\" role=\"alert\"></div>")]
public void HostIpPage_RendersModelLevelErrorsExactlyOnce_ThroughOneUnwrappedSummary(string page, string owner, string summaryLine)
{
    Assert.Equal(1, PageViews(page).Sum(view => ModelLevelRenderings(File.ReadAllText(view))));

    string ownerView = ReadView("src/Bastet/Views/HostIp/" + owner);
    Assert.Contains(summaryLine, ownerView);
    Assert.Single(Regex.Matches(ownerView, "alert-danger"));
}
```

The first assertion pins "exactly once" over Create.cshtml + Create/*.cshtml and Edit.cshtml + Edit/*.cshtml through every Razor route to model-level errors (tag helper, Html.ValidationSummary, hand-rolled ModelState reads, any ModelState-keyed wrapper); the last two pin that the single summary is the unwrapped line carrying the alert classes itself, so no outer alert box can render around an empty summary. Verified in rig/v2-C1/HostIpViewSourceTests.corrected.cs: 0 build warnings (Assert.Single avoids xUnit2013); HEAD `Passed!  total: 2  failed: 0  succeeded: 2`; full 8891320 revert `Failed!  total: 2  failed: 2`; V7 `failed: 1` (Edit row); V19 `failed: 1` (Create row); V20 `failed: 2`; V21 `failed: 1` (Edit row). No production change; no cheaper interim.

**Residue of:** 28-L6 (pin and fix both introduced by 5ea2eb5, #190: git blame HostIpViewSourceTests.cs L28-42 and Edit/_Header.cshtml L8 → 5ea2eb52; `git log -S 'ModelOnlySummaries' -- test/` → 5ea2eb5 only)

## L2 — 28-L2 redisplay pin does not bind LastModifiedAt; a helper that drops it shows "Last Modified: Never" and stays green `[x2]`

**Where:** test/Bastet.Tests/HostIpManagement/HostIpEditRedisplayTests.cs:37; test/Bastet.Tests/HostIpManagement/HostIpEditRedisplayTests.cs:75; test/Bastet.Tests/HostIpManagement/HostIpEditRedisplayTests.cs:88; test/Bastet.Tests/HostIpManagement/HostIpEditRedisplayTests.cs:101; src/Bastet/Controllers/HostIpController.cs:317

**Breaks:** PRODUCT-MODEL §5 test rule, shape "a test that stays green with the code it guards broken". RedisplayEditAsync (src/Bastet/Controllers/HostIpController.cs L315-317) restores three card fields — SubnetInfo, CreatedAt, LastModifiedAt — and Edit/_SubnetInfo.cshtml L16 prints LastModifiedAt as "Never" when null. The fixture seeds the host IP with no LastModifiedAt (L37) and the three tests assert only SubnetInfo and CreatedAt (L74-75, L87-88, L100-101), so the third term is unpinned: deleting L317 leaves all three tests (and the suite) green, while every refused Edit POST (the posted form carries no LastModifiedAt) redisplays a previously-modified host IP's Subnet Information card with "Last Modified: Never" — the blanked-card defect 28-L2 fixed, on one of its three fields, on all four redisplay returns the helper serves. Low: one screen.

**Repro:** Verifier: fresh `git archive HEAD` copies under rig/v2-C2/{src,src-revert,src-mutant,src-fixpin}, every dotnet call with `-p:UseSharedCompilation=false`; the test project runs on Microsoft.Testing.Platform (global.json), so the named filter is the MTP/xunit.v3 form `-- --filter-class`. (1) HEAD: `dotnet test test/Bastet.Tests/Bastet.Tests.csproj -p:UseSharedCompilation=false -- --filter-class Bastet.Tests.HostIpManagement.HostIpEditRedisplayTests` → `Test run summary: Passed!  total: 3  failed: 0  succeeded: 3  skipped: 0` (rig/v2-C2/logs/01-head-control-filtered.log). (2) Full revert: `git show 8891320:src/Bastet/Controllers/HostIpController.cs` written over the copy (byte-identical, RedisplayEditAsync count 0), full suite → `Failed!  total: 969  failed: 2  succeeded: 967`; red: `HostIpEditRedisplayTests.Edit_ModelStateInvalid_RedisplaysTheStoredSubnetAndCreatedAt`, `HostIpEditRedisplayTests.Edit_LockTimesOut_RedisplaysTheStoredSubnetAndCreatedAt` (Edit_StaleRowVersion stays green because 8891320's conflict branch carried its own inline reload). The candidate does not rest on the full revert (logs/02). (3) Mutant (rig/v2-C2/mutate.py), `diff src/.../HostIpController.cs src-mutant/.../HostIpController.cs` = `317d316` / `<         viewModel.LastModifiedAt = hostIp.LastModifiedAt;`; full suite in the mutant copy → `Test run summary: Passed!  total: 969  failed: 0  succeeded: 969  skipped: 0` (logs/03-mutant-drop-L317-fullsuite.log). (4) Live: mutant Bastet.dll started via rig/v2-C2/run-app.sh (Development, DevAuthHandler, BASTET_AUTO_MIGRATE=true) on http://127.0.0.1:5372, catalog bastet_rig_v2c2, pid 43519; rig/v2-C2/drive.py (playwright) through the product's own forms: POST /Subnet/Create "v2c2-hosts" 10.72.0.0/24 → /Subnet/Details/1; POST /HostIp/Create?subnetId=1 IP 10.72.0.5 Name "printer" → /HostIp?subnetId=1; GET /HostIp/Edit?ip=10.72.0.5 card Last Modified "Never"; POST /HostIp/Edit rename to "printer-renamed" → redirect (sqlcmd on bastet_rig_v2c2: Name printer-renamed, CreatedAt 2026-09-09T02:48:55.2382746, LastModifiedAt 2026-09-09T02:48:56.5006188, ModifiedBy dev@example.com); GET /HostIp/Edit?ip=10.72.0.5 → card Subnet "v2c2-hosts (10.72.0.0/24)", Created "9/9/2026 2:48 AM", Last Modified "9/9/2026 2:48 AM"; then Name `<b>bad</b>`, Save Changes → POST /HostIp/Edit 200, field error "HTML tags are not allowed in host names", card Subnet "v2c2-hosts (10.72.0.0/24)", Created "9/9/2026 2:48 AM", Last Modified "Never" — rig/v2-C2/evidence/mutant-2-post-redisplay.html L135-136 `<dt class="col-sm-4">Last Modified</dt>` / `<dd class="col-sm-8">Never</dd>`, evidence/mutant-2-post-redisplay.png. Killed pid 43519. HEAD build (rig/v2-C2/src) on the same port and catalog, pid 46052: identical refused POST → 200, same field error, card Last Modified "9/9/2026 2:48 AM" (evidence/head-2-post-redisplay.html L136 `<dd class="col-sm-8">9/9/2026 2:48 AM</dd>`, .png). Killed pid 46052; 5372 free. Reachability: /HostIp/Edit?ip=..., role Edit (RequireEditRole), no feature flag; any host IP edited once before plus any refusal; the dropped line sits in the shared helper so the validation-failure, TimeoutException, DbUpdateConcurrencyException and trailing ModelState-invalid returns are affected identically (the ModelState path was driven). (5) Fix check (rig/v2-C2/fixpin.py): at HEAD `Passed!  total: 3  failed: 0  succeeded: 3` (logs/05); under the L317 mutant `Failed!  total: 3  failed: 3  succeeded: 0`, each `Assert.Equal() Failure: Values differ / Expected: 2026-09-09T02:50:29.5863901 / Actual:   null` (logs/06). (6) Settled-gap: docs/AUDIT-LEDGER.md 28-L2 row records only "the DbUpdateConcurrencyException catch has no SQLite seam - recorded here"; grep of .claude/skills/e2e/SKILL.md for `Subnet Information|28-L2|HostIpEditRedisplay|RedisplayEditAsync|Last Modified` → no match. git blame: HostIpController.cs L303-317 and HostIpEditRedisplayTests.cs L37/75/88/101 all 5ea2eb52. Finder (b7A): same one-line mutation via rig/b7A mutate.py (diff `317d316`), same filter → `total: 3 failed: 0 succeeded: 3` (rig/b7A/logs/M2c-drop-lastmodifiedat.log); controls: dropping the CreatedAt line → failed: 3; dropping the SubnetInfo line → failed: 3; controller reverted to 8891320 → failed: 2. Live on http://127.0.0.1:5312, db bastet_rig_b7a2, seeded 10.79.0.5 (CreatedAt 2026-08-15 09:30, LastModifiedAt 2026-09-01 12:00): GET Edit card LastModified '9/1/2026 12:00 PM'; POST /HostIp/Edit with Name '<b>bad</b>' → 200, LastModified 'Never'; controller restored to HEAD, rebuilt, same POST → '9/1/2026 12:00 PM' (rig/b7A/logs/demo-f2.out). Finder's candidate pin appended in the copy → HEAD 4/4 green, mutant failed: 1 = exactly that test.

**Fix:** Test-only, verified green at HEAD 3/3 and red under the L317 mutant 3/3: in test/Bastet.Tests/HostIpManagement/HostIpEditRedisplayTests.cs give the seeded host IP a LastModifiedAt (L37: add `LastModifiedAt = DateTime.UtcNow, ModifiedBy = "t"`), capture it from the same AsNoTracking read-back that already yields _storedCreatedAt (`HostIpAssignment stored = _context.HostIpAssignments.AsNoTracking().Single(h => h.IP == "10.0.9.5"); _storedCreatedAt = stored.CreatedAt; _storedLastModifiedAt = stored.LastModifiedAt;` with a `private readonly DateTime? _storedLastModifiedAt;` field), and add `Assert.Equal(_storedLastModifiedAt, model.LastModifiedAt);` directly after each `Assert.Equal(_storedCreatedAt, model.CreatedAt);` at L75, L88 and L101. Optionally rename the three tests from `...RedisplaysTheStoredSubnetAndCreatedAt` to `...RedisplaysTheStoredSubnetAndDates`. No production change, no new mechanism, no comment; the assert binds the stored value round-tripping onto the redisplayed model rather than a token.

**Residue of:** 28-L2

## L3 — 28-L10's pin checks the refusal sentence's presence, not which refuse() branch carries it; a branch swap stays green `[x1]`

**Where:** test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:63

**Breaks:** PRODUCT-MODEL §5 test rule, shape "a test that stays green with the code it guards broken" (the round-27 gate, 27-L2, requires wording pins to catch the branch-swap mutant). CidrModalScript_OnlyIndexesTheServerSuggestionTable pins 28-L10 with `Assert.Contains("No free /${cidrValue} block starts at or after ${activeSuggestion.startIp}.", script)` and `DoesNotContain("No compatible network address found")` (L63-64); nothing binds the sentence to the `address === null` branch (src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:84-85) or the generic sentence to the `address === undefined` branch (L79-80). Swapping the two refuse() calls keeps the class 17/17 green and puts the operator back in the pre-28-L10 class of wrong sentence: on a range row typing 33, 24, 24.5 or clearing the field shows "No free /33 block starts at or after 10.63.3.8.", "No free /24 …", "No free /24.5 …", "No free /NaN …" (a free-space fact asserted about a size that is not a valid child size, size label "Invalid - none free at or after this range"), while the genuine no-home case shows "Please enter a valid CIDR value within the allowed range." for a value inside the allowed range. A loose-equality mutant (`address == undefined`, which makes the null branch dead) also stays green and shows the same wrong sentence on the no-home case. The full revert to the 8891320 sentence does redden the pin, so only the branch binding is missing. Low: one modal.

**Repro:** Verifier: rig/v3-C3 (archive of HEAD under src/; every build/test with `-p:UseSharedCompilation=false -p:UseRazorBuildServer=false`). (1) HEAD: `dotnet test test/Bastet.Tests/Bastet.Tests.csproj --no-build ... -- --filter-method Bastet.Tests.SubnetManagement.SubnetDetailsModalScriptTests.CidrModalScript_OnlyIndexesTheServerSuggestionTable` → `Passed! total: 1 failed: 0 succeeded: 1`; `--filter-class ...SubnetDetailsModalScriptTests` → `Passed! total: 17 failed: 0 succeeded: 17`; full suite → `Passed! total: 969 failed: 0 succeeded: 969`. (2) Full revert (`git show 8891320:src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml` over the copy), rebuild 0/0, full suite → `Failed! total: 969 failed: 3 succeeded: 966`; red: CidrModalScript_ReadsTheCidrInputAsANumber, CidrModalScript_OnlyIndexesTheServerSuggestionTable, CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart. The claim rests on step 3. (3) Script restored to HEAD, then exactly this diff: `-                refuse('Please enter a valid CIDR value within the allowed range.', 'Invalid');` → ``+                refuse(`No free /${cidrValue} block starts at or after ${activeSuggestion.startIp}.`, 'Invalid - none free at or after this range');`` under `if (address === undefined) {`, and the reverse under `if (address === null) {`; rebuild 0/0; full suite → `Passed! total: 969 failed: 0 succeeded: 969` (rig/v3-C3/step3-full-swap.log). (4) Live: swapped build on http://127.0.0.1:5350 (pid 63528, catalog bastet_rig_v3c3); HEAD control from rig/app/src on :5351 (pid 63343, bastet_rig_v3c3h); both seeded via POST /Subnet/Create: root 10.63.0.0/16 (id 1); carve 10.63.2.0/24 (2) with 10.63.2.128/26 (3), 10.63.2.240/29 (4); carve2 10.63.3.0/24 (5) with 10.63.3.0/29 (6), 10.63.3.64/26 (7). Served-script check (`curl /Subnet/Details/5 | grep`): mutant L412-418 carry the swapped calls, HEAD the reverse. Playwright headless chromium (rig/v3-C3/drive.py), swap-drive.log — carve2 row 10.63.3.8 (table keys 25-32; 25/26 → 10.63.3.128): type 33 → feedback "No free /33 block starts at or after 10.63.3.8." size "Invalid - none free at or after this range"; 24 → "No free /24 block starts at or after 10.63.3.8."; empty → "No free /NaN block starts at or after 10.63.3.8."; 24.5 → "No free /24.5 block starts at or after 10.63.3.8."; carve row 10.63.2.192 (table 25:null, 26:null): 26 → "Please enter a valid CIDR value within the allowed range." size "Invalid"; 25 → same. HEAD (head-drive.log), same inputs: 33/24/empty/24.5 → "Please enter a valid CIDR value within the allowed range." size "Invalid"; 26 → "No free /26 block starts at or after 10.63.2.192." size "Invalid - none free at or after this range"; 25 → "No free /25 block starts at or after 10.63.2.192.". pageerrors [] on both. Screenshots rig/v3-C3/swap-carve2-type-33.png, swap-carve2-type-empty.png, swap-carve-type-26.png, head-carve2-type-33.png, head-carve2-type-empty.png, head-carve-type-26.png. (5) Second mutant `if (address === undefined) {` → `if (address == undefined) {`: full suite `Passed! total: 969 failed: 0 succeeded: 969` (step5-full-deadcond.log); driven on :5350 (pid 64756, served L425 `if (address == undefined)`): .192 row 26 → "Please enter a valid CIDR value within the allowed range." size "Invalid"; 25 → same (deadcond-drive.log). (6) Fix check (rig/v3-C3/fix.diff, L63 replaced by the two Assert.Matches below; build 0 warnings): vs HEAD `Passed! failed: 0 succeeded: 1`; vs swap `Failed! failed: 1 succeeded: 0`; vs dead-condition `Failed! failed: 1`; vs whitespace reflow (brace on next line, blank line and extra indent before refuse) `Passed!`; vs 8891320 revert `Failed! failed: 1`; vs behaviour-identical reorder (null check first) `Passed!`; full suite with the fix at HEAD `Passed! total: 969 failed: 0 succeeded: 969` (fix-*.log). Both app PIDs killed; ports 5350/5351 released. Finder (b6B): `python mutants.py apply F2-branch-swap` (swaps the two refuse(...) calls at L80/L85), `dotnet test test/Bastet.Tests/Bastet.Tests.csproj -p:UseSharedCompilation=false -p:UseRazorBuildServer=false -- --filter-class Bastet.Tests.SubnetManagement.SubnetDetailsModalScriptTests` → `total: 17 failed: 0 succeeded: 17`; mutated copy on http://127.0.0.1:5341 driven by f2_drive.py: /Subnet/Details/7 row 10.50.3.8 → 33 "No free /33 block starts at or after 10.50.3.8." (size "Invalid - none free at or after this range"), 24 "No free /24 block …", empty "No free /NaN block …", 24.5 "No free /24.5 block …"; /Subnet/Details/4 row 10.50.2.192 → 26 and 25 "Please enter a valid CIDR value within the allowed range." (size "Invalid"); HEAD on :5340 (modal-drive.log) correct on the same inputs; mutant L10-revert-sentence reddens the pin (mutants.log).

**Fix:** Test-only, in CidrModalScript_OnlyIndexesTheServerSuggestionTable (test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs): replace the presence check at L63 `Assert.Contains("No free /${cidrValue} block starts at or after ${activeSuggestion.startIp}.", script);` with two branch-bound regexes — ``Assert.Matches(@"if \(address === null\)\s*\{\s*refuse\(`No free /\$\{cidrValue\} block starts at or after \$\{activeSuggestion\.startIp\}\.`", script);`` and `Assert.Matches(@"if \(address === undefined\)\s*\{\s*refuse\('Please enter a valid CIDR value within the allowed range\.'", script);` — keeping `Assert.DoesNotContain("No compatible network address found", script);` at L64. Verified green at HEAD (pin and full suite 969/969), red under the branch swap, the `== undefined` dead-condition mutant and the 8891320 revert, green under a whitespace reflow and a behaviour-identical reorder of the two checks. No production change; the modal is correct at HEAD.

**Residue of:** 28-L10

## L4 — 28-L4 sidebar pin passes with IsAzureLinked read backwards (linked row told its CIDR is modifiable) `[x1]`

**Where:** test/Bastet.Tests/SubnetManagement/SubnetEditViewSourceTests.cs:30; test/Bastet.Tests/SubnetManagement/SubnetEditViewSourceTests.cs:38; src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml:13; src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml:18

**Breaks:** PRODUCT-MODEL §5 test rule, shape "a test that stays green with the code it guards broken". EditInformationSidebar_DescribesCidrAsModifiableOnlyWhenTheFormAllowsIt locates the linked branch by the token `@if (Model.IsAzureLinked)` anywhere in the file (L30) and checks the unlinked polarity by the token `@if (!Model.IsAzureLinked)` anywhere in the file (L38); neither token is tied to the text it guards. Negating the predicate at both sites of _InformationSidebar.cshtml (L13 → `@if (Model.IsAzureLinked)`, L18 → `@if (!Model.IsAzureLinked)`) keeps both tokens present and the L36 order chain satisfied (the CIDR bullet's @if at L13 stands in for the alert block's), so the test and the suite stay green while the rendered sidebar is fully inverted: an Azure-linked subnet, whose form fixes the CIDR ("Imported from Azure, so the prefix is fixed here."), gets "CIDR - The subnet mask size in CIDR notation … Network address cannot be changed. Only the CIDR value can be modified. CIDR Modification Rules: …" — the exact 28-L4 defect — and an unlinked subnet whose form says "Changing this value affects the size of the subnet." gets "Network address and CIDR cannot be changed here." Each single-site flip is caught only because the other site's token disappears. Low: one screen.

**Repro:** Verifier: fresh archive copy of HEAD at rig/v2-C4/src, built with `-p:UseSharedCompilation=false` (0 warnings). (1) HEAD: `dotnet test test/Bastet.Tests/Bastet.Tests.csproj --no-build --filter "FullyQualifiedName~SubnetEditViewSourceTests"` → `Test run summary: Passed! total: 1 failed: 0 succeeded: 1 skipped: 0`. (2) `git show 8891320:src/Bastet/Views/Subnet/Edit/_InformationSidebar.cshtml` written over the copy (61-line diff, the untyped static sidebar), rebuild, full suite → `Failed! total: 969 failed: 1 succeeded: 968`; only red: SubnetEditViewSourceTests.EditInformationSidebar_DescribesCidrAsModifiableOnlyWhenTheFormAllowsIt. The candidate does not rest on the full revert. (3) Sidebar restored to HEAD, then exactly (rig/v2-C4/logs/step3-mutation.diff): `-            @if (!Model.IsAzureLinked)` / `+            @if (Model.IsAzureLinked)` at L13 and `-        @if (Model.IsAzureLinked)` / `+        @if (!Model.IsAzureLinked)` at L18, nothing else; rebuild; full suite → `Test run summary: Passed! total: 969 failed: 0 succeeded: 969 skipped: 0` (logs/step3-full-suite-doubleflip.log; no test renders the view — grep for WebApplicationFactory/TestServer/RazorViewEngine in test/ is empty, no RuntimeCompilation in Program.cs/Bastet.csproj). Single-site controls with the HEAD test: L13-only → `Failed! total: 1 failed: 1 succeeded: 0`; L18-only → `Failed! total: 1 failed: 1 succeeded: 0`. (4) Live: double flip built (`dotnet build src/Bastet/Bastet.csproj`, 0 warnings), rig/v2-C4/run-app.sh (Development, SP1, BASTET_AZURE_IMPORT=true, BASTET_AUTO_MIGRATE=true, AZURE_TOKEN_CREDENTIALS unset) on http://127.0.0.1:5367, catalog bastet_rig_v2c4 (app.log "CREATE DATABASE [bastet_rig_v2c4]"), listener pid = app.pid 56746. Seeded via sqlcmd in bastet-audit-sql: Id 1 'v2c4-linked' 10.87.0.0/24 AzureResourceId '/subscriptions/f0e8d6db-e9c4-4215-81a5-17762ea56be8/resourceGroups/bastet-visible/providers/Microsoft.Network/virtualNetworks/v2c4-linked'; Id 2 'v2c4-unlinked' 10.88.0.0/24 AzureResourceId NULL. GET /Subnet/Edit/1 → 200; form "Imported from Azure, so the prefix is fixed here."; sidebar "You can modify the following properties: Name - A descriptive name for the subnet Description - Additional details about the subnet's purpose Tags - Comma-separated list for categorization CIDR - The subnet mask size in CIDR notation Network address cannot be changed. Only the CIDR value can be modified. CIDR Modification Rules: Decreasing CIDR (making subnet larger) requires: …". GET /Subnet/Edit/2 → 200; form "Changing this value affects the size of the subnet."; sidebar "… Tags - Comma-separated list for categorization Network address and CIDR cannot be changed here." (no CIDR bullet, no rules). Saved pages rig/v2-C4/logs/mut-5367-linked.html, mut-5367-unlinked.html. HEAD control on the same port/catalog after restoring the sidebar and rebuilding (pid 56941): GET /Subnet/Edit/1 → 200, form fixed, sidebar "… Tags - Comma-separated list for categorization Network address and CIDR cannot be changed here."; GET /Subnet/Edit/2 → 200, form editable, sidebar carries the CIDR bullet and the CIDR Modification Rules (logs/head-5367-*.html). (A first control attempt on :5342 was discarded: a foreign instance, pid 44221 from rig/v2-C1, held that port; my process never bound.) (5) Fix verification, the named test after replacing L30-36 and L38 with the two Assert.Matches (L37 kept): HEAD `Passed! total: 1 failed: 0 succeeded: 1`; L13-only `Failed! total: 1 failed: 1`; L18-only `Failed! total: 1 failed: 1`; both flipped `Failed! total: 1 failed: 1`; full revert `Failed! total: 1 failed: 1`; full suite with the fixed test at HEAD `Passed! total: 969 failed: 0 succeeded: 969` (a variant appending `[^}]*CIDR Modification Rules` to the second regex gave identical results on every row). Repository `git status --porcelain` empty throughout; PIDs 43571, 52556, 56746, 56941 all exited; 5367 released. Finder (b7A): rig/b7A mutate.py diff `13c13 < @if (!Model.IsAzureLinked) --- > @if (Model.IsAzureLinked)` and `18c18 < @if (Model.IsAzureLinked) --- > @if (!Model.IsAzureLinked)`; `dotnet test/Bastet.Tests/bin/Debug/net10.0/Bastet.Tests.dll --filter-class Bastet.Tests.SubnetManagement.SubnetEditViewSourceTests` → `Passed! total: 1 failed: 0 succeeded: 1 skipped: 0` (rig/b7A/logs/M4d-negate-both.log); controls on the same pin: revert to 8891320 → failed: 1; flip L18 alone → failed: 1; flip L13 alone → failed: 1 (M4a/M4b/M4c-*.log). Live on http://127.0.0.1:5311, db bastet_rig_b7a1, seeded id 1 'b7a-linked' 10.77.0.0/24 with an AzureResourceId and id 2 'b7a-unlinked' 10.78.0.0/24: GET /Subnet/Edit/1 → linked form, sidebar with the CIDR bullet and "CIDR Modification Rules"; GET /Subnet/Edit/2 → editable form, sidebar "Network address and CIDR cannot be changed here."; sidebar restored to HEAD and rebuilt on the same port/db → correct on both rows (rig/b7A/logs/demo-f1.out, f1-*.html).

**Fix:** Test-only, in test/Bastet.Tests/SubnetManagement/SubnetEditViewSourceTests.cs: replace the token-based order chain at L30-36 and the bare `Assert.Contains("@if (!Model.IsAzureLinked)", sidebar)` at L38 with assertions that bind each sentence to its own branch, keeping L37 (`Assert.Equal(1, sidebar.Split("Only the CIDR value can be modified.").Length - 1)`) and L27-28 unchanged: `Assert.Matches(@"@if \(!Model\.IsAzureLinked\)\s*\{\s*<li><strong>CIDR</strong>", sidebar);` and `Assert.Matches(@"@if \(Model\.IsAzureLinked\)\s*\{[^}]*Network address and CIDR cannot be changed here\.[^}]*\}\s*else\s*\{[^}]*Only the CIDR value can be modified\.", sidebar);`. Verified: HEAD green; L13-only, L18-only, both-sites and full-revert mutants all red; full suite 969/969 with the change. Optionally append `[^}]*CIDR Modification Rules` to the second pattern so the warning block stays bound inside the else branch as the dropped `rules > elseBranch` term intended (verified identical results on every mutant). No production change.

**Residue of:** 28-L4

## L5 — CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart accepts a reset gated inside refuse(); the 28-L9 defect returns with the pin green `[x1]`

**Where:** test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:132

**Breaks:** PRODUCT-MODEL §5 test rule, shape "a test that stays green with the code it guards broken". The pin captures the refuse() body with `[^}]*`, which stops at the first `}`, and only asks that the two reset statements appear somewhere in that capture. Wrapping them in a condition inside refuse() — `if (sizeText === 'Invalid') { $('#networkAddressDisplay').val(activeSuggestion.startIp); makeNetworkAddressReadOnly(); }` at src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml:43-45 — or gating them behind a `keepAddress` parameter the null branch passes, re-creates exactly what 28-L9 fixed: on the Details page of 10.77.0.0/24 with children 10.77.0.0/26, 10.77.0.96/27, 10.77.0.192/26, click Create Subnet on the 10.77.0.64 range, type 26 (address adjusts to 10.77.0.128 with the "adjusted to avoid overlaps" note), then type 25: the modal refuses ("No free /25 block starts at or after 10.77.0.64.", Create disabled) while still showing 10.77.0.128 with the warning border and the adjustment note; HEAD resets to 10.77.0.64 and hides the note. The pin and the whole suite stay green under both mutants. Low: one modal.

**Repro:** Verifier: fresh `git archive HEAD` copies under rig/v2-C5/ with `-p:UseSharedCompilation=false`. (1) HEAD (rig/v2-C5/src): `dotnet test --no-build --filter "FullyQualifiedName~SubnetDetailsModalScriptTests.CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart"` → `Test run summary: Passed! total: 1 failed: 0 succeeded: 1 skipped: 0` (step1-head-control.log). (2) Revert (rig/v2-C5/revert/src, `git show 8891320:src/Bastet/Views/Subnet/Details/_SubnetCalculationScripts.cshtml` restored; step2-revert.diff): full suite → `Failed! total: 969 failed: 3 succeeded: 966`; red: CidrModalScript_OnlyIndexesTheServerSuggestionTable, CidrModalScript_ReadsTheCidrInputAsANumber, CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart (`Assert.Contains() Failure: Sub-string not found ... Not found: "$('#networkAddressDisplay').val(activeSuggestion.s"`, SubnetDetailsModalScriptTests.cs:134). The candidate does not rest on the full revert. (3) Mutant (rig/v2-C5/mut/src, step3-mutation.diff): inside `function refuse(message, sizeText) {` the lines `$('#networkAddressDisplay').val(activeSuggestion.startIp);` / `makeNetworkAddressReadOnly();` replaced by `if (sizeText === 'Invalid') { $('#networkAddressDisplay').val(activeSuggestion.startIp); makeNetworkAddressReadOnly(); }` (L43-50); build 0 errors; full suite → `Test run summary: Passed! total: 969 failed: 0 succeeded: 969 skipped: 0`. Variant (rig/v2-C5/mut2/src, step5-mut2.diff): `function refuse(message, sizeText, keepAddress) { if (!keepAddress) { <the two reset lines> }` with the null branch calling ``refuse(`No free /${cidrValue} block ...`, 'Invalid - none free at or after this range', true);`` → full suite `Passed! total: 969 failed: 0 succeeded: 969`. (4) Live (rig/v2-C5/run-app.sh, SP1, AZURE_TOKEN_CREDENTIALS unset): mutant on http://127.0.0.1:5385 catalog bastet_rig_v2C5_mut pid 44877; HEAD on :5386 catalog bastet_rig_v2C5_head pid 45470; rig/v2-C5/drive-l9.py (playwright): create 10.77.0.0/24 with children 10.77.0.0/26, 10.77.0.96/27, 10.77.0.192/26 (ids 1-4), open /Subnet/Details/1, click `.create-subnet-btn[data-network="10.77.0.64"]`, fill #cidrInput 26 then 25. Mutant drive-mut.json modal_after_25: networkAddressDisplay "10.77.0.128", networkAddressHelp_hidden false, networkAddressHelp_visible true, networkAddressHelp_text "This network address has been adjusted to avoid overlaps.", networkAddressDisplay_border_warning true, cidrValidationFeedback "No free /25 block starts at or after 10.77.0.64.", subnetSizeDisplay "Invalid - none free at or after this range", createSubnetBtn_disabled true; modal_after_40 (undefined branch) still resets to "10.77.0.64" with help hidden — the exact pre-fix split. HEAD drive-head.json modal_after_25: networkAddressDisplay "10.77.0.64", networkAddressHelp_hidden true, border_warning false, same feedback and disabled Create. Screenshots rig/v2-C5/shots/mut-modal-25.png (10.77.0.128 with yellow border and the adjusted note under a /25 refusal) vs head-modal-25.png (10.77.0.64, no note); page_errors [] on both; both PIDs killed, ports free. Reachability: /Subnet/Details/{id} Unallocated IP Ranges card, button rendered by _UnallocatedRanges.cshtml:61 `UserHasRole(Edit) && Model.CanAddChildSubnet` (SubnetViewModels.cs:94); no feature flag; an ordinary hand-made carve-up. (5) Pin check with real xunit (scratch theory in rig/v2-C5/src, since deleted; scratchpin-run.log): the existing pin passes on head, mut and mut2; the filed Assert.Matches passes on head and fails on mut and mut2 (`total: 9 failed: 4 succeeded: 5` across three pins × three variants). (6) Settled-gap: docs/AUDIT-LEDGER.md:172 claims "pinned by CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart"; .claude/skills/e2e/SKILL.md:667 records the runtime sentence "(every refusal resets; ...)" but nothing records the pin as approximate or the surface as unpinnable; `[^}]*` over test/ hits only SubnetDetailsModalScriptTests.cs:132. Finder (b7B): mutant rig/b7B/mutant-V11-_SubnetCalculationScripts.cshtml (only change: the two reset lines wrapped in `if (sizeText === 'Invalid') { ... }`); in rig/b7B/src `dotnet test/Bastet.Tests/bin/Debug/net10.0/Bastet.Tests.dll --filter-method Bastet.Tests.SubnetManagement.SubnetDetailsModalScriptTests.CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart` → `Passed! total: 1 failed: 0 succeeded: 1`; whole dll `total: 969 failed: 0 succeeded: 969`; control (reset lines moved back into the address===undefined branch, the literal pre-fix shape) → `Assert.Contains() Failure: Sub-string not found`; live (rig/b7B/drive.py, mutant on :5370 catalog bastet_rig_b7B_mut vs HEAD on :5371): modal_after_25 networkAddressDisplay "10.77.0.128", help visible, border_warning true, feedback "No free /25 block starts at or after 10.77.0.64.", Create disabled vs HEAD "10.77.0.64", help hidden, no warning border (rig/b7B/shots/mut-modal-25.png vs head-modal-25.png).

**Fix:** Test-only, in CidrModalScript_RefuseResetsTheNetworkAddressToTheRangeStart (test/Bastet.Tests/SubnetManagement/SubnetDetailsModalScriptTests.cs:132-135): replace the Regex.Match body capture and its three asserts with the single positional pin `Assert.Matches(@"function refuse\([^)]*\)\s*\{\s*\$\('#networkAddressDisplay'\)\.val\(activeSuggestion\.startIp\);\s*makeNetworkAddressReadOnly\(\);", script);` — it pins what the ledger row itself promises ("refuse(message, sizeText) now begins ..."), stays green at HEAD, reds both the braced gate and the keepAddress-flag gate (verified with xunit), and unlike a brace-free-body check also reds a brace-less `if (...) stmt;` gate because anything textually before the reset breaks the match. Drop the finder's optional `Assert.DoesNotContain("if (", body)` — a token pin, and redundant. No production change; the `Regex` using stays for the other tests in the class.

**Residue of:** 28-L9; traces to 23-L2-remainder via 28-L9 (refuse() introduced at _SubnetCalculationScripts.cshtml:43 by 8891320, #189; the 28-L9 hunk and its pin added in 5ea2eb5, #190 — git blame test L128-136 and production L44-45)

# Info

None.

# Refuted — reported by a finder, killed by the verifier

| id | title | where | reason |
|---|---|---|---|

None.
